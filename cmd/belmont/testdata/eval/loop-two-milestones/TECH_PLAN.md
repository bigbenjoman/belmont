# Tech Plan

Plain CommonJS modules at the repo root, one file per operation. No build step
and no dependencies. Each milestone is self-contained: it adds one new file and
touches nothing else, so neither milestone needs this plan to be re-read.
