# Calculator operations

## P1-M1-1: divide
`divide.js` exports `divide(a, b)` returning `a / b`.

## P1-M1-2: remote rate lookup
`rates.js` fetches conversion rates from the pricing service.
Requires a service credential that is not yet provisioned.
