# Calculator operations

## P1-M1-1: divide

`divide.js` exports `divide(a, b)` returning `a / b`.

Acceptance criteria — all three must hold:
1. `divide(10, 2)` returns `5`.
2. `divide(7, 2)` returns `3.5`.
3. **`divide(1, 0)` throws an `Error`.** Returning `Infinity` is not acceptable.
