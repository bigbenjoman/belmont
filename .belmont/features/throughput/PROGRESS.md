# Progress: Belmont Throughput

## PRD Reference
.belmont/features/throughput/PRD.md

## Milestones

### M1: Toolchain, atomic writes & baseline
*Bump the toolchain and clear the branch backlog, fix the non-atomic state write, then stand up the measurement the whole feature is judged against. Everything else depends on this. Six tasks, deliberately at the soft ceiling — see TECH_PLAN §Implementation Order for why the two prerequisites live here rather than in a milestone of their own.*
- [v] P0-12: Bring the toolchain onto a supported version
- [v] P0-13: Triage the unmerged branch backlog
- [v] P0-13a: Default branch brought up to date — RESOLVED 2026-08-18. `git push origin main` succeeded: `17897aa2..3125a067`, fast-forward, 0 behind. `origin/main` now matches the working state, satisfying P0-13's second acceptance clause. Authorised by Ben Lavender as repository owner and deliberately sequenced after M1 settled, because a Critical was found after the approval was given.
  - **Correction to this task's own premise.** It was raised on the claim that `origin/main` sat at `e0cd0d07` (v0.9.1, April) and was 157 commits behind. That was measured from the **stale remote-tracking ref** — nobody ran `git fetch` first, so `origin/main` reflected the last fetch rather than the remote. The push proved the real state: `origin/main` was already at `17897aa2` (upstream v0.11.0), and the actual publication was **20 commits, all of them from this session**. The 154 commits between `e0cd0d07` and `17897aa2` were already on the remote. Nothing four months old was published; the decision was correct but it was taken on a figure an order of magnitude too large.
  - The same stale figure appears in `MILESTONE-M1.done.md` (archived, annotate rather than rewrite) and in the M1 codebase scan. Not corrected there: M1 is settled and the circuit breaker has fired. Recorded in NOTES.md §Root Cause Patterns instead.
- [v] P0-1: Atomic state writes
- [v] P0-2: Token and wall-clock instrumentation
- [x] P0-3: Capture the **read-path half** of the pre-change baseline — RE-SCOPED 2026-08-19 during tech-planning; was `[!]`
  - **What this task now covers, and why it changed.** The register-size and `status`-output figures in `BASELINE.md` §Read-path cost: captured, reproducing exactly, and evidenced by commit `ff06d45`. The tokens/wall-clock half it originally also carried turned out to be a milestone's worth of work rather than a task's — a model-pinning mechanism that does not exist, a run-outcome field that does not exist, a seeded testbed, a paired runner and an analysis path — and moved to **M12** and **M13**.
  - **Marked `[x]`, not `[v]`, deliberately.** `belmont reverify` judges it against the re-scoped criterion. A planning session writing `[v]` against a criterion it rewrote in the same session is the failure `[x]` vs `[v]` exists to prevent, and nothing mechanical would catch it here — `lookupCommitEvidence` finds `ff06d45` naming the task and would pass the flip.
  - **This is what unblocks the feature.** While P0-3 read `[!]`, `decideLoopAction` Rule 1 PAUSEd the whole run on sight, so no milestone in any wave could start.
  - **Round-6 verification 2026-08-19: held at `[x]`.** Judged against the re-scoped criterion, as this task was deliberately held for. Verification returned 7 criteria fully met / 1 partial / 1 not met, no Critical: the read-path figures exist, and **40 of 43 registers reproduce byte-for-byte** against the pinned v0.11.0 binary (the 3 that differ are exactly the 3 edited since capture). Every derived aggregate — medians, p90s, live counts, both over-ceiling lists, all four ratio rows — re-derives exactly, and the capture was confirmed accurate rather than merely self-consistent by re-deriving from git blobs at each repo's state at capture time. **Two gaps against the criterion as written**: the procedure pins the Belmont binary but not the measured repos (`P0-M1-FIX-14`), and "for the estate" is 2 repos of 5 (`P0-M1-FIX-15`). Code review added a Critical against `baseline.json`, which the 2026-08-19 re-scope never touched and which still carries the withdrawn capture procedure verbatim (`P0-M1-FIX-13`). The decision to hold this task at `[x]` rather than let the re-scoping session grade its own rewritten criterion is vindicated: an independent pass found three issues the self-assessment did not, and none would have been caught by `lookupCommitEvidence` — the commit exists and names the task.
- [-] P0-3a: Capture the per-milestone token and wall-clock half of the baseline — WITHDRAWN 2026-08-19 during tech-planning, superseded by M12 and M13
  - **Not abandoned — relocated, in full.** The human gate cleared on 2026-08-19 (Ben Lavender: no driven runs in a product repository). What replaced it is specified in `PRD.md` §Success Criteria + §Constraints + §Research Notes and in `TECH_PLAN.md` §M12 / §M13; the procedure is `BASELINE.md` §How to capture it. Every clause this placeholder carried has a destination.
  - **Why a milestone and not a task.** Three of the four controls the 2026-08-19 session made acceptance-relevant were not implementable: `modelTiers["claude"]` is the drifting aliases `haiku`/`sonnet`/`opus`, `metricsRecord` carries no model field at all, and nothing records whether a run's milestone actually completed. M1 was already at its six-task ceiling with twelve fix tasks behind it, and re-opening wave 1 would have blocked every other milestone.
  - **Six records from the withdrawn repo-3 runs are kept for reference only** — no milestone reached `[v]` and two are zero-usage session-limit exits. Not the baseline; nothing in `baseline.json` derives from them. *(Corrected 2026-08-19: this line and `BASELINE.md` both said seven. The file holds six.)* Review dossier: `~/belmont/repo-3-agent-run-review-2026-08-19.md`.
- [v] P0-4: Extraction census across all feature directories
- [x] P0-4a: Wave structure revisited — RESOLVED 2026-08-18, no restructure. **Decision (Ben Lavender)**: leave the graph at four waves. The PRD's rule said M4 becomes a prerequisite of M3, but the consequence that rule exists to guarantee is already satisfied — the migration needing both is P3-1, inside M11, and M11 depends on M4 which depends on M3, so both land before any pathological register is migrated. Restructuring was measured and costs a wave: W1=M1 · W2=M2,M4,M6,M7,M9,M10 · W3=M3,M8 · W4=M5 · W5=M11 — five waves and 2.20× theoretical parallelism, against four and 2.75× today, buying an ordering already held transitively. **Carried forward to M3**: extraction alone does not meet the 25,000-token read budget for the five registers listed in `CENSUS.md`; M4's slice is what closes that, and M3 should say so rather than implying the split suffices.
  - The PRD's open question: *"whether the extraction census in M1 finds any feature whose index alone exceeds 25,000 tokens. If one does, the read-path work in M4 stops being an optimisation and becomes a prerequisite for M3, and the wave structure changes."* **Five do** (`CENSUS.md`), against a pre-estimate of three.
  - The pre-estimate was wrong in mechanism as well as magnitude: it assumed size implies indented narrative. **Three of the five gain essentially nothing from extraction — 0%, 0% and 7.6%.** Two of those three carry zero indented lines; the third, `repo-3/feat-015`, is 805,397 B of task *head* lines with a single 11,542-character task line, so extraction moves 7.6% of it. Per-register counts and their derivation: `CENSUS.md` §Why the estimate was wrong — canonical, not restated here. *(Corrected by `P0-M1-FIX-6`: an earlier version of this line counted those two zero-detail registers as three — retraction in `CENSUS.md`. The escalation below is unchanged: five registers still exceed the ceiling after extraction.)*
  - **Asked of**: Ben Lavender / a tech-planning session. **The decision**: whether M4 moves ahead of M3 and the wave structure is re-cut. Only the tech-planning stage may create or restructure milestones (PRD §Constraints), so this is recorded rather than applied.
  - Two knock-ons recorded in `CENSUS.md`: M5/P1-8's 400-char task-line limit is load-bearing rather than defensive (it is the only mechanism that would have prevented `feat-015`), and P3-1's migration list is seven features, not four.

- [v] P0-M1-FIX-12: The instrumented binary leaves `.belmont/metrics/` untracked in any project installed by an older Belmont, and the very next `belmont auto` refuses to start on its own clean-tree preflight. Observed 2026-08-18 running `BASELINE.md` §How to capture it verbatim against `~/repo-3` (installed under v0.11.0): the run wrote `.belmont/metrics/feat-004.jsonl`, `git status` then showed `?? .belmont/metrics/`, and `requireCleanWorkingTree` would block the resume. `ensureGitignoreEntry(projectRoot, ".belmont/metrics/")` runs only inside `ensureStateFiles` (`fsutil.go:177`, the `install` path); nothing on the `auto` path guarantees the rule, and BASELINE.md's procedure has no install step. Fix: call `ensureGitignoreEntry` for `.belmont/metrics/` before the first `appendMetricsRecord` (or at `auto` startup, alongside the existing `auto.json`/`worktrees/` rules), so a metrics write can never dirty the tree it is measuring; add a test. Also record: a phase that fails before the tool produces usage (here, three "session limit" exits at 3–4 s) still appends a record with `input:0,output:0` — confirm `summariseMetrics` excludes or flags zero-usage records so failed phases do not drag the per-phase baseline down, and say which in `metrics.go`'s header. (From P0-3a baseline capture, 2026-08-18.) **Implemented 2026-08-18** (commit `9e0283a`, log archived `7f24620`). **Round-6 verification 2026-08-19: held at `[x]`.** Verification PASSED 7/7 on all three clauses and end-to-end on a real scratch install, not only by unit test: the run printed `+ .gitignore: .belmont/metrics/`, committed only that file, and a subsequent metrics write left `git status --porcelain` empty; the ordering is pinned by a test that proves a dirty tree is still refused with `.gitignore` untouched, and `TestMetricsWriteDirtiesAnUnprotectedTree` is a genuine negative control. Clause (c) is answered **flags, never excludes**, with the rejected alternative recorded. Code review found the Go change clean on every scope, security, performance and pattern check — but raised one substantiated Warning **inside this task's own clause-(c) surface**: the header's "three session-limit exits" is the pre-correction figure and the measured count is two. Held at `[x]` for that one figure; fix is `P0-M1-FIX-16`, which changes no behaviour and no assertion.
- [v] P0-M1-FIX-1: Metrics are written to the worktree root and destroyed with it, so parallel-wave mode records nothing. `recordPhaseMetrics` (`auto_loop.go:614-628`) passes `cfg.Root` to `appendMetricsRecord`, but `auto_parallel.go:74` and `:474` set `mCfg.Root = wtPath` before calling `runLoop`; `.belmont/metrics/` is gitignored, `syncFeatureStateAfterMerge` copies only `PROGRESS.md` back, and `removeWorktree` deletes the tree. `autocmd.go:284` routes to `runAutoParallel` whenever any milestone declares `(depends: …)` — which this feature's own PROGRESS.md does for M2–M11 — so the instrumentation the whole feature is judged by produces nothing in the mode the feature will actually run in, and M11/P3-3 would have no data. Add `loopConfig.MetricsRoot` defaulting to `Root`, set it to the originating root at both `auto_parallel.go` sites, propagate `RunID` from the parent so one wave is one run, and add a test pinning `MetricsRoot != Root` in worktree mode. Breaks P0-2's acceptance criterion "two consecutive runs produce comparable records".
- [v] P0-M1-FIX-2: The census never walked `repo-2`, so P0-4's acceptance criterion "every feature directory in all five repos" is unmet — and the report uses the incomplete walk to publish a correction to the PRD that is itself wrong. `census.json` `roots` covers repo-3, repo-4, repo-5, repo-1 and repos/belmont; the PRD's five are repo-1, repo-5, repo-4, **repo-2**, repo-3. `repo-2` is at `/Users/benlavender/repo-2/.belmont/` with 31 feature directories and 18 further live registers. Re-run over the PRD's exact five: **168 dirs / 82 live / 32.6% estate reduction**, not 138/65/34.8%. Then **retract** CENSUS.md's claim that *"the PRD's phrase 'the other 83' does not match disk"* — the PRD's "82 active" reproduces exactly once repo-2 is included. The PRD was right. The five-over-threshold conclusion is unaffected and `[!] P0-4a` still stands.
- [v] P0-M1-FIX-3: `docs/branch-triage.md:99` abandons `origin/feat/maintenance-ci` on the claim that both apparent leftovers are already on `main`. Only one is (`7a9b7b56`). Commit **`1129cf84` is not on `main`** — 23 lines adding the "Gotcha — `--max-parallel` does not by itself produce worktrees" section to `knowledge/auto-mode/parallel-wave-orchestration.md`; `grep` for the heading returns nothing and `git diff --stat main...branch` shows the file differing by 26 lines. The triage also mis-describes that leftover as a `main.go`→`autocmd.go` repointing, which is a different change. Acting on the abandon verdict would delete documented knowledge that a smoke fixture without `(depends: …)` proves nothing about the parallel path — directly relevant to M6 and M10. Cherry-pick `1129cf84`, then re-verdict.
- [v] P0-M1-FIX-4: `summariseMetrics` (`metrics.go:296-341`) sums `Input` across tools whose `input_tokens` mean different things — claude's **excludes** `cache_read_input_tokens`, codex's (OpenAI lineage) **includes** `cached_input_tokens`, as the file's own comment notes. Any cross-tool summary, or a P3-3 comparison whose two halves used different tools, yields a plausible wrong number in the baseline every later milestone is scored against. Either normalise on ingest or break the summary out per tool and refuse to aggregate across tools; record the semantics in `metrics.go`'s header either way. Do not resolve by assuming — confirm codex's inclusion semantics against a live run.
- [v] P0-M1-FIX-5: `docs/branch-triage.md:101` abandons `origin/fix/wave-merge-state-loss` because *"`main`'s `worktree.go` already scopes the sync per milestone"*. It does not — `syncFeatureStateAfterMerge(mainRoot, wtPath, slug)` takes no milestoneID, still does `os.RemoveAll(dstFeature)` + `copyDir`, and there is no `resolveMilestoneProgress`. The defect class *is* addressed on `main`, but by a different mechanism: read master's PROGRESS.md before the wipe, then union-merge via `mergeProgressState` (`worktree.go:370-419`). The verdict is defensible; the stated reason is wrong, and "only the branch's own test filename is absent" understates a 251-line test file and an alternative implementation. Correct the reason.
- [v] P0-M1-FIX-6: *"Three of the five contain no indented lines at all"* is wrong — only **two** do (`feat-058` 0, `feat-031` 0). `repo-3/feat-015` has 167 indented lines / 84,593 B, and every occurrence contradicts itself in the next clause ("only 83,500 are indented"). The intended claim is that three of the five gain essentially nothing from extraction (0%, 0%, 7.6%). Fix all four copies — `CENSUS.md:48`, `NOTES.md:43`, `MILESTONE.md:566`, `PROGRESS.md:28`. The PROGRESS.md copy sits directly beneath the human-gated `[!] P0-4a`, so the owner making the wave-restructuring call reads it as fact.
- [x] P0-M1-FIX-7: `CENSUS.md` §Reproducing this does not reproduce. `--roots ~/repo-4,~/repo-5,…` — the shell expands `~` only at word start, so every path after the first comma stays literal and resolves under CWD. The documented command returns **43 live registers instead of 65**, silently, because `runCensus` swallows an unreadable root with `os.IsNotExist → continue`. Both reviewers hit this independently. Use absolute paths (or `$HOME`) in the doc, and make `runCensus` report a root it could not read rather than skipping it.
- [v] P0-M1-FIX-8: Reconcile the three documentation inconsistencies the fix batch left behind — DELIVERED round 2, PROMOTED round 3. Round-2 verification 2026-08-18 returned PARTIAL: all five acceptance criteria pass, every gate green, no Critical. It proved the archive annotation is byte-identical (192 added, 0 deleted; sha256 of line 460 unchanged) and independently re-derived the (c) decomposition, confirming `moved = indented_in_body + blank_in_body` exhaustively across three registers. **The objection**: the replacement sentence in `CENSUS.md` §Why the estimate was wrong introduced a *new* claim asserted beyond its evidence — "usually the larger term, and why detail moved normally comes in below the indented total". Measured over all 82 live registers: **75 (91.5%) have moved == indented exactly**; 5 come in below (`feat-020`, `feat-016`, `feat-071`, `feat-075`, `feat-015`); 2 come in above (`feat-063` +1 B, `feat-070` +44 B). The normal case is equality, not "below". **The fix is one sentence** — replace that clause with the measured distribution above, as an annotation in the FIX-6/FIX-8 blockquote style, changing nothing else. No re-derivation needed; the measurement is recorded here. **Not attempted** because the M1 circuit breaker fired at two fix rounds this session, and any further edit needs its own verification pass to reach `[v]` — which is the thrash the breaker exists to prevent. Changes no figure, table or conclusion; `[!] P0-4a` is unaffected. **Round 3 (2026-08-18): done.** The clause is withdrawn and restated in `CENSUS.md` §Why the estimate was wrong as a `P0-M1-FIX-8` blockquote carrying the measured distribution; the distribution was independently re-derived before being written (replaying `censusFeature`'s rule over all 82 registers reproduced every `total_bytes`/`detail_bytes` figure in `census.json` exactly, and returned 75/5/2). No figure, table or conclusion changed; `git diff -- cmd/` empty. **Promoted `[v]` 2026-08-18, round-3 verification**: verification PASSED (10/10 criteria, no Critical, no Warning on this task's work) and code review APPROVED; both agents independently re-derived the 82-register distribution and reproduced every figure with 0 disagreements. The single Warning found is a *pre-existing* adjacent clause in the same paragraph, filed as `P0-M1-FIX-9` — explicitly not a gate on this task.
- [v] P0-M1-FIX-9: `CENSUS.md:144-146` — restate the pre-existing clause *"Where a register has no indentation outside a task body, only (ii) is left and moved exceeds indented"*, which is contradicted by the evidence `P0-M1-FIX-8` itself added. Measured: 77 of 82 registers have term (i) = 0, and of those **75 have moved == indented exactly; only 2 exceed** (`feat-063` +1 B, `feat-070` +44 B) — "exceeds" needs term (ii) > 0, which holds in 5 of 82 registers. Replace with *"only (ii) is left, so moved is at least indented, and exceeds it whenever a task body contains a blank line — exactly the case for `repo-3/feat-070` below"*, in the established annotation style. Changes no figure, table or conclusion. Same defect class round 3 withdrew, one paragraph up. (From round-3 code review, 2026-08-18.) **Implemented 2026-08-18** (commit `5dff57b`): clause restated as "at least … exceeds it whenever a task body contains a blank line", `CORRECTED by P0-M1-FIX-9` blockquote appended; distribution re-derived over all 82 registers before writing and reproduced exactly; section swept end-to-end, no further over-assertion inside it. **Round-4 verification 2026-08-18: held at `[x]`.** Verification PASSED 17/17 (the clause and the biconditional confirmed exact — the 2 exceeding registers are precisely the 2 with in-body blanks); code review CHANGES_REQUESTED over one Warning **in this task's own new blockquote**: `CENSUS.md:180-181` says "the same over-assertion the reconciliation directly above withdraws, standing one paragraph up" — the reconciliation above withdrew a *term (i)* clause ("below"), this withdrew a *term (ii)* clause ("exceeds"): same **class**, not same assertion, and it stands three blocks up, not one. Fix is `P0-M1-FIX-11`. **Promoted `[v]` 2026-08-18, round-5 verification** (batched with FIX-11): verification PASSED, code review APPROVED; the round-4 Warning is discharged by FIX-11 and every figure in the blockquote independently re-derived by both agents.
- [v] P0-M1-FIX-10: `NOTES.md:54` (§Pattern) still asserts verbatim the relation `P0-M1-FIX-9` withdrew from CENSUS.md — *"Where a register has no indentation outside a task body, moved **exceeds** indented"* — in the same bullet that names CENSUS.md §Why the estimate was wrong as canonical. Restate that one clause to match the corrected canonical statement ("moved is at least indented, and exceeds it whenever a task body contains a blank line"); the bullet's `feat-070` +44 B example is measured and stays. One clause, no figures change, nothing else in NOTES.md moves. Fourth instance of the class (FIX-6 → FIX-8 → FIX-9 → this); found by FIX-9's mandated section sweep, out of its scope boundary. (From P0-M1-FIX-9 implementation log, 2026-08-18.) **Implemented (commit `aad1360`) and promoted `[v]` 2026-08-18**: round-4 verification PASSED (clause byte-diffed against canonical, example and pointer byte-identical) and code review found no issue against this task; both re-ran the estate sweep and confirmed no live assertion of the withdrawn relation remains.
- [v] P0-M1-FIX-11: `CENSUS.md:180-181` — the `CORRECTED by P0-M1-FIX-9` blockquote misdescribes its own history: *"the same over-assertion the reconciliation directly above withdraws, standing one paragraph up"*. The reconciliation directly above (`RECONCILED by P0-M1-FIX-8`) withdrew a **term (i)** clause asserting "below"; FIX-9 withdrew a **term (ii)** clause asserting "exceeds" — same **class of** over-assertion, opposite directions, and the clause stands three blocks up, not one (`PROGRESS.md` and `NOTES.md`'s root-cause entry both carry the correct "class" qualifier; the published annotation dropped it). Fix: "the same **class of** over-assertion the reconciliation directly above withdraws" and replace "standing one paragraph up" with "in the paragraph above both corrections". Optionally append "(5 of the 82 registers)" to the adjacent "frequently zero" hedge so the canonical file carries its figure. Changes no figure, table or conclusion. Fifth instance of the class; per round-4 code review, batch its verification with the next verify pass rather than a standalone round. (From round-4 code review, 2026-08-18.) **Implemented 2026-08-18**: both wording corrections applied inside the existing `CORRECTED by P0-M1-FIX-9` blockquote (no fourth sibling blockquote); its dateline now reads `(2026-08-18; wording corrected by P0-M1-FIX-11 same day)`. The optional figure was appended as *"is frequently zero — only 5 of the 82 registers have any"* rather than the literal `(5 of the 82 registers)`: attached directly to "frequently zero" that parenthetical reads as *"zero in 5 of 82"* (the true count is 77), and the sibling blockquote above already names a different 5 (the registers coming in *below*), so the referent is stated explicitly. The corrected locator was checked against the block layout before writing — the withdrawn term-(ii) clause is in the italic paragraph that precedes all three blockquotes. `git diff -- cmd/` empty; `belmont validate` clean. One report-only observation from the mandated blockquote sweep, in pre-existing text and out of this task's scope: the blockquote's *"both quoted numbers were right"* is inherited from the first correction, which did quote two numbers (141,001 / 140,957) — the clause **this** blockquote withdraws quotes none. **Implemented (commit `979de03`) and promoted `[v]` 2026-08-18, round-5 verification**: PASSED 8/8 + APPROVED; "class of" present, locator true against the page (paragraph 137-150 precedes all three blockquotes), "only 5 of the 82 registers have any" re-derived exactly by both agents (feat-015 50, feat-020 32, feat-063 1, feat-070 44, feat-075 217), deviation from the literal parenthetical judged correct by both. The "both quoted numbers" back-reference ruled acceptable, not a Warning (141,001/140,957 sit in the same sentence as the withdrawn clause). No follow-up minted.
- [x] P0-M1-FIX-13: `baseline.json` was never touched by the 2026-08-19 re-scope, so it still carries the **withdrawn** capture procedure verbatim. `per_milestone_cost_baseline.how_to_capture` reads *"run at least one milestone to `[v]` in each repo with that binary"* — the exact method `BASELINE.md` §How to capture it marks **Withdrawn**, that `PRD.md` §Prohibited forbids by name, and that `PRD.md` §Constraints records as already violated once (four `belmont auto` runs, 13 commits on repo-3's `main`, two DB migrations). Not inert prose: `BASELINE.md` designates `baseline.json` the machine-readable companion and instructs M11/M13 to append to it and set `status`, so an agent reading the JSON alone receives a live instruction to do the prohibited thing. `git log` confirms none of `8acefe5`, `fb5580d`, `6139ea2` touched the file. Fix: replace `how_to_capture` with a pointer to `BASELINE.md` §How to capture it + `PRD.md` §Constraints/§Prohibited (two-arm design, seeded testbed, `belmont-pre`). Also correct the adjacent `reason` field and `BASELINE.md`'s numbered fact 2, which both assert *"Neither repo has ever recorded a metric … Verified, not assumed"* while the same file two sections later says six records exist at `~/repo-3/.belmont/metrics/feat-004.jsonl`; fact 3 carries a *(Revised 2026-08-19)* note and fact 2 does not. Restate fact 2 as true-at-capture-time and mirror it into the JSON. Changes no measured figure. (From verify round 6 code review, 2026-08-19 — Critical + Warning.)
  - **Done 2026-08-19.** `baseline.json`'s `how_to_capture` no longer carries the withdrawn method; it now names `BASELINE.md` §How to capture it, `PRD.md` §Constraints/§Out of Scope and `TECH_PLAN.md` §M12/§M13, and opens with the prohibition explicitly, because an agent reading the JSON alone was the failure mode. `status` and `reason` rewritten with it — `reason` now records the two-arm replacement and that six reference-only records exist. `BASELINE.md` fact 2 restated from present-perfect to true-at-capture-time, which is what made it contradict §Records that exist two sections below.
- [x] P0-M1-FIX-14: The read-path reproduction procedure pins the Belmont binary but not the repositories it measures. `BASELINE.md` §Reproducing the read-path half runs three commands against the **live** working trees of repo-3 and repo-4; neither `BASELINE.md` §What this was taken against nor `baseline.json` records those repos' revisions, so re-running the documented commands returns today's estate, not the baseline. Measured one day after capture: 3 of 43 registers have already moved — repo-3's register total +21,037 B, its max +14,371 B, its master register +12,593 B. `TECH_PLAN.md:427` says the figures "reproduce exactly", which holds for the capture and for 40/43 features but not for the procedure as written. M11/P3-3 scores this feature against this file, so without the pin, organic register growth is indistinguishable from the change's effect — the confound the control section exists to remove. Recoverable, not lost: `repo-3 af16dabd3411fc68852e51f3868d69f091326fb4`, `repo-4 2c1f7e857b30722018a3d860bb05a079f8daf1be`, noting `repo-4/feat-031`'s 129,354 B was the uncommitted working-tree state, committed verbatim the same day as `79fee00e`. Record the revisions in both files (`baseline.json` is the better home — `P0-16` already writes there), add `git -C <repo> rev-parse HEAD` to the reproduction block, and soften `TECH_PLAN.md:427` to name what it is exact against. (From verify round 6 verification, 2026-08-19 — Warning W1.)
  - **Done 2026-08-19.** Both measured repo revisions recorded in `BASELINE.md` §What this was taken against, with the +21,037 B / +14,371 B / +12,593 B one-day drift stated as the reason they are load-bearing. `git -C <repo> rev-parse HEAD` and a checkout step added to §Reproducing the read-path half. `TECH_PLAN.md`'s "reproduce exactly" softened to distinguish the **capture** (exact, 40/43 on re-run) from the **procedure** (was not re-runnable, because it pinned the binary and not the trees).
- [v] P0-M1-FIX-15: `BASELINE.md` claims the read-path figures cover "the estate" but scopes itself to two repos without saying so. The estate is the five repos `PRD.md` names and `P0-M1-FIX-2` re-established; `BASELINE.md` covers repo-3 and repo-4 only — **43 of 82 live registers (52%), 78.0% of register bytes**. Missing: repo-1 (10 registers, 574,327 B), repo-2 (18, 386,620 B), repo-5 (11, 219,500 B). Estate-wide *register sizes* do exist in `CENSUS.md` / `census.json` from the same day (P0-4), but `BASELINE.md` never points at it, and `status`-output sizes exist for no repo outside the two. One consequence is concrete: `repo-1/feat-020` (360,542 B) is one of census's seven `over_threshold_before` entries and therefore on P3-1's migration list, with no read-path baseline to be re-measured against at P3-3. Fix: state the coverage explicitly with those figures, cross-reference `census.json` as the estate-wide register-size record, and either capture `feat-020`'s three figures now (recoverable from git history) or record at P0-3 that P3-3 reports it unbaselined — per the PRD's own rule that an unmeasured half is declared, never estimated. (From verify round 6 verification, 2026-08-19 — Warning W2.)
  - **Done 2026-08-19.** `BASELINE.md` §What this was taken against now states the coverage as a row (43 of 82 live registers, 52%, 78.0% of bytes) and carries a §What this file does NOT cover block naming the three absent repos with their register counts and byte totals, cross-referencing `census.json` as the estate-wide size record. `repo-1/feat-020` is called out by name: on P3-1's migration list, no read-path baseline, and P3-3 reports it **unbaselined rather than estimated**.
- [v] P0-M1-FIX-16: `P0-M1-FIX-12`'s own shipped header records a figure its evidence contradicts. `cmd/belmont/metrics.go:42-48` states *"the three session-limit exits on 2026-08-18 produced exactly that"*, and `metrics_test.go`'s `TestSummariseFlagsZeroUsagePhasesWithoutExcludingThem` doc comment repeats it. `BASELINE.md` §Records that exist, corrected 2026-08-19, says the file holds six records of which four are non-zero — i.e. **two** zero-usage phases. FIX-12 shipped 2026-08-18, before the seven→six correction, and faithfully copied the pre-correction figure from its own task text. **Settled by measurement during verify round 6**: `belmont metrics --root ~/repo-3 --feature ai-cost-and-model-selection` returns 6 records with **2 flagged zero-usage** — the header is wrong, `BASELINE.md` is right. Correct both the header and the test comment to two. This is FIX-12's clause (c) surface ("say which in `metrics.go`'s header"), which is why FIX-12 is held at `[x]`. Changes no behaviour and no test assertion. (From verify round 6 code review, 2026-08-19 — Warning.)
  - **Done 2026-08-19.** `metrics.go`'s header and `metrics_test.go`'s doc comment corrected from three to **two**. Re-measured before writing rather than taking the task's word: `belmont metrics` over the six records returns `phases: 6, zero-usage: 2`. No behaviour and no assertion changed. This was `P0-M1-FIX-12`'s clause-(c) surface, so FIX-12's only outstanding objection is now discharged.
- [x] P0-M1-FIX-17: `docs/branch-triage.md` no longer covers the backlog, so `P0-13`'s first acceptance clause (*"Every unmerged branch carries a recorded verdict"*) does not hold. `git branch -r --no-merged origin/main` returns **38**; the file's headline, its `## Verdicts — all 35 branches` section title, its line-227 derivation and its Revisions footer all still say **35**, and three branches have no verdict anywhere in it: **`origin/fix/issue-sweep`** (9 commits, 33 files, +1,336/−211, forked from `17897aa`, pushed after the triage landed), `upstream/belmont` and `upstream/v1-rebuild`. `origin/fix/issue-sweep` is not stale work — it rewrites `skills/belmont/_partials/dispatch-strategy.md`, `_partials/loop-recipe.md`, `_src/{implement,loop,next,status}.md` and adds `knowledge/cross-cutting/dependency-eligibility.md`; `dispatch-strategy.md` is **M8/P1-11's own deliverable**, which is exactly the shared-file collision `P0-13` exists to find before merge time. Fix: re-run §How this was measured, add a verdict row for each of the three, and correct all four count sites plus the footer. Decide deliberately whether `upstream/*` is in scope for "the backlog" and say so in the document rather than leaving it implied. (Confirmed independently by both agents and by the orchestrator, verify round 7, 2026-08-19.)
  - **Done 2026-08-19, and the finding's own count was one of the things corrected.** Re-measured after `git fetch --prune`: **36** in-scope branches, not 38. `docs/branch-triage.md` §How this was measured has carried `grep -v upstream/` since the first triage, so `upstream/belmont` and `upstream/v1-rebuild` were never in the backlog — they are the third party's branches and this fork proposes no verdict on them. The file was **one** verdict short, not three. The scope now says so in prose in §Headline rather than living inside a shell filter, which is what let a re-run reach 38 and read the file as stale.
  - **`origin/fix/issue-sweep` verdicted `merge`, and it is the real finding.** 9 unlanded commits (`git cherry`: 9 `+`, 0 landed), 33 files, +1,336/−211, closing #54/#56/#58/#59/#61; `git merge-tree` gives **zero conflicts** against `main` today. It rewrites `_partials/dispatch-strategy.md` (M8/P1-11's deliverable), `_partials/loop-recipe.md` and `_src/{implement,loop,next,status}.md` (M2/P1-5's). **Recommended to land before wave 2 and deliberately not merged here** — a 33-file merge touching two milestones' deliverables is an owner call, not a documentation task's.
  - Counts corrected at all four sites plus the Revisions footer; §Headline now reads 36 / 30 dead / 3 merge / 3 rebase. Revisions entry also warns that pre-rewrite SHAs in the file are unreachable and to match commit subjects (`P0-M1-FIX-25`).
- [v] P0-M1-FIX-18: `tailWriter` is written concurrently by two goroutines with no mutex, on every tool path. `attachUsageCapture` (`auto_loop.go:596-612`) sets `cmd.Stdout` to a wrapper (`claudeStreamWriter` for claude, `usageCapture` for codex) while leaving `cmd.Stderr = tw`. `os/exec`'s `childStderr` dedups to a single pipe and a single goroutine only when `interfaceEqual(c.Stderr, c.Stdout)`; the wrapper breaks that equality, so two pipes and two copy goroutines both reach `tailWriter.Write`, which appends to unguarded `tw.buf` **and** (when `prefix` is set, i.e. every non-claude tool) `tw.lineBuf`. Consequence: interleaved output and a corrupt `executionResult.Output`, which feeds error reporting and `extractDecisionJSON`. The suite never drives a real `exec.Cmd` through this path, which is why `-race` is green. Fix: add a `sync.Mutex` to `tailWriter` guarding `Write`; add a test that drives two concurrent writers through one `tailWriter` under `-race`. The same shape exists at `repair.go:1181` and `reverify.go:194` — one mutex covers all of them. (From verify round 7 code review, 2026-08-19; orchestrator re-confirmed the `interfaceEqual` mechanism against GOROOT.)
  - **Implemented 2026-08-19.** `sync.Mutex` added to `tailWriter` (`render.go`), guarding `Write` **and** `String` — `String` reads `tw.buf` and raced with the writers too, which the finding did not name. One mutex on the type covers all three construction sites (`auto_loop.go`, `repair.go:1181`, `reverify.go:194`) as predicted; no call site changed.
  - **The test was proved to catch it before being kept.** `TestTailWriterConcurrentWriters` (new, `tailwriter_test.go`) drives 8 concurrent writers and 2 concurrent `String` readers through one prefixed `tailWriter`. With the mutex reverted it reports `DATA RACE` under `-race`; with it, clean. It also asserts no byte is lost and no line is spliced or double-prefixed, because the production symptom is a corrupt `executionResult.Output` reaching `extractDecisionJSON`, not a crash.
  - **`go test -race ./cmd/belmont` added to CI** (`.github/workflows/ci.yml`), which is the mechanism half of `P0-M1-FIX-19`. Done here rather than in the prose batch because shipping the fix without the guard that catches its class leaves the next one equally invisible. FIX-19's remaining half — restating `fsutil_test.go:22`'s comment as an OS-atomicity assertion — is untouched.
  - Gates after the change: `go build`, `gofmt -l cmd/` clean, `go vet ./...` clean, `go test -race -count=1 ./cmd/belmont` ok 26.281s, `belmont validate` clean.
- [x] P0-M1-FIX-19: `P0-1`'s central proof is mis-described and its stated precondition is never run. `cmd/belmont/fsutil_test.go:22` says the read-during-write proof "is meaningful only under `go test -race`", but (a) no `-race` step exists anywhere in `.github/workflows/ci.yml` or in `CLAUDE.md` §Verify, so on the claim's own terms the proof is unrun in CI, and (b) the claim is false — writer and reader share no Go memory except channels, so the test asserts *filesystem* atomicity and `-race` is orthogonal to it. Meanwhile the race `-race` would catch is `P0-M1-FIX-18`. Fix: add `go test -race ./cmd/belmont` to CI, and restate the comment as an OS-atomicity assertion. (From verify round 7 code review, 2026-08-19.)
  - **Done 2026-08-19, both halves.** The CI step landed with `P0-M1-FIX-18` (`go test -race ./cmd/belmont`). The comment at `fsutil_test.go:22` is restated: the test asserts **filesystem** atomicity, a property of `rename(2)`, so `-race` was never its precondition — and it now points at `TestTailWriterConcurrentWriters` as the test that genuinely needs `-race`.
- [v] P0-M1-FIX-20: `P0-M1-FIX-7`'s defect survives one level down. A census *root* that cannot be read is now a reported failure; a *register* that cannot be read is still swallowed. `extract.go:220-228` buckets every `os.ReadFile(progressPath)` error into `rep.NoRegisterDirs++` with no `os.IsNotExist` distinction, so an EACCES/EIO register is indistinguishable from a genuinely absent one, `LiveRegisters` silently undercounts, every derived figure shrinks — and `coverage_complete` still reports `true`, which is the honesty guarantee FIX-7 added. This is the same shape as the `os.IsNotExist(err) -> continue` line that produced 138/65 instead of 168/82. `TestCensusUnlistableRootIsReportedNotAborted` is root-level only. Fix: distinguish `os.IsNotExist` from other errors, route the rest into `UnreadableRoots` or a sibling `unreadable_registers` field so `CoverageComplete` reflects them, and add the register-level analogue of the existing test. (From verify round 7 code review, 2026-08-19.)
  - **Implemented 2026-08-19.** `os.IsNotExist` now separates the two cases at register level: a genuinely absent register still counts into `dirs_without_register`, while an unreadable one goes to a new `unreadable_registers` field (feature, path, reason) and is excluded from `NoRegisterDirs`. `CoverageComplete` is now `len(UnreadableRoots)==0 && len(UnreadableRegisters)==0`, so the honesty guarantee FIX-7 added at root level holds one level down.
  - **Both renderers had to change too, which the finding did not name.** `renderCoverage` and `renderCoverageFooter` derived their text solely from `UnreadableRoots`, so with coverage now also falsifiable by a register they would have printed `INCOMPLETE — 0 of 5 requested roots could not be read` — a self-contradicting line a reader dismisses. Each cause is now named separately, in the header and the footer.
  - `TestCensusUnreadableRegisterIsReportedNotSwallowed` (new): two features, one readable and one chmod-000, asserting `coverage_complete=false`, exactly one entry in `unreadable_registers`, `dirs_without_register == 0` (the register exists), `live_registers == 1` (the walk continues), and that the run refuses without `--allow-unreadable-roots`.
  - Gates: `gofmt` clean, `go vet ./...` clean, `go test -race -count=1` ok 26.9s, Tier 1 evals ok 23.4s, `belmont validate` clean.
- [x] P0-M1-FIX-21: `CENSUS.md:172` — *"only 8 registers balance two non-zero terms"* asserts beyond its evidence **and** contradicts a sibling clause eleven lines below it. The 8 is derived as 75 equal − 67 equal-at-zero, which establishes only that those registers are equal at a **non-zero value**; it says nothing about terms (i) and (ii) individually, and "two non-zero terms cancelling exactly, eight times over" is not the natural reading of the data. It is also arithmetically incompatible with `CENSUS.md:183` (`P0-M1-FIX-11`'s added figure, independently re-derived as exact): *"Term (ii) … only 5 of the 82 registers have any"* — at most 5 registers can have a non-zero term (ii), so 8 cannot balance two non-zero terms. Fix: restate as "8 are equal at a non-zero value", which is all the arithmetic supports, or measure the per-register term-(ii) count and reconcile both clauses in one edit. **Sixth instance of the class** FIX-6 → FIX-8 → FIX-9 → FIX-10 → FIX-11 chased, and the first found inside a correction that had itself already been verified twice. (From verify round 7 code review, 2026-08-19; orchestrator re-read both clauses on the page.)
  - **Done 2026-08-19.** Clause withdrawn and restated as *"the remaining 8 are equal at a non-zero value"* — the whole of what 75 − 67 supports. The trailing *"neither term dominates estate-wide"* sentence is withdrawn too and **not replaced**: characterising the terms individually needs a per-register measurement of both that no document here has taken, and inventing one is the defect. A `CORRECTED by P0-M1-FIX-21` blockquote records the incompatibility with the 5-of-82 figure eleven lines below.
  - **The class is now recorded as a root-cause pattern**, not just fixed a sixth time — `NOTES.md` §Root Cause Patterns: verify the *paragraph*, not the clause. Every round so far was scoped to the objected clause and each shipped a fresh relational claim beside it.
- [v] P0-M1-FIX-22: `autocmd.go:342-343` claims *"The commit carries an explicit pathspec, so unrelated work (possible under `--allow-dirty`) is never swept in."* The pathspec scopes to `.gitignore` — it does not exclude a **pre-existing uncommitted edit to `.gitignore` itself**, which `git add -- .gitignore` stages and the commit then lands under the message `belmont: gitignore .belmont/metrics/`. Reachable whenever `--allow-dirty` is used and the user has an unstaged `.gitignore` change that lacks the metrics rule. Fix: either narrow the claim to "nothing outside `.gitignore`", or skip the auto-commit when `git diff --quiet -- .gitignore` already fails before the append. Add the test; the round-6 notes record this path as untested. (From verify round 7 code review, 2026-08-19.)
  - **Fixed in code, not narrowed in prose 2026-08-19.** The finding offered either; narrowing would have documented the bug. `ensureMetricsIgnored` now samples `gitPathIsClean(root, ".gitignore")` **before** the append — after it, the write is itself the dirt — and skips the commit when the file was already dirty, warning instead. The ignore rule is still written, which is the guarantee that matters; what is dropped is committing somebody else's edit under a Belmont message.
  - `gitPathIsClean` (new, `guards.go`) fails **closed**: git unable to answer reads as "not clean", because the caller is deciding whether a commit would sweep in another person's work. `TestEnsureMetricsIgnoredLeavesADirtyGitignoreUncommitted` (new) was reverted-and-checked — it fails with the exact message `Belmont committed the user's edit` without the guard.
- [x] P0-M1-FIX-23: The two CLI surfaces M1 shipped are undocumented, against `CLAUDE.md`'s standing rule that README and `docs/` are kept current. `belmont metrics` (`P0-2`) and `belmont extract` (`P0-4`) appear in `printUsage` but in **neither** README's command block **nor** `docs/cli-commands.md` §Usage, which lists every other command including `repair`, `blockers` and `validate`; `.belmont/metrics/` is absent from `docs/directory-structure.md`. Verified by grep: zero hits for either command across `README.md` and `docs/`. Fix: add a section for each to `docs/cli-commands.md` — `--allow-unreadable-roots`' coverage semantics and `summariseMetrics`' null-with-reason behaviour both need explaining — plus README one-liners and a `.belmont/metrics/` row in `docs/directory-structure.md`. (From verify round 7 code review, 2026-08-19; orchestrator confirmed the grep.)
  - **Done 2026-08-19.** `belmont metrics` and `belmont extract` documented in all three surfaces: `README.md` (worked examples in the auto section), `docs/cli-commands.md` (a section each), and `AGENTS.md` (added to the command inventory sentence and described alongside the others).
  - Documented the **rules**, not just the flags, because the flags are already in `belmont help` and it is the rules that a caller gets wrong: metrics never estimates and reports `null` with a reason; `input_semantics` refuses to aggregate across tools; zero-usage phases are flagged, not dropped. For extract: `--dry-run` is mandatory, `--roots` must be absolute (`~` expands only in the first word), and an unreadable root **or register** refuses rather than shrinking the denominator.
  - **Supersedes `P3-4-REVIEW`**, which the 2026-08-19 plan review had filed against M11 for exactly this work before round 7 minted this task. That one is now `[-]`; see Decisions Log.
- [x] P0-M1-FIX-24: Both §Reproducing sections now read as reproducible and are not. The 2026-08-19 estate redaction replaced real repository paths with the aliases `repo-1`…`repo-5` (625 substitutions; the map is held outside the repo at `~/belmont/private/estate-alias-map.json`), so `CENSUS.md` §Reproducing this run verbatim resolves every root to nothing and `BASELINE.md` §Reproducing the read-path half has the same problem. Neither section says the roots are aliases or names the substitution source, and `BASELINE.md`'s block writes bare `belmont status …` when the figures only reproduce against the pinned v0.11.0 binary — stated at the top of the document but not inside the block a reader copies. Same class as FIX-6…FIX-11: a document asserting more than it can deliver. Fix: state the aliasing and name the map in both sections, and move the pinned-binary requirement inside the copied block. (From verify round 7, 2026-08-19.)
  - **Done 2026-08-19.** Both §Reproducing blocks now carry an explicit note that their paths are **aliases, not paths**, naming `~/belmont/private/estate-alias-map.json` as the only place the mapping exists and stating that substitution is the last step before running, never something committed back. `CENSUS.md`'s note also scopes its coverage claim to the run that produced the document, made before the redaction — the claim was true and was being read as a promise the printed command still runs.
- [x] P0-M1-FIX-25: Every commit SHA quoted in M1's task bodies is unreachable from HEAD. The 2026-08-19 `git filter-repo` redaction rewrote `main`, so `c952426c` (`P0-3`), `17847b8f` (FIX-9), `c682b8a7` (FIX-10), `acd11dc8` (FIX-11) and `23e71008` (FIX-3) now resolve only as unreachable objects that `git gc` will collect, and none exists on `origin` or in any other clone. The post-rewrite equivalents are `ff06d45`, `5dff57b`, `aad1360`, `979de03`, `24db490`. **The commit-evidence guard is unaffected** — all 25 M1 task IDs still have reachable commits naming them, so `lookupCommitEvidence` and `runEvidenceCheck` pass; the damage is to the human audit trail M11 will follow. Fix: refresh the SHAs, or replace bare SHAs with commit subjects, which survive a rewrite. This is a consequence of a deliberate cross-cutting act rather than a defect in any one task, so no task was held at `[x]` for it. (From verify round 7, 2026-08-19.)
  - **Done 2026-08-19.** Live files rewritten in place — `PROGRESS.md`, `TECH_PLAN.md`, `NOTES.md`, `docs/branch-triage.md`. Each new SHA was verified by matching its commit subject to the task it belongs to before any substitution, and all five old SHAs confirmed unreachable from HEAD.
  - **`MILESTONE-M1.done.md` is annotated, not rewritten** — it is an archive, and the convention `P0-13a` set for the stale-remote figure is to annotate. A block at its head carries the remap and tells a reader to resolve any other SHA by commit *subject*, which is what actually survives a rewrite. This line's own body keeps both old and new SHAs on purpose; it is the record of the remap.
  - **Not fixed by refreshing hashes alone.** `1129cf84` (on `origin/feat/maintenance-ci`) was **not** rewritten — only `main` was in scope — so a blanket "all SHAs are stale" claim would itself have been false. The audit trail M11 follows is now correct; the commit-evidence guard was never affected, as this task already recorded.

- [x] P0-M1-FIX-26: `BASELINE.md` + `baseline.json` reconciliation sweep — verify the document, not the clause. Four findings, one file pair, deliberately grouped because five prior rounds each fixed the clause they were pointed at and left the same claim standing elsewhere in the same file.
  - **Closed 2026-08-19 — (a) and (e) done; the finding's own diagnosis of (a) was wrong and is corrected here.** Published p90 was **not** linear-interpolation-lower: that yields 103,406.6 and 108,751.2, matching neither repo. Re-deriving every candidate rule against both published values identifies it exactly as `sorted[int(p*(n-1))]` — off by one in the index base against the shipped `sorted[int(p*n)]`. The median was mean-of-two-middles, as stated.
  - **Documents moved to the shipped rule, not the code to the documents.** M11/P3-3 re-measures with `belmont extract`, so the baseline must be re-derivable by the statistic the shipped code produces or the comparison is between two different quantities. Changing `percentile` would instead have silently restated every figure in `CENSUS.md` — which two agents independently re-derived — and `CENSUS.md`'s p90 (71,379) was verified to already match the shipped rule exactly, so only `BASELINE.md`/`baseline.json` had diverged.
  - Restated: repo-3 p90 100,523 → **114,941**; repo-4 median 17,089 → **20,297**, p90 60,678 → **129,354**. **No measurement changed** — totals and maxima reproduce byte-for-byte and every per-feature figure is untouched. The rule is now stated in both documents, `percentile_rule` added to `baseline.json`, and §Reproducing carries the derivation (it previously documented three `wc -c` commands and no aggregate rule at all, so P0-3's reproduce-from-documented-commands criterion was unmet). Verified: the documented derivation reproduces every aggregate row exactly. **(e)** done — the `belmont version` pin now sits inside the copied command block.
  - **Partially done 2026-08-19 — (b), (c) and (d) closed; (a) and (e) remain open, which is why this stays `[ ]`.** (b) `measured_repo_revisions` added to `baseline.json` with both revisions and the `feat-031` working-tree caveat — FIX-14 named this file the better home and only `BASELINE.md` had got them. (c) closed as a side effect of `P0-M1-FIX-27`'s enumeration: the orphaned `belmont_commit` was remapped to its reachable equivalent in both files. (d) the lead paragraph's present-tense *"no `.belmont/metrics/` directory exists"* re-tensed to capture time, matching what FIX-13 did to fact 2 two lines below.
  - **(a) is a Critical and is deliberately untouched**: the published median/p90 use linear-interpolation-lower and mean-of-two-middles, while shipped `percentile` uses `sorted[int(p*n)]`. Reconciling them means deciding which rule is correct and re-deriving every quoted figure under it — a measurement task, not a wording one, and doing it badly is how this milestone got here. **(e)** (move the pinned-binary requirement inside the copied command block) is small and simply not reached.
  - **(a) Critical — the published median/p90 are derived by an undocumented rule incompatible with the shipped one.** `BASELINE.md:52-54` / `baseline.json` use linear-interpolation-lower for p90 and mean-of-two-middles for median; `cmd/belmont/extract.go:310` (`percentile`) uses `sorted[int(p*n)]`, which is what `CENSUS.md` publishes. Re-derived independently at verification time: repo-4 p90 = **60,678 published vs 129,354** under the shipped rule (2.13x), median 17,089 vs 20,297; repo-3 p90 = 100,523 vs 114,941. Neither document states its convention, and §Reproducing documents only three `wc -c` commands and no derivation for the aggregate rows — so P0-3's "reproduce from the documented commands" criterion is not met, and M11/P3-3 would compare two statistics that are not the same statistic. Fix: state the rule in both documents, or recompute with `extract.go`'s `percentile`.
  - **(b) `P0-M1-FIX-14`'s second half never landed.** FIX-14 requires the measured repo revisions in *both* files and names `baseline.json` "the better home". Confirmed at verification: `baseline.json` has no revision field at any level (top-level keys are `captured`, `captured_by`, `belmont_version`, `belmont_commit`, `go_toolchain`, `bytes_per_token`, `orchestrator_binary`, `read_path_baseline`, `per_milestone_cost_baseline`). The machine-readable companion M11/M13 append to cannot pin the trees.
  - **(c) The reproduction pin is unreachable.** `BASELINE.md:12` and `baseline.json.belmont_commit` carry `fc33be901b4fd04e0196b50f780810a8fe52d575`, orphaned by the 2026-08-19 `filter-repo` and `git gc`-collectable. Post-rewrite equivalent, verified reachable and subject-matched: **`fc33be9`** ("P0-2: Token and wall-clock instrumentation"). Unlike the other rewritten files, neither carries an annotation warning the reader.
  - **(d) `BASELINE.md:81` still carries the claim `P0-M1-FIX-13` re-tensed two lines below it.** The lead paragraph reads *"no `.belmont/metrics/` directory exists in either repo"* (present tense), contradicted by §Records that exist at `:232`, which names six records under `~/repo-3/.belmont/metrics/feat-004.jsonl`. FIX-13 tensed **fact 2** (`:87`) for exactly this reason — and its own parenthetical at `:90-92` acknowledges the contradiction — while leaving the identical claim standing in the paragraph above.
  - **(e) `P0-M1-FIX-24`'s second clause is unmet.** "Move the pinned-binary requirement inside the copied block" — `BASELINE.md:249-262` still writes bare `belmont status …`; the v0.11.0 pin remains only at the top of the document. On this machine `belmont` happens to be the pinned build; on any other it silently is not.
- [x] P0-M1-FIX-27: Complete `P0-M1-FIX-25` by enumeration rather than by known-ID list. FIX-25 refreshed the five SHAs it was looking for and never enumerated the files it claims it rewrote in place, so four bare pre-rewrite SHAs survive. All four confirmed at verification time with `git cat-file -e` (object survives) + `git merge-base --is-ancestor` (fails) + subject match on the replacement:
  - **Done 2026-08-19 by enumeration, and the enumeration changed the answer.** Every 7–40 char hex token in every tracked `.md`/`.json` was tested: is it a commit object, is it unreachable from HEAD, and is it reachable from **any ref**. That last test is what FIX-25's known-ID list and a naive subject match both get wrong. **30** dangling pre-rewrite objects remapped across 6 live files (12 lines); **10** left untouched because they are reachable from a remote branch and referencing them is correct — including `1129cf84`, whose subject matches its own cherry-pick `24db490` on main, so subject-matching alone would have destroyed the distinction the branch-triage row depends on. 0 unresolved.
  - Two intermediate bugs of my own, both caught before writing: truncating subjects to 70 chars made the first match run return 0 remappable, and matching by subject alone would have rewritten the 10 branch references. The archive stays annotated rather than rewritten, per convention.
  - `PROGRESS.md:12` (P0-13a's own body) — `17897aa2..3125a067`, where `3125a067` -> **`3125a06`** ("belmont: settle M1"). Missed because it is written as a range. This is inside an M1 task body, precisely FIX-25's defined scope.
  - `NOTES.md:104` — `ad8da7ec` -> **`ad8da7e`** ("P0-M1-FIX-8: Replace the over-asserted term-(i) clause…"). Bare SHA, no remap note anywhere in the file.
  - `docs/branch-triage.md:3` — `4f71915c` -> **`4f71915`** ("P0-12: Bring the toolchain onto a supported version"). This is the provenance anchor for every verdict in that document; partially mitigated by the footer's blanket "match subjects" caveat, but not refreshed.
  - `BASELINE.md:12` + `baseline.json.belmont_commit` — see `P0-M1-FIX-26(c)`; fix in whichever task lands first, not twice.
  - **Not defects, do not "fix" these**: `1129cf84` / `7a9b7b56` / `1fcbc7d4` / `39c5f219` / `db254236` live on unrewritten remote branches and resolve correctly. `TECH_PLAN.md:40`'s `d13f0fbf` -> `ff11815` is a deliberate **old->new pairing** recording the force-push, checked at verification; the pre-rewrite SHA belongs there. `MILESTONE-M1.done.md` is covered by its declared annotate-don't-rewrite convention.
- [x] P0-M1-FIX-28: Discharge `P0-M1-FIX-23`'s two unmet clauses and correct two factual errors it shipped. The prose sections it added (`docs/cli-commands.md:173`, `:188`) are good and the documented flags match `printUsage`; these are the gaps around them.
  - **Done 2026-08-19, all four.** (a) `.belmont/metrics/` row added to `docs/directory-structure.md`, noting gitignored/local-only and both its writer and reader. (b) both commands added to `docs/cli-commands.md` §Usage, so the page's own index no longer says they do not exist. (c) three→**five** hosts that cannot report usage — `toolReportsUsage` returns true for `claude` and `codex` only, so gemini and cursor record null too, and the correction says why (schema never verified against a live run). (d) "above"→"below", the preflight section is at `:250`.
  - **(a) `docs/directory-structure.md` has no `.belmont/metrics/` row** — FIX-23 requires one explicitly. Confirmed: zero occurrences of "metrics" in the file; the `.belmont/` tree at `:86-100` does not mention it.
  - **(b) `docs/cli-commands.md` §Usage (`:5-56`) omits both new commands** — it lists `install`, `update`, `status`, `auto`, `reverify`, `repair`, `blockers`, `sync`, `recover`, `steer`, `validate`, `version` and neither `metrics` nor `extract`, so the page's own index says the commands do not exist. FIX-23's finding named §Usage as one of the two gaps.
  - **(c) `docs/cli-commands.md:181` names three tools that cannot report usage; there are five.** `usageUnavailableNote` (`metrics.go:185-191`) covers copilot, pi, opencode **plus gemini and cursor**; `toolReportsUsage` (`:196-202`) returns true only for `claude` and `codex`. A Gemini or Cursor user reads this and expects figures they will never get.
  - **(d) `docs/cli-commands.md:179`** — "see the clean-tree preflight **above**"; that section is at `:250`, below.
- [x] P0-M1-FIX-29: Reconcile two documents with decisions that were taken the other way. Both were edited *after* the decision landed without picking it up.
  - **Done 2026-08-19, all three.** (a) `CENSUS.md` §Consequences item 1 asserted the M4-before-M3 restructure as settled and cited `[!] P0-4a` as a live escalation at two sites; `P0-4a` resolved 2026-08-18 with **no restructure** and is `[v]`. Restated so the antecedent (five registers over the ceiling) stands while the consequent is recorded as measured and declined, with the five-wave/2.20× costing that decided it.
  - (b) `docs/branch-triage.md` §Not done here still called `origin/main` **157 commits behind** and P0-13a a live blocker. Both false, and the 157 was wrong when written — read from a stale remote-tracking ref; the real gap was 20 commits, all from that session. Annotated rather than silently rewritten, because the wrong figure is the evidence for the root-cause pattern it produced.
  - (c) is the one with teeth, per `AGENTS.md`'s rule that every follow-up needs a documented home: `P0-4a`'s *"extraction alone does not meet the 25,000-token read budget"* lived only inside `P0-4a`'s own task body. It now sits in `TECH_PLAN.md` §P1-1/P1-2/P1-3 (M3), with the two consequences spelled out — M3's acceptance criteria are round-trip integrity and deliberately **not** any register landing under the ceiling (a criterion M3 cannot meet alone would fail it for M4's absence), and no M3 report may claim the read budget is met.
  - **Not started.** Out of scope for the single sweep pass agreed 2026-08-19; recorded here so it is not mistaken for done. Its three parts are factual reconciliations against decisions already taken: (a) `CENSUS.md` still asserts the restructure `P0-4a` declined, (b) `docs/branch-triage.md` still calls `origin/main` 157 commits behind and P0-13a a live blocker, both false and both already corrected in P0-13a's own body, (c) `P0-4a`'s "Carried forward to M3" note still has no destination in M3's tasks or the TECH_PLAN — the one with real consequences, since it is the AGENTS.md rule about follow-ups needing a documented home.
  - **(a) `CENSUS.md:236-241` and `:123` still assert the restructure `P0-4a` declined.** §Consequences item 1 reads *"M4's read path becomes a prerequisite of M3, not its successor"* and cites `[!] P0-4a` as a live escalation at two sites. P0-4a resolved 2026-08-18 with **no restructure** ("leave the graph at four waves") and is now `[v]`. CENSUS.md was edited twice afterwards (FIX-21, FIX-24) without reconciling. Every other superseded claim in this document carries a WITHDRAWN/CORRECTED blockquote; this one has none — and it is the document M11/P3-1 reads as its migration authority.
  - **(b) `docs/branch-triage.md:203-214`, `:241` still assert `P0-13`'s second clause is unsatisfied.** §"Not done here" says `origin/main` is *"**157 commits behind**"* and *"tracked as a blocked task"*. Both false: `git ls-remote` shows `refs/heads/main` == HEAD, and P0-13a is resolved. Worse, P0-13a's own body records 157 as *itself wrong* — measured off an unfetched tracking ref, real figure 20 — and lists where the bad figure survives without naming this mainline doc, which carries it three times.
  - **(c) `P0-4a`'s "Carried forward to M3" note has no destination.** `PROGRESS.md:27` records that extraction alone does not meet the 25,000-token read budget for the five registers in `CENSUS.md`, and that M3 should say so. That appears nowhere but inside P0-4a's own body — not in M3's tasks, not in `TECH_PLAN.md`. Per `AGENTS.md`, every discovered follow-up needs a documented destination. **M3 is an existing milestone; route it there via `/belmont:tech-plan`, never by minting a milestone.**
- [x] P0-M1-FIX-30: `NOTES.md:213` restates as fact the relation `P0-M1-FIX-21` withdrew the previous day — the seventh instance of the class, in the very section FIX-21 nominated as its prevention record. It reads *"75 of the **77 term-(i)=0 registers** are equal, only 2 exceed."* The 77 is derived as 75 equal + 2 above, which is the exact labelling move FIX-21 withdrew: equality only proves term(i) == term(ii), so an equal-at-non-zero register with both terms positive is **not** term-(i)=0. With term (ii) non-zero in 5 registers (2 of them the "above" pair), the supported bound is **74-77**, not exactly 77. Restate to what the arithmetic supports, or measure term (i) per register. Note this is also the miss `P0-M1-FIX-10` was raised to prevent — NOTES.md carrying a relation CENSUS.md had already withdrawn.
  - **Done 2026-08-19.** `NOTES.md` restated: *"75 registers are equal and 2 exceed"*, with the withdrawal recorded inline. The `77` was 75 + 2 relabelled as a term-(i)=0 count, which equality does not establish; the supported bound is 74–77. Found by sweeping the estate for the class rather than the site — which is what my own `P0-M1-FIX-21` pass should have done and did not.
- [x] P0-M1-FIX-31: `runCensus` double-counts a duplicated root and still reports `coverage_complete: true`. `extract.go:215` iterates `roots` with no dedup and `:477-493` only `filepath.Abs`es each entry. Reproduced at review time: `belmont extract --dry-run --root /tmp/censustest --roots /tmp/censustest --format json` returns `"feature_dirs": 2, "live_registers": 2`, the same feature listed twice, `"coverage_complete": true`. The likely trigger needs no flag mistake — `--root` defaults to `"."`, so running the documented five-repo command from *inside* one of the listed repos doubles that repo. This defeats the honesty guarantee `P0-M1-FIX-7` and `P0-M1-FIX-20` exist to provide: a doubled denominator reads exactly like a correct one. Dedup after `filepath.Abs` (ideally after `filepath.EvalSymlinks`, so a symlinked duplicate collapses too), report or drop the collapsed entries, and add the regression test. **The shipped `census.json` is unaffected** — its five roots are distinct — so this is a defect in the tool, not in the published census.
  - **Implemented 2026-08-19.** `dedupeRoots` (new) collapses roots naming the same directory, after `filepath.EvalSymlinks` so a symlinked alias collapses too; collapsed entries are reported on stderr, never silently dropped. Resolution failure is **not** fatal — an unresolvable root is kept and left for `runCensus` to report as unreadable, keeping identity and readability as separate questions.
  - Two tests: `TestDedupeRootsCollapsesTheSameDirectory` (order preserved, symlinked alias collapses) and `TestCensusDoesNotDoubleCountADuplicatedRoot` (end-to-end — `feature_dirs`, `live_registers`, `total_bytes` and the features list all counted once). The trigger needed no flag mistake: `--root` defaults to `"."`, so the documented five-repo command run from inside one of those repos doubled it.
- [x] P0-M1-FIX-32: `P0-M1-FIX-19`'s correction reached `fsutil_test.go` but not the two other places carrying the same disproved claim, and one of them now also asserts a CI fact `P0-M1-FIX-18` made false. `NOTES.md:95` explicitly named **both** `state-atomicity.md` and `fsutil_test.go`, so the miss is against a note this run wrote for itself.
  - **Done 2026-08-19 by enumeration.** Four live sites, not the two the finding named: `knowledge/cross-cutting/state-atomicity.md` (the claim **and** its now-false "not in ci.yml" line), `NOTES.md:118` (same CI claim, struck through with the resolution), and `TECH_PLAN.md:421` (*"Proof: a concurrent read-during-write test under `go test -race`"* → the negative control, which is what actually makes it a result rather than a test that cannot fail).
  - `knowledge/cross-cutting/state-atomicity.md:41` — *"is meaningful **only under `go test -race`** — an atomicity claim with no race test is an assertion, not a result"* is verbatim the claim FIX-19 established is wrong: the test asserts filesystem atomicity via `rename(2)`, and writer and reader share no Go memory, so `-race` is orthogonal. Restate it, and point at `TestTailWriterConcurrentWriters` as the test that genuinely needs `-race`.
  - `knowledge/cross-cutting/state-atomicity.md:43` — *"`go test -race ./cmd/belmont` is not in `ci.yml` as of this entry"*. It is, since `ebb73dd` (`.github/workflows/ci.yml:33-34`). Verified at verification time.
  - `TECH_PLAN.md:421` (P0-1's own spec section) carries the same withdrawn sentence.
  - Per `AGENTS.md`: amend the knowledge entry **in place** and add one `Revisions` line, do not append a dated block.

### M2: The verify read (depends: M1)
*Highest-scoring item in the backlog. Stop reading the same archives three times and stop recommending the largest read path.*
- [ ] P0-5: Read archived milestone records once
- [ ] P0-6: Stop recommending the largest read path
- [ ] P0-7: Give verification a coordination document

### M3: Split the register safely (depends: M1)
*Separate narrative from register — 62% of the worst file is indented detail already separable by indentation, so an extraction rather than a re-modelling — and close the merge holes that splitting would otherwise widen.*
- [ ] P1-1: Separate narrative detail from the register
- [ ] P1-2: Validate index and detail integrity
- [ ] P1-3: A durable grammar for detail records
- [ ] P1-14: Refuse a merge that loses state
- [ ] P1-15: Close the verified-state laundering path

### M4: The read path (depends: M3)
*Serve one milestone's slice on demand and make it the mandatory path. This is where most of the measured saving is realised.*
- [ ] P1-4: Serve a single milestone's slice on demand
- [ ] P1-5: Make the cheap path the mandatory one

### M5: Growth ceilings (depends: M3)
*Without a write-time ceiling an extracted register regrows within a quarter. Warnings alone are documented as insufficient.*
- [ ] P1-6: Configurable size ceilings
- [ ] P1-7: Refuse to start over the ceiling
- [ ] P1-8: Reject oversized writes at the source

### M6: Bound the loop (depends: M1)
*58% of a failing run's tokens are spent after the failure was detectable. Largest reliable saving available, and none of it costs a quality check.*
- [ ] P0-8: Detect failure before the budget is spent
- [ ] P0-9: Cap the verify-and-fix cycle at two rounds
- [ ] P0-10: Start every retry from clean context
- [ ] P0-11: Gate the expensive agents behind deterministic checks

### M7: Skill payload (depends: M1)
*Stop shipping every host's prose to every host. Multi-host support is retained; only the per-host build contents change.*
- [ ] P1-9: Stop shipping every skill's shared prose to every skill
- [ ] P1-10: Prove the skill payload shrank

### M8: Cacheable dispatch (depends: M7)
*Move 91 KB of agent definitions into the cacheable prefix. Goes through the eval harness, and reverts on a negative result.*
- [ ] P1-11: Put agent definitions where the cache can hold them
- [-] P1-12: Stop invalidating the cache with steering text — withdrawn during tech-plan: measured identical cache figures prefixed and suffixed (proposal 0004 rev 2)
- [ ] P1-13: Verify the caching change against the harness

### M9: Master file & journal (depends: M1)
*The master file is 420 KB of hand-appended narrative nobody authors. Generate it; move activity to an append-only record that merges by union.*
- [ ] P2-1: Generate the master progress file
- [ ] P2-2: Move the activity narrative to an append-only record
- [ ] P2-3: Retire hand-edited session history

### M10: Scheduler correctness (depends: M1)
*Three defects in the dependency layer. Not a throughput play — a correctness play that any parallel work would otherwise amplify.*
- [ ] P2-4: Make a dangling dependency an error
- [ ] P2-5: Correct the documented milestone syntax
- [ ] P2-6: Cap concurrency and say when parallelism is unavailable

### M12: Trustworthy measurement (depends: M1)
*Three of the four controls the 2026-08-19 re-plan made acceptance-relevant cannot be implemented against the code as it stands. Until they can be, neither arm of the baseline is worth capturing.*
- [ ] P0-14: Pin exact model IDs and record what was actually served
- [ ] P0-15: Record whether a run's milestone actually completed
- [ ] P0-16: Open and verify the passive observation window

### M13: The seeded paired suite (depends: M12)
*Build the instrument and size the experiment. The suite it sizes runs at M11, both arms interleaved against a binary frozen at M1.*
- [ ] P0-17: Generate a disposable, resettable seeded testbed
- [ ] P0-18: The paired runner
- [ ] P0-19: A/A variance pilot and sample sizing
- [ ] P0-20: One analysis path for the pilot and the M11 verdict

### M11: Roll out & re-measure (depends: M2, M4, M5, M6, M8, M9, M10, M13)
*Migrate the pathological minority, bring all five repos onto one build, run the paired suite, and report against the M1 baseline including anything that missed.*
- [ ] P3-1: Migrate the pathological features
- [ ] P3-2: Bring all five repos onto one build
- [ ] P3-3: Re-measure and report against the baseline
- [-] P3-4-REVIEW: Document the CLI surface this feature added — WITHDRAWN 2026-08-19, superseded by P0-M1-FIX-23 — `belmont metrics` and `belmont extract` ship and are in `belmont help`, but appear in no doc. Filed here rather than M1 (which delivered them) because reopening M1 moves prerequisite 8's reverify gate; full reasoning in PRD.md. (Review audit 2026-08-19.)
  - Withdrawn in favour of `P0-M1-FIX-23`, which covers the identical scope and sits in M1 where the gap was created. Keeping both would have had two milestones own one edit.

> **Dependency syntax**: `(depends: M1)` after the milestone name declares dependencies. Independent milestones run in parallel via git worktrees under `belmont auto`.
>
> **Wave structure**: W1 = M1 · W2 = M2, M3, M6, M7, M9, M10, M12 · W3 = M4, M5, M8, M13 · W4 = M11. Four waves for thirteen milestones — 3.25× theoretical parallelism, up from 2.75× because both milestones added on 2026-08-19 landed inside waves that already existed. Expect roughly 1.4× in practice on a single machine; see PRD §Research Notes on why local parallelism does not scale with slot count. **Wave 2 now holds seven milestones — run at `--max-parallel 2` or `3`, never seven.**
>
> **Four gates before wave 2 starts**, all owner actions, all in TECH_PLAN §Prerequisites — before wave 2: redact the product estate and force-push; do not start wave 2 before that lands; `belmont reverify` M1 and build `belmont-pre` from its merge commit; repoint the pin to `belmont-pre`.
>
> **Task states**: `[ ]` todo, `[>]` in_progress, `[x]` done, `[v]` verified, `[!]` blocked, `[-]` withdrawn. Milestone status is computed from its tasks — do not add status emoji to milestone headers.
>
> **Model tiers**: Opus for implementation, verification and planning; Haiku only for genuinely mechanical work; skip the Sonnet tier. Per PRD §Clarifications — measured rework rates make the middle tier a false economy.
>
> **Design agent**: skip on every milestone. No task in this feature has design input.

## Session History

| Date | Action | Details |
|------|--------|---------|
| 2026-08-19 | Tech plan | Placed the seeded harness the product re-plan left open. **M12** (wave 2) and **M13** (wave 3) added — 13 milestones, 59 tasks as `belmont status` counts them (7 new), still four waves, parallelism 2.75×→3.25×. M11 gains `depends: M13`. **M1 unblocked**: P0-3 re-scoped to the read-path half `[x]`, P0-3a withdrawn `[-]`; while P0-3 read `[!]` no wave could start. Six decisions taken: both arms run at M11 against a binary frozen at M1 (alternation needs both builds to coexist); harness as build-tagged test files, never a subcommand; seed generated rather than copied; arm B migrates before its timed run; the fork's pin repointed from Homebrew v0.11.0 to `belmont-pre` so the feature's own twelve remaining milestones are measured at all; sample size beats wall-clock when they conflict. Four owner-run gates added before wave 2. |
| 2026-08-19 | Plan review | `/belmont:review-plans`, all four layers. Five findings, five resolved, no code touched. **The substantive one**: this feature's own register was measured against the ceilings the feature introduces — 45,576 B (89% of M5's 50 KB warn) and **13 task lines already over the 400-char limit**, longest 2,925, all of it `P0-M1-FIX-8`…`FIX-11` narrative. §Edge Cases covered a register over `progress_error_bytes` but said nothing about one already holding over-length *lines*, leaving P1-8's scope unspecified; on the whole-file reading Belmont's own register becomes unwritable by its own tooling at wave 3, one wave before M11 writes to it. Resolved by specifying P1-8 as checking only the lines a write introduces or modifies, as an acceptance criterion on the task and a new §Edge Cases row. `P3-4-REVIEW` added to M11 (60 tasks) — `belmont metrics` and `belmont extract` ship but are in no doc. Master PRD/TECH_PLAN corrected here: wave 2 six→seven milestones, "six Kind B rules"→seven (the omitted one gates wave 2), rule 7 reordered after rule 6, and the absence of a master `.belmont/PROGRESS.md` recorded as a decision with its two triggers rather than left as a gap. Workspace row at `~/belmont/` refreshed from "Not Started, 0/11, 0/37" (committed there as `da20804`). **`.belmont/PR_FAQ.md` stub replaced with a pointer** — `worktree.go:253` copies it into every worktree and seven planning skills instruct agents to read it, so the 63-byte install stub was telling wave-2 agents to run `/belmont:working-backwards`; `implement` and `verify` do not read it, so no execution phase was affected. `PRFAQReady` now true. **Verified accurate and unchanged**: `writeStateFile` and its 20 call sites, `MetricsRoot`/`RunID` propagation, `ensureMetricsIgnored`, `ZeroUsage` flagged-not-dropped, `unreadableRoots`, the cherry-picked parallel-wave gotcha section, `go 1.26.0`, the wave graph against the declared dependencies, `belmont validate` clean, and prerequisite 6's count of twelve product-naming tracked files — exactly twelve. |
| 2026-08-19 | Confidentiality | `github.com/bigbenjoman/belmont` measured `visibility: PUBLIC`, a fork of `blake-simpson/belmont`, with `.belmont/` on `origin/main` since the P0-13a push. `census.json` names 82 feature slugs across all five products with per-feature sizes; `MILESTONE-M1.done.md` carries 107 further references; `cmd/belmont/extract.go:31` and `knowledge/auto-mode/clean-tree-preflight.md:61` name products in *product source*, which would travel in any upstream PR. Redaction to opaque identifiers adopted as prerequisite 6, gating wave 2. Residual exposure accepted by decision — see Decisions Log. |
| 2026-08-18 | Tech plan | Six interview rounds. Self-hosting in the `bigbenjoman/belmont` fork at `~/repos/belmont` confirmed; proposal 0004 rev 5 absorbed into M2 and its lazy-Setup half recorded as superseded by M4's slice; P1-12 withdrawn on an in-repo measurement; P0-12 (Go toolchain) and P0-13 (branch triage) added to M1. Detail tier, slice command, config home, thresholds, journal format, state precedence, gate set and slot cap all specified in TECH_PLAN.md. |
| 2026-08-18 | Created | Product plan session. Eleven milestones, 35 tasks, four dependency waves. (37 now — P0-12 and P0-13 were added during tech-plan.) Preceded by measurement of repo-3 and repo-4 state files and four research passes (graph engineering practice, arXiv literature, GitHub ecosystem and rival spec-driven frameworks, milestone-tracker architecture). |
| 2026-08-18 | Plan review | `/belmont:review-plans`. Task-count history corrected 33→35 (37 now, less P0-12/P0-13 added at tech-plan). P0-12 and P0-13 task lines aligned with their PRD titles. **P0-13 scope made explicit**: `origin/docs/pr-proposals` must carry its own verdict, because verification found no `docs/proposals/` directory on the fork's `main` at `17897aa2` and no mention of the convention in `AGENTS.md` or `CONTRIBUTING.md` — M2's specification (proposal 0004 rev 5) and the homes for the three proposals this feature owes are all off-mainline. Also recorded: `origin/proposal/0001-quick-mode` and `origin/proposal/0002-prd-hygiene` carry `framework-evaluation`'s P6-1/P6-2 specs. Verified accurate and unchanged: the 35-branch count, `go.mod` at `go 1.21`, the six agent files' absent YAML frontmatter, the symlinked `.claude/commands/belmont/`, `eval_harness_test.go` present on `main`, the wave structure against the declared dependencies, and `belmont validate` clean. |
| 2026-08-18 | Prerequisites | Two further findings from the review's Layer 4 measurement, both fixed. **(a) The user-level install was stale and it mattered.** `~/.agents/skills/belmont/` was last written 2026-05-04 and lacked `loop`, `repair` and `codex-plan-apply` — v0.10.x against a v0.11.0 orchestrator. Because the fork gitignores `.claude/`, that install is the sole resolver of `/belmont:*` inside worktrees, so all six wave-2 milestones would have run 3½-month-old prose (including the `verify.md` M2 rewrites) and the M1 baseline would not have been comparable with the M11 re-measurement. Refreshed, and recorded as prerequisite 0 with an explicit do-not-repeat-until-M11 note. **(b) `_research/` is 436 KB of the feature's 572 KB and the fork has no ignore rule for it**; added as prerequisite 2 so it lands with the move rather than being committed. Prerequisites renumbered 0–5. |

## Decisions Log

- **Repository**: work lands in `bigbenjoman/belmont` as a tracking fork of `blake-simpson/belmont`, upstream remote retained, file formats kept backward-compatible. Blake leaves Studia at the end of this week; upstream shipped v0.11.0 on 2026-08-17 and remains active.
- **Host support**: multi-host retained, host-specific prose made build-time conditional. Preserves opencode for the Oct 2026 local-LLM move while unlocking Claude-Code-specific caching.
- **Success bar**: tokens **and** wall-clock per *verified* milestone, against a baseline captured in M1. `[v]`, not `[x]`, is the unit.
- **Goal shape**: same spend, more shipped. Token efficiency buys throughput, not savings.
- **Sequencing**: cheapest-first by ICE score, with one deviation — instrumentation sits in M1 despite a mid-table score, because the success bar cannot be an acceptance criterion on later milestones if nothing measures it.
- **Model tiers**: Opus default, Haiku for mechanical work, Sonnet tier skipped. Supersedes the master PRD's previous "Sonnet is the current default". Basis: measured rework ~37% (Sonnet 4.6) against ~19% (Opus 4.7), plus routing research showing downgrades are catastrophic on the one or two nodes carrying real difficulty. Input cost is cut through structure, never by downgrading the model that determines rework.
- **Verify cap**: escalate after 2 failed rounds.
- **Atomic writes**: fixed as the first task of M1 rather than shipped as a separate PR, keeping the plan self-contained.
- **Migration**: opt-in per feature; bulk-run only on the pathological minority identified by the M1 census. Median feature is 14 KB and gains nothing.
- **Rollout**: all five repos, proven on repo-3 and repo-4 first.
- **Metrics**: local-only and gitignored. Bookkeeping already accounts for 41–46% of commits in these repos.
- **Feature-level register stays authored, master file becomes generated.** Commit-history measurement showed regenerating the feature register produces large spurious diffs; the master file is 420 KB of narrative nobody authors as a human artefact.
- **Register line grammar is not changing.** It is parsed in seventeen places across three dialects and changing it buys nothing measurable. Only the detail tier gets a new grammar.
- **Scored out of scope on evidence**: code-graph index (peer-reviewed result is +2.3pp resolve for 12–38% more spend; two-hop retrieval scores below no graph at all), speculative execution (rollback trigger collapses to AUC ≈ 0.5 on code and math), blast-radius verification (depends on the code graph; a wrong radius silently skips a check), rolling wave scheduler (level-by-level is already within 2.6–11.2% of the critical-path bound, and scheduling discipline is irrelevant when slot-limited), task-level dependency metadata (the milestone is the worktree boundary; a task DAG needs a new isolation unit).
- **Parallelism expectations revised down during planning.** Belmont's wave scheduler is correct and already ahead of the field. The available speedup is real but bounded by memory bandwidth on a single machine, not by graph structure — which is why this feature spends its effort on token structure rather than on widening fan-out.

### Added during tech-plan, 2026-08-18

- **Work location**: self-host in the fork at `/Users/benlavender/repos/belmont` (`origin=bigbenjoman`, `upstream=blake-simpson`, local `main` at `17897aa2` = upstream v0.11.0). The feature directory moves there before M1; the research workspace keeps the master files and `framework-evaluation`. Contained by pinning the orchestrating binary to the Homebrew v0.11.0 build — see TECH_PLAN §Prerequisites.
- **Prior art**: proposal `0004-context-budget-with-evidence.md` rev 5 is M2's specification for its conditional-archive and read-once halves. Its lazy-Setup optimisation is **superseded, not rejected** — M4's `belmont slice` replaces that first read two waves later.
- **P1-12 withdrawn** `[-]`. The mechanism is real (steering prepended at `auto_loop.go:259` and `:404`); the saving is not. 0004 rev 2 measured prefixed against suffixed steering on `claude -p` and got identical cache figures both ways (13,233 creation / 21,689 read) — Belmont assembles a ~549-byte user message and the cache breakpoint sits at its end.
- **P0-12 and P0-13 added to M1.** Go toolchain bump sequenced *before* P0-2/P0-3 so the baseline is captured on the toolchain everything after is built with. Branch triage before any milestone touches shared files — five of the 35 branches sit on ground M3/M4/M6 rewrite.
- **Detail tier**: `{base}/details/M<N>.md`, one file per milestone, `## <task-id>` blocks, supersession as a one-line note with the old text left to git. Absent directory = today's behaviour.
- **Read path**: new `belmont slice --feature X --milestone M3` subcommand, text by default, nothing written to disk. Explicitly not a status cache — stated in `knowledge/cross-cutting/context-budget.md`.
- **Config home**: project-level `.belmont/config.json` (stdlib JSON), not YAML — the binary has zero dependencies and no `go.sum`.
- **Thresholds**: warn 50 KB, error 100 KB, task lines rejected above 400 chars. 100 KB is 25,000 tokens at 4 bytes/token, so the error threshold *is* the success criterion. Measured against all 139 feature registers across the five repos.
- **Instrumentation**: parse each tool's own JSON usage; record `null` with a stated reason where the tool cannot report (copilot, pi, opencode). Never estimate.
- **Host builds**: per-host generated files replace the symlinks at `.claude/commands/belmont/`, `.opencode/command/belmont/`, `.cursor/rules/belmont/`, `.windsurf/rules/belmont/`. P1-10 measures per-skill, per-host built size — not aggregate, which is disk rather than context.
- **P1-11** opens with a spike: the six agent files carry no YAML frontmatter, which is why nothing registers and the prose falls back to `general-purpose`. If registration does not preserve tool access, `models.yaml` overrides and the FORBIDDEN ACTIONS blocks, P1-11 is withdrawn rather than worked around.
- **State precedence**: single total order `[-] > [!] > [v] > [x] > [>] > [ ]`, applied uniformly so merging is order-independent. Blocked above verified is what makes "a blocker is never overwritten" true. Unsupported `[v]` is demoted to `[x]` before the order is applied, reusing `runEvidenceCheck`'s existing predicate.
- **Escalation**: two failed verify rounds mark that milestone's tasks `[!]`; `checkHardGuardrails` is narrowed so under parallel execution a blocked task pauses only that lane. Serial runs keep today's whole-run pause.
- **Gates may fail, never promote.** `[x]` → `[v]` still requires both review agents and every check they run today.
- **Journal**: `.belmont/journal/<feature>/M<N>.jsonl`, committed, one file per milestone — structurally conflict-free with no merge driver. Metrics stay at `.belmont/metrics/`, gitignored.
- **Slot cap**: `parallel_slots` in `.belmont/config.json`; effective concurrency = `min(--max-parallel, parallel_slots)`, always reported. Run this feature at 2–3, not the default 5.
- **Proposals owed**: `0007-detail-tier` (M3), `0008-size-ceilings` (M5), `0009-master-and-journal` (M9) — the three milestones that change a contract 139 feature directories depend on. The other eight ship as ordinary PRs.
- **Distribution**: source-mode install (`belmont install --source ~/repos/belmont`) into all five repos. No fork tap, no release pipeline. Not run between M1 and M11.
- **Migration safety**: `belmont extract` proves a byte-for-byte round-trip before writing, supports `--dry-run`, and commits once per feature.
- **`P3-4-REVIEW` withdrawn `[-]` 2026-08-19, superseded by `P0-M1-FIX-23`.** The plan review filed it against M11 on the reasoning that a cross-cutting follow-up goes to the highest-numbered milestone whose work it touches. Verify round 7 then found the same gap from the code side and filed `P0-M1-FIX-23` into M1. Same scope, same three files. M1 wins: that is where `P0-2` and `P0-4` shipped the undocumented commands, and `P0-2` is **held at `[x]` on this task**, so leaving it in M11 would have blocked M1's own verification behind a wave-4 milestone. Withdrawn rather than deleted — a deleted line does not survive a sibling worktree merge.

### Added during tech-plan, 2026-08-19 — placing the seeded harness

- **Arm timing**: both arms of the paired suite run at **P3-3**, interleaved and alternating, against `belmont-pre` — a binary frozen at M1's merge commit. BASELINE.md's alternating-order control requires both builds to exist simultaneously, and at M1 the post-change build does not. Arms months apart would confound cache warmth, vendor drift and machine state *with the arm*. A frozen binary is also immune to sibling merges, so M13 need not serialise a wave behind it.
- **Placement**: **M12** (wave 2, depends M1) and **M13** (wave 3, depends M12). Free on wave count. One 7-task milestone would have mixed shipped Go, a bench harness and a five-repo ops step; folding into M11 would discover harness infeasibility at wave 4 and lose the passive window entirely.
- **M1 unblocked**: P0-3 re-scoped to the read-path half and marked `[x]` (commit evidence `ff06d45`; `belmont reverify` judges the new criterion); P0-3a withdrawn `[-]`, superseded by M12/M13.
- **Harness form**: build-tagged test files (`//go:build bench`, `BELMONT_BENCH_LIVE=1`, `-timeout 0`), mirroring `eval_harness_test.go`. Never a shipped subcommand — a one-off instrument does not belong in a surface 139 feature directories depend on. Three of its four tests run offline and free, including the analysis that computes P3-3's verdict.
- **Seed generated, not copied.** Calibrated to the measured distribution (1,860,979 B, 30 milestones, 529 tasks, 458 with detail). Read-path cost is a function of bytes and structure, not words. Copying the real register would publish a pre-launch product's plans; gitignoring the copy would make the seed unreproducible anywhere else.
- **Arm B migrates before its timed run**, and the migration cost is reported separately. M5's ceiling would otherwise refuse the deliberately-pathological seed. **Acceptance criterion on P0-17**: the seed must be one that extraction lands under `progress_error_bytes` — five of 82 real registers do not have that property.
- **Model IDs**: extend the Pi/opencode resolution chain to every tool rather than hardcoding; record `model_requested` **and** `model_served`. Recording the request alone cannot see a vendor serving something else, which over a months-long passive window is the exact confound. `modelTiers`' aliases stay as defaults — pinning them globally would pin every user to serve one experiment.
- **Analysis**: sample median of per-pair log-ratios as the point estimate (criterion 26's literal rule), exact Wilcoxon/Hodges–Lehmann interval as the CI (stable at n = 6–20, and it matches the signed-rank calculation that sized the suite), completion rate published beside every median. Blocked by task, never pooled.
- **No retries.** A failed run is recorded with its exit reason, counts in the completion-rate denominator and is excluded from the ratio. A retry silently repairs the number that exists to stop cheap failures flattering a median.
- **Sample size beats wall-clock.** If the pilot's derived pair count will not fit the block, cut the seeded work, then verify rounds, then measured phases — the pair count last, and say so in the report. Basis: `P0-M1-FIX-6`…`FIX-11`, five consecutive rounds correcting claims stated beyond their evidence in these files.
- **The fork's pin moves from Homebrew v0.11.0 to `belmont-pre`.** Isolation is identical — both frozen — but v0.11.0 has no metrics code, so the feature that exists to measure Belmont would have recorded nothing across its own twelve remaining milestones. Amends master TECH_PLAN rule 1. Reverts in one command.
- **Product estate redacted in place; residual exposure accepted.** `bigbenjoman/belmont` is a PUBLIC fork of `blake-simpson/belmont` and `origin/main` already carries 82 product feature slugs with sizes. **Decision (Ben Lavender, 2026-08-19), taken against the recommendation and recorded as such**: rewrite to opaque identifiers and force-push, rather than moving to a private repo and deleting the fork. Objects already pushed to a public fork stay reachable through the parent's network, so this reduces future exposure without undoing the publication already made. The alias↔slug mapping lives at `~/belmont/`, never here.
- **Model tiers unchanged.** M12 and M13 were assessed against `models.yaml` and sit inside its existing rationale — P0-14 is the same "a miss is silent bad data" class as the other Go work, and P0-20 computes the number criterion 26 turns on. `models.yaml` is per-feature, so P0-16's mechanical ops script cannot be lowered without lowering the six tasks around it.

- **Model tiers** (`models.yaml`, profile `framework-go-and-prose`): every agent `high` except `design: low`. Rationale per agent — codebase must find all 16 state writers and all 17 parse sites (a miss is a torn file); implementation touches atomicity, merge precedence and scheduling where errors are silent data loss; verification checks invariants rather than features; code-review reads concurrency semantics, not style; reconciliation is always high and this feature changes merge semantics *while* merging; design is never dispatched. Consistent with the barbell rule — high or low, never medium.
