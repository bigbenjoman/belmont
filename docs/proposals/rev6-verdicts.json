{
 "raised": 69,
 "judged": 69,
 "survived_blockers": [
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:94 (R1) vs :225-227 (\u00a74.6 branches), :233 (fourth rule), :281 (\u00a74.9), :379 (Step 7), :426 (DoD)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "blocker",
   "reasoning": "Every quote checks out verbatim: :94 (\"Record the outcome in the contract's `**Mode**` field \u2026 Backend features record `N/A \u2014 no UI`; Figma features record `N/A \u2014 Figma present`. **Silence is not allowed**\"), :154-155 (Mode is the first line under `## Design Contract`; no other container exists), :225 (\"plus contract checks if a contract is present\"), :226 (\"No references, contract present\"), :227, :233 (\"a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE\"), :379, :426. I searched the whole 491-line document for a definition of \"a contract is present\" \u2014 grep for `contract is present|contract present|no contract|contract exists` returns only :95, :225, :226, :227, :233, :418, :426. None defines the discriminator. So the failure mode (b) refutation (\"addressed elsewhere\") does not apply.\n\nThe defect is real and gets WORSE than the finding states. `skills/belmont/_src/references/tech-plan-feature-format.md:44` is `## Design Tokens (from Figma)` \u2014 an unconditional section of the feature template, not a conditional one \u2014 and \u00a75 (:292) generalises it to `## Design Contract` \"incl. all six subsections\". So after this PR every feature TECH_PLAN written from the template carries the `## Design Contract` heading, Figma feature included (holding the Figma-extracted tokens tech-plan already loads inline at `tech-plan.md:158`). Section presence is therefore guaranteed for all three modes by two independent mechanisms (R1's Mode requirement AND the template), while \u00a74.6's branches, the fourth rule, and DoD :426 all speak of \"no contract\" for exactly the cases that carry the heading.\n\nThe historical claim also checks out: `docs/proposals/rev5-verdicts-round2.json` refuted[6] is idx 28 and its stated reason is \"that is a bold *field line*, not the `## Design Contract` H2 heading that branch 2 (:182) keys on \u2026 R1 therefore does not 'guarantee the section is always present'\". Rev 6's own minor fix (:491, \"`**Design contract**` phantom field removed in favour of `**Mode**`\") moved the value inside the heading and voided that premise.\n\nInternal contradiction as written: R1 requires a Figma feature to record `N/A \u2014 Figma present` *in the contract's Mode field*, while DoD :426 asserts that same feature has \"no contract\" and Step 7 (:379) concludes \"the fourth enforcement rule does not fire without a contract\". Under section-presence discrimination (the reading rev 5 used verbatim at :182, and the only mechanical test available) branch 1/2 demand contract checks on a Figma feature, none exist, and the fourth rule forces FAIL \u2014 the exact v5 blocker \u00a712 (:485) claims fixed. Blocker confirmed: one plausible implementation ships a false result, and the R1/DoD pair is not simultaneously satisfiable as literally written.",
   "fix_to_apply": "1. In \u00a74.6, insert immediately after the intro paragraph at :223 and before branch 1: \"**\\\"A contract is present\\\" means one thing only:** `{base}/TECH_PLAN.md` contains `## Design Contract` whose `**Mode**` is `derived \u2014 UI, no Figma`. Both `N/A` modes and an absent section are \\\"no contract\\\" for all three branches and for the fourth enforcement rule. **Never key on the presence of the `## Design Contract` heading** \u2014 the feature template carries it unconditionally (`tech-plan-feature-format.md:44`, generalised per \u00a74.4), so a Figma feature's plan has the heading too, holding its Figma-extracted tokens.\"\n2. Restate the fourth rule at :233 as: \"*`**Mode**` is `derived \u2014 UI, no Figma` but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE*\", and narrow the `:131` rewrite to \"no design references **and** no `derived` design contract\".\n3. In \u00a74.4, after the schema block, state what each `N/A` mode's section contains: Mode line only for `N/A \u2014 no UI`; Mode line plus today's Figma-extracted tokens for `N/A \u2014 Figma present`; no baseline subsections in either case.\n4. Reword Step 7 (:379) to: \"Expect `**Mode**: N/A \u2014 Figma present`, **no** Token Contract, per-task Figma sections and the Figma Sources table exactly as today, and a verify verdict of PASS (not FAIL) \u2014 this asserts the fourth enforcement rule does not fire when `**Mode**` is not `derived`.\"\n5. Reword DoD :426 to: \"Figma feature: `**Mode**: N/A \u2014 Figma present`, no contract checks performed, verdict PASS, behaviour unchanged (Step 7)\", and DoD :418 to \"\u2026branch 1 performing contract checks when `**Mode**` is `derived`\u2026\".\n6. Tighten \u00a74.9 (:281) to: \"\u2026has a `{base}/TECH_PLAN.md` with no `## Design Contract` and therefore no `**Mode**`, so \u00a74.6 branch 3 applies\" (keeps it accurate for legacy plans while matching the Mode-based discriminator)."
  }
 ],
 "survived_majors": [
  {
   "spec_location": "\u00a76 \"Both invocation paths\" (line 322) and \u00a79 Definition of Done \u2192 \"Corrections carried by this PR\" (line 430)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 the spec says exactly what the finding claims. Line 322: \"For **six of eight CLIs** that is false: `buildLoopPrompt` emits a bare slash reference (`main.go:7168`, `:7189`)\" and \"For **Pi and opencode**, `adaptPromptForTool` (`main.go:7143-7157`) deliberately rewrites the reference\". Line 430: \"`AGENTS.md:26` and `dual-invocation-paths.md:9` corrected **per tool** \u2014 bare slash reference for six CLIs, deliberate rewrite for Pi and opencode\". No misquote.\n\nCODE CHECK \u2014 every cited fact verifies. cmd/belmont/main.go:5338 `case \"claude\", \"codex\", \"gemini\", \"copilot\", \"cursor\", \"pi\", \"opencode\":` followed by :5340-5341 `default:` / `return fmt.Errorf(\"auto: unsupported tool %q (use claude, codex, gemini, copilot, cursor, pi, or opencode)\", cfg.Tool)`. `toolHeadlessArgs` (main.go:7922) has exactly those seven cases and no windsurf case. Windsurf appears on the install path only \u2014 main.go:4159 `case \"windsurf\":` / `fmt.Println(\"Linking Windsurf...\")` \u2014 and in the 8-element install list at main.go:3644 `tools := []string{\"claude\", \"codex\", \"cursor\", \"windsurf\", \"gemini\", \"copilot\", \"pi\", \"opencode\"}`. `adaptPromptForTool` gate confirmed at main.go:7143 `if tool != \"pi\" && tool != \"opencode\" { return prompt }`. buildLoopPrompt cited lines confirmed: :7168 `prompt := fmt.Sprintf(\"/belmont:implement --feature %s\", feature)`, :7189 `return fmt.Sprintf(\"/belmont:tech-plan --feature %s\", feature)`.\n\nRAN IT \u2014 built the CLI and executed `belmont auto --tool windsurf --feature foo`: output `auto: unsupported tool \"windsurf\" (use claude, codex, gemini, copilot, cursor, pi, or opencode)`. `--tool claude` on the same command got past validation (`auto: feature \"foo\" not found`). Windsurf is mechanically excluded from auto mode.\n\nSOURCE OF THE 8 \u2014 AGENTS.md:197 \"All eight supported CLIs auto-discover `.agents/skills/` natively for skill content\" is an install-surface statement, and docs/supported-tools.md:3 repeats it. The repo already documents the split correctly elsewhere: the \"## Headless invocation\" table at docs/supported-tools.md:28-36 lists exactly seven rows (Claude Code, Codex, Cursor, Gemini, GitHub Copilot, Pi, opencode) \u2014 no Windsurf.\n\nSEARCHED THE WHOLE SPEC \u2014 `grep -in windsurf` on the 491-line proposal returns exactly one hit, line 121, which is about interactive skill activation (\"Codex/Cursor/Windsurf/Copilot/Pi auto-activate on `description:`\"), not auto mode. The doc nowhere notes that Windsurf has no auto path, so the finding is not a restatement of something handled elsewhere.\n\nNOT AN ACKNOWLEDGED LIMITATION \u2014 the line-9 caveat covers only `main.go:NNNN` line numbers, not CLI counts.\n\nSEVERITY \u2014 major stands. This is a DoD item (line 430) instructing that a specific per-tool split be written into AGENTS.md:26 and knowledge/cross-cutting/dual-invocation-paths.md:9 \u2014 the file every session loads. Shipping \"bare slash reference for six CLIs\" would newly assert that Windsurf runs under `belmont auto`, replacing one wrong statement with another, in a section whose stated purpose (line 322) is \"Correct both statements with that split, not with a blanket claim.\"",
   "fix_to_apply": "In \u00a76 line 322, replace \"For **six of eight CLIs** that is false\" with \"For **five of the seven CLIs `belmont auto` can shell out to** that is false\", and append after the Pi/opencode sentence: \"Windsurf is an install-only target \u2014 `belmont auto --tool windsurf` is rejected (`main.go:5338-5341`) and `toolHeadlessArgs` (`:7922`) has no windsurf case \u2014 so the eight-CLI install surface (`AGENTS.md:197`) and the seven-CLI auto surface must not be conflated.\" In \u00a79 line 430, replace \"bare slash reference for six CLIs, deliberate rewrite for Pi and opencode\" with \"bare slash reference for the five auto-capable CLIs (claude, codex, gemini, copilot, cursor), deliberate rewrite for Pi and opencode, and Windsurf noted as install-only with no auto path\"."
  },
  {
   "spec_location": "\u00a74.7 Downstream consumers, `skills/belmont/_src/verify.md` bullet (line 267); scope row at line 296",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 line 267 reads verbatim: \"the **sub-agent dispatch prompt** (`:109`, `:146`) steers the agent away from the new behaviour by enumerating only Figma/screenshot references and injecting \\\"No design references found\\\"\". The parenthetical citation attaches to \"the sub-agent dispatch prompt\", and the clause immediately following describes the specific defect. No ellipsis, no misquote.\n\nFILE CHECK \u2014 skills/belmont/_src/verify.md:109 is `> Read \\`{base}/TECH_PLAN.md\\` for technical specifications (if it exists).` and :146 is the byte-identical same line. Neither contains a Figma/screenshot enumeration nor the \"No design references found\" string. The described content is at :114 `> **Design References for Visual Verification**:`, :116 `> - Task [ID]: Figma fileKey=\\`xxx\\`, nodeId=\\`yyy\\``, :117 `> - Task [ID]: Reference screenshot at [path or URL]`, :119 `> If no MILESTONE files or references were found, write: \"No design references found in archived MILESTONE files or PRD.\"]`, and the Figma-only load instruction at :121 `> **Visual Verification**: \u2026 If design references are listed above, you MUST load them \u2014 call \\`mcp__plugin_figma_figma__get_screenshot\\` for Figma references, Read for local images, WebFetch for URLs \u2026`.\n\nSECTION CHECK \u2014 the finding's structural claim also holds: `### Agent 1: Verification (verification-agent)` opens at :92 and its prompt block runs :98-123, so :109 and :114-121 are inside it. `### Agent 2: Code Review (code-review-agent)` opens at :129 and its prompt block runs :135-151, so :146 sits in the code-review dispatch, which contains no design-reference block at all (:145-149 are PRD read, TECH_PLAN read, MILESTONE read, package-manager detection).\n\nSEARCHED THE WHOLE SPEC \u2014 the only other verify.md citations are the scope row at line 296 (\"Step 1b collects the contract; dispatch prompt carries it; focused-reverify exemption at `:50`\") and \u00a74.6 line 256 (`verify.md:50`). Both `:50` citations are correct \u2014 line 50 is \"**Do NOT** re-check visual specs against design references unless a follow-up task specifically addressed UI changes.\" Nowhere does the document cite :114-121, so the correct location is not covered elsewhere.\n\nSTALENESS CHECK \u2014 verify.md's last commit is ad5f84c, predating the proposal branch, so the line numbers were not invalidated by drift; they were wrong when written. The line-9 caveat covers only `main.go` citations, so this is not an acknowledged limitation.\n\nSEVERITY \u2014 major stands. An implementer following the pointer edits two generic TECH_PLAN-read lines, adds a contract read into the code-review dispatch that \u00a74.7 never asks for, and leaves the actual Figma-only enumeration and the injected \"No design references found\" string intact \u2014 which is the exact steering \u00a74.7 says it is removing. That is rework plus a wrong edit, not mere imprecision.",
   "fix_to_apply": "In \u00a74.7 line 267, change the middle clause from \"the **sub-agent dispatch prompt** (`:109`, `:146`) steers the agent away from the new behaviour\" to \"Agent 1's dispatch prompt (`:114-121`) steers the agent away from the new behaviour\". Keep the rest of the sentence unchanged. Drop `:146` entirely; if a contract read is also wanted in the code-review dispatch, add it as a separate, explicitly-labelled fourth edit citing `:146` for that narrow addition rather than folding it into the steering defect."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:233 (\u00a74.6 fourth enforcement rule) vs :256 + :373 (focused re-verification) vs :418 + :421 (DoD)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTES CHECKED, ALL ACCURATE. :233 reads verbatim \"add a fourth enforcement rule: *a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE*\" \u2014 the antecedent keys on EXISTENCE, with no qualifier. :256 reads verbatim \"Contract checks are **exempt from that skip when the milestone's follow-ups touched UI**; otherwise the attestation records `Contract checks performed: NO \u2014 focused re-verification, no UI follow-ups`\". :258 legalises `NO \u2014 <reason>`. :373 calls that outcome \"legitimately\" (the evidence quote drops the trailing \"unless the follow-ups touched UI\" \u2014 immaterial, that clause is the UI-touched branch the finding already concedes). I grepped the whole 491-line file for \"fourth\": hits at :229, :233, :379, :418, :485 \u2014 none carves out focused re-verification, so this is not addressed elsewhere (failure mode (b) does not apply). CODE CHECKED: the shipping analog at agents/belmont/verification-agent.md:130 is \"If design references were loaded but structured comparison was not performed, Visual Verification MUST be **FAIL** or **INCOMPLETE** \u2014 never PASS\" \u2014 note its antecedent is an ACTION (\"loaded\"), where the proposed fourth rule's is a STATE (\"exists\"), which is exactly why it fires when the checks are deliberately skipped. skills/belmont/_src/verify.md:50 and cmd/belmont/main.go:7184 (\"FOCUSED RE-VERIFICATION: \u2026 Do NOT re-check visual specs unless a FWLUP specifically addressed UI\") both confirm focused is the default second-pass behaviour, and prompts/belmont/post-verify-triage.md:22 confirms \"**If fix round >= 2**: Choose `defer_and_proceed` for ALL remaining follow-ups\". SHARPENING THE FINDING: the collision is tighter than stated \u2014 \u00a75 line 301 puts BOTH the fourth rule AND the focused exemption into the same file (`verification-agent.md` row: \"fourth enforcement rule; attestation field; focused-reverify exemption\"), so the produced artifact self-contradicts; and the `NO \u2014 <reason>` arm of the :258 attestation schema is dead on arrival, since `NO` can only be recorded when a contract exists, which rule 4 makes fatal in every case. WHERE I BREAK THE FINDING: the headline \"the two DoD checkboxes cannot both be satisfied\" does not hold. An implementer can satisfy :418 and :421 simultaneously by writing the rule with its exemption inline \u2014 the finding's own suggested fix is proof. So this is not unsatisfiable-as-a-matter-of-logic (the v4 mutually-exclusive-checkbox class); it is an unqualified rule stated 23 lines before its exemption, which will produce false FAILs if transcribed literally into an agent-read instruction file. That is rework and a wrong result on a real path, not an unimplementable spec \u2014 major, not blocker.",
   "fix_to_apply": "Three edits. (1) \u00a74.6 line 233 \u2014 replace \"and add a fourth enforcement rule: *a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE*.\" with: \"and add a fourth enforcement rule: *a contract exists (`**Mode**: derived \u2014 UI, no Figma`), contract checks were required, and they were not performed \u21d2 MUST be FAIL/INCOMPLETE*. Checks are **not** required only in the focused-re-verification case defined below (focused scope, no UI follow-ups); that is the single legitimate `NO`. Every other `NO` is fatal. State the exemption inline with the rule in `verification-agent.md` \u2014 the two must not sit in separate paragraphs.\" (2) \u00a74.6 line 258 \u2014 replace the attestation line with: \"**Attestation gains a field:** `Contract checks performed: [YES against <path> | NO \u2014 focused re-verification, no UI follow-ups (legitimate) | NO \u2014 <reason> (fatal, enforcement rule 4) | N/A \u2014 no contract]`.\" (3) DoD line 418 \u2014 append to the checkbox: \"\u2026 fourth enforcement rule present and satisfiable, with its focused-re-verification exemption stated inline in `verification-agent.md`, matching Step 5 (:373)\". No change needed to :256, :373 or :421."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:82 (the write rule) vs \u00a75 in-scope table :289-308",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECKED: :82 reads verbatim \"> **Only the `tech-plan` skill may write `{base}/TECH_PLAN.md`. `implement`, `verify`, `next` and `debug` read it and never write it.**\", introduced at :80 as \"**Therefore, the rule of this PR \u2014 and what enforces it:**\" \u2014 so it is normative, and its first sentence is unqualified. CODE CHECKED, ALL THREE CITATIONS EXACT: knowledge/cross-cutting/debug-spec-reconciliation.md:9 \u2014 \"Interactive `/belmont:debug-manual` is the **only** skill that may edit spec prose in place (PRD task text, TECH_PLAN narrative, \u2026)\"; AGENTS.md:35 carries the same exception verbatim. I also found the authoritative permission the finding did not cite, which strengthens it: skills/belmont/_partials/debug-scope-rules.md:16 is a \"### What you MAY edit\" table row \u2014 \"| `{base}/TECH_PLAN.md` | Decision narrative, library choices, file-path references, API shapes \u2014 anything where reality has moved past the written record |\" \u2014 and skills/belmont/_src/references/debug-manual-spec-reconcile.md:17 \u2014 \"| 5 | TECH_PLAN decision wrong or stale | `{base}/TECH_PLAN.md` or `.belmont/TECH_PLAN.md` | Edit narrative in place; preserve heading structure |\". \"Preserve heading structure\" keeps `## Design Contract` as a heading while licensing a rewrite of the token values beneath it. SCOPE CHECKED: \u00a75's table (:289-308) contains no debug file \u2014 I grepped the whole proposal for \"debug\": exactly one hit, line 82. So nothing in this PR makes the rule true. REFUTATION ATTEMPTS THAT FAILED: (a) \"`debug` means debug-auto\" \u2014 skills/belmont/_src/debug-auto.md contains zero occurrences of TECH_PLAN, so that reading is true of debug-auto, but the rule's FIRST sentence (\"Only the `tech-plan` skill may write\") is a blanket exclusion that still contradicts debug-manual; (b) \"already acknowledged at :84\" \u2014 :84 acknowledges the absence of a MECHANISM (\"prose-enforced and unguarded\"), which is a different thing from a documented affirmative permission pointing the other way, so this is not a re-report of an acknowledged limitation; (c) \"harmless because it is user-approved\" \u2014 the per-edit `y / N / edit / skip` gate is design-blind: nothing tells the user or the agent that the edited section carries `**Approval**: approved <ISO date>` which \u00a74.6 then verifies against, so a reconciliation edit leaves a stale approval stamp on a standard the user never approved as design. Real, and major: the PR also ships a new `knowledge/cross-cutting/design-authority.md` (\u00a77:334) and an AGENTS.md invariants line (\u00a75:305) that would assert a rule contradicting an existing knowledge entry \u2014 exactly the kind of contradictory-invariant state that causes wrong decisions in later sessions. Not a blocker: the PR is implementable as written and the primary auto path is unaffected.",
   "fix_to_apply": "Four edits, and note the finding names the wrong file for two of them \u2014 the permission lives in the partial and the reference, not in `_src/debug-manual.md` (which merely `@include`s the partial at :29 and restates it at :299-304). (1) \u00a74.1 line 82 \u2014 replace with: \"> **Only the `tech-plan` skill may write `{base}/TECH_PLAN.md`'s `## Design Contract`. `implement`, `verify`, `next` and `debug-auto` read it and never write it.** When `tech-plan` itself runs headlessly as `actionReplan`, R4 constrains what it may change. **One documented exception:** interactive `/belmont:debug-manual`'s Spec Reconciliation phase may edit `{base}/TECH_PLAN.md` in place under per-edit user approval (`_partials/debug-scope-rules.md:16`, `knowledge/cross-cutting/debug-spec-reconciliation.md:9`). It must treat `## Design Contract` as out of bounds; if the user explicitly approves a contract edit anyway, it must re-stamp `**Approval**: approved <ISO date>` so the verification gate is never measured against a silently-changed standard.\" (2) \u00a75 in-scope table \u2014 add two rows: \"| `skills/belmont/_partials/debug-scope-rules.md` | `{base}/TECH_PLAN.md` row (`:16`) excludes `## Design Contract`; add the re-stamp rule to `### What you MUST NOT do` |\" and \"| `skills/belmont/_src/references/debug-manual-spec-reconcile.md` | Drift rows 5\u20136 (`:17-18`) exclude `## Design Contract` from 'edit narrative in place' |\". (3) \u00a75 in-scope table \u2014 add \"| `knowledge/cross-cutting/debug-spec-reconciliation.md` | `Revisions` line \u2014 records the `## Design Contract` carve-out |\", and add it to \u00a77's `Revisions` list (currently three lines at :335 \u2014 make it four). (4) \u00a77:334 `Don't re-do` \u2014 append: \"letting `debug-manual`'s Spec Reconciliation rewrite an approved `## Design Contract` (rejected \u2014 the approval stamp would go stale under a design-blind approval gate)\"."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:267 (\u00a74.7 verify.md bullet) and the \u00a75 row at :296",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Spec quote confirmed verbatim at :267. Code evidence confirmed by opening skills/belmont/_src/verify.md: :109 and :146 are the identical line `> Read \\`{base}/TECH_PLAN.md\\` for technical specifications (if it exists).` \u2014 :109 inside `### Agent 1: Verification (verification-agent)` (:92) and :146 inside `### Agent 2: Code Review (code-review-agent)` (:129, block runs to :152). The content the spec describes is at :114 `> **Design References for Visual Verification**:`, :119 `> If no MILESTONE files or references were found, write: \"No design references found in archived MILESTONE files or PRD.\"]`, and :121 `> **Visual Verification**: \u2026 call \\`mcp__plugin_figma_figma__get_screenshot\\` for Figma references \u2026`. So neither cited line contains the described behaviour, and one points at the wrong agent. I tried two rescues and both fail: (a) generated-file line numbers \u2014 in skills/belmont/verify/SKILL.md the same block sits at :423/:428, further away, and the spec names `_src/verify.md` explicitly; (b) intentional dual-anchor \u2014 the sentence's predicate (\"enumerating only Figma/screenshot references and injecting 'No design references found'\") describes nothing in the code-review dispatch, whose only design-adjacent content is a single `- **Design Fidelity** - UI matches specifications?` bullet in code-review-agent.md:78, with no reference enumeration at all. Kept at major rather than minor: the mis-citation drives an actual wrong edit (adding contract text to a code-review dispatch that needs none \u2014 a straight context cost, which matters given 0004 lands first as a context-budget PR) and puts the real edit site 5-12 lines away from where the reader is sent, in a document whose stated history is exactly this failure (main.go:9980 cited for an os.RemoveAll at :10010, propagated into four documents). Also verified the finding's secondary point: \u00a74.6:254's install-independence instruction names only `verification-agent.md:93-94` and `implementation-agent.md:192-193` (both confirmed to contain `mcp__playwright__browser_*`) plus \"the same files\" for the Figma prefix \u2014 `verify.md:121`'s pinned `mcp__plugin_figma_figma__get_screenshot` is covered by neither :254 nor the \u00a75 row at :296. Note the finding's spec_location over-reaches on \":296\": that \u00a75 row reads `Step 1b collects the contract; dispatch prompt carries it; focused-reverify exemption at \\`:50\\`` and carries no line citation, so it needs no edit; only :267 is defective.",
   "fix_to_apply": "In docs/proposals/0003-design-quality-without-figma.md line 267, replace the middle clause only.\n\nOLD (middle clause): `the **sub-agent dispatch prompt** (\\`:109\\`, \\`:146\\`) steers the agent away from the new behaviour by enumerating only Figma/screenshot references and injecting \"No design references found\";`\n\nNEW: `the **Agent 1 (verification) dispatch prompt** (\\`:114-121\\`) steers the agent away from the new behaviour \u2014 \\`:114-118\\` enumerates only Figma/screenshot references and \\`:119\\` injects \"No design references found\". Agent 2's code-review dispatch (\\`:129-152\\`) has no reference enumeration and is **out of scope**; \\`:109\\` and \\`:146\\` are the same boilerplate TECH_PLAN read line in each and are not the edit site. \\`:121\\` additionally pins \\`mcp__plugin_figma_figma__get_screenshot\\`, which \u00a74.6's install-independence instruction does not cover (it names only \\`verification-agent.md\\` and \\`implementation-agent.md\\`) \u2014 either extend \u00a74.6 to \\`verify.md:121\\` or state that it stays pinned under the out-of-scope \"Figma extraction behaviour\" line;`\n\nLeave the \u00a75 row at :296 unchanged \u2014 it carries no line citation."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:111 + :119 (\u00a74.3) vs :51-55 (\u00a73 table) vs :158-159 (\u00a74.4 Authorities schema)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "All three sub-defects are real and I traced them to the exact commit that caused them. `git show 1d060a7` (\"docs(0003): drop interactive-prototype from the design ladder\") deletes three things and replaces only one of them:\n\n(a) \u00a73 table \u2014 the diff removes `| [interactive-prototype](\u2026) | State Inventory \u2014 gesture and transition states a static spec omits |` and does NOT add State Inventory to the ux-designer/ux-motion rows. The table's column header is literally \"Contract section it feeds\" (:50), i.e. the same relation \u00a74.3:111 encodes, so the table now attributes State Inventory to nobody while :111 attributes it to two skills. Verified verbatim at :53 (\"| [ux-designer](\u2026) | UX Strategy |\") and :55 (\"| [ux-motion](\u2026) | Motion Contract |\").\n\n(b) :119 \u2014 the same commit rewrote \"holding four of the five the baseline for all five sections\" into \"holding three of the four the baseline for all four sections\". Pre-edit the two numbers coincided (5 skills, 5 sections); post-edit they diverge (4 skills, 5 enriched sections: Token, UX Strategy, State Inventory, Microcopy, Motion). \"three of the four\" is correct (skills); \"all four sections\" undercounts by one. Classic mechanical find-replace staleness \u2014 exactly the class the header at :6 warns about.\n\n(c) :158-159 \u2014 the same commit deleted `interactive-prototype (states)` from the Authorities template and put nothing in its place, so the template enumerates tokens/strategy/microcopy/motion/aesthetic and cannot express State Inventory. That template is shipped content (it is the block \u00a75:292 says `tech-plan-feature-format.md` must carry), and it contradicts the rule at :117 (\"naming the skill per section\") and DoD :409 (\"`**Authorities**` names the skill per section, never omitted\") for the one section that is jointly owned.\n\nOne supporting sub-claim in the finding IS wrong and must not be carried into the edit: it asserts \u00a75:293 \"says the new baseline file must reproduce\" \u00a73's attribution table. :293 actually reads \"**New** \u2014 per-section ladder + baseline rules for all six contract sections, Belmont-original, crediting the four Yummy Labs skills by name and link\", and :48 asks only for credit \"by name and link in the reference file header\" \u2014 no requirement to reproduce the section mapping. So \"the shipped reference file will contradict its own ladder\" is unsupported. Removing that inflation, the defect still stands on (a)+(b)+(c) as verified above.\n\nGrade held at major rather than minor because (c) lands in shipped schema content that the DoD explicitly audits, so it costs a second pass after implementation, and (a) is an attribution error in the section whose whole subject is third-party credit under a no-redistribution licence.",
   "fix_to_apply": "Three edits, all in docs/proposals/0003-design-quality-without-figma.md.\n\n1. \u00a73 table, replace lines 53 and 55 with:\n`| [ux-designer](https://yummy-design.notion.site/Claude-UX-UI-Design-Skills-31462791470981a99fe1c993b08c5347) | UX Strategy; the component-state half of State Inventory |`\n`| [ux-motion](https://drive.google.com/drive/folders/1gYrK4aT4A-LYr4GbM88-PaKu0wYyY4C6) | Motion Contract; the transition half of State Inventory |`\n(mirrors the split already stated at :111; leave the ui-designer and ux-copywriter rows alone).\n\n2. Line 119, replace \"the baseline for all four sections\" with \"the baseline for all five enriched sections (Token Contract, UX Strategy, State Inventory, Microcopy Rules, Motion Contract)\". Leave \"The four skills are complementary\" and \"holding three of the four\" unchanged \u2014 those count skills and are correct.\n\n3. Lines 158-159, replace the Authorities template with:\n`**Authorities**: baseline[; ui-designer (tokens); ux-designer (strategy, states); ux-copywriter (microcopy);`\n`             ux-motion (motion, state transitions); frontend-design:frontend-design (aesthetic)]`\nand append to the sentence at :117, after \"naming the skill per section\": \", and naming both skills where a section is jointly owned (State Inventory)\".\n\nDo NOT act on the finding's claim that \u00a75:293 requires the baseline file to reproduce \u00a73's table \u2014 it does not; leave :293 unchanged."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:94 (R1 gate) vs :264 (\u00a74.7 design-agent) and :300 (\u00a75 design-agent row)",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "major",
   "reasoning": "Quotes verified: :94, :264 (\"mode keyed on Figma-URL presence in the PRD, never on load outcome. With Figma: unchanged. Without: derive per-task component specs *against the approved contract*\"), :300 (\"Consume the contract; replace `## Handling No Design`; read master TECH_PLAN\"). Code evidence verified: `agents/belmont/design-agent.md:253` (\"If Figma URLs are provided for a task, they MUST load. Block ONLY that task if they fail \u2014 other tasks continue\"), `:270` (`## Handling No Design`), `:272` (\"If no Figma URLs are provided for a task:\") \u2014 design-agent's mode is unambiguously per-task. `skills/belmont/_src/references/product-plan-prd-format.md:59-60` confirms Figma nodes live in an optional free-form per-task `**Notes**` field, so a mixed feature is a normal shape, not a contrived one. I searched the proposal for any handling of partial Figma coverage: :96 covers only backend-only *milestones*; :223 covers only the contract-plus-screenshot overlap; nothing addresses a UI task with no Figma inside a feature that has Figma elsewhere.\n\nPartial refutation considered and rejected: :95's author decision (\"Figma features keep today's behaviour untouched\") arguably absorbs mixed features by fiat, and a careful implementer would naturally write the `## Handling No Design` replacement with a no-contract arm. That is why I downgrade from blocker to major rather than confirming blocker \u2014 the PR is implementable and the worst realistic outcome for those tasks is today's (already-broken) behaviour, not a regression. But the gap is real and consequential: \u00a71's Broken row is a *task-level* condition (`design-agent.md:270-276` is per-task) that this PR gates at *feature* level, and \u00a75 tells the implementer to \"replace\" the only per-task handler without specifying what a no-Figma task in a Figma feature gets. With \u00a74.7's \"Without: derive \u2026 against the approved contract\" as the sole instruction for that arm, an implementer who deletes rather than branches leaves design-agent with an unexecutable instruction \u2014 and inventing values there would not even be caught by the NO FALLBACK rule, which is scoped to *failed loads* (`design-agent.md:61-68`), not absent ones.",
   "fix_to_apply": "1. Add to R1 (after :96's scope paragraph): \"**Mixed Figma coverage.** A feature counts as a Figma feature if **any** task in `{base}/PRD.md` carries a Figma URL, even when other UI tasks carry none. Those uncovered tasks keep today's per-task no-Figma behaviour \u2014 deliberately, since a partially-covered feature has no single design authority to reconcile against. The replacement for `## Handling No Design` MUST therefore keep a no-contract arm.\"\n2. Rewrite \u00a74.7's design-agent bullet (:264) third sentence as: \"Without Figma **and** with `**Mode**: derived`: derive per-task component specs against the approved contract. Without Figma and **without** a `derived` contract (the feature carries Figma on other tasks, or is pre-contract per \u00a74.9): today's `## Handling No Design` behaviour, unchanged.\"\n3. Rewrite the \u00a75 design-agent row (:300) as: \"Consume the contract when `**Mode**` is `derived`; keep today's `## Handling No Design` bullets verbatim as the no-contract arm; read master TECH_PLAN. `FORBIDDEN ACTIONS` untouched.\"\n4. Add a DoD line under **Gating and scope**: \"- [ ] A UI task with no Figma URL inside a Figma-bearing feature still gets today's `## Handling No Design` behaviour \u2014 the replacement section names the no-contract case explicitly\"."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:99 (R4) vs :281-283 (\u00a74.9) and :404 (DoD)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK: exact. :281 = \"**This gate is not retroactive.** A feature planned before this PR has a `{base}/TECH_PLAN.md` with no `## Design Contract`, so \u00a74.6 branch 3 applies \u2026 That is deliberate: silently applying a new quality gate to already-approved plans would fail milestones on a standard their author never agreed to.\" :404 = \"Pre-contract features fall to branch 3 and are documented as non-retroactive (\u00a74.9)\". R4 :99 = \"preserve an existing `## Design Contract` verbatim, deriving only sections that are absent; mark it `**Approval**: unreviewed (headless replan <ISO date>)`\".\n\nTHE CONTRADICTION IS REAL. On a pre-contract feature every contract section is absent, so R4's \"deriving only sections that are absent\" derives the whole thing; R1's gate is satisfied (UI, no Figma); and R4 supplies the exact stamp for the case. The feature then matches \u00a74.6 branch 2 (:226 \"No references, contract present \u2192 contract checks only\") plus the fourth enforcement rule (:233), i.e. gated on a standard nobody approved \u2014 the precise harm \u00a74.9 says is deliberately avoided \u2014 and DoD :404 becomes unsatisfiable. The alternative reading (\"an existing\" implies the contract exists, so R4 simply does not apply) does not rescue the document: it leaves headless behaviour on a pre-contract feature undefined, which is what R4's own title (\"Headless behaviour is defined, not implied\") exists to prevent.\n\nEVIDENCE VERIFIED LINE-BY-LINE IN THE CODE, and it is exact:\n- cmd/belmont/main.go:7188-7189 \u2014 `case actionReplan:` / `return fmt.Sprintf(\"/belmont:tech-plan --feature %s\", feature)`\n- cmd/belmont/main.go:7642-7644 \u2014 `if verifyFailCount >= 2 {` / `// Delegate to AI for REPLAN/DEBUG decision` / `return nil`\n- cmd/belmont/main.go:7887 \u2014 `actionReplan` is in the accepted-action switch for the AI decision\n- and the path I checked myself that the finding did not cite: main.go:6391-6392, `if action == nil { aiAction, err := decideLoopActionAI(...)`, which is what consumes the nil at :7644. The route is live in shipped code for any feature in an auto run \u2014 including every pre-contract one, which is by definition the whole current install base.\n\n`**Approval**` IS WRITE-ONLY, as claimed. grep for \"Approval|unreviewed\" returns only :99 (sets it), :160 (schema), :219, :354 and :376 (smoke assertions). \u00a74.6's three branches (:225-227) and the fourth rule (:233) never consult it, so nothing downstream distinguishes an unreviewed contract from an approved one.\n\nAND THE DOCUMENT ALREADY CONTRADICTS R4 ELSEWHERE, which the finding missed and which settles author intent: :386 states \"*`belmont auto` cannot author a contract \u2014 the only auto path to tech-plan is `actionReplan` \u2014 so this must be the interactive path.*\" That is a third statement inconsistent with R4 as written, and it tells you which way to resolve the fix. Severity stands at major: two documented behaviours cannot both be implemented, and the literal reading ships the retroactive gate the document promises not to ship.",
   "fix_to_apply": "Append a fourth clause to R4 (line 99), after \"\u2026and never silently regenerate an approved contract.\": \"**A headless replan never creates a contract that does not exist.** If `{base}/TECH_PLAN.md` contains no `## Design Contract` heading at all, Phase 3.5 writes nothing and the feature stays on \u00a74.6 branch 3 \u2014 'deriving only sections that are absent' means subsections of a contract that is already present, never the contract itself. Contract authoring requires the interactive approval step (R3); \u00a74.9's re-run is the only adoption path. This is what \u00a78 Step 9c means by '`belmont auto` cannot author a contract'.\" Then, in \u00a74.9 (line 281), after \"\u2026so \u00a74.6 branch 3 applies\", add \"\u2014 and R4's fourth clause keeps it there: a headless `actionReplan` on such a feature writes no contract.\" Do NOT take the finding's alternative of gating \u00a74.6 branch 2 on `**Approval**: approved`: an approved contract that had one subsection derived headlessly is stamped `unreviewed` by R4, so that gate would silently switch off checking for an otherwise-approved contract. If `**Approval**` is to stay, add one line under the \u00a74.4 schema block (after :160) stating plainly that it is provenance metadata for review and is not read by any branch in \u00a74.6."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:151 (\u00a74.4) and :292 (\u00a75 tech-plan-feature-format row) vs :95 and :379/:426",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Every quote checks out and the code evidence is exact.\n\nSpec :151: \"The feature template's `## Design Tokens (from Figma)` (`tech-plan-feature-format.md:44`) is **generalised**, not supplemented\". Spec :292: \"Generalise `## Design Tokens (from Figma)` \u2192 `## Design Contract` incl. all six subsections\". Spec :95: \"Figma features keep today's behaviour untouched.\" Spec :379: \"Expect `**Mode**: N/A \u2014 Figma present`, **no** Token Contract\". Spec :426: \"Figma feature: no contract, verdict PASS, behaviour unchanged (Step 7)\".\n\nCode verified: `skills/belmont/_src/references/tech-plan-feature-format.md:44-45` is exactly \"## Design Tokens (from Figma)\" / \"[Exact values extracted from Figma - colors, spacing, typography]\". I read the whole template \u2014 its other sections are Overview, PRD Task Mapping, File Structure, Component Specifications (which carries only per-component `**Figma Node**` / `**Styling Notes**`), API Integration, Existing Components to Reuse, State Management, Verification Checklist, Edge Cases, Implementation Order, Notes. There is no other destination for global extracted tokens (colour/spacing/typography scales). `skills/belmont/_src/tech-plan.md:158` is verbatim \"If any Figma URLs are included in the PRD, load them **inline** \u2026 Extract design tokens, layout, typography, and component specs. Document findings in the tech plan.\", and `:310` is \"**Read `references/tech-plan-feature-format.md` for the feature template**, then write `{base}/TECH_PLAN.md` using that structure\" \u2014 so the template is the write instruction, not a suggestion.\n\nI checked whether the spec resolves this anywhere else: `grep -n \"Design Tokens\"` over the proposal returns only :151 and :292, and `Token Contract` appears at :52, :111, :162, :354, :379 \u2014 none of which gives Figma-extracted tokens a home. So on the Figma path the section that today holds them is replaced by a `**Mode**: N/A \u2014 Figma present` stub with, per Step 7, **no** Token Contract. That is a structural change to the path :95 and :426 promise is unchanged, and it leaves the implementer choosing between an N/A stub (tokens lost) and a populated Token Contract (contradicts :379).\n\nNot an acknowledged limitation \u2014 \u00a74.4 justifies generalising by citing the Figma-scoped heading and checklist, and never notes the loss. Severity: major, not blocker \u2014 the PR remains implementable, and design-agent re-extracts per-task Figma specs into MILESTONE during implement, so no false result ships; but it forces rework and an unguided judgement call, and falsifies a smoke assertion and a DoD line.",
   "fix_to_apply": "Resolve the ambiguity in favour of keeping the Figma destination, in three places:\n\n1. \u00a74.4 (:151) \u2014 replace \"is **generalised**, not supplemented \u2014 it is Figma-scoped today, and its verification checklist hard-codes \\\"Matches Figma design pixel-perfect\\\"\" with: \"is **generalised**, not supplemented \u2014 it is Figma-scoped today, and its verification checklist hard-codes \\\"Matches Figma design pixel-perfect\\\". Under `**Mode**: N/A \u2014 Figma present` the `### Token Contract` subsection is not empty: it carries the Figma-extracted values exactly as `## Design Tokens (from Figma)` does today (`tech-plan.md:158` writes them there), labelled `Source: Figma nodes <ids>`. Only the *derived* baseline contract \u2014 grid, ratio, 60/30/10 \u2014 is absent on the Figma path. No other subsection is populated in that mode.\"\n\n2. \u00a75 (:292) \u2014 extend the `tech-plan-feature-format.md` row's Change cell to: \"Generalise `## Design Tokens (from Figma)` \u2192 `## Design Contract` incl. all six subsections; **`### Token Contract` remains the destination for Figma-extracted tokens when `**Mode**: N/A \u2014 Figma present`, so the Figma path loses no section**; contract branch in the Figma-only verification checklist; carry R4's preserve rule\".\n\n3. Smoke Step 7 (:379) \u2014 replace \"Expect `**Mode**: N/A \u2014 Figma present`, **no** Token Contract\" with \"Expect `**Mode**: N/A \u2014 Figma present`, **no derived** Token Contract \u2014 the subsection instead carries the Figma-extracted tokens exactly as `## Design Tokens (from Figma)` does today \u2014 and no UX Strategy / State Inventory / Microcopy / Motion content\". Keep DoD :426 as-is but reword \"no contract\" to \"no derived contract\" so it matches R1's `**Mode**: N/A \u2014 Figma present` stub.\n\n(If the author prefers the alternative \u2014 retain `## Design Tokens` as a sibling section and add `## Design Contract` beside it \u2014 the same three locations must say so; what is not acceptable is leaving :151/:292 as the only statement.)"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:370 (Step 4)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quote checked verbatim against spec line 370 \u2014 accurate. Every cited fact verified independently: `skills/belmont/_src/references/verify-report-format.md:23` is exactly `- Visual Verification: [PASS/FAIL/N/A]`; `skills/belmont/_src/verify.md:125` is exactly `**Collect**: The verification report document.`; `agents/belmont/verification-agent.md:210` is `## Visual Verification (if applicable)` and `:231` is `### Visual Comparison Attestation`; `cmd/belmont/main.go:6760` is `if sl.Type != \"assistant\" {` and `:6917` is `cmd.Stdout = &claudeStreamWriter{tw: tw, prefix: prefix}`. I read verify.md end-to-end: the only things it persists are Polish items and Five Whys entries to `{base}/NOTES.md` (`:200`, `:214`) \u2014 the verification report itself is never written to disk, and a repo-wide grep for a persisted report finds only debug-auto's `{base}/DEBUG.md`. `verify-report-format.md` is confirmed absent from the \u00a75 scope table (which lists `_src/verify.md` with three named edits only). I settled the decisive open question by RUNNING it: `claude -p --output-format stream-json --verbose` with a Task dispatch (claude 2.1.223). The sub-agent's returned text arrives on two events, both non-`assistant`: `type=\"user\"` with `parent_tool_use_id=toolu_01RSsMU5G8WRoZbvPm9njP7g` carrying `text`, and `type=\"user\"` carrying `tool_result`. No `assistant` event ever carries the sub-agent report. `claudeStreamWriter` therefore drops it \u2014 measured, not reasoned. I searched the whole spec for any other place naming where the report is read (grep on `report|attestation|Visual Verification|stderr`): \u00a74.6:258 defines the attestation field, \u00a75:301 scopes the sub-agent's Output Format block, Step 1b:358 says only 'a verify report with contract checks'. Nowhere does the document say where an author opens it. So the finding is not a 'missing but handled elsewhere' case. DOWNGRADED from blocker to major on two grounds. (a) The finding overstates 'nothing to open': Step 1b is an interactive `/belmont:verify`, and in the Claude Code REPL the Task result is present in the transcript and expandable \u2014 so the artifact is observable on the interactive path, just not the auto path where Step 4 sits. (b) I tested the finding's implied wider consequence and it does not hold: `post-verify-triage.md` does not depend on the report text \u2014 `executeTriageAction` passes `\"VerifyOutput\": \"\"` (main.go:7019) and the template instructs the agent to read follow-up descriptions from `{{.FeatureBase}}/PROGRESS.md`, so contract failures still reach triage via follow-up task text. The defect is therefore confined to the smoke test: Step 4 asserts an artifact with no named observation point, and under auto has none at all, so an author records a false pass on the PR's central gate assertion. That is rework and a wrong decision, not a broken deliverable.",
   "fix_to_apply": "Replace line 370 in full with:\n\n\"**Step 4 \u2014 the gate fires.** *Observed in the Step 1b interactive run, not the auto run.* The per-row table and the attestation exist only in the verification sub-agent's returned report (`verification-agent.md` `## Output Format`, `:210` / `:231`); `verify.md:125` only *collects* it into context and never writes it to disk, and the orchestrator's combined summary (`references/verify-report-format.md:23`) collapses it to `- Visual Verification: [PASS/FAIL/N/A]`. Under `belmont auto` it is unobservable: the sub-agent report arrives as `tool_result` / `text` on `user`-typed stream events and `claudeStreamWriter` drops everything that is not `assistant` (`main.go:6760`, installed on the shell-out at `:6917`) \u2014 measured against `claude -p --output-format stream-json --verbose`. So expand the verification sub-agent's Task result in the Step 1b REPL and confirm `## Visual Verification` shows per-row pass/fail/UNVERIFIABLE with a Mechanism column, and that the attestation reads `Contract checks performed: YES against {base}/TECH_PLAN.md`.\"\n\nOptional but stronger (choose one, not both): instead of relocating, add `skills/belmont/_src/references/verify-report-format.md` to the \u00a75 In-scope table with the change 'carry the contract-check table and the `Contract checks performed:` line through into the combined summary rather than collapsing Visual Verification to one line', and keep Step 4 where it is. If you take this option, also add a matching DoD bullet under **Gate**."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:388 (Step 10) vs :434 (DoD Mechanics)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Both quotes checked verbatim \u2014 line 388 is `**Step 10 \u2014 plugin surface.** `./scripts/generate-plugin.sh <ver>`; confirm \u2026` and line 434 is `- [ ] `plugin/` regenerated and committed; verified with `./scripts/generate-plugin.sh --check` (version-agnostic after 0006), **not with a version argument that mutates `plugin.json`**`. The 'not with a version argument' clause modifies 'verified with', and Step 10 is a verification step in the Author smoke test that uses exactly a version argument. Direct self-contradiction inside one document \u2014 the same class as rev 4's mutually exclusive checkboxes. Script evidence confirmed line-for-line: `scripts/generate-plugin.sh:19` `VERSION=\"dev\"`, `:25` `VERSION=\"$arg\"` (any non-`--check` arg becomes VERSION, `:21-27`), `:35` `rm -rf \"$PLUGIN_DIR\"`, `:44` `\"version\": \"$VERSION\",`. Reproduced the mutation myself on a copy of the repo at /private/tmp/.../scratchpad/repo: `./scripts/generate-plugin.sh 0.99.0` rewrote `plugin/.claude-plugin/plugin.json` from the committed `0.10.14` to `0.99.0` and left `git status --porcelain` reporting ` M plugin/.claude-plugin/plugin.json`. I checked the obvious refutation \u2014 that 0006 (which this PR depends on) makes the problem vanish \u2014 and it does not: 0006 \u00a73.2 (`docs/proposals/0006-plugin-generator-correctness.md:102-106`) changes only `--check` to ignore the `version` field; generate mode still stamps VERSION into the tracked plugin.json, and 0006's own smoke step at `:141` still runs `./scripts/generate-plugin.sh 0.10.14`. So the contradiction survives 0006. Two of the finding's *consequential* claims are wrong and must NOT be carried into the fix: Step 10 runs `./scripts/\u2026` so it executes in the belmont source repo, whereas Steps 0\u20139 run in `~/path/to/real-project` (line 344) \u2014 so the dirty tree cannot trip `requireCleanWorkingTree` (main.go:5349, func at :9818) for any later `belmont auto` (there is none after Step 10 anyway), and the `git add -A` at line 346 is in the project repo, not the belmont repo. The core defect stands: an author following Step 10 with any version string other than the current release commits a wrong `\"version\"` into a tracked release artifact, which the DoD explicitly forbids. Major, not blocker \u2014 the PR design is unaffected and the fix is a one-command edit.",
   "fix_to_apply": "Replace line 388 in full with:\n\n\"**Step 10 \u2014 plugin surface.** Run from the **belmont source repo**, not the smoke project:\n```bash\ncd ~/belmont\n./scripts/generate-plugin.sh --check && echo \"plugin/ up to date\"\ntest -s plugin/skills/tech-plan/references/design-authority-baseline.md\n! find plugin/agents -name '*.md' -size 0 | grep -q .   # 0006's fix\n```\nRegeneration and commit of `plugin/` happen inside the PR at the release version, **not as a smoke step**: any non-`--check` argument becomes `VERSION` (`generate-plugin.sh:21-27`, defaulting to `dev` at `:19`) and is stamped into the tracked `plugin/.claude-plugin/plugin.json` at `:44` after `rm -rf` at `:35` \u2014 measured: `./scripts/generate-plugin.sh 0.99.0` rewrites the committed `0.10.14` to `0.99.0` and dirties the tree. This is what \u00a79 Mechanics forbids.\"\n\nDo not add the requireCleanWorkingTree or `git add -A` rationales \u2014 Step 10 runs in a different repo from Steps 0\u20139 and neither applies."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:360-363 (Step 2)",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "major",
   "reasoning": "Quote checked verbatim against lines 360-363 \u2014 accurate, including the trailing clause 'to hash-match master's after resume'. Every supporting citation the finding offers is correct and I re-derived each: `mergeWorktreeBranch` calls `syncFeatureStateAfterMerge(cfg.Root, wtPath, cfg.Feature)` at main.go:9157 and `removeWorktree(cfg.Root, wtPath, milestoneID)` at :9164, then `git branch -d` at :9166-9170; `hasExplicitDeps` is main.go:5441-5453 routing to `runAutoParallel` at :5449-5450; the banner at :8584 is `Belmont Auto (parallel) \u2014 %s`; the resume prompt is :6099/:6100 with `Resuming with existing worktree at %s` at :6113. I checked the strongest available refutation \u2014 that a single-milestone wave 1 might take a master-tree shortcut and never fork a worktree, making the finding's premise false \u2014 and it does not hold: `runAutoParallel` (main.go:8558) routes **every** wave through `runWaveParallel` with an explicit comment 'Every wave \u2014 including single-milestone waves \u2014 runs through the worktree path' (:8628-8636), and \u00a75's own out-of-scope list already flags `AGENTS.md:207`'s master-tree shortcut as deleted. `mergeWorktreeBranch` is called from inside `runWaveParallel` (:8897, :9000), so wave 1's worktree is removed before wave 2 starts. I also grepped the whole spec for `hash|shasum|worktree` \u2014 no other passage names an observation point for this assertion; Step 3 (:367) uses a committed-revision `git diff`, which is a different check. So this is not 'addressed elsewhere'. Kept at major rather than downgraded because DoD line 398 ('Contract survives `[r]`-resume (Step 2)') names Step 2 as the evidence for a DoD item, so an unrunnable assertion leaves that item verified by nothing. Confidence is medium, not high, for one reason: 'after resume' is arguably readable as 'immediately after the resume event' rather than 'after the command returns', and a second shell would satisfy it \u2014 but the step names no such window and `belmont auto` blocks the foreground, so an author following it literally finds the directory gone. One correction that must be applied: the finding's suggested path `.belmont/worktrees/<slug>-m1` is WRONG. `worktreeBasePath` (main.go:544-555) resolves to `~/.belmont/worktrees/<project-basename>/`, not a project-relative `.belmont/`, and `wtPath` is `filepath.Join(worktreeBasePath(cfg.Root), fmt.Sprintf(\"%s-%s\", cfg.Feature, strings.ToLower(m.ID)))` (:8925). Worth also stating that a post-merge comparison would be vacuous anyway, since `syncFeatureStateAfterMerge` makes master a byte copy of the worktree.",
   "fix_to_apply": "Replace line 363 in full with:\n\n\"Expect the banner `Belmont Auto (parallel)` \u2014 a serial fallback means the precondition was not met. Then, **in a second shell while wave 1 is still running**, confirm the resume-time state copy preserved the contract. The window is narrow and must be named: the worktree exists only between `Resuming with existing worktree at \u2026` (`main.go:6113`) and the wave-1 merge, which runs `syncFeatureStateAfterMerge` (`:9157`) and then `removeWorktree` (`:9164`) \u2014 after that the directory is gone, and every wave including a single-milestone one goes through the worktree path (`runAutoParallel`, `:8628-8636`).\n```bash\nWT=~/.belmont/worktrees/$(basename \"$PWD\")/<slug>-m1   # worktreeBasePath, main.go:544-555; wtPath at :8925\nshasum \"$WT/.belmont/features/<slug>/TECH_PLAN.md\" .belmont/features/<slug>/TECH_PLAN.md\n```\nExpect identical hashes. Note that comparing **after** the merge proves nothing \u2014 `syncFeatureStateAfterMerge` replaces master's feature dir with the worktree's, so the two are equal by construction. Step 3's `git diff $PLAN..HEAD` remains the durable assertion.\""
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:366-368 (Step 3)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quote check passes verbatim. Spec :367-368 reads \"`git diff $PLAN..HEAD -- .belmont/features/<slug>/TECH_PLAN.md` \u2014 expect **empty**.\" / \"Do **not** use a bare `git diff`: `.belmont/` is `--assume-unchanged` in a worktree, so `git diff`, `git diff -- .belmont/` and `git status --porcelain` all report nothing even after the file has been completely rewritten (measured).\"\n\nCode check passes. cmd/belmont/main.go:10169-10170 is `os.RemoveAll(dstFeature)` / `if err := copyDir(srcFeature, dstFeature); err != nil` inside `syncFeatureStateAfterMerge` \u2014 a plain filesystem write to master, no git. :8638-8640 is `if err := runWaveParallel(cfg, w, activeWorktrees); err != nil { return err }`, which precedes the only `commitBelmontState(cfg.Root)` in that function at :8650. `grep -n 'syncFeatureStateAfterMerge\\|commitBelmontState'` shows the commit is never per-merge: calls at :6020 (after ALL multi-feature merges) and :8650 (after ALL waves), while the syncs are at :6297 and :9157. :10107 `lsCmd.Dir = wtPath` and :10120 `cmd.Dir = wtPath` are correct.\n\nI ran both claims rather than reasoning about them. (a) In a scratch repo, `git diff HEAD~1..HEAD -- .belmont/features/nope/TECH_PLAN.md` produced no output with `exit=0`; an untracked/gitignored path behaves identically. \u00a74.5:217 explicitly calls gitignored `.belmont/` \"a supported configuration\", so the check can pass with no positive control. (b) I reproduced the assume-unchanged isolation exactly: after `git worktree add` + `git update-index --assume-unchanged .belmont/features/x/TECH_PLAN.md` in the worktree, `git ls-files -v` shows `h` in the worktree and `H` in the main tree. Simulating `syncFeatureStateAfterMerge` (rm -rf + cp from the worktree onto master), `git status --porcelain .belmont/features/x/TECH_PLAN.md` from the MASTER root printed ` M .belmont/features/x/TECH_PLAN.md` while `git diff HEAD~1..HEAD -- <path>` printed nothing. So the one command the spec tells you not to use is the only one that sees the clobber, and the spec's blanket enumeration reads as a ban even though its causal clause is worktree-scoped \u2014 and Step 3 is run from the master root, since `mergeWorktreeBranch` removes the worktree at :9164 before Step 3 happens.\n\nThe window is not hypothetical and is arguably wider than the finding states: Step 3 says \"After the wave **merges**\", not \"after the run completes\". Run at the literally instructed moment \u2014 post-merge, mid-run \u2014 the diff is *guaranteed* empty regardless of whether the contract was clobbered, because `.belmont/` is assume-unchanged on the branch so the merge commit carries no TECH_PLAN.md change and `commitBelmontState` has not run. It also stays empty if a later wave returns err (:8638-8640), if the user interrupts, or if `commitBelmontState` bails on `.git/MERGE_HEAD` (:10214-10215).\n\nThis is not an acknowledged limitation. \u00a74.1:84 acknowledges the *rule* is unguarded and names \"smoke Step 3's content hash\" as the detection; \u00a79:395 and \u00a710:444 both stake the assertion on this exact diff. So the document's only stated detector can pass while the defect is present \u2014 the same class of defect v6 claims to have fixed (\u00a712:487 \"Smoke Step 3's `git diff` could not fail \u2014 Measured blind\"). Confirmed, major.",
   "fix_to_apply": "Edit \u00a78 Step 3 (lines 366-368). (1) Add a lead-in sentence: \"Run these three from the **master repo root**, after the auto run has fully exited \u2014 not mid-run.\" (2) Replace the single command with:\n\n```bash\n# positive control \u2014 fails loudly if the contract is untracked or .belmont/ is gitignored\ngit ls-files --error-unmatch .belmont/features/<slug>/TECH_PLAN.md\n# committed clobber\ngit diff $PLAN..HEAD -- .belmont/features/<slug>/TECH_PLAN.md   # expect empty\n# clobber landed on master's disk but not yet committed\ngit status --porcelain .belmont/features/<slug>/TECH_PLAN.md    # expect empty\n```\n\n(3) Replace the warning paragraph with: \"Do **not** run a bare `git diff` **from inside the worktree**: `.belmont/` is `--assume-unchanged` there, so `git diff`, `git diff -- .belmont/` and `git status --porcelain` all report nothing even after the file has been completely rewritten (measured). That bit is set with `cmd.Dir = wtPath` (`untrackBelmontInWorktree`, `main.go:10104-10122`) and lives in the worktree's own index, so from the **master** root `git status --porcelain <path>` works normally \u2014 and it is the only one of the three that catches the window where `syncFeatureStateAfterMerge` has already landed the worktree's copy on master's disk (`main.go:10169-10170`) but `commitBelmontState` has not run. That commit sits at the end of `runAutoParallel` (`main.go:8650`) and is skipped whenever a wave returns an error (`:8638-8640`), the run is interrupted, or a merge is in progress \u2014 so the committed-revision diff alone can pass over a live clobber.\"\n\n(4) \u00a79 DoD line 395: change \"asserted by smoke Step 3's committed-revision diff\" to \"asserted by smoke Step 3's three-command check \u2014 tracked positive control, committed-revision diff, and working-tree status from the master root\". (5) \u00a710 Risks line 444: change \"Detection is smoke Step 3's committed-revision diff, not git status\" to \"Detection is smoke Step 3's three-command check; `git status` is blind only *inside* a worktree, and from the master root it is what catches an uncommitted clobber\"."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:402 (DoD) vs \u00a78 Steps 1-10",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quote check passes. Line 402 reads verbatim: \"- [ ] Contract created only when the feature has UI **and** no Figma URLs; `**Mode**` is the single signal, with `N/A` values for both exclusions\".\n\nI searched the whole 492-line document for the second exclusion rather than trusting the finding's step inventory. `grep -n 'no UI\\|backend\\|Backend'` returns only :23 (problem framing), :94 (R1, \"Backend features record `N/A \u2014 no UI`\"), :96 (scope note), :155 (the `**Mode**` enum), :256 and :373 (focused-reverify wording, unrelated), and :276 (models-yaml heuristics). Nothing in \u00a78. I also re-read \u00a78 end to end: its preamble at :339 scopes the whole test to \"a UI-bearing feature with no Figma URLs\"; Steps 1/1b/2/3/4/5/6 all run that one feature; Step 7 is \"A feature **with** Figma URLs\"; Step 8 is a failed Figma load on it; 9a re-runs the same slug under codex; 9c is explicitly \"a **second** UI-bearing no-Figma feature\"; Step 10 is the plugin build. So exactly one of the two exclusions the DoD asserts is exercised, and it is the rarer one.\n\nThe untested branch carries real new behaviour, not a no-op: \u00a75 scopes `tech-plan-feature-format.md` to \"Generalise `## Design Tokens (from Figma)` \u2192 `## Design Contract` incl. all six subsections\", so after this PR *every* feature template carries the section and a backend feature must actively resolve R1's UI test and write `**Mode**: N/A \u2014 no UI`. R1's UI predicate is a model judgement with no mechanical signal (unlike the Figma-URL half, which is a grep), so a wrong call adds a design interview round and a spurious contract to every backend plan \u2014 the majority case \u2014 and no step in \u00a78 would notice. \u00a710's risk table likewise has a row for \"Gate fires on a Figma feature\" (:447) and none for a backend feature.\n\nThis is not an acknowledged limitation and is not covered elsewhere. AGENTS.md makes the author smoke test non-negotiable precisely because \"all unit tests pass\" does not prove the change works, and this PR has zero Go changes and zero unit-test coverage \u2014 \u00a78 is the *only* evidence path. A DoD line asserting behaviour the smoke test cannot observe is the same class of defect \u00a712 lists as already fixed (\"`--check` DoD items replaced with checks that can actually fail\"). Confirmed, major.",
   "fix_to_apply": "Add a new step to \u00a78, immediately after Step 7 (line 379), so the two exclusions sit together:\n\n\"**Step 7b \u2014 backend regression (the other exclusion).** A feature with **no user interface** (API, pipeline, infra). `claude` \u2192 `/belmont:tech-plan --feature <backend-slug>`.\nExpect: the interview asks **no design questions**; `{base}/TECH_PLAN.md` carries `## Design Contract` with `**Mode**: N/A \u2014 no UI` and **no** `### Token Contract`, `### UX Strategy`, `### State Inventory`, `### Microcopy Rules` or `### Motion Contract` body; no `{base}/design-preview.html` is written; `**Approval**`, `**Source**` and `**Authorities**` are absent or `N/A`. Then `/belmont:verify --feature <backend-slug>` on a done milestone: expect \u00a74.6 branch 3 (acceptance-criteria fallback) and `Contract checks performed: N/A`.\nFail: a populated Token Contract or a `design-preview.html` \u2192 R1's UI half is not gated and every backend plan now pays for a design interview.\"\n\nThen amend \u00a79 DoD line 402 to name both witnesses: \"...with `N/A` values for both exclusions \u2014 `N/A \u2014 Figma present` asserted by Step 7, `N/A \u2014 no UI` by Step 7b\". Optionally add a \u00a710 risk row: \"| Gate fires on a backend feature | R1 requires a UI **and** no Figma; `**Mode**: N/A \u2014 no UI`; Step 7b asserts no Token Contract and no preview |\"."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:279-283 (\u00a74.9) and :404 (DoD)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK: both exact. :283 reads \"To adopt the contract on an existing feature, re-run `/belmont:tech-plan --feature <slug>`; Phase 3.5 derives the missing section and the user approves it.\" :404 reads \"- [ ] Pre-contract features fall to branch 3 and are documented as non-retroactive (\u00a74.9)\".\n\nCOVERAGE CHECK (failure mode (b) \u2014 is it handled elsewhere?): I enumerated every step header in \u00a78 \u2014 Preconditions, 1, 1b, 2, 3, 4, 5, 6, 7, 8, 9(a/b/c), 10. None runs interactive tech-plan against a feature with a populated `{base}/TECH_PLAN.md` lacking `## Design Contract`. Step 6 is the closest and is explicitly the opposite shape: \"run `claude -p` \u2026 **with the contract already present**\". I also grepped the whole 492-line document for migrat/pre-contract/retroactiv/preserve: \u00a74.9, :291-292 (scope rows), :330 (docs/workflow.md), :334 (Don't re-do), :399 and :404. Nothing tests it. The gap is real.\n\nEVIDENCE CHECK: `skills/belmont/_src/tech-plan.md:310` verified verbatim \u2014 \"**Read `references/tech-plan-feature-format.md` for the feature template**, then write `{base}/TECH_PLAN.md` using that structure.\" The finding's second citation is off by one: \"When updating PRD or PROGRESS (CRITICAL): If the files have real content, NEVER replace the entire file.\" is at :150, not :151. The quote is exact so the finding does not fail on misquotation, and the substance holds \u2014 that rule names PRD and PROGRESS and pointedly not TECH_PLAN. Cross-checked ALLOWED ACTIONS at :96-105 (the proposal's own citation) as a line-number calibration: it matches exactly, so :150 is the correct number, not a drift artifact.\n\nTHE UNDERLYING HOLE IS REAL, and larger than the finding states: R4 (:99) scopes the preserve rule to `## Design Contract` only \u2014 \"preserve an existing `## Design Contract` verbatim, deriving only sections that are absent\" \u2014 and R8 (:103) propagates that same section-scoped rule to the Phase 4 write. Nothing in the proposal, and nothing at tech-plan.md:150, protects the REST of a populated TECH_PLAN.md against the unconditional whole-file template write that \u00a74.9 now prescribes as the migration path for every existing feature.\n\nWHERE THE FINDING IS IMPRECISE, without being defeated:\n(a) \"Step 1 runs tech-plan on a fresh feature\" \u2014 the spec never says fresh; \u00a78's precondition only greps an existing `{base}/PRD.md`. But Step 1 asserts nothing about preservation of pre-existing content, so the assertion gap stands regardless of which feature Blake picks.\n(b) The suggested assertion \"every pre-existing section of TECH_PLAN.md is byte-identical\" is too strong and would fail against a correct implementation \u2014 interactive re-planning legitimately rewrites prose. The fix below weakens it to section-survival, which is what actually needs guarding.\n(c) On :404 the finding is half right: \"documented as non-retroactive\" is doc-checkable and cannot fail, but \"fall to branch 3\" is testable \u2014 and I confirmed it is also untested (Step 7's Figma feature carries `**Mode**: N/A \u2014 Figma present`, i.e. it HAS a `## Design Contract`, so it is branch 1, not branch 3).\n\nGRADE: major stands. This is the path every existing Belmont user takes, \u00a74.9 is the least-scrutinised material in rev 6, and AGENTS.md makes an author smoke test covering changed behaviour non-negotiable.",
   "fix_to_apply": "Two edits.\n\n(1) In \u00a78, insert after Step 1b (line 358) a new step:\n\n**Step 1c \u2014 a pre-contract feature adopts the contract (\u00a74.9 migration).** Take a **second** UI-bearing, no-Figma feature `<slug3>` whose `{base}/TECH_PLAN.md` was written before this branch and has no `## Design Contract` (or strip the section from a copy of Step 1's feature). Record `PRE=$(git rev-parse HEAD)`, then `claude` \u2192 `/belmont:tech-plan --feature <slug3>` and approve.\nExpect: (a) `## Design Contract` present with `**Mode**: derived \u2014 UI, no Figma` and `**Approval**: approved <date>`; (b) `git diff $PRE..HEAD -- .belmont/features/<slug3>/TECH_PLAN.md | grep '^-## '` is **empty** \u2014 no pre-existing section heading deleted; (c) `git diff $PRE..HEAD -- .belmont/features/<slug3>/PROGRESS.md` shows no task-state changes.\nFail: a dropped section heading \u21d2 the Phase 4 whole-file write (`tech-plan.md:310`) carries R8's preserve rule for `## Design Contract` only, and re-planning an existing feature loses its plan.\n\n(2) In \u00a74.2 R8 (line 103), extend the rule beyond the contract section \u2014 append: \"The preserve rule the write instruction carries must cover **the whole file**, not only `## Design Contract`: `tech-plan.md:150` states 'NEVER replace the entire file' for PRD and PROGRESS but not for TECH_PLAN, and \u00a74.9 makes an interactive re-run the prescribed migration for every pre-contract feature.\"\n\nAlso replace DoD :404 with two items: `- [ ] Pre-contract features fall to \u00a74.6 branch 3 \u2014 asserted by a feature with no \\`## Design Contract\\` verifying on acceptance criteria, not FAIL` and `- [ ] \u00a74.9's re-run migration adds \\`## Design Contract\\` without dropping any pre-existing TECH_PLAN section (Step 1c)`."
  },
  {
   "spec_location": "\u00a78 Step 3, lines 366-368; DoD line 395; \u00a710 line 444",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 verbatim. Line 366: \"**Step 3 \u2014 design-agent consumes, never writes.** After the wave **merges**:\"; line 367: \"`git diff $PLAN..HEAD -- .belmont/features/<slug>/TECH_PLAN.md` \u2014 expect **empty**.\"; line 368 ends \"...which is why this compares committed revisions.\" DoD line 395 and \u00a710 line 444 are quoted accurately.\n\nCODE CHECK \u2014 every call site is exactly as cited. main.go:8638-8640 `if err := runWaveParallel(cfg, w, activeWorktrees); err != nil { return err }`; the wave loop closes at :8643; `commitBelmontState(cfg.Root)` is at :8650, after it. `runWaveParallel` merges successful siblings in the loop at :9000 (`mergeWorktreeBranch`) and only then returns `fmt.Errorf(\"auto: wave %d had %d failure(s)\", ...)` at :9018. `commitBelmontState`'s MERGE_HEAD early return is at :10214-10216, and the empty-status early return at :10224-10226. The SIGINT handler (:8621-8627) calls `os.Exit(1)`, so an interrupted run also never reaches :8650.\n\nMEASURED \u2014 I replayed merge \u2192 `syncFeatureStateAfterMerge` in a scratch repo with a rogue worktree edit to `TECH_PLAN.md`. Master's working-tree file read `WORKTREE-EDITED CONTRACT` while `git diff $PLAN..HEAD -- .belmont/features/myfeat/TECH_PLAN.md` printed **nothing**. Only after replaying `commitBelmontState` (`git add .belmont/` + commit) did the same command print `-APPROVED-CONTRACT / +WORKTREE-EDITED CONTRACT`. I also confirmed the finding's proposed alternative works at the earlier timing: `git diff $PLAN -- <path>` (no `..HEAD`) printed the hunk immediately after the sync. The mechanism is exactly as claimed \u2014 the branch never carries the edit (assume-unchanged, which I verified separately), so the merge commit is clean, and the damage lives only in the working tree until the run's final state commit.\n\nNOT A DUPLICATE OR ACKNOWLEDGED LIMIT \u2014 I grepped the whole document for `commitBelmontState`, `All waves complete`, and every occurrence of `$PLAN`. Nothing anywhere retimes Step 3 or notes the dependency on a completed run. \u00a712 line 487 claims this exact check was already fixed for being \"Measured blind\", so it is not filed as a known limitation.\n\nWHY MAJOR, NOT BLOCKER \u2014 the check is not universally incapable of failing. A tester who runs `belmont auto --from M1 --to M2` to completion and then runs the diff does get a working check (measured above), and Steps 4-6 presuppose a completed run. It yields a false PASS only (a) at the literal moment the step names \u2014 after a wave's merges, before the run ends, which in a two-wave `--from M1 --to M2` run is a real intermediate point \u2014 and (b) permanently, when any sibling fails and `runAutoParallel` returns at :8639 with successful siblings already merged and synced. That is a wrong decision on the PR's load-bearing durability assertion in a realistic subset, and it is cheap to fix precisely, but it does not make the PR unimplementable.",
   "fix_to_apply": "Rewrite \u00a78 Step 3's timing and check (lines 366-368). Replace \"After the wave **merges**:\" and the `$PLAN..HEAD` command with:\n\n\"**Step 3 \u2014 design-agent consumes, never writes.** Run this **after `belmont auto` exits with `\u2713 All waves complete`**, not after an individual wave merges:\n`git diff $PLAN..HEAD -- .belmont/features/<slug>/TECH_PLAN.md` \u2014 expect **empty**.\n**If the run aborted mid-wave, that check is invalid and silently passes.** `syncFeatureStateAfterMerge` lands the worktree's copy in master's *working tree*; nothing commits it until `commitBelmontState` (`main.go:10211`), which `runAutoParallel` calls once at `:8650` after the wave loop \u2014 a wave failure returns at `:8639` first, and Ctrl-C exits before it. Use the working-tree form in that case, which fires regardless:\n`git diff $PLAN -- .belmont/features/<slug>/TECH_PLAN.md` \u2014 expect **empty** (measured: this printed the rogue hunk immediately after the merge+sync, while the `..HEAD` form printed nothing).\"\n\nKeep the existing \"Do **not** use a bare `git diff`\" paragraph \u2014 it is correct and still needed \u2014 but change its closing clause from \"which is why this compares committed revisions\" to \"which is why this compares against the recorded `$PLAN` revision rather than the index.\"\n\nAlso amend \u00a710 line 444's mitigation cell to end: \"...Detection is smoke Step 3's diff against `$PLAN`, not `git status`; the committed-revision form only works once the run reaches `commitBelmontState`. A mechanical guard is deferred to its own proposal.\" And amend DoD line 395 to read \"...asserted by smoke Step 3's `$PLAN` diff, run after the auto run completes\"."
  },
  {
   "spec_location": "\u00a74.6 line 260",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK: line 260 reads verbatim \"**Severity** uses the existing Critical / Warning / Polish ladder: contrast failure, missing focus indicator, missing label \u2192 Critical; missing state, off-scale spacing, out-of-band duration \u2192 Warning; radius, elevation or easing inconsistency \u2192 Polish. Off-scale spacing stays Warning so pre-existing drift cannot block a milestone.\" The finding's ellipsis removes nothing that changes meaning; the quote is faithful.\n\nEVIDENCE CHECK: all citations are exact. `agents/belmont/verification-agent.md:307` = \"### Warning (Blocks Milestone \u2014 Should Fix)\"; `:299` = \"### Critical (Blocks Milestone \u2014 Must Fix)\"; `:314` = \"### Polish (Does NOT Block Milestone \u2014 Minor Improvement)\"; `:292` = \"**Note**: Only Critical and Warning issues should become FWLUP tasks. Polish items are reported here for reference but should NOT generate follow-up tasks\u2026\"; `:319` = \"- Minor spacing inconsistencies (1-2px off)\"; `:320` = \"- Animation/transition polish\". I also checked `:297`: \"The distinction between Warning and Polish is critical \u2014 it determines whether the auto loop creates follow-up tasks or defers the issue.\"\n\nI tried three ways to break it. (1) Could \"stays Warning\" mean \"stays at Warning rather than being promoted to Critical\"? Even under that reading the trailing causal clause \"so pre-existing drift cannot block a milestone\" is false, because Warning blocks by definition at `:307`. (2) Could the auto loop defuse it downstream? No \u2014 the opposite: \u00a74.7 line 269 instructs \"Add a non-deferrable contract list\" to `post-verify-triage.md` precisely so contract failures are not deferred, which makes a Warning-tier contract failure *more* blocking, not less. (3) Is it handled elsewhere in the 492-line document? `grep -n \"Warning\\|Polish\\|Critical\"` returns line 260 and nothing else \u2014 the ladder appears exactly once.\n\nThe finding does overreach in two places, neither fatal: the sentence only makes the claim about off-scale spacing (not about missing state / durations, as the defect prose implies), and \"the existing ladder already classifies this class as Polish\" is only half true \u2014 `:301` puts \"spacing significantly off\" in Critical and `:319` puts \"1-2px off\" in Polish, with nothing at Warning, so Warning is a new tier rather than a straight promotion.\n\nGRADE: not a blocker. The mapping is implementable as written; nothing is unsatisfiable and no pipeline breaks. What ships is a false justification plus a mis-tiered severity, whose worst case is FWLUP tasks and a fix round for drift the milestone did not introduce (bounded by the fix-round>=2 circuit breaker in `prompts/belmont/post-verify-triage.md:22`). That is rework and a wrong decision \u2192 major.",
   "fix_to_apply": "In `docs/proposals/0003-design-quality-without-figma.md`, replace all of line 260 with:\n\n\"**Severity** uses the existing Critical / Warning / Polish ladder (`verification-agent.md:295-326`). Note that **Warning blocks the milestone and generates FWLUP tasks** (`:307`, `:292`) \u2014 only Polish does not (`:314`). Contrast failure, missing focus indicator, missing label \u2192 Critical. Missing state, out-of-band duration \u2192 Warning. Off-scale spacing, radius, elevation or easing inconsistency \u2192 **Polish**, matching the existing `Minor spacing inconsistencies (1-2px off)` bullet (`:319`), so pre-existing drift on a brownfield project cannot block the first contract-checked milestone.\"\n\nThe sentence \"Off-scale spacing stays Warning so pre-existing drift cannot block a milestone.\" must be deleted \u2014 it is false against `verification-agent.md:307`.\n\nThen amend \u00a74.7's `post-verify-triage.md` bullet (line 269) so the change does not re-introduce the defect: after \"Add a non-deferrable contract list\", add \"\u2014 Critical-tier contract failures only; Polish-tier contract findings stay deferrable, matching `verification-agent.md:292`.\""
  },
  {
   "spec_location": "\u00a74.1 line 82 (the PR's central rule); \u00a75 in-scope table lines 289-308; DoD line 395; \u00a77 line 335",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK: line 82 reads verbatim \"> **Only the `tech-plan` skill may write `{base}/TECH_PLAN.md`. `implement`, `verify`, `next` and `debug` read it and never write it.** When `tech-plan` itself runs headlessly as `actionReplan`, R4 constrains what it may change.\" DoD line 395 and \u00a77 line 335 are as quoted; `grep -n -i debug` over the whole 492-line document returns only lines 82 and 375, so nothing elsewhere addresses this.\n\nEVIDENCE CHECK: `skills/belmont/_partials/debug-scope-rules.md:16` is exactly \"| `{base}/TECH_PLAN.md` | Decision narrative, library choices, file-path references, API shapes \u2014 anything where reality has moved past the written record |\", under \"### What you MAY edit\" (`:11`). `skills/belmont/_src/references/debug-manual-spec-reconcile.md:17` is exactly row 5 as quoted. The partial is included only at `skills/belmont/_src/debug-manual.md:29` (`grep -rn debug-scope-rules` over `skills/`), and `debug-auto.md:20` includes `milestone-immutability.md` instead \u2014 so the finding's own carve-out for debug-auto is correct. The \"What you MUST NOT do\" block (`:23-29`) carries structural prohibitions for PROGRESS.md and \"No restructuring\" for milestone headings, but nothing excluding a `## Design Contract`; reconcile row 5's \"preserve heading structure\" stops deletion of the heading, not rewriting of its contents.\n\nI tried to break it on the reading of \"debug\". The best available refutation is that \u00a74.1's whole paragraph is about auto-loop phases and worktree merge copies, and that `actionDebug` dispatches `/belmont:debug-auto`, which has no grant. That reading dies on two facts: there IS a shipped skill literally named `debug` (`skills/belmont/_src/debug.md`, a router), and it routes \"UI bugs, visual issues, styling problems\" to `/belmont:debug-manual` (`debug.md:33`, and `:15`) \u2014 i.e. exactly the bug class a design contract is implicated in lands on the one skill holding the grant.\n\nThe finding does overstate one thing: \"The DoD checkbox as written cannot be satisfied\" is wrong. DoD line 395 self-limits to \"asserted by smoke Step 3's committed-revision diff\", which only exercises the auto wave, so the box is satisfiable as stated. Mitigation also exists that the finding underweights: debug-manual never runs from `belmont auto` (`debug-scope-rules.md:7`), edits are per-edit human-approved (`:8`), and they happen in the master tree \u2014 so this is a provenance/approval defect, not the merge-copy durability hazard \u00a74.1 is otherwise about.\n\nWhat is left is still real and worth fixing: the PR's central rule is stated falsely against shipped content, the `**Approval**` stamp is never re-dated when debug-manual amends the contract, and closing it requires two files the \u00a75 scope table does not contain plus a fourth `Revisions` line. That is rework and a wrong decision \u2192 major stands.",
   "fix_to_apply": "Three edits to `docs/proposals/0003-design-quality-without-figma.md`.\n\n1. Replace line 82 with:\n\"> **Only the `tech-plan` skill may write the `## Design Contract` section of `{base}/TECH_PLAN.md`. `implement`, `verify` and `next` read it and never write it.** When `tech-plan` itself runs headlessly as `actionReplan`, R4 constrains what it may change.\"\nand add immediately below it:\n\"**One documented exception must be carved back.** Interactive `/belmont:debug-manual` holds a standing grant to edit `{base}/TECH_PLAN.md` narrative in place under per-edit user approval (`skills/belmont/_partials/debug-scope-rules.md:16`; `references/debug-manual-spec-reconcile.md:17` row 5), and `/belmont:debug`'s router sends UI and visual bugs there (`debug.md:33`). This PR narrows that grant to exclude `## Design Contract` \u2014 contract changes route through `/belmont:tech-plan`, which re-stamps `**Approval**`. The grant is master-tree and human-gated and debug-manual never runs from `belmont auto` (`debug-scope-rules.md:7`), so this is an approval-provenance concern, not the merge-copy hazard above. `/belmont:debug-auto` has no such grant (`debug-auto.md:20` includes `milestone-immutability.md`) and needs no change.\"\n\n2. Add two rows to \u00a75's in-scope table (and update the header count at line 5 from 13 tracked source files to 15, and the \u00a75 line-count note at line 6 accordingly):\n| `skills/belmont/_partials/debug-scope-rules.md` | Add `## Design Contract` to `### What you MUST NOT do`: TECH_PLAN narrative remains editable, the contract section does not |\n| `skills/belmont/_src/references/debug-manual-spec-reconcile.md` | Row 5 excludes the `## Design Contract` section; point at `/belmont:tech-plan` |\n\n3. \u00a77 line 335 and DoD line 437: add `knowledge/cross-cutting/debug-spec-reconciliation.md` to the `Revisions` list and change \"three `Revisions` lines\" to \"four\". Also reword DoD line 395's first clause to \"the `## Design Contract` section is written by `tech-plan` only\" so it matches the narrowed rule."
  },
  {
   "spec_location": "\u00a74.9 lines 279-283; R4 line 99; R1 line 94",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "PRIMARY CLAIM SURVIVES. Quotes check out verbatim: line 281 'This gate is not retroactive. A feature planned before this PR has a `{base}/TECH_PLAN.md` with no `## Design Contract`, so \u00a74.6 branch 3 applies\u2026'; line 99 R4 'preserve an existing `## Design Contract` verbatim, deriving only sections that are absent'. Code evidence verified line-by-line: main.go:7188-7189 `case actionReplan:` / `return fmt.Sprintf(\"/belmont:tech-plan --feature %s\", feature)`; main.go:9104-9123 `mCfg := cfg` / `mCfg.Root = wtPath` \u2026 `return runLoop(mCfg)` (worktree root, headless); main.go:7642-7644 `if verifyFailCount >= 2 { // Delegate to AI for REPLAN/DEBUG decision \\n return nil }`; main.go:7840 decider prompt lists 'REPLAN: Re-run tech planning when current approach has systemic issues'; main.go:12066-12068 `if action.Type == actionReplan { return }` in runScopeGuard. I searched the whole 492-line document for a carve-out (`grep -n 'retroactive|pre-contract|branch 3'` \u2192 lines 279, 281, 330, 334, 404, 457 only) and there is none. Worse, \u00a74.5 line 219 confirms the finding's reading rather than contradicting it: 'R4 is the one exception: a headless replan that derives a missing section regenerates the preview and stamps it unreviewed.' R4 contains only a prohibition on *regenerating* an approved contract, never one on *creating* one, and R1's gate (UI + no Figma) is satisfiable by a pre-contract feature with no user present. So an unattended REPLAN mints a full unreviewed contract on a plan whose author never saw one, after which \u00a74.6 branch 2 plus the fourth enforcement rule ('a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE') gate the milestone \u2014 the exact outcome \u00a74.9 promises to avoid. \u00a74.9's guarantee is therefore false in auto mode. Note the frequency is low (needs two verify failures AND the AI decider to pick REPLAN over DEBUG \u2014 the proposal itself concedes this at Step 6, line 375), which is why this is major and not a blocker. SECOND HALF OF THE FINDING IS REFUTED and must not drive the edit: the claim that 'a verification agent taught that silence is illegal has no defined response to a legitimately silent pre-contract plan' is failure mode (b). R1's 'Silence is not allowed' (line 94) is an instruction to the *authoring* skill about the `**Mode**` field *inside a contract it is writing* \u2014 'Record the outcome in the contract's `**Mode**` field'. The verifier's response to an absent contract is defined explicitly at \u00a74.6 branch 3 (line 227): 'No references, no contract \u2192 existing acceptance-criteria fallback, unchanged.' Nothing is undefined. ALSO REJECT THE FINDING'S OWN SUGGESTED FIX: writing `**Mode**: N/A \u2014 pre-contract feature` would create a `## Design Contract` heading where none existed, which flips \u00a74.6 from branch 3 to branch 2 and arms the fourth enforcement rule against a contract with no sections \u2014 it manufactures the failure it is trying to prevent, and adds a fourth Mode value not in \u00a74.4's enumeration at line 155.",
   "fix_to_apply": "Two edits, both in the proposal.\n\n1. \u00a74.2, R4 (line 99) \u2014 after 'and never silently regenerate an approved contract' insert: 'And never *create* one: if `{base}/TECH_PLAN.md` carries no `## Design Contract` heading at all, the headless step derives nothing and leaves the file's design surface untouched \u2014 a pre-contract feature is adopted only by an interactive run (\u00a74.9). R4's derive-missing-sections behaviour applies only to a contract that already exists with a partial section set.'\n\n2. \u00a74.9 (line 281) \u2014 after 'so \u00a74.6 branch 3 applies and it keeps today's acceptance-criteria verification' insert: 'This holds under `belmont auto` too: `actionReplan` invokes `/belmont:tech-plan` headlessly (`buildLoopPrompt`), and R4 forbids that path from creating a contract where none exists, so an unattended run cannot mint one and arm \u00a74.6's fourth enforcement rule against a plan its author never approved.'\n\nOptional, cheap, and harmless: in \u00a74.6 branch 3, name the signal explicitly \u2014 'No references, no contract (no `## Design Contract` heading in `{base}/TECH_PLAN.md`)'. Do NOT add a `**Mode**: N/A \u2014 pre-contract` value, and do not touch R1's 'Silence is not allowed' clause."
  },
  {
   "spec_location": "\u00a74.1 write rule (line 82); \u00a75 In-scope table",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quotes check out. Spec line 82 reads exactly: \"**Only the `tech-plan` skill may write `{base}/TECH_PLAN.md`. `implement`, `verify`, `next` and `debug` read it and never write it.**\" \u2014 the first clause is universal over skills, not scoped to loop phases, so any other skill that writes that path contradicts a rule the PR states. `skills/belmont/_src/review-plans.md` is that skill: `:36` `- **DO** edit PRDs, Tech Plans, PROGRESS, and NOTES files based on user decisions`; `:50` loads `- `TECH_PLAN.md` (feature technical plan)` per feature; `:215` `- **Updating PRDs/Tech Plans**: Use Edit to modify specific sections.`; Drift resolution `:191-192` `**Drift** (something changed from plan):` / `1. Update plan to match reality \u2014 edit the planning document to reflect current state` (the finding cites :192-194, a one-line slip on the header \u2014 content is exact). `grep -n 'review-plans' docs/proposals/0003-\u2026md` returns nothing, and the Out-of-scope list (`:312`) does not exclude it, so the document is genuinely silent \u2014 this is not a case of \"covered elsewhere\". I tried three refutations and none held. (a) \"It's only a loop-phase rule\" \u2014 defeated by the universal first clause and by DoD `:395` \"no other phase writes that path\", which the smoke test only exercises for an auto wave. (b) \"The scenario is speculative\" \u2014 partly: Layer 4 (`:150-170`) is explicitly \"NOT a full code audit\" and static, so contrast/spacing drift is unlikely to surface there. But Layer 2 (`:108`) \u2014 \"Check if feature tech plan follows master tech plan's architecture decisions \u2192 **Drift** if it diverges\" \u2014 is triggered directly by this PR's own derivation ladder rung 0 (`:205`, a `## Design Contract` in the master `.belmont/TECH_PLAN.md`) plus the tier-1 allowance that a skill \"may add detail or argue a deviation and must record it\" (`:113`). A recorded deviation from the master contract is textbook Layer 2 Drift, and option 1 rewrites the feature plan. That is a likely, not exotic, trigger. (c) \"The user approves it\" \u2014 true and it defeats the word *silently*: `review-plans.md:19` \"**Interactive** \u2014 every finding is presented to the user with resolution options. Never auto-resolve.\" But the residual defect survives regardless: review-plans has no notion of the `**Approval**` field (`:160`), so a Drift/Conflict rewrite mutates contract content while leaving `**Approval**: approved <ISO date>` intact \u2014 the approval stamp becomes a lie, and \u00a74.6's checks then run against the loosened contract. Not blocker: it needs an explicit user choice in an audit skill and does not by itself ship a false PASS unattended. Grade stands at major.",
   "fix_to_apply": "1) Add a row to \u00a75's In-scope table (after the `_src/next.md` row):\n\n| `skills/belmont/_src/review-plans.md` | Treat `## Design Contract` as read-only: exclude it from Drift/Conflict rewrite options; emit a finding instead |\n\n2) In \u00a74.1, immediately after the blockquoted write rule at line 82, add:\n\n\"One existing skill also writes this path and is therefore in scope: `/belmont:review-plans` loads every feature `TECH_PLAN.md` (`review-plans.md:50`), is permitted to edit Tech Plans (`:36`, `:215`), and its **Drift** resolution offers *\"Update plan to match reality \u2014 edit the planning document to reflect current state\"* (`:192`). Applied to `## Design Contract` that reconciles an approved contract to non-compliant code while leaving `**Approval**` stamped \u2014 the approval becomes false and \u00a74.6's checks then pass against the loosened contract. Its Layer 2 check (`:108`, feature plan vs master plan) makes this a likely trigger once rung 0 puts a contract in the master `TECH_PLAN.md`. Every edit is user-approved (`:19` never auto-resolves), so this is not silent \u2014 but it is unguarded, exactly as the rule above is. `review-plans.md` must therefore state: **never edit `## Design Contract`**; when the implementation or a sibling/master contract diverges from it, report it as a finding worded *\"implementation deviates from the approved Design Contract \u2014 fix the code, or re-run `/belmont:tech-plan` to re-approve the contract\"*, with no 'update plan to match reality' option offered for that section. A missing contract on a UI feature may still be reported as a Gap.\"\n\n3) Add to \u00a79 Definition of Done under **Placement and durability**:\n- [ ] `review-plans.md` refuses to edit `## Design Contract` and offers a re-approve-via-tech-plan finding instead\n\n4) Add to \u00a77's `design-authority.md` `Don't re-do` list: \"letting `review-plans` reconcile an approved contract to shipped code (rejected \u2014 it would invalidate the `**Approval**` stamp while leaving it in place)\"."
  },
  {
   "spec_location": "\u00a74.1 write rule (line 82, \"debug\"); \u00a74.6 fourth enforcement rule (line 233); \u00a75 In-scope table",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "major",
   "reasoning": "All citations verified: debug-phase1-design.md:1 and :3 exact; debug-auto.md:30, :60-61, :93 exact; debug-auto.md Phase 3 at :125 with `**DEBUG MODE OVERRIDE**` at :132 and `**Outcome**: [FIXED | PARTIAL | NO_CHANGE | REGRESSION]` at :147; debug-manual.md:114 and the same include at :150, override at :160, Outcome at :226. Spec lines 82 and 233 say what is claimed, and no debug file is in \u00a75.\n\nTwo of the finding's three limbs break.\n\n(1) The design-phase limb is not a defect. \u00a74.1 line 82 is a WRITE prohibition (\"read it and never write it\") naming debug as a non-author; it is not a promise that debug performs contract-aware design analysis. Debug skipping design input on a no-Figma UI bug is today's behaviour, unchanged by this PR, and \u00a71 scopes the PR to the milestone design/verify pipeline. Making debug contract-aware is an enhancement, not a fix.\n\n(2) The vocabulary limb is a misread. The three existing enforcement rules live at verification-agent.md:128-131 and constrain **Visual Verification** status (\"Visual Verification MUST be **FAIL** or **INCOMPLETE** \u2014 never PASS\"), not the debug `Outcome` line, which is a separate classification the debug prompt adds. A fourth rule in the same slot introduces no vocabulary conflict \u2014 the existing three already coexist with DEBUG MODE OVERRIDE.\n\n(3) The kernel survives, and it is PR-caused rather than pre-existing. verification-agent.md:131 today reads \"If no design references existed at all, Visual Verification CAN pass based on acceptance criteria alone\". That clause is exactly what makes debug-mode verification clean on a UI-no-Figma feature today. \u00a74.6 line 233 narrows it to \"no design references **and no design contract**\" and adds \"a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE\". verification-agent.md:20 instructs the agent to read TECH_PLAN unconditionally, and the debug prompt does not forbid it. So after this PR, every debug-auto/debug-manual verification on a contract-bearing feature \u2014 the exact class this PR exists for \u2014 flips from \"CAN pass\" to a mandatory FAIL/INCOMPLETE, because debug verification is bug-scoped and never runs the browser contract pass. debug-auto retries up to 3 iterations off the Outcome line, so a genuinely fixed bug can be pushed to PARTIAL and burn iterations. Confidence is medium because whether the agent propagates a FAIL Visual Verification into `Outcome` is a judgement call, but the Visual Verification field itself is deterministically wrong.\n\nMajor stands: it ships a false result on a whole skill path, but the fix is one clause and no new file enters scope.",
   "fix_to_apply": "In \u00a74.6, extend the \"Close the escape clause\" paragraph (line 233). After \"...and add a fourth enforcement rule: *a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE*\", append:\n\n\"**Milestone mode only.** Both the narrowing of `:131` and the fourth rule apply to milestone-mode verification. Under **DEBUG MODE OVERRIDE** (`debug-auto.md:132`, `debug-manual.md:160`) verification is scoped to the reported bug and never runs the browser contract pass, so the presence of a `## Design Contract` must NOT by itself force Visual Verification to FAIL/INCOMPLETE. In debug mode the attestation records `Contract checks performed: N/A \u2014 debug mode` and `:131`'s acceptance-criteria pass remains available.\"\n\nAdd to \u00a75's existing `agents/belmont/verification-agent.md` row: \"; DEBUG MODE carve-out on the narrowed `:131` and the fourth rule\". No new files enter \u00a75 \u2014 the carve-out lives entirely in a file already in scope.\n\nDo NOT re-gate `_partials/debug-phase1-design.md` on contract presence, and do NOT add `_src/debug-auto.md` / `_src/debug-manual.md` to scope or a new smoke step \u2014 that limb of the finding is an enhancement to debug's design input, not a defect this PR introduces."
  }
 ],
 "survived_minors": [
  {
   "spec_location": "\u00a78 Author smoke test, Step 2 precondition (line 361)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK \u2014 line 361 reads verbatim: \"otherwise `hasExplicitDeps` is false (`main.go:5441-5453`) and `runAuto` routes to `runLoop` \u2014 master tree, no worktree, no `[r]` prompt.\" No misquote.\n\nCODE CHECK \u2014 the behaviour and the line range are correct: main.go:5441 `hasExplicitDeps := false`, :5443 `if len(m.Deps) > 0 {`, :5449 `if hasExplicitDeps {`, :5450 `return runAutoParallel(cfg, inRange)`, :5453 `return runLoop(cfg)`. The identifier is not: `grep -n \"func runAuto\" cmd/belmont/main.go` returns only :5273 `func runAutoCmd(args []string) error {`, :5700 `func runAutoMultiFeature`, :8558 `func runAutoParallel`. The enclosing function of :5441-5453 is `runAutoCmd` (it closes at :5454). There is no function named `runAuto` anywhere in the repo \u2014 `grep -rn \"runAuto\\b\"` across cmd/, knowledge/ and docs/ hits only proposal/review documents, never source.\n\nMINOR IMPRECISION IN THE FINDING, NOT FATAL \u2014 the finding says `grep -n 'func <name>'` \"returns nothing for `runAuto`\", which is not literally true (it prefix-matches `runAutoCmd`). Its own evidence block states the accurate result. The substantive claim \u2014 the named function does not exist \u2014 is correct, so this does not break the finding.\n\nNOT AN ACKNOWLEDGED LIMITATION \u2014 the line-9 caveat disclaims only line numbers and explicitly elevates function names as the durable identifier: \"Function names are the durable identifier and are given first throughout \u2026 Re-resolve at branch time with `grep -n 'func <name>'`.\" That makes a wrong function name the one citation class the doc promises will survive the 0005 split, so it is squarely in scope rather than excused.\n\nSEARCHED THE WHOLE SPEC \u2014 `runAuto` appears exactly once in this document (line 361); `runAutoParallel` and `runAutoMultiFeature` are used correctly elsewhere (e.g. lines 322 area and \u00a74.1 citations). Single-site fix.\n\nSEVERITY \u2014 minor is correct as graded. It is a one-token naming error with no effect on the prescribed action; the smoke-test precondition it supports is substantively right. Note a prior review round already flagged this and dismissed it as \"trivial\" \u2014 that judgement was about severity, not truth, and the defect is still present in rev 6.",
   "fix_to_apply": "In \u00a78 Step 2 precondition (line 361), replace `runAuto` with `runAutoCmd` so the clause reads \"\u2026and `runAutoCmd` routes to `runLoop` \u2014 master tree, no worktree, no `[r]` prompt.\" No other change."
  },
  {
   "spec_location": "\u00a74.4 Contract format, \"Per-task specs stay in MILESTONE\" (line 211)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check first: line 211 reads verbatim \"design-agent still produces one `### Design: [Task ID]` section per active task (`design-agent.md:232`), because `implementation-agent.md` Step 1.1 looks its per-task spec up there.\" No ellipsis, no misreading.\n\nEvidence check, all confirmed by running greps:\n- `grep -n \"Step 1\\.1\" agents/belmont/implementation-agent.md` \u2192 exit 1, no match. The file's headings are `#### Step 0`, `#### Step 0b`, `#### Step 0c`, `#### Step 1: Preparation` (:77), `#### Step 2: Implementation` (:85) \u2026 There is no sub-numbered step anywhere in the file. The relevant content is numbered list item 1 under Step 1, at :79: \"1. **Identify the current task** - Find this task's definition in the PRD (task IDs listed in MILESTONE's `### Active Task IDs`), its codebase context in `## Codebase Analysis`, and its design spec in `## Design Specifications`\".\n- `grep -n \"Design:\" agents/belmont/implementation-agent.md` \u2192 exit 1, no match. Every reference is to the section `## Design Specifications` (:22, :79, :99, :194, :272). The `### Design: [Task ID]` sub-heading is never named.\n- The parenthetical the finding concedes is correct is correct: `agents/belmont/design-agent.md:232` is \"**IMPORTANT**: Produce one `### Design: [Task ID]` section for EACH task listed in MILESTONE's `### Active Task IDs`. Do not skip any.\"\n\nThe finding's rationale also holds: line 267 of the spec really does warn \"*This file has Steps, not Phases \u2014 `verification-agent.md` has Phases. Never conflate them.*\", and `verification-agent.md` really does carry sub-numbered headings in exactly the `Step N.M` shape (`#### Step 2.0` :53, `#### Step 2.1` :65, `#### Step 2.4` :96), which the spec itself cites at line 28. So \"Step 1.1\" reads as a real heading reference in a document that has trained the reader to expect that form.\n\nI searched the rest of the spec for a compensating correct citation: \"Step 1.1\" appears only at line 211; the other implementation-agent citations (`:192-193` at line 254, `:190-197`/`:196`/`:197`/`:272` at line 266) are unrelated. Nothing elsewhere fixes it.\n\nSubstance is only partly damaged: implementation-agent :79 does tell the agent to find *this task's* design spec, so \"looks its per-task spec up there\" is defensible in spirit. But the causal justification the sentence offers for preserving the `### Design:` shape is overstated \u2014 implementation-agent reads the whole `## Design Specifications` section and parses no sub-heading. Both halves are citation imprecision, not a design error, so minor is the correct grade. Given this proposal series has shipped four generations of propagated mis-citations, it is worth fixing.",
   "fix_to_apply": "In `docs/proposals/0003-design-quality-without-figma.md`, replace line 211 in full:\n\nOLD:\n**Per-task specs stay in MILESTONE, unchanged in shape.** design-agent still produces one `### Design: [Task ID]` section per active task (`design-agent.md:232`), because `implementation-agent.md` Step 1.1 looks its per-task spec up there.\n\nNEW:\n**Per-task specs stay in MILESTONE, unchanged in shape.** design-agent still produces one `### Design: [Task ID]` section per active task (`design-agent.md:232`), because `implementation-agent.md` Step 1, item 1 (`:79`) sends the implementer to \"its design spec in `## Design Specifications`\". Note implementation-agent reads that whole section and never parses the `### Design:` sub-heading \u2014 the per-task shape is design-agent's own contract for making each task's spec findable, not a format a consumer depends on."
  },
  {
   "spec_location": "\u00a74.2 R8 (line 103)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check first: line 103 reads verbatim \"R4 constrains Phase 3.5, but the instruction that actually writes the file is Phase 4 (`tech-plan.md:310`) via `tech-plan-feature-format.md`, which is an unconditional whole-file template write.\" Accurate, no truncation.\n\nEvidence check against `skills/belmont/_src/tech-plan.md` (310 lines total), all confirmed:\n- `### Phase 4 - Write Plan` is at :219; the next phase heading `### Phase 4.5 - PRD Reconciliation (MANDATORY)` is at :245, so Phase 4's body is :219-:243.\n- Phase 4's own `{base}/TECH_PLAN.md` write instructions are :226 (\"2. Then write `{base}/TECH_PLAN.md` using the **Feature TECH_PLAN.md Format** below.\", Scenario A) and :230 (\"1. Write `{base}/TECH_PLAN.md` using the **Feature TECH_PLAN.md Format** below.\", Scenario B).\n- `## Feature TECH_PLAN.md Format` is a separate top-level section at :308, and :310 is its body and the file's last line: \"**Read `references/tech-plan-feature-format.md` for the feature template**, then write `{base}/TECH_PLAN.md` using that structure.\"\n\nSo line 310 is factually not inside Phase 4. The line *number* is the right place to add the preserve rule (grep confirms :226, :230, :308, :310 are the only occurrences of \"Feature TECH_PLAN.md Format\" in the file, and both Phase-4 writes delegate to :310); it is the *phase label* attached to it that is wrong.\n\nI checked whether the spec repairs this elsewhere: it does not \u2014 it propagates. \u00a75 line 291 says \"preserve rule at Phase 4\" and DoD line 399 says \"stated at **both** Phase 3.5 and the Phase 4 write instruction (R8)\". Three places carry the same loose label.\n\nI weighed refuting this as taste: \"Phase 4 \u2026 via `tech-plan-feature-format.md`\" can be read as \"Phase 4's write path, which routes through the feature template\", and the spec's own disclaimer at line 9 covers only `main.go:NNNN` line numbers, not skill sources \u2014 so skill-source citations are held to full accuracy. The finding's projected harm (implementer edits only one of two write points) is somewhat weak, since a single edit at :310 covers both Scenario A and Scenario B. But the structural claim is plainly false as written, cheap to fix, and this is exactly the mis-citation class that has burned four prior revisions. Minor, as graded.",
   "fix_to_apply": "In `docs/proposals/0003-design-quality-without-figma.md`, edit line 103.\n\nOLD fragment:\nR4 constrains Phase 3.5, but the instruction that actually writes the file is Phase 4 (`tech-plan.md:310`) via `tech-plan-feature-format.md`, which is an unconditional whole-file template write.\n\nNEW fragment:\nR4 constrains Phase 3.5, but Phase 4's two feature-plan writes (`tech-plan.md:226` Scenario A, `:230` Scenario B) both delegate to the separate `## Feature TECH_PLAN.md Format` section (`:308-310`), whose redirect to `references/tech-plan-feature-format.md` is an unconditional whole-file template write. State the preserve rule at `:310` \u2014 the single point both scenarios reach \u2014 and in the reference file itself.\n\nAlso align the two echoes of the loose label:\n- \u00a75 line 291, change \"preserve rule at Phase 4\" \u2192 \"preserve rule at the `## Feature TECH_PLAN.md Format` redirect (`:310`)\".\n- DoD line 399, change \"the Phase 4 write instruction (R8)\" \u2192 \"the `## Feature TECH_PLAN.md Format` redirect at `:310` that Phase 4 delegates to (R8)\"."
  },
  {
   "spec_location": "\u00a712 Changes from v5, author-decisions table, `interactive-prototype` row (line 475)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check first: line 475 reads verbatim \"`interactive-prototype` is a *builder*: five of its eight steps produce React code with a named animation library, and its own description excludes static specs.\" Accurate.\n\nEvidence check against `~/.claude/skills/interactive-prototype/SKILL.md` (the installed user-scope skill, which is the artifact \u00a74.3 says these tier-1 skills are). `grep -n \"^## Step\"` returns exactly nine headings, at precisely the lines the finding cites: :19 Step 1 Read the Design, :46 Step 2, :119 Step 3, :146 Step 4, :177 Step 5 Build With Motion, :262 Step 6, :279 Step 7, :301 Step 8, :388 Step 9 After Presenting. Eight is wrong.\n\nThe qualitative half of the spec's claim verifies cleanly and should be kept: the frontmatter `description` at :3 ends \"Do NOT use for static mockups, dashboards, landing pages, or marketing sites.\", and Step 5 (:177-:260) is explicitly build-with-a-library (`import { motion, AnimatePresence, useMotionValue, \u2026 } from \"framer-motion\"` at :200; React Spring + use-gesture at :211-:232).\n\nI looked hard for a defense of \"eight\". The best one is the skill's own line 16: \"Steps 3-8 (decide + design + build) happen after\" \u2014 an author counting the pre-delivery workflow could land on eight, treating Step 9 (After Presenting) as post-hoc. That mitigates authorial carelessness but does not rescue the sentence: the spec asserts the skill has eight steps, and it has nine. The \"five of\" numerator is separately soft \u2014 steps that actually emit React/animation-library code are Step 5 unambiguously, plus JSX fragments in Step 6 (`onError`, `draggable={false}`, `pointerEvents`), Step 7 (`whileTap={{ scale: 0.97 }}`) and the fix pass in Step 8; getting to five requires counting Step 4 (`useMotionValue`/`useTransform`, though it says \"before writing code\") or Step 9 (mentions Motion, but post-delivery). So the ratio is not reconstructible either way.\n\nThis sits in \u00a712, a changelog row justifying an already-taken author decision, and the decision (drop `interactive-prototype` from tier 1) is unaffected \u2014 so it cannot ship a broken result. Minor is right. Worth fixing because line 6 of this same document already flags a prior wrong count (\"rev 5 claimed 9 against a table of 10\") as a defect class it is policing.",
   "fix_to_apply": "In `docs/proposals/0003-design-quality-without-figma.md` line 475, replace the ratio clause. Drop the numerator/denominator rather than recount, since only Step 5 emits library code unambiguously.\n\nOLD fragment:\n`interactive-prototype` is a *builder*: five of its eight steps produce React code with a named animation library, and its own description excludes static specs.\n\nNEW fragment:\n`interactive-prototype` is a *builder*: its Step 5 is literally \"Build With Motion\" and hands the agent `framer-motion` / `@react-spring/web` imports, with further JSX in Steps 6-8 of its nine; its own `description` ends \"Do NOT use for static mockups, dashboards, landing pages, or marketing sites.\"\n\nLeave the rest of the row unchanged \u2014 the overlap-with-`ux-designer`/`ux-motion` argument and the \"belongs to implementation, not planning\" conclusion stand."
  },
  {
   "spec_location": "\u00a74.3 \"Use exact registered names\" (line 123)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check first: line 123 reads verbatim \"`tech-plan.md:160` and `product-plan.md:165` currently name `vercel-react-best-practices` and `security`; neither resolves (`vercel:react-best-practices` is the real one; `security` is a slash command).\" No ellipsis, no distortion. Source citations are exact \u2014 skills/belmont/_src/tech-plan.md:160 `- Load relevant skills (figma:*, frontend-design, vercel-react-best-practices, security, etc.)` and skills/belmont/_src/product-plan.md:165 `1. Load relevant skills for the domain (figma:*, frontend-design, vercel-react-best-practices, security, etc.)`. The `vercel:react-best-practices` and `frontend-design:frontend-design` corrections are right. The `security` claim is verifiably false, and doubly so. (a) There is no `/security` slash command: `~/.claude/commands` is empty and `find ~/.claude -maxdepth 4 -iname 'security*'` returns nothing. (b) The real registration in the Claude Code binary /Users/benlavender/.local/share/claude/versions/2.1.223 is `pmp({name:\"security-review\",description:\"Complete a security review of the pending changes on the current branch\",progressMessage:\"analyzing code changes for security risks\",pluginName:\"security-review\",pluginCommand:\"security-review\",...})`, and the binary's own error text reads `Tell the user: /security-review needs to run inside a git repository`. (c) `security-review` is additionally present in this session's Skill-tool roster, so calling it merely \"a slash command\" is wrong in the other direction too \u2014 it is a skill name, which is precisely the category the sentence is adjudicating. Tried and failed to refute three ways. Charitable-reading defence fails: the sentence gives the exact corrected name for the other two entries and withholds it only here, so the omission is not stylistic. \"Addressed elsewhere\" defence fails: the only other mentions are the DoD at :415 (\"the bogus `security` entry ... corrected\" \u2014 equally silent on the target) and the smoke grep at :349, and grepping the whole document for `security` finds nothing more. Consequence is real but bounded: the stated rationale (\"it's a slash command\") implies delete, while the true fact (a registered skill named `security-review`) implies rename, and those are different edits. Note the smoke grep at :349 (`^- Load relevant skills.*security`, expect 0) silently assumes delete \u2014 `security-review` would still match `.*security` \u2014 so the document already leans one way without saying so. Minor is the correct grade: it is an imprecision in a justification clause, not a broken instruction; the DoD and smoke check still identify the line to fix.",
   "fix_to_apply": "In \u00a74.3 line 123, replace the sentence:\n\n  **Use exact registered names.** `tech-plan.md:160` and `product-plan.md:165` currently name `vercel-react-best-practices` and `security`; neither resolves (`vercel:react-best-practices` is the real one; `security` is a slash command). `frontend-design` is likewise a plugin skill registered as `frontend-design:frontend-design`. Fix all three; do not copy that line's style.\n\nwith:\n\n  **Use exact registered names.** `tech-plan.md:160` and `product-plan.md:165` currently name `vercel-react-best-practices`, `security` and `frontend-design`; none of the three resolves. The registered names are `vercel:react-best-practices` and `frontend-design:frontend-design`. There is no `security` and no `/security` \u2014 the real registration is `security-review` (`name: \"security-review\"`, invokable both as the `/security-review` slash command and as a skill). **Drop the `security` entry rather than renaming it**: `security-review` reviews pending changes on the current branch and has nothing to contribute to a planning session. Fix the other two in place; do not copy that line's style.\n\nKeep the smoke grep at :349 as-is \u2014 it expects zero matches of `^- Load relevant skills.*security`, which is consistent with deleting the entry. If the author instead prefers renaming to `security-review`, :349 must change (because `security-review` still matches `.*security`), e.g. to `grep -c \"vercel-react-best-practices\\|frontend-design,\" .agents/skills/belmont/tech-plan/SKILL.md`. Optionally tighten the DoD at :415 to `- [ ] \\`vercel:react-best-practices\\` and \\`frontend-design:frontend-design\\` corrected, and the bogus \\`security\\` entry removed, in \\`tech-plan.md\\` and \\`product-plan.md\\``."
  },
  {
   "spec_location": "\u00a76 (line 322)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check first: line 322 reads verbatim \"`AGENTS.md:26` and `dual-invocation-paths.md:9` say auto mode bypasses the tool's skill discovery and that Belmont injects the skill body, agent files and project state.\" The finding's quote is exact, including the emphasised clause. Both cited lines verified by opening the files at the exact line numbers. AGENTS.md:26 (CLAUDE.md is a symlink to it, confirmed via `ls -l`): \"- **Auto mode** \u2014 the Go CLI shells out to the AI tool's headless print mode (`claude -p`, `codex exec`, `pi -p`, \u2026) and parses its stdout. Belmont assembles the prompt; the tool's own skill discovery is bypassed.\" It makes no injection claim of any kind \u2014 no skill body, no agent files, no project state. knowledge/cross-cutting/dual-invocation-paths.md:9: \"The tool's own skill discovery is bypassed entirely; Belmont injects the skill body, agent files, project state, and steering text directly into the subprocess prompt.\" So the conjunctive attribution is wrong: only the knowledge entry carries the injection half, and the default English reading of \"A and B say X and Y\" \u2014 reinforced by the paragraph's own follow-up \"Correct both statements with that split\" \u2014 treats them as one conflated claim. Tried to refute two ways and failed. (1) \"Belmont assembles the prompt\" is not equivalent to the injection list: it states who builds the prompt, not what goes in it, and the spec's own account (`buildLoopPrompt` emits a bare slash reference plus milestone-scope prose) leaves \"assembles the prompt\" true while the injection list is false. Confirmed against cmd/belmont/main.go:7165 `func buildLoopPrompt` \u2014 the actionImplementMilestone case returns `fmt.Sprintf(\"/belmont:implement --feature %s\", feature)` plus MILESTONE-SCOPED prose, and the adapt path immediately above emits \"Read .agents/skills/belmont/%s/SKILL.md fully...\" So AGENTS.md:26 needs one correction (the bypass clause) and dual-invocation-paths.md:9 needs two (bypass clause plus the injection list). (2) \"Addressed elsewhere\" fails: \u00a75's scope rows say only \"Correct `:26`\" and \"Correct `:9`\", and the DoD at :430 says \"corrected **per tool**\" \u2014 neither disambiguates which file carries which claim. Real defect, correctly graded minor: it is an attribution imprecision that risks a confusing edit to AGENTS.md, not a broken instruction.",
   "fix_to_apply": "In \u00a76 line 322, replace:\n\n  **Correct the record on auto mode, per tool.** `AGENTS.md:26` and `dual-invocation-paths.md:9` say auto mode bypasses the tool's skill discovery and that Belmont injects the skill body, agent files and project state.\n\nwith:\n\n  **Correct the record on auto mode, per tool.** Both statements overstate, but not identically. `AGENTS.md:26` says only \"Belmont assembles the prompt; the tool's own skill discovery is bypassed\". `dual-invocation-paths.md:9` repeats the bypass and adds an injection claim on top: \"The tool's own skill discovery is bypassed entirely; Belmont injects the skill body, agent files, project state, and steering text directly into the subprocess prompt.\"\n\nThen, in the same paragraph, replace:\n\n  Correct both statements with that split, not with a blanket claim.\n\nwith:\n\n  Correct the **bypass** clause in both files with that split, not with a blanket claim. `dual-invocation-paths.md:9` needs a second edit its counterpart does not: narrow its injection list from \"the skill body, agent files, project state, and steering text\" to steering text plus milestone-scope prose. `AGENTS.md:26` makes no injection claim and must not be edited as though it did \u2014 \"Belmont assembles the prompt\" stays true.\n\nOptionally mirror the split in the DoD at :430: `- [ ] \\`AGENTS.md:26\\` and \\`dual-invocation-paths.md:9\\` corrected **per tool** \u2014 bare slash reference for six CLIs, deliberate rewrite for Pi and opencode; \\`:9\\` additionally drops the skill-body/agent-files/project-state injection list, \\`:26\\` keeps \"Belmont assembles the prompt\" unchanged`."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:413 (DoD) vs :139 (\u00a74.3 warning box)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote checked and accurate. :413 reads verbatim \"- [ ] Every link in that table checked resolving at branch time \u2014 they are third-party URLs and this PR does not control them\" (no bold in the source; the finding's `claim` field adds emphasis, but its `evidence` quote is verbatim, so the quote survives). :139 reads verbatim \"**Never verify a link with an HTTP status code.** \u2026 A link is verified when its content has been fetched and identified, not when it responds.\" and :137 confirms \"Every Drive link above was verified by **downloading the archive and inspecting its contents**\". So the wording conflict is real: \"resolving\" is a synonym for \"responds\", which is precisely the standard :139 rejects in bold, and this document's DoD elsewhere carries its qualifying caveat inline (:434 spells out \"not with a version argument that mutates plugin.json\"), so the omission here is an inconsistency in the document's own style, not a deliberate shorthand. Downgraded from major to minor for three reasons the finding does not weigh. (1) The body addresses it twice \u2014 :139's bold rule and :141's \"Re-check every row at branch time and prefer a Notion page wherever one becomes available\" \u2014 so the standard is not absent, only under-restated in the checklist. (2) The links currently in the table were content-verified (:137), so a lax branch-time re-check degrades detection of *newly* broken links rather than shipping a known-bad table. (3) The consequence is a dead link in a reference file, not a broken mechanism. It is a one-word imprecision worth fixing, which is the definition of minor here.",
   "fix_to_apply": "In docs/proposals/0003-design-quality-without-figma.md, replace line 413 in full:\n\nOLD: `- [ ] Every link in that table checked resolving at branch time \u2014 they are third-party URLs and this PR does not control them`\n\nNEW: `- [ ] Every link in that table re-verified at branch time by **fetching and identifying its content** (Notion page title / Drive archive contents) \u2014 never by HTTP status code (\u00a74.3). They are third-party unauthenticated share links and this PR does not control them`"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:388 (smoke Step 10) vs :434 (DoD Mechanics)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Both quotes check out verbatim, and the mechanical claim is confirmed by measurement, not reasoning: in a scratch copy of scripts/ skills/ agents/ plugin/, `bash scripts/generate-plugin.sh 9.9.9` rewrote the tracked plugin/.claude-plugin/plugin.json from `\"version\": \"0.10.14\"` to `\"version\": \"9.9.9\"` (generate-plugin.sh:21-27 treats any non-`--check` positional as VERSION; :35 `rm -rf \"$PLUGIN_DIR\"`; :44 `\"version\": \"$VERSION\",`). `--check` is a distinct mode that redirects PLUGIN_DIR to a mktemp dir (:29-32) and leaves the committed tree untouched. So Step 10 and DoD :434 do give opposing instructions about the same command form, and that inconsistency is worth removing. Downgraded to minor because three mitigations the finding does not weigh cut the impact. (1) The DoD's prohibition is syntactically scoped to *verification* (\"verified with \u2026 --check \u2026, not with a version argument\"), while Step 10 is a regeneration-plus-inspection step; the two are not strictly the same act. (2) `<ver>` is an unfilled placeholder \u2014 an author who passes the current release version (0.10.14) produces a byte-identical plugin.json and no dirt at all; the mutation is possible, not certain. (3) There is no clean third option today: the bare form stamps `\"version\": \"dev\"` (:19), which is worse than a real version, so the finding's own suggested fix (\"plus a plain ./scripts/generate-plugin.sh regeneration if the tree is stale\") would introduce the very mutation it objects to \u2014 I have corrected that in fix_to_apply. Also note the finding slightly overreads \u00a712:491, which claims only that \"`--check` **DoD items**\" were fixed; Step 10 is not a DoD item, so \u00a712 is not itself false. Verified that Step 10's assertions are not vacuous and cannot simply be dropped: four of six committed plugin/agents/*.md are 0 bytes today (code-review, codebase, reconciliation, verification), which is 0006's bug, and 0006:46 states `--check` is blind to it \u2014 so the replacement must keep an explicit non-zero test against the regenerated-and-committed tree.",
   "fix_to_apply": "In docs/proposals/0003-design-quality-without-figma.md, replace line 388 in full:\n\nOLD: `**Step 10 \u2014 plugin surface.** \\`./scripts/generate-plugin.sh <ver>\\`; confirm \\`plugin/skills/tech-plan/references/design-authority-baseline.md\\` exists and is non-empty, and that \\`plugin/agents/*.md\\` are all non-zero (0006's fix).`\n\nNEW: `**Step 10 \u2014 plugin surface.** In the Belmont repo (not the smoke project), with \\`plugin/\\` already regenerated and committed: \\`./scripts/generate-plugin.sh --check\\` \u2014 expect exit 0 (0006 makes it version-agnostic). Then assert against the committed tree, which \\`--check\\` has just proved identical to a fresh generation: \\`test -s plugin/skills/tech-plan/references/design-authority-baseline.md\\` and \\`find plugin/agents -name '*.md' -size 0\\` \u2192 expect no output (0006's fix; four of six are 0 bytes on today's main). **Do not pass a version positional** \u2014 measured, it rewrites \\`plugin/.claude-plugin/plugin.json\\`'s \\`version\\` field (\\`generate-plugin.sh:25\\`, \\`:44\\`), and the bare form stamps \\`\"version\": \"dev\"\\` (\\`:19\\`); \\`release.sh:41\\` is the only caller that should stamp a version.`\n\nLeave DoD :434 unchanged \u2014 it is the correct side of the pair."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:143-145 (\u00a74.3)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Confirmed verbatim: :143 reads \"**Two packaging notes**, both verified against the downloaded packages:\" and exactly one bullet follows at :145. :147 (\"These are **user-scope Claude Code skills**\u2026\") is an unbulleted portability paragraph that duplicates :121, not a packaging note, and \"verified against the downloaded packages\" does not describe it.\n\nThe cause is settled by git, not inference: `git show 1d060a7` deletes the second bullet \u2014 \"**`interactive-prototype` is mis-packaged upstream.** \u2026\" \u2014 while leaving the \"Two packaging notes\" lead-in untouched. So the finding's first disjunct (\"a note was deleted without fixing the lead-in\") is exactly what happened, and its second disjunct is unnecessary. Same commit, same class of collateral as finding 1.\n\nSeverity minor is right: it is a count/lead-in mismatch in proposal prose with no downstream implementation consequence. Note the finding's suggested alternative fix \u2014 restoring the no-LICENSE/no-redistribution point from :57 as the second note \u2014 should be rejected: that is a licensing statement, not a packaging one, and duplicating it here would create a second maintenance site for it.",
   "fix_to_apply": "docs/proposals/0003-design-quality-without-figma.md:143 \u2014 replace\n`**Two packaging notes**, both verified against the downloaded packages:`\nwith\n`**One packaging note**, verified against the downloaded packages:`\nDo not restore a second note (the deleted one was the `interactive-prototype` mis-packaging bullet, removed with that skill in 1d060a7), and do not promote :147 into the bullet list."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:304 (\u00a75 plugin row) and :320 (\u00a76 Plugin row)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quotes verified verbatim. :298 puts `skills/belmont/_src/codex-plan-apply.md` in scope; :304 reads \"| `plugin/` | Regenerate \u2014 `plugin/skills/{tech-plan,product-plan,verify,next,implement}/**` and `plugin/agents/*`. **Requires 0006** |\" and :320 repeats the same five-skill set. Verified against the tree: `plugin/skills/codex-plan-apply/SKILL.md` exists, and `scripts/generate-plugin.sh:60-74` iterates `for skill_dir in \"$SKILLS_SRC\"/*/` skipping only `_*` dirs and dirs lacking SKILL.md \u2014 so editing `_src/codex-plan-apply.md` provably changes `plugin/skills/codex-plan-apply/SKILL.md`. The enumeration is therefore incomplete by exactly one skill, and \u00a712:491's \"plugin enumerations completed\" is wrong. I checked for further omissions and found none: `models-yaml-format.md` and `tech-plan-feature-format.md` ship only into `plugin/skills/tech-plan/references/` (grep -rl over plugin/skills), which the existing `tech-plan/**` glob already covers. Tried to refute on the grounds that codex-plan-apply is a Codex adapter irrelevant to a Claude plugin \u2014 that fails, because the generator ships it today regardless, so the regenerated diff will contain a file the enumeration does not predict. Also tried to refute as harmless \u2014 the finding already concedes the build cannot break; the damage is confined to the review checklist and Step 10, which is exactly minor severity. Correctly graded.",
   "fix_to_apply": "Two edits, both single-line.\n\n1. Line 304, replace the cell text\n`| `plugin/` | Regenerate \u2014 `plugin/skills/{tech-plan,product-plan,verify,next,implement}/**` and `plugin/agents/*`. **Requires 0006** |`\nwith\n`| `plugin/` | Regenerate \u2014 `plugin/skills/{tech-plan,product-plan,verify,next,implement,codex-plan-apply}/**` and `plugin/agents/*`. **Requires 0006** |`\n\n2. Line 320, replace\n`| **Plugin** | `plugin/agents/*` and `plugin/skills/{tech-plan,product-plan,verify,next,implement}/**` are a third tracked surface; `release.sh:41` regenerates and `:96` stages it |`\nwith\n`| **Plugin** | `plugin/agents/*` and `plugin/skills/{tech-plan,product-plan,verify,next,implement,codex-plan-apply}/**` are a third tracked surface; `release.sh:41` regenerates and `:96` stages it |`\n\nNo change is needed to Step 10 (:388) or to \u00a712:491."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:94 (R1) vs :264 (\u00a74.7) vs :348 (smoke precondition)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote verified: :94 reads \"Run only when the feature has a user interface **and** no active task carries a Figma URL.\" The terminology defect is real and confirmed against the code \u2014 \"active task\" has exactly one established meaning in Belmont and it is loop-time: `agents/belmont/design-agent.md:29` (\"Read the full task definitions for every ID listed in `### Active Task IDs`\"), `:232`, `agents/belmont/codebase-agent.md:29`, `agents/belmont/implementation-agent.md:79`, and the section is created by `/belmont:implement` (`skills/belmont/_src/implement.md:57`, `references/implement-milestone-template.md:26`). `grep -ni \"active task\" cmd/belmont/*.go` returns nothing, so the Go CLI has no such concept either. Nothing in Phase 3.5's context can produce an \"active task\". I searched the whole document for a disambiguating definition and found none \u2014 but I did find that every other statement of the same predicate uses the correct planning-scope wording (:223 \"a contract now exists only when there are no Figma URLs (R1)\", :264 \"Figma-URL presence in the PRD\", :348 grep of `{base}/PRD.md`, :402 \"no Figma URLs\", :447). That cuts both ways: the author's intent is unmistakable, which caps this at minor, but it also makes R1 the lone odd phrasing and the fix trivially safe.\n\nI partially refute the finding's second and third limbs. (a) \u00a74.7:264 and smoke Step 1 are NOT a third and second conflicting scope \u2014 they already say \"in the PRD\", i.e. they are the correct wording and need no alignment; only R1 is out of step. (b) The ordering hazard (Phase 3.5 at tech-plan.md:217-219, before Phase 4's `Update {base}/PRD.md with the new task definitions` at :238-243 \u2014 both line citations verified) is real but already absorbed by the design: \u00a74.6:223 explicitly anticipates \"A feature can therefore have a contract *and* a reference\" and branch 1 performs contract checks alongside the comparison flow, so a Figma-bearing task added in Phase 4 degrades into a handled state, not a broken one. Consequently the finding's suggested fix \u2014 \"evaluated after Phase 4's task additions\" \u2014 must NOT be applied verbatim: it contradicts \u00a74.2:92's own placement rationale (\"after Phase 3's interview (it consumes the `:197` answers) and before Phase 4 writes the plan\") and would itself ship a defect. Fix reduced to terminology plus a one-clause note.",
   "fix_to_apply": "Single edit at line 94. Replace\n`- **R1 \u2014 Gate: UI **and** no Figma.** Run only when the feature has a user interface **and** no active task carries a Figma URL.`\nwith\n`- **R1 \u2014 Gate: UI **and** no Figma.** Run only when the feature has a user interface **and** no task definition in `{base}/PRD.md` carries a Figma URL. (\"Active task\" is deliberately avoided: `### Active Task IDs` is a MILESTONE section written by `/belmont:implement` and does not exist at planning time.) If Phase 4 later adds a Figma-bearing task, the feature simply lands in \u00a74.6 branch 1 \u2014 contract present *and* references present \u2014 which that branch already handles.`\n\nDo NOT move Phase 3.5's insertion point (:92) and do NOT edit :264 or :348 \u2014 both already scope the predicate to the PRD and are the wording R1 is being aligned to."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:281 (\u00a74.9) vs :227 (\u00a74.6 branch 3)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Both quotes verified verbatim. :281 asserts \"so \u00a74.6 branch 3 applies and it keeps today's acceptance-criteria verification\"; :227 defines branch 3 as \"**No references, no contract**\" and :225 defines branch 1 as \"**Design references exist**\". Branch selection is keyed on reference presence, not on plan vintage, so a pre-contract feature carrying Figma URLs, screenshots or mockups lands in branch 1. Confirmed that such references are plentiful in pre-contract features: `agents/belmont/verification-agent.md:57-59` lists archived-MILESTONE Figma Sources tables, raw Figma URLs in Orchestrator Context, and \"`**Figma**:` fields, linked screenshots, mockups, or reference images\" in PRD task definitions \u2014 and the proposal itself stresses this at :223. I tried to refute on the grounds that the paragraph's real point (non-retroactivity) is correct regardless of branch number; that fails, because the sentence is wrong twice over \u2014 the branch label is wrong AND \"keeps today's acceptance-criteria verification\" is wrong for a reference-bearing feature, which today keeps the *comparison* flow, not the acceptance-criteria fallback. The error also propagates to a checkbox a reviewer must tick: :404 \"Pre-contract features fall to branch 3\", which is unsatisfiable as stated. Not major: \u00a74.6's branch table at :225-227 is the normative statement and is correct, so the mis-statement is confined to the migration note and one DoD line rather than to the mechanism. Minor is the right grade; the finding's suggested wording is sound but must also be carried to :404.",
   "fix_to_apply": "Two edits.\n\n1. Line 281, replace the first sentence pair\n`**This gate is not retroactive.** A feature planned before this PR has a `{base}/TECH_PLAN.md` with no `## Design Contract`, so \u00a74.6 branch 3 applies and it keeps today's acceptance-criteria verification.`\nwith\n`**This gate is not retroactive.** A feature planned before this PR has a `{base}/TECH_PLAN.md` with no `## Design Contract`, so it falls to \u00a74.6 branch 1 or branch 3 depending on whether design references exist \u2014 Figma URLs, screenshots and mockups all count (`verification-agent.md:57-59`). In both branches the contract checks are skipped because no contract is present, so verification behaves exactly as it does today: comparison against the references in branch 1, the acceptance-criteria fallback in branch 3.`\n(Leave the following sentence \"That is deliberate: \u2026\" unchanged.)\n\n2. Line 404, replace\n`- [ ] Pre-contract features fall to branch 3 and are documented as non-retroactive (\u00a74.9)`\nwith\n`- [ ] Pre-contract features take branch 1 or branch 3 by reference presence, run no contract checks either way, and are documented as non-retroactive (\u00a74.9)`"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:260 (\u00a74.6 severity ladder) vs the check table at :237-250",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "QUOTES VERIFIED. :260 reads verbatim \"radius, elevation or easing inconsistency \u2192 Polish\"; :193-194 reads \"Easing \u2014 one documented curve per class (enter / exit / move). Bounce and overshoot are opt-in per product voice, never a default.\"; the table at :237-250 has rows for spacing, type sizes, contrast, touch targets, focus, labels, radius, elevation, State Inventory, transition durations, animated properties, reduced motion \u2014 grep confirms \"easing\" occurs in exactly two lines of the whole 492-line document (:193 and :260), and \"timing\"/\"curve\" appear nowhere in \u00a74.6. SEARCHED ALL OF IT: no easing check exists elsewhere. The asymmetry is the decisive evidence: eight of the nine categories named in the :260 ladder map 1:1 onto a table row (contrast\u2192:241, missing focus indicator\u2192:243, missing label\u2192:244, missing state\u2192:247, off-scale spacing\u2192:239, out-of-band duration\u2192:248, radius\u2192:245, elevation\u2192:246). Easing is the only orphan, which reads as an oversight rather than a design choice \u2014 and \u00a74.6's own thesis is \"Named tooling per check\" (:235), so a named outcome with no named tooling is a genuine hole. The mechanism is trivially available: the durations row already uses browser_evaluate \u2192 transitionDuration; transitionTimingFunction sits on the same getComputedStyle object. PARTIAL REFUTATION of the second half. The claim that the fourth enforcement rule is \"undefined\" for UX Strategy and Microcopy Rules does not survive: the rule as stated at :233 is \"a contract exists but contract checks were not performed \u21d2 MUST be FAIL/INCOMPLETE\", and the attestation it feeds (:258, \"Contract checks performed: [YES against <path> | NO \u2014 <reason> | N/A]\") is per-contract binary, not per-section \u2014 it is well-defined without an exhaustive section-to-row map. Nothing in \u00a74.6 claims the table exhaustively covers every contract line (Token Contract's \"Max 4 sizes\", \"Max 3 hues\", \"Never pure #000/#FFF\" and the semantic-colour quartet also have no rows), so \"no row\" does not mean \"the enforcement rule breaks\". Also note the ladder is not orphaned in the way the finding implies: agents/belmont/verification-agent.md:314-320 already carries \"Animation/transition polish\" under Polish, so an easing finding has a home in the report if the agent happens to make one. The defect is that nothing instructs the agent to look. Grade minor is correct \u2014 imprecision worth fixing, no rework or wrong decision results.",
   "fix_to_apply": "Add one row to the \u00a74.6 check table, inserted between the `Transition durations within declared bands` row (:248) and the `Only transform/opacity animated` row (:249):\n\n| Easing curve matches the declared curve for its class (enter / exit / move) | `browser_evaluate` \u2192 `transitionTimingFunction` / `animationTimingFunction` |\n\nThen update the three places that fix the motion-row count at three, since it becomes four:\n- :252 \u2014 \"the three motion rows are recorded `N/A` and skipped\" \u2192 \"the four motion rows are recorded `N/A` and skipped\"\n- :410 (DoD) \u2014 \"verification records those three rows `N/A`, never `UNVERIFIABLE`\" \u2192 \"verification records those four rows `N/A`, never `UNVERIFIABLE`\"\n- :476 (\u00a712) \u2014 \"verification records its three rows `N/A` rather than `UNVERIFIABLE`\" \u2192 \"verification records its four rows `N/A` rather than `UNVERIFIABLE`\"\n\n(Acceptable cheaper alternative if the author prefers not to add a check: delete the two words \" or easing\" from :260 so it reads \"radius or elevation inconsistency \u2192 Polish\", and leave all three counts at three. Do not do both.) Do NOT add the finding's suggested sentence about which contract sections are \"review-only\" \u2014 that half of the finding is refuted."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:94 (R1, \"no active task carries a Figma URL\")",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "The quote is exact, and the terminology collision is real. `skills/belmont/_src/references/implement-milestone-template.md:26` defines `### Active Task IDs` as \"[Comma-separated list of the **incomplete** task IDs in this milestone\u2026]\", and that term is consumed as a term of art in `agents/belmont/design-agent.md:29,:72,:232`, `agents/belmont/codebase-agent.md:29,:72`, `agents/belmont/implementation-agent.md:20,:25,:79`, `skills/belmont/_src/implement.md:57` and `skills/belmont/_src/next.md:94`. `skills/belmont/_src/tech-plan.md` has no MILESTONE in scope (Phase 4 at :219-243 writes TECH_PLAN/PRD/PROGRESS only), so the term is genuinely undefined at the point R1 uses it.\n\nI weighed refuting this. Two things cut against the finding's severity. First, the surrounding prose disambiguates hard: :94 immediately says \"Figma features record `N/A \u2014 Figma present`\" and :95 says \"Figma features keep today's behaviour untouched\", so no implementer will land on the vacuous reading that fires the gate on Figma features \u2014 the finding overstates that branch. Second, \u00a74.7 (:264, \"Figma-URL presence in the PRD\") and the risk table (:448, \"Mode keyed on URL presence only\") both restate the trigger without any activity qualifier, so the intent is recoverable from elsewhere in the document \u2014 which is close to failure mode (b). What survives is the milder reading the finding also gives: \"active\" read as \"not yet completed\" makes the gate state-dependent, and \u00a74.9 (:283) explicitly instructs re-running `/belmont:tech-plan` on an existing feature, at which point a Figma feature whose Figma-bearing tasks are all `[v]` could flip to `derived`. R4's preserve-verbatim rule blunts this on the headless path but says nothing about the interactive re-run \u00a74.9 prescribes. That is an imprecision worth fixing in a gate condition, not a wrong decision the implementer is likely to make \u2014 minor, not major.",
   "fix_to_apply": "In R1 at :94, replace \"no active task carries a Figma URL\" with \"no task in `{base}/PRD.md` carries a Figma URL \u2014 every task, regardless of `[ ]`/`[x]`/`[v]` state\". Repeat the same wording in the Phase 3.5 body when it is written into `skills/belmont/_src/tech-plan.md`, so the runtime agent never has to infer the scope. Do not use the phrase \"active task\" anywhere in this PR outside a MILESTONE context, where it means the incomplete task IDs of the current milestone (`implement-milestone-template.md:26`)."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:94 (R1, \"the feature has a user interface\")",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK: accurate. :94 reads exactly \"Run only when the feature has a user interface **and** no active task carries a Figma URL. Record the outcome in the contract's `**Mode**` field \u2026 Backend features record `N/A \u2014 no UI`; Figma features record `N/A \u2014 Figma present`.\" The ellipsis removes only the `**Mode**`/no-`**Design contract**`-field clause, which the finding itself then addresses, so nothing disambiguating was hidden.\n\nEVIDENCE CHECK: both citations are exact. agents/belmont/verification-agent.md:51 = \"If the task involved UI changes (pages, components, layouts, styles, design tokens, or any visual output), you MUST perform visual verification.\" agents/belmont/implementation-agent.md:188 = \"Skip this section for tasks with zero visual output (CLI tools, API routes, database migrations, config files, build scripts, pure backend logic).\" Neither is cited anywhere in the proposal (grep confirms). :112's shadcn evidence rule and :117's \"Never omit it\" are quoted correctly.\n\nSEARCHED THE WHOLE DOCUMENT: grep for \"user interface|UI-bearing|has UI|no UI\" returns :19, :94, :96, :155, :256, :339, :373, :386, :402. None defines how the UI determination is made. The gap is real and is a genuine internal asymmetry: the same sentence's Figma half names a detectable signal (\"no active task carries a Figma URL\"), \u00a74.3 :112 demands file-level evidence for shadcn, and \u00a74.4 rung 1 (:207) gives three detection signals for Storybook. Only the UI half is bare judgement.\n\nBUT THE FINDING IS OVERGRADED, on two counts.\n(1) Its escalation argument misreads :96. Full text: \"**Scope is the whole feature, one conversation** (author decision): the contract covers every milestone, including ones that turn out to be backend-only. Milestone-level scoping was rejected \u2014 planning does not reliably know which milestones are UI-bearing before the work is broken down.\" The finding claims feature-level scope \"asks the planner to judge 'has UI' for milestones that have not been broken down yet\" \u2014 the opposite of what the spec says. The spec moved the judgement *off* milestones precisely because per-milestone UI-bearing is unknowable; the retained call is \"does this feature have a UI\", which is materially easier and is made against a PRD whose tasks already exist (Phase 3.5 sits between tech-plan.md:217 and :219, after the interview, with the PRD written).\n(2) \"Permanent by design\" is overstated. \u00a74.9 :283 supplies a correction path (\"re-run `/belmont:tech-plan --feature <slug>`\"), and R4's preserve-verbatim rule is scoped to headless replan only (\":99 Under `belmont auto`, `actionReplan` invokes\u2026\"), so an interactive re-run can revise the call. The finding concedes this parenthetically but grades as if it did not.\n\nStripped of both, what remains is an underspecified prose rule in a document that specifies comparable rules everywhere else \u2014 worth fixing, cheap to fix, but it does not cause rework or a wrong decision on its own. Minor, not major.",
   "fix_to_apply": "In \u00a74.2, extend R1 (line 94) with an evidence rule, immediately after \"Run only when the feature has a user interface **and** no active task carries a Figma URL.\": add \u2014 \"**Both halves are decided from the feature's active PRD tasks, by the interactive tech-plan session.** The feature has a UI if any active task produces a page, component, layout, style, design token or other visual output (the test `verification-agent.md:51` already uses); it does not if every active task is a CLI tool, API route, database migration, config file, build script or pure backend logic (the exclusion list `implementation-agent.md:188` already uses). Reuse that shipped vocabulary rather than minting a third definition.\" Do NOT add the finding's suggested `**UI evidence**:` schema field unless you also add it to the \u00a74.4 schema block (:155-160), smoke Step 1's expected fields (:354) and the DoD (:402) in the same edit \u2014 a field added in one place only is the exact class of inconsistency this document has shipped in each prior revision."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:301 (\u00a75 verification-agent row) vs :235/:252/:260 (\u00a74.6) and :419 (DoD)",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Factually correct on every point I could check, including the history claim.\n\nSpec :301 reads exactly as quoted and stops at \"**rework the `## Output Format` Visual Verification block (`:210-221`)** \u2014 Figma-shaped today, needs a contract branch and a Mechanism column\". I enumerated every file row in the \u00a75 In-scope table (:289-308): tech-plan.md, tech-plan-feature-format.md, design-authority-baseline.md, product-plan.md, implement.md, verify.md, next.md, codex-plan-apply.md, models-yaml-format.md, design-agent.md, verification-agent.md, implementation-agent.md, post-verify-triage.md, plugin/, AGENTS.md, dual-invocation-paths.md, codex-plan-handoff.md, design-authority.md. There is no `verify-report-format.md` row and no Severity-Guide clause. Confirmed.\n\nCode confirmed: `skills/belmont/_src/references/verify-report-format.md:23` = \"- Visual Verification: [PASS/FAIL/N/A]\" \u2014 no INCOMPLETE, no UNVERIFIABLE. `agents/belmont/verification-agent.md:311` (under `### Warning (Blocks Milestone \u2014 Should Fix)` at :307) = \"- Accessibility issues that affect usability (missing focus management, no keyboard nav for interactive elements)\", which does collide with \u00a74.6:260's \"missing focus indicator \u2192 Critical\". The finding's guide range is slightly short: the Severity Classification Guide runs :295-327, not :295-320.\n\nThe history claim also checks out \u2014 `docs/proposals/rev5-verdicts-round2.json:129` contains verbatim both \"Add one new \u00a75 row: | `skills/belmont/_src/references/verify-report-format.md` |\" and \"**amend the Severity Classification Guide (`:295-320`)**\". Rev 6 applied only the third element (the Output Format rework), so this is a genuinely partially-applied confirmed fix, not a re-litigation.\n\nWhere I downgrade it: the consequences are cosmetic, not decision-changing. (a) Severity: both buckets block \u2014 the shipped headers are \"### Critical (Blocks Milestone \u2014 Must Fix)\" and \"### Warning (Blocks Milestone \u2014 Should Fix)\", and verify-report-format.md:8-9 makes any Critical *or* Warning a non-pass, while its note at the end of the Output Format block says \"Only Critical and Warning issues should become FWLUP tasks\". So a contract failure graded Warning instead of Critical still blocks and still produces a follow-up. The one place the distinction could have mattered \u2014 post-verify-triage deferring \"Missing aria-labels\" / \"Minor accessibility notes\" \u2014 is already handled elsewhere in this spec (\u00a74.7:269 adds \"a non-deferrable contract list\"). (b) Roll-up: the round-2 skeptic itself noted UNVERIFIABLE \u21d2 never PASS, so it rolls up as FAIL rather than looking like a pass; and INCOMPLETE is *already* absent from that vocabulary today (verification-agent.md:129-130 emit it), so this is a pre-existing looseness the PR merely widens. That places both halves at \"imprecision worth fixing\", not \"causes rework or a wrong decision\".",
   "fix_to_apply": "Two edits in \u00a75 plus one DoD line.\n\n1. \u00a75 In-scope table \u2014 insert a new row immediately after the `skills/belmont/_src/references/models-yaml-format.md` row (:299):\n\"| `skills/belmont/_src/references/verify-report-format.md` | Roll-up line `:23` \u2014 extend the Visual Verification vocabulary to `[PASS/FAIL/INCOMPLETE/N/A]`, and state the roll-up rule: any `UNVERIFIABLE` contract row forces a value other than `PASS`; motion rows recorded `N/A` under `**Applies**: N/A` do not |\"\n\n2. \u00a75 (:301) \u2014 append to the `agents/belmont/verification-agent.md` row's Change cell, after the Output Format clause: \"; **amend the Severity Classification Guide (`:295-327`)** so contract-check failures \u2014 contrast below threshold, missing visible focus indicator, missing input label \u2014 are listed under Critical and carved out of the general Warning clause at `:311` (\\\"Accessibility issues that affect usability (missing focus management, no keyboard nav \u2026)\\\")\".\n\n3. \u00a79 DoD, under **Gate**, after :420: \"- [ ] Severity Classification Guide amended so contrast / focus-indicator / label failures are Critical and not caught by the `:311` Warning clause; roll-up vocabulary in `verify-report-format.md:23` covers a run containing `UNVERIFIABLE` rows\".\n\n4. Consequential counts: \u00a7Type (:5) \"13 tracked source files\" \u2192 \"14 tracked source files\" (and the `2 existing skill references` figure \u2192 `3`); adjust the \u00a7Size line at :6 accordingly."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:267 (\u00a74.7 verify.md, \"the **sub-agent dispatch prompt** (`:109`, `:146`)\")",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Confirmed. The spec at :267 reads: \"the **sub-agent dispatch prompt** (`:109`, `:146`) steers the agent away from the new behaviour by enumerating only Figma/screenshot references and injecting \\\"No design references found\\\"\". Neither cited line does either thing.\n\nVerified directly in skills/belmont/_src/verify.md:\n  109: > Read `{base}/TECH_PLAN.md` for technical specifications (if it exists).\n  146: > Read `{base}/TECH_PLAN.md` for technical specifications (if it exists).\nLine 146 sits inside `### Agent 2: Code Review (code-review-agent)` (heading at :129, prompt body :135-151, \"**Collect**\" at :153) \u2014 a dispatch whose prompt contains no design-reference text at all and which this PR never scopes (code-review-agent appears nowhere in \u00a74.7 or \u00a75).\n\nThe behaviour the sentence describes is at :114-121:\n  114: > **Design References for Visual Verification**:\n  119: > If no MILESTONE files or references were found, write: \"No design references found in archived MILESTONE files or PRD.\"]\n  121: > **Visual Verification**: ... call `mcp__plugin_figma_figma__get_screenshot` for Figma references ...\n\nI tried to break it three ways and could not. (1) Wrong frame? No \u2014 the other two citations in the same sentence are correct against _src (:50 is the focused-reverify \"Do NOT re-check visual specs...\" clause; :62 is `## Step 1b: Gather Design References` and :72 its last line), so _src is definitively the reference frame, and both files are unmodified vs HEAD (`git status --porcelain` empty). (2) Generated file? skills/belmont/verify/SKILL.md is 39KB with partials inlined; :105-150 there is monorepo/milestone-immutability partial text, unrelated. (3) Charitable reading \u2014 that the author cited the *insertion points* (next to the TECH_PLAN read line) rather than the offending text? Even then it is defective, because it silently pulls the code-review dispatch into a PR that scopes only three edits to this file and never justifies a contract check in code review.\n\nThe blanket line-number disclaimer at :9 covers `main.go` citations only (\"All `main.go:NNNN` citations in this document are pre-0005\"), so skill-file line numbers are held to accuracy. Severity minor is correct: it misdirects an implementer working the \"three narrow edits, not one\" instruction, but the surrounding prose describes the right behaviour, so it causes confusion rather than a shipped-broken result. Note the finding's aside that code review \"is one of the two places this PR must leave alone\" is its own embellishment \u2014 the spec never says that \u2014 but it does not affect the core defect.",
   "fix_to_apply": "In docs/proposals/0003-design-quality-without-figma.md:267, replace the middle clause of the `_src/verify.md` bullet.\n\nFROM:\n  the **sub-agent dispatch prompt** (`:109`, `:146`) steers the agent away from the new behaviour by enumerating only Figma/screenshot references and injecting \"No design references found\"\n\nTO:\n  the **verification-agent dispatch prompt** (`:114-121` \u2014 the `**Design References for Visual Verification**` block at `:114`, the \"No design references found\u2026\" fallback string at `:119`, and the Figma-pinned `**Visual Verification**` instruction at `:121`) steers the agent away from the new behaviour by enumerating only Figma/screenshot references; the code-review dispatch (Agent 2, `:129-153`) is out of scope\n\nDo not change \u00a75's `verify.md` row at :296 \u2014 it reads \"Step 1b collects the contract; dispatch prompt carries it; focused-reverify exemption at `:50`\" and carries no line number for the dispatch prompt, so there is nothing to correct there. Optionally insert \"verification-agent\" before \"dispatch prompt\" in that row for consistency."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:247 (verification table, State Inventory row)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK: exact. Line 247 reads `| State Inventory entries present | \\`browser_click\\` / \\`browser_hover\\` / \\`browser_press_key\\` to enter each state + \\`browser_take_screenshot\\` |`. The load-bearing words are \"to enter EACH state\" \u2014 a completeness claim, not an illustrative list. \u00a74.4:180-182 is also quoted exactly: \"default \u00b7 hover \u00b7 focus \u00b7 active \u00b7 disabled \u00b7 loading \u00b7 empty \u00b7 error, plus any gesture or transition state the interaction introduces (drag, swipe, long-press, optimistic update)\". So the mismatch the finding names is present in the document itself and needs no external evidence.\n\nEVIDENCE CHECK: I loaded the live schemas. `browser_run_code_unsafe` = \"A JavaScript function containing Playwright code to execute. It will be invoked with a single argument, page\" \u2014 confirmed, so `page.route()` and `page.mouse.down()` are both reachable. `browser_network_request` = \"Returns full details (headers and body) of a single network request\u2026 Use the number from browser_network_requests\" with `part` enum `request-headers|request-body|response-headers|response-body` \u2014 confirmed read-only. The deferred-tool surface contains no route/emulate tool \u2014 confirmed. `browser_drag` (\"Perform drag and drop between two elements\"), `browser_type`, `browser_select_option`, `browser_fill_form` all exist and none is named in the row.\n\nWHERE THE FINDING IS WRONG, and why it does not survive at `major`:\n(1) Its conclusion \u2014 \"under \u00a74.6's own rule those rows must be recorded UNVERIFIABLE\" \u2014 is false. \u00a74.6:235 triggers UNVERIFIABLE when \"a check whose mechanism is unavailable\". `browser_run_code_unsafe` IS available and is already named two rows down at :250, so the mechanism exists; the table just fails to point at it. Nothing is forced to UNVERIFIABLE and no false PASS is forced either.\n(2) \"loading, error and usually empty are driven by network responses\" is overstated. Form-validation `error` and filtered `empty` are reachable with `browser_type`/`browser_fill_form` + `browser_click`; `loading` is often reachable with `browser_click` + immediate `browser_take_screenshot`.\n(3) The finding asserts click reaches `active`. It does not reliably: Playwright's click is press+release, so `:active` needs `page.mouse.down()` held \u2014 i.e. `browser_run_code_unsafe`. The finding under-states its own case there.\n\nWHY IT STILL SURVIVES: the phrase \"to enter each state\" makes this a false completeness claim inside the one table cell covering the richest section of the contract, and DoD :419 (\"Every check row names a mechanism\") would be ticked against an inadequate one. Unlike the rev-5 `browser_snapshot` focus-visible defect (a flat impossibility shipping a false PASS, correctly graded major), this under-covers rather than mis-reports. That is \"imprecision worth fixing\" = minor.\n\nCOLLATERAL the fix must not break: :250 says of the reduced-motion row \"**This is the only row needing a write-capable tool**\". Adding `browser_run_code_unsafe` to :247 falsifies that sentence, so it must be amended in the same edit \u2014 this is exactly the class of self-inflicted regression the review history warns about.",
   "fix_to_apply": "Two coupled edits, both required.\n\n(1) Replace line 247 in full with:\n`| State Inventory entries present | Enter each state with the interaction it requires: \\`browser_hover\\` (hover) \u00b7 \\`browser_press_key Tab\\` (focus) \u00b7 \\`browser_click\\` (disabled/toggled states) \u00b7 \\`browser_type\\` / \\`browser_fill_form\\` / \\`browser_select_option\\` (validation \\`error\\`, filtered \\`empty\\`) \u00b7 \\`browser_drag\\` + \\`browser_drop\\` (drag, swipe). States no UI interaction reaches escalate to \\`browser_run_code_unsafe\\`: \\`page.mouse.down()\\` held for \\`active\\` and long-press, \\`page.route()\\` to stall or fail a request for \\`loading\\`, \\`error\\`, \\`empty\\` and optimistic update. Capture each with \\`browser_take_screenshot\\`. Record \\`UNVERIFIABLE\\` per state, not per row, and only when no listed mechanism reaches it |`\n\n(2) In the reduced-motion row at line 250, replace the sentence `**This is the only row needing a write-capable tool**` with `**This row always needs a write-capable tool, and the State Inventory row needs one for its escalation path**` \u2014 otherwise edit (1) leaves the document self-contradictory.\n\nDo not change :252 (motion rows N/A rule) \u2014 it is unaffected."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:397 (DoD, Placement and durability)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK: exact. :397 reads \"- [ ] \u00a74.1 states plainly that the rule is prose-enforced and unguarded, and does **not** claim `--assume-unchanged` as a backstop\". The two facts it asserts are already in the document: :84 \"**This is prose-enforced and unguarded, and the PR says so plainly.**\" and :78 \"**`--assume-unchanged` is not a backstop.**\" Both verified verbatim.\n\nSCOPE CHECK: I confirmed `docs/proposals/` appears in neither \u00a75's in-scope table (I enumerated all 18 rows: tech-plan, tech-plan-feature-format, design-authority-baseline, product-plan, implement, verify, next, codex-plan-apply, models-yaml-format, design-agent, verification-agent, implementation-agent, post-verify-triage, plugin/, AGENTS.md, dual-invocation-paths, codex-plan-handoff, design-authority) nor in \u00a77's docs list. So :397 asserts a property of a file the PR does not deliver.\n\nDECISIVE PRECEDENT \u2014 this is not a matter of taste. The previous round confirmed **m27** as a `vacuous-dod` defect on identical reasoning: \"DoD :355 and :356 describe edits rev 5 already made to this proposal document, not deliverables of the PR \u2014 `docs/proposals/` appears in neither \u00a75's in-scope table (:234-249) nor \u00a77.\" `rev5-CONFIRMED-DEFECTS.md` carries it in the confirmed list, not the refuted list. :397 is the same class and, per `git log -S`, was introduced in rev 6's own fix commit (5c2e9cf) rather than surviving from rev 5 \u2014 so \u00a712's \"two vacuous DoD items deleted\" is accurate about m27 and simply does not cover this one. The finding's phrasing \"this one survived\" is slightly wrong on provenance (it was added, not survived); the substance is unaffected.\n\nDISAMBIGUATION I TESTED AND REJECTED: could \"\u00a74.1\" mean the PR description rather than this proposal's section? No. The document uses an explicit form when it means the PR description \u2014 :31 \"## 2. Root cause \u2014 state this first in the PR description\" \u2014 and \u00a7 references elsewhere (:95, :199, :219, :281, :404, :447) uniformly denote sections of this proposal.\n\nGRADE: minor, as filed. It cannot ship a false result; it wastes a DoD slot and gives false assurance that a durability property was checked.\n\nON THE SUGGESTED FIX: the finding's replacement (assert the shipped `design-authority.md` `Don't re-do` records the `--assume-unchanged` rejection) largely duplicates \u00a77:334, which already mandates exactly that entry, and DoD :437 which requires the file with a `Don't re-do`. Rewording to the PR-description scope preserves the author's evident intent (:84 says \"the PR says so plainly\") and is checkable at review time, so I make that the primary fix.",
   "fix_to_apply": "Replace line 397 with a PR-scoped, checkable item:\n`- [ ] The PR description states plainly that the contract write rule is prose-enforced and unguarded, and claims no mechanical backstop \u2014 specifically does **not** cite \\`--assume-unchanged\\` (\u00a74.1)`\n\nThis matches the existing precedent at \u00a72:31 (\"state this first in the PR description\") and can fail at review. Do not add a second item duplicating \u00a77:334 / DoD :437, which already require `design-authority.md`'s `Don't re-do` to record the `--assume-unchanged` rejection."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:364 (Step 2)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote is exact (:364 reads verbatim as claimed). The code claim holds and I reproduced it end-to-end rather than reasoning about it. `ensureStateFiles` runs unconditionally from the install path (cmd/belmont/main.go:3274 `if err := ensureStateFiles(projectRoot); err != nil`) and at :4611 does `ensureGitignoreEntry(projectRoot, \".belmont/auto.json\")`. `requireCleanWorkingTree` at :9818-9831 runs `exec.Command(\"git\", \"status\", \"--porcelain\", \"--untracked-files=normal\")` and returns nil when the output is empty; `--untracked-files=normal` does not list ignored paths. Measured in a scratch repo: after `belmont install --source /Users/benlavender/belmont` + `git add -A && git commit` (the smoke test's own line :346), `.gitignore` contains `.belmont/auto.json` and `.belmont/worktrees/`; writing `.belmont/auto.json` then produced zero porcelain output and `git check-ignore -v .belmont/auto.json` printed `.gitignore:1:.belmont/auto.json`. So the named condition cannot occur on the smoke test's own path. I looked for an escape: `prepareWorktreesGitignore` (:9914) would also add the entry but is dead code (only the definition and its comment match a repo-wide grep), so the install-time write is the sole source \u2014 and the smoke test performs that install. The only residual case is a project that had `.belmont/auto.json` *tracked* from before the ignore existed, which is not what the sentence describes ('untracked'). The finding's alternate cause also checks out: `syncFeatureStateAfterMerge` (:10160; `os.RemoveAll(dstFeature)` :10169, `copyDir` :10170) is invoked from `mergeWorktreeBranch` at :9157 with no accompanying commit, and `commitBelmontState(cfg.Root)` only runs at :8650 after the wave loop in `runAutoParallel` \u2014 so an interrupt after a merge genuinely leaves master's feature dir modified-but-uncommitted. Severity minor is correct: it is a false diagnostic in a smoke step, and its practical harm (steering the author to `--allow-dirty`, defeating the preflight `clean-tree-preflight` exists for) is bounded.",
   "fix_to_apply": "In \u00a78 Step 2, delete line 364 in full: \"The interrupted run may leave `.belmont/auto.json` untracked, which `requireCleanWorkingTree` rejects on the re-run; delete it or pass `--allow-dirty`.\"  If a dirty-tree note is still wanted in that position, replace it with: \"`.belmont/auto.json` and `.belmont/worktrees/` are gitignored by `ensureStateFiles` at install time (`ensureGitignoreEntry`, `main.go:4611`), so an interrupt leaves neither visible to `requireCleanWorkingTree`. What *can* block the re-run is master's `.belmont/features/<slug>/`: `syncFeatureStateAfterMerge` rewrites it as a plain file copy after each merge (`main.go:10160`, called from `mergeWorktreeBranch` `:9157`) and it is not committed until `commitBelmontState` at the end of `runAutoParallel` (`main.go:8650`) \u2014 so an interrupt *after* a merge leaves it modified. Commit it before re-running; do not reach for `--allow-dirty`.\""
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:348 (Step 1 preconditions)",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Quote is exact: :348 reads `grep -c \"Figma\" .belmont/features/<slug>/PRD.md              # expect 0`. R1 is quoted correctly too (:94 'no active task carries a Figma URL'). The mismatch is real and the codebase confirms the narrower form is the one Belmont actually detects: `skills/belmont/_src/verify.md:69` \u2014 \"Check `{base}/PRD.md` task definitions for `**Figma**:` fields or linked visual references\" \u2014 and `agents/belmont/verification-agent.md:58` \u2014 \"Raw Figma URLs (format: `figma.com/design/:fileKey/:fileName?node-id=:nodeId`)\". The false-positive surface in the template is confirmed at `skills/belmont/_src/references/product-plan-prd-format.md:38` (\"(Figma URLs, technical decisions from interview\u2026)\"), `:57`, `:60`, and `product-plan.md:191` (\"Design intent (if no Figma: what should it look and feel like?)\") invites exactly the prose that trips it. Measured with `/usr/bin/grep`: a file containing only \"No Figma designs exist for this feature.\" returns `1` for the bare-word grep and `0` for `grep -ci 'figma\\.com\\|\\*\\*Figma\\*\\*:'` \u2014 and BSD grep does honour `\\|` in BRE, so the proposed replacement is portable on the author's macOS. I tried hard to break this. Two mitigations exist but neither cancels it: :339 states the real criterion (\"a UI-bearing feature with no Figma URLs\") immediately above the block, and over-strictness fails safe (it rejects a valid subject rather than admitting an invalid one). The secondary `set -e` point is a hypothetical \u2014 the block at :343-351 is not `set -e` and has no `&&` chaining across those lines, so nothing aborts and the author sees the count printed. Downgrading on that alone would be wrong, though: the primary defect stands as an imprecision worth fixing, and the fix is strictly safer than what is there.",
   "fix_to_apply": "Replace line 348 with:\n\n```\ngrep -ci 'figma\\.com\\|\\*\\*Figma\\*\\*:' .belmont/features/<slug>/PRD.md || true   # expect 0\n```\n\nand add one sentence after the fenced block: \"R1 gates on a Figma **URL** or a `**Figma**:` task field \u2014 the forms `verify.md:69` and `verification-agent.md:58-59` actually detect. Do not grep the bare word: a conforming no-Figma PRD may legitimately say so in prose, and the PRD template itself carries 'Figma' in its guidance text (`product-plan-prd-format.md:38`, `:57`, `:60`).\" Leave line 349-350's `grep -c` as-is; it greps for strings that must genuinely be absent."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:370 (Step 4) vs :252 (\u00a74.6)",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Both quotes are exact and neither is truncated in a way that changes meaning \u2014 :370 reads \"shows per-row pass/fail/UNVERIFIABLE with a Mechanism column\", :252 reads \"the three motion rows are recorded `N/A` and skipped. Do **not** record them `UNVERIFIABLE`\". I then applied the 'covered elsewhere' test the brief warns about and grepped every `Applies`/`N/A` occurrence in the document: :94, :155, :190, :199, :201, :252, :258, :379, :402, :410, :476. The motion-`N/A` rule is stated four times in the design sections and once in the DoD (:410 \"verification records those three rows `N/A`, never `UNVERIFIABLE`\") \u2014 but **nowhere in \u00a78**. \u00a78 is the runnable assertion; the DoD is not executable. `AGENTS.md:59` requires the smoke section to \"List the **exact expected output** at each step so success/failure is unambiguous\". The asymmetric risk is the one that matters: because UNVERIFIABLE is listed as an accepted value, an implementation that wrongly emits UNVERIFIABLE for a no-motion feature \u2014 precisely the \u00a74.6/:252 violation \u2014 passes Step 4 unnoticed, so nothing runnable tests the newest rule in the proposal. Counterpoint that keeps this at minor rather than major: :370 reads as a loose description of the verdict column, not a closed enumeration, and if the Step-1 smoke feature does animate then `**Applies**: yes` and the case never arises (the spec does not specify whether the subject animates). Note the existing block being reworked uses MATCH/MISMATCH (`verification-agent.md:210-221`), so the vocabulary is being redefined by this PR anyway and stating it fully costs nothing.",
   "fix_to_apply": "In \u00a78 Step 4 (line 370), replace \"shows per-row pass/fail/UNVERIFIABLE with a Mechanism column\" with: \"shows per-row PASS / FAIL / N/A / UNVERIFIABLE with a Mechanism column \u2014 and if the contract's `**Applies**` is `N/A \u2014 no motion in this feature`, the three motion rows must read `N/A` and never `UNVERIFIABLE` (\u00a74.6)\". Leave the rest of the sentence (the attestation clause) unchanged."
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:413 (DoD) vs :139 (\u00a74.3)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Both quotes are verbatim and complete. Line 413 reads \"Every link in that table checked resolving at branch time \u2014 they are third-party URLs and this PR does not control them\"; line 139 reads \"**Never verify a link with an HTTP status code.** \u2026 Notion \u2014 and Google Drive \u2014 are client-rendered and return 200 with an empty shell for almost any path \u2026 A link is verified when its content has been fetched and identified, not when it responds.\" \"Resolving\" is a synonym for \"responds\", which is precisely the check \u00a74.3 declares meaningless, and this document's own history records three fabricated Notion URLs \"verified\" that way. The document does state the correct method twice in \u00a74.3 (:137 \"verified by **downloading the archive and inspecting its contents**\"; :141 \"Re-check every row at branch time\"), so this is an imprecision rather than a flat contradiction \u2014 but the DoD checklist is the artifact an implementer ticks mechanically, and it is the one place the weaker wording appears. The fix is a one-line wording change with no schema or scope consequence and zero risk of introducing an error, and it forecloses the exact recurrence the document spends a paragraph warning about. Minor is the correct grade.",
   "fix_to_apply": "In \u00a79 Definition of Done, under **Authority and portability**, replace the bullet at :413:\n\n`- [ ] Every link in that table checked resolving at branch time \u2014 they are third-party URLs and this PR does not control them`\n\nwith:\n\n`- [ ] Every link in that table re-checked at branch time by **fetching its content and identifying it** \u2014 for the archive rows, the archive downloaded and inspected. A status code is not a check (\u00a74.3). These are third-party URLs this PR does not control`"
  },
  {
   "spec_location": "\u00a74.1, lines 67-78 (the two-row \"State movement is bidirectional\" table)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK \u2014 all three quotes are verbatim. Line 70: \"as a **plain filesystem copy outside git**\". Line 78: \"so the *branch* cannot carry the edit \u2014 verified, even an explicit `git add` stages nothing\" and \"it covers only files **already in the index**, so a *new* file such as `design-preview.html` is not covered at all\". No ellipsis abuse.\n\nCODE CHECK \u2014 every cited line is accurate. `writeWorktreeGitExcludes` at cmd/belmont/main.go:10057, `gitDir := strings.TrimPrefix(line, \"gitdir: \")` at :10067, `infoDir := filepath.Join(gitDir, \"info\")` at :10072 (the finding's range \"10057-10070\" is two lines short of the infoDir line; snippets themselves are exact). `commitWorktreeChanges` `addCmd := exec.Command(\"git\", \"add\", \"-A\")` at :10192, called from `mergeWorktreeBranch` :9141 before `attemptMerge` :9147.\n\nMEASURED, not reasoned \u2014 I reproduced all three of the finding's experiments in scratch repos at git 2.50.1:\n(1) After the full `copyBelmontStateToWorktree` sequence (worktree add \u2192 copy feature dir \u2192 write `<gitdir>/info/exclude` \u2192 `git update-index --assume-unchanged` over `git ls-files .belmont/`), a new `.belmont/features/myfeat/design-preview.html` came back `git check-ignore -v` \u2192 exit 1 (NOT ignored) and `git status --porcelain` \u2192 `?? .belmont/features/myfeat/design-preview.html`. The exclude is inert because git resolves `info/exclude` through `$GIT_COMMON_DIR`.\n(2) `git add -A` + commit produced `A\\t.belmont/features/myfeat/design-preview.html` on the branch, and `git merge --no-ff` reported `create mode 100644 .belmont/features/myfeat/design-preview.html`. The file reaches main through git, not only through the copy. So the finding's CORE claim \u2014 a third movement path exists and the table omits it \u2014 is TRUE.\n(3) Two siblings both writing the preview: second merge \u2192 `CONFLICT (add/add)`.\n\nI also independently confirmed the spec's own claim it depends on: editing the tracked `TECH_PLAN.md` in a worktree left `git status --porcelain` empty and `git add <path>` staged nothing. So the table's conclusion about the *contract* is right; only its enumeration is incomplete.\n\nWHERE THE FINDING BREAKS \u2014 the headline consequence is FALSE. `attemptMerge`'s `handleConflict:` label (main.go:10421) calls `autoResolveBelmontConflicts(cfg.Root)` (main.go:10471) FIRST, which for any conflicted path under `.belmont/` that is not `PROGRESS.md` runs `git checkout --theirs -- <file>` + `git add <file>` (main.go:10497-10506) and marks it resolved; the following unmerged-files check (:10430-10440) then commits the merge. `runReconciliationAgent` (:10445) is never reached. I executed this against the real add/add conflict: `git checkout --theirs` succeeded, `git diff --diff-filter=U` came back empty, the merge committed, and the file resolved to the second worktree's version. No AI is ever asked to merge two HTML pages. The finding's stated blocker rationale therefore does not exist.\n\nThe remaining consequences are benign. `design-preview.html` reaching main via the branch produces the same end state as reaching it via `syncFeatureStateAfterMerge` \u2014 the file lands, stamped unreviewed, exactly as R4 (line 99) and \u00a74.5 line 219 intend. No false or broken result is shipped. The finding's prescription that \"R4 must NOT regenerate `design-preview.html` when the file is absent from main\" rests entirely on the refuted reconciliation claim and must NOT be applied.\n\nPARTIAL DUPLICATION \u2014 the spec already states both enabling facts in the same paragraph (line 78: assume-unchanged \"covers only files already in the index\"; `writeWorktreeGitExcludes` \"does nothing\"). What it never does is join them to `git add -A` and draw the conclusion. That gap is real but is one row of documentation, not a design defect. Minor.",
   "fix_to_apply": "Add a third row to the \u00a74.1 table (after the `worktree \u2192 master` row), and one clarifying sentence. Row:\n\n| **worktree \u2192 main via git** | `commitWorktreeChanges` (`main.go:10179`; `git add -A` at `:10192`) then `attemptMerge` (`:10295`) | Any `.belmont/` path **not already in the worktree's index** is plain untracked \u2014 `writeWorktreeGitExcludes` is inert \u2014 so `git add -A` commits it to the milestone branch and the merge lands it on main. Applies to a *new* file such as `design-preview.html`, never to the already-tracked `TECH_PLAN.md` |\n\nThen amend the `--assume-unchanged` paragraph at line 78 so the consequence is drawn, replacing \"so a *new* file such as `design-preview.html` is not covered at all\" with: \"so a *new* file such as `design-preview.html` is not covered at all \u2014 it is plain untracked, and `commitWorktreeChanges`' `git add -A` puts it on the branch (row 3). Two siblings that both create it produce an add/add conflict, which `autoResolveBelmontConflicts` (`main.go:10471`, `git checkout --theirs` at `:10499`) resolves automatically for any non-`PROGRESS.md` path under `.belmont/`; the reconciliation agent is not invoked. Last merge wins, then `syncFeatureStateAfterMerge` overwrites with the same worktree's copy \u2014 consistent, if arbitrary.\"\n\nDo NOT add the finding's proposed restriction on R4 preview regeneration, and do NOT add `design-preview.html` to smoke Step 3's diff \u2014 R4 (line 99) and \u00a74.5 (line 219) deliberately permit a stamped headless regeneration, so a non-empty preview diff there would be a false failure."
  },
  {
   "spec_location": "\u00a74.5 line 217; \u00a710 line 452; \u00a78 Step 3 lines 366-368",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK \u2014 verbatim on both. Line 217: \"...**unless `.belmont/` is gitignored**, a supported configuration in which that partial short-circuits entirely and neither contract nor preview is ever committed. Durability is unaffected (the copy paths are filesystem-level), but reviewability is; say so rather than promising a diff that may not exist.\" Line 452: \"Durability is unaffected; reviewability is not promised\".\n\nEVIDENCE CHECK \u2014 every cited artifact is real. `skills/belmont/_partials/commit-belmont-changes.md:5-9` reads \"1. **Check if `.belmont/` is git-ignored** \u2014 run: `git check-ignore -q .belmont/ 2>/dev/null` \u2026 If exit code is 0, `.belmont/` is ignored \u2014 skip this section entirely.\" `docs/directory-structure.md:124-126` confirms the configuration is documented (\"Generally, no \u2026 The only case to gitignore it is if you're a solo developer\u2026\"), so \"supported\" is fair. `syncFeatureStateAfterMerge`'s `os.RemoveAll(dstFeature)` / `copyDir(srcFeature, dstFeature)` are at main.go:10169-10170; `commitBelmontState`'s empty-status early return at :10218-10226.\n\nMEASURED \u2014 I reproduced the whole flow with `.gitignore` = `.belmont/`. `git check-ignore -q .belmont/` exit 0 (partial skips). `git worktree add` produced no `.belmont/` in the worktree at all. `git ls-files .belmont/` in the worktree was empty, so `untrackBelmontInWorktree` marked nothing. The branch commit contained only `M src/a.js`. After the sync, master's `TECH_PLAN.md` read `ROGUE`. `git status --porcelain .belmont/` was empty, so `commitBelmontState` commits nothing. `git diff $PLAN..HEAD -- .belmont/features/myfeat/TECH_PLAN.md` printed nothing, exit 0. And `git checkout $PLAN -- .belmont/features/myfeat/TECH_PLAN.md` failed with \"did not match any file(s) known to git\" \u2014 recovery genuinely impossible, where the tracked configuration recovers.\n\nSO WHAT SURVIVES, AND WHAT DOES NOT. The half that survives cleanly is the *detection* claim: \u00a710 line 444 names smoke Step 3's committed-revision diff as the mitigation for the PR's top risk, unqualified, and in this configuration no committed revision of the contract exists at all, so that check is structurally blind. Nothing anywhere in the 491-line document qualifies line 444 for this case \u2014 I grepped every occurrence of \"gitignor\", and line 217 and line 452 are the only two, both scoped to reviewability. That gap is real and unstated.\n\nThe half that does NOT survive is the headline. \"Durability is unaffected\" carries an explicit parenthetical scoping it \u2014 \"(the copy paths are filesystem-level)\" \u2014 and that statement is TRUE: `copyBelmontStateToWorktree` and `syncFeatureStateAfterMerge` move the contract regardless of git, so the file persists and reaches the worktree exactly as in the tracked config. The finding wins its \"durability *is* affected\" claim only by re-defining durability as post-overwrite *recoverability*, which the spec never asserted; \u00a74.1 line 74 already frames the risk as \"You lose the approval, not the edit\" in both configurations. That is an expansive reading of an accurate sentence, not a false statement.\n\nWHY MINOR \u2014 the configuration is a documented minority that the project's own docs discourage, the smoke test is specified for a repo that commits `.belmont/` (Step 1 line 354 asserts both artifacts are \"committed by the skill's commit step\", which fails visibly on a gitignored install before Step 3 is reached), and \u00a710 line 444 already tells the reader the rule is \"**explicitly unguarded**\" prose. What is genuinely owed is a one-clause qualification plus one sentence on recoverability, not a new sidecar-hash mechanism \u2014 and the finding's suggested \"write a sidecar hash/backup under a path outside `.belmont/`\" would add an unreviewed artifact and a new write surface to a PR whose stated scope is prose only, so it must not be applied.",
   "fix_to_apply": "Two edits, no new mechanism.\n\n1. \u00a74.5 line 217 \u2014 replace the final clause \"Durability is unaffected (the copy paths are filesystem-level), but reviewability is; say so rather than promising a diff that may not exist.\" with: \"The contract still moves and still persists \u2014 the copy paths are filesystem-level \u2014 but two things are lost in that configuration and must be stated: there is no committed revision to restore from after a worktree phase overwrites the contract (`git checkout $PLAN -- <path>` fails with \\\"did not match any file(s) known to git\\\" \u2014 measured), and smoke Step 3's diff has nothing to compare against, so the \u00a710 detection does not exist. Say so rather than promising a diff that may not exist.\"\n\n2. \u00a710 \u2014 append to line 444's mitigation cell: \"**This detection does not exist when `.belmont/` is gitignored** \u2014 no revision of the contract is ever committed, `commitBelmontState` no-ops on an empty `git status --porcelain .belmont/` (`main.go:10218-10226`), and the diff returns empty regardless. Users on that configuration have neither detection nor recovery; the docs already discourage it (`docs/directory-structure.md:124`).\" And change line 452's cell to \"...Durability of the file is unaffected (the copy paths are filesystem-level); recoverability and detection are not \u2014 see \u00a74.5.\"\n\nDo NOT add a sidecar hash or backup artifact \u2014 the PR is prose-only and a new write surface outside `.belmont/` is out of scope."
  },
  {
   "spec_location": "\u00a74.6 check table lines 237-250 and severity line 260; \u00a74.3 line 113",
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "QUOTE/COUNT CHECK: the table is header 237-238 plus twelve rows at 239-250. Line 260 assigns nine severities that land on eight of those rows (239 spacing, 241 contrast, 243 focus, 244 labels, 245 radius, 246 elevation, 247 state inventory, 248 durations \u2014 \"easing inconsistency\" is not a table row at all). The four rows named as unmapped \u2014 240 \"Type sizes from declared scale\", 242 \"Touch targets \u2265 44\u00d744\", 249 \"Only `transform`/`opacity` animated\", 250 \"Reduced motion removes movement, not function\" \u2014 are genuinely unmapped. Supporting cites verified: line 171 \"Targets \u2265 44\u00d744px (SC 2.5.5, AAA \u2014 adopted as Belmont's floor)\"; line 195 \"Animating layout properties is a defect\"; line 113 tier-2 \"may never lower the a11y floor\"; `verification-agent.md:299` and `:307` confirm both tiers block. So the completeness observation survives.\n\nBut the argument that carries the *major* grade does not. Three breaks:\n\n(1) \"a failure has no defined disposition and the safe reading is Critical\" is false. Line 260 says severity \"uses the existing ladder\", and that ladder is a general classifier with a11y rows already in it: `verification-agent.md:311` \"Accessibility issues that affect usability\" \u2192 Warning, `:317` \"Minor accessibility notes\" \u2192 Polish, `:320` \"Animation/transition polish\" \u2192 Polish. An unlisted check falls through to those rules, not into a vacuum. The gap is imprecision, not a hole.\n\n(2) \"an AAA criterion the project never chose\" is contradicted by the spec, and by the finding itself two sentences earlier. \u00a73 line 46 states \"**SC 2.5.5** (44\u00d744 targets, **Level AAA**) \u2026 The baseline adopts them as *Belmont's* floor, which is a project choice, not a WCAG requirement\", and \u00a74.4 line 171 discloses the level inline. The project chose it explicitly and disclosed it; that half of the suggested fix is arguing against a deliberate, documented author decision.\n\n(3) \"nothing handles 'this element is not ours'\" is overstated. \u00a74.4 line 182 handles it for the State Inventory: \"Components the feature only consumes get no entry.\" Line 207 handles the token side: existing components are \"**LAW** \u2014 the contract records them rather than redesigning them.\" And line 260 is itself the author's (broken \u2014 see the \u00a74.6-line-260 verdict) attempt at the pre-existing-drift problem. The ownership gap for the a11y-floor rows is real but is adjacent to, and largely remedied by, the line-260 re-tiering.\n\nThe suggested ownership rule is also not applicable as written: \"DOM subtrees the milestone's tasks created or modified\" is not evaluable from `browser_evaluate` at verify time, and a milestone that composes only third-party components would skip the a11y gate entirely.\n\nGRADE: what survives is the bare completeness gap \u2014 four of twelve rows carry no severity in a section whose whole purpose is a checkable gate. Imprecision worth fixing \u2192 minor.",
   "fix_to_apply": "In `docs/proposals/0003-design-quality-without-figma.md` \u00a74.6, extend the severity mapping so all twelve check rows are covered, and add a fallback clause. Apply ONLY this \u2014 do not adopt the ownership rule or the touch-target downgrade (see reasoning).\n\nAfter the mapping sentence produced by the \u00a74.6-line-260 fix, append:\n\n\"The remaining rows: type sizes off the declared scale \u2192 Polish (same class as off-scale spacing); touch targets below 44\u00d744 \u2192 Warning (matches `verification-agent.md:311`, \\\"Accessibility issues that affect usability\\\"); animating a layout property rather than `transform`/`opacity` \u2192 Polish (matches `:320`, \\\"Animation/transition polish\\\"); `prefers-reduced-motion` not honoured \u2192 Warning (`:311`). Any check not named here falls to `verification-agent.md`'s existing Severity Classification Guide (`:295-326`) \u2014 no check row is left without a disposition.\"\n\nOptionally reflect this in the DoD by extending line 419 from \"Every check row names a mechanism\" to \"Every check row names a mechanism and a severity\"."
  },
  {
   "spec_location": "\u00a74.7 line 267",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check first: the finding's quotation of \u00a74.7 line 267 is exact and unellipsed \u2014 \"the **sub-agent dispatch prompt** (`:109`, `:146`) steers the agent away from the new behaviour by enumerating only Figma/screenshot references and injecting \\\"No design references found\\\"\". Confirmed verbatim via grep against docs/proposals/0003-design-quality-without-figma.md:267.\n\nEvidence check: the spec cites `skills/belmont/_src/verify.md` (not the generated SKILL.md), and its sibling citations in the same bullet resolve correctly against _src (`:50` = \"**Do NOT** re-check visual specs against design references\u2026\"; `:62-72` = \"## Step 1b: Gather Design References\" through \"Collect whatever you find\u2026\"), so :109/:146 are unambiguously _src line numbers too. `grep -n 'Read \\`{base}/TECH_PLAN.md\\`' skills/belmont/_src/verify.md` returns exactly two hits, 109 and 146, both the identical string \"> Read `{base}/TECH_PLAN.md` for technical specifications (if it exists).\" The behaviour the sentence describes lives at :114 \"> **Design References for Visual Verification**:\", :116-118 (the Figma fileKey/nodeId + screenshot enumeration), :119 \"> If no MILESTONE files or references were found, write: \\\"No design references found in archived MILESTONE files or PRD.\\\"]\" and :121 (\"> **Visual Verification**: \u2026 call `mcp__plugin_figma_figma__get_screenshot` \u2026\"). Line 146 sits inside `### Agent 2: Code Review (code-review-agent)` (:129), whose prompt body (:135-151) contains no design-reference enumeration at all \u2014 its only content is PRD/TECH_PLAN/MILESTONE reads plus package-manager detection and code-quality review. So the attributed behaviour is absent from :146 entirely and is 5+ lines away from :109.\n\nSearched the whole document for a place that handles this correctly elsewhere: only three other mentions of verify.md exist (`:256` focused-reverify, `:296` scope row \"dispatch prompt carries it\", `:461` rebase note). None carries the :114-121 anchor, so the misattribution is not corrected anywhere. \u00a75:296's \"dispatch prompt carries it\" is compatible with :109/:146 being intended as *edit targets* for adding the contract read \u2014 the most charitable reading \u2014 but even under that reading the attached rationale clause (\"by enumerating only Figma/screenshot references and injecting \u2026\") is factually inapplicable to Agent 2, and the bullet silently fuses two distinct edits (add contract path at :109/:146; de-Figma the enumeration at :114-121) under one anchor.\n\nNot a taste matter: this document polices its own citations to the line (\u00a712 lists `user-questions.md:4` \u2192 `:8` as a fixed minor). Not an acknowledged limitation either \u2014 the pre-0005 line-drift caveat at :9 covers `main.go` only, explicitly (\"All `main.go:NNNN` citations\").\n\nSeverity is correctly graded minor, not major: the offending text is named verbatim in the prose (\"No design references found\"), which is a unique, greppable string at :119, so an implementer cannot be led to ship a wrong result \u2014 worst case they hunt for a few seconds or make an incomplete edit that the prose itself flags. It does not block implementation and does not produce a false claim about the codebase.",
   "fix_to_apply": "In /Users/benlavender/belmont/docs/proposals/0003-design-quality-without-figma.md, replace line 267 in full with:\n\n- **`skills/belmont/_src/verify.md`** \u2014 three narrow edits, not one: Step 1b's collect list (`:62-72`) is Figma/image/URL-only; the **Agent 1 (verification) dispatch prompt** steers the agent away from the new behaviour at `:114-121`, enumerating only Figma/screenshot references and injecting \"No design references found\" \u2014 that block must also present the contract. Both dispatch prompts' `Read {base}/TECH_PLAN.md` lines (`:109` in Agent 1, `:146` in Agent 2) gain the contract read; **Agent 2 (code review) needs the contract path only \u2014 its prompt contains no reference enumeration to change.** And the focused-reverify clause at `:50` needs the contract exemption. *This file has Steps, not Phases \u2014 `verification-agent.md` has Phases. Never conflate them.*\n\nOptionally mirror the split in the \u00a75 scope row at line 296: `| skills/belmont/_src/verify.md | Step 1b collects the contract; Agent 1 dispatch prompt de-Figma'd (:114-121) and both dispatch prompts read it (:109, :146); focused-reverify exemption at :50 |`. Leave \u00a712 as is."
  },
  {
   "spec_location": "\u00a74.4 derivation ladder rung 0 (line 205); \u00a74.4 `**Source**` enum (line 156); \u00a710 risk row (line 450)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "The factual core checks out and I could not break it. Verified:\n- Line 205 is exactly \"0. A `## Design Contract` in the **master** `.belmont/TECH_PLAN.md`.\" and line 156 lists `master TECH_PLAN` in the `**Source**` enum.\n- `grep -n \"master\" 0003-\u2026md` returns 14 hits; none is a write instruction. Line 63 is the only placement statement and says `{base}/TECH_PLAN.md` only. Line 264 and \u00a75 row 300 tell `design-agent` to *read* master, never tell `tech-plan` to write a contract there.\n- `skills/belmont/_src/references/tech-plan-master-format.md` headings (`grep -n '^##'`): Overview, Stack & Tooling, Project Structure, Architecture Decisions, Coding Conventions, Testing Strategy, Security Baseline, CI/CD Pipeline, Deployment, Shared Patterns, Decision Log, References \u2014 no `## Design Contract`; the file is absent from \u00a75.\n- Scenario C is real and unmentioned: `grep -n \"Scenario\" 0003-\u2026md` returns **zero matches**. `tech-plan.md:137` \"### Scenario C \u2014 User explicitly wants to update master only\", `:141` \"- Skip feature detection and feature plan creation.\", `:233-234` \"**Scenario C \u2014 Master only:** / 1. Update `.belmont/TECH_PLAN.md` in-place\u2026\". Phase 3.5 is inserted between `:217` and `:219`, above all three Phase-4 branches.\n\nSo rung 0 has no producer and the `**Source**: master TECH_PLAN` value can never be emitted.\n\nBut the severity is badly overgraded. \"Blocker = cannot be implemented as written, or ships a false/broken result.\" Neither holds: the ladder says \"stop at the first rung that supplies a token family\", rung 0 simply never matches, and derivation falls through to 0b/1/2 and works. Nothing false ships. The \u00a710 row is named \"Sibling features mint different scales\" \u2014 that named risk is carried entirely by rung 0b (sibling feature contracts), which this PR *does* produce; only the phrasing \"rungs 0 and 0b\" overclaims. And the Scenario C \"undefined behaviour\" largely resolves on the natural reading: R1 gates on \"the feature\", Scenario C has no selected feature and no `{base}`, so Phase 3.5 is a no-op \u2014 and \"Silence is not allowed\" attaches to a `**Mode**` field inside a contract that has no file to live in.\n\nWhat survives is an internal inconsistency: a ladder rung, an enum value and half a \u00a710 mitigation clause with nothing behind them, plus an unstated scenario. That is \"imprecision worth fixing\" = minor. The finding's option (a) fix must be rejected \u2014 adding `tech-plan-master-format.md` to \u00a75 breaks the verified 13-file count in the header and expands a deliberately bounded scope.",
   "fix_to_apply": "Apply option (b) only \u2014 four small edits, no scope change:\n1. \u00a74.4, delete line 205 (\"0. A `## Design Contract` in the **master** `.belmont/TECH_PLAN.md`.\") and renumber the existing `0b.` (line 206) to `0.`, keeping its text and its italic greenfield note.\n2. \u00a74.4, line 156: remove `master TECH_PLAN |` from the `**Source**` enum, leaving `[storybook | tailwind.config.ts | globals.css | components.json | sibling feature <slug> | none \u2014 established here]`.\n3. \u00a710, line 450: replace the mitigation cell \"Ladder rungs 0 and 0b consult master and sibling contracts\" with \"Ladder rung 0 consults sibling feature contracts (most recently approved wins; `**Source**` must name the slug)\".\n4. \u00a74.2, append one sentence to R1: \"Phase 3.5 is a no-op under tech-plan's Scenario C (master-only, `tech-plan.md:137-141`, `:233-234`) \u2014 there is no selected feature to gate on and no `{base}` to write into; the master TECH_PLAN carries no design contract.\"\nLeave line 264 and \u00a75 row 300 (design-agent reads master TECH_PLAN for cross-cutting styling decisions) unchanged \u2014 that consumer is independent of the deleted rung. Do NOT add `references/tech-plan-master-format.md` to \u00a75."
  },
  {
   "spec_location": "\u00a74.1 write rule (line 82); \u00a75 In-scope table; \u00a77 knowledge list (lines 334-335)",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "The quote at line 82 is accurate and the cited evidence is real (`references/debug-manual-spec-reconcile.md:17` is exactly the category-5 row permitting \"Edit narrative in place\" on `{base}/TECH_PLAN.md`; `debug-manual.md:297` is `### Hard limits during reconciliation` with four prohibitions, none about design). But four of the finding's five claims break under the surrounding context.\n\n(1) The enumeration is not a skill catalogue, it is the auto-loop phase set. \u00a74.1 is entirely about worktree phases and merge copies: the sentence immediately after is \"Nothing mechanical prevents a **worktree phase** from writing that path\", the clause inside the rule is \"When `tech-plan` itself runs headlessly as `actionReplan`\", and DoD line 395 says \"no other **phase** writes that path\". `implement`/`verify`/`next`/`debug` map onto the loop actions (`main.go:395-407`: actionImplementMilestone, actionImplementNext, actionVerify, actionDebug\u2026).\n\n(2) Under that reading the rule is *true*, verified in code: `cmd/belmont/main.go:7190-7191` \u2014 `case actionDebug:` / `return fmt.Sprintf(\"/belmont:debug-auto --feature %s\", feature)`. Auto's DEBUG is `debug-auto`, and `skills/belmont/_src/debug.md:18` states \"**Auto** commits the code change and ends. The PRD, TECH_PLAN, NOTES stay as they are.\" `knowledge/cross-cutting/debug-spec-reconciliation.md` says the same: \"`debug-manual` is never invoked from `belmont auto` \u2014 `actionDebug` routes to `/belmont:debug-auto`\".\n\n(3) \"debug-manual silently rewrites approved design contracts\" is contradicted by the finding's own evidence. The same knowledge entry: \"**Human-gated.** Each spec edit is presented as a unified diff and gated on explicit `y / N / edit / skip` approval. **No silent writes.**\" Nothing is silent.\n\n(4) \"The PR silently repeals an approved capability\" is false \u2014 the PR edits no debug file, so debug-manual's behaviour is unchanged. The \u00a75 and \u00a77 omissions are therefore correct, not oversights: a `Revisions` line is only owed when an entry's content changes.\n\n(5) The suggested fix collides with an explicit `Don't re-do`: \"**Routing spec edits through `/belmont:tech-plan` instead.** Considered. Rejected because (a) Blake explicitly asked for in-place edits without the extra step, (b) `/belmont:tech-plan` is a planning skill, not a debugging skill\u2026\". Adding `_src/debug-manual.md` + its reference to \u00a75 also breaks the header's verified 13-file count.\n\nWhat genuinely survives is one word. `debug` is the literal name of a real skill (`skills/belmont/_src/debug.md`, the router) whose Manual branch does write `{base}/TECH_PLAN.md`, and this PR creates a new knowledge entry (`design-authority.md`, \u00a77) that would carry the unqualified sentence into the knowledge tree alongside `debug-spec-reconciliation.md`'s contrary invariant. A one-word tightening removes a future contradiction between two knowledge entries at zero cost. Minor.",
   "fix_to_apply": "Two edits to \u00a74.1 only \u2014 no scope change:\n1. Line 82, change the rule to: \"> **Only the `tech-plan` skill may write `{base}/TECH_PLAN.md`. The auto-loop phases \u2014 `implement`, `verify`, `next` and `debug-auto` \u2014 read it and never write it.** When `tech-plan` itself runs headlessly as `actionReplan`, R4 constrains what it may change.\" (`actionDebug` routes to `/belmont:debug-auto`, `main.go:7190-7191`; `debug-auto` leaves TECH_PLAN untouched, `debug.md:18`.)\n2. Add one sentence immediately after the rule block: \"Interactive `/belmont:debug-manual` is the documented exception \u2014 its Spec Reconciliation may edit `{base}/TECH_PLAN.md` narrative in place under per-edit diff approval (`references/debug-manual-spec-reconcile.md:17`; `knowledge/cross-cutting/debug-spec-reconciliation.md`). This PR does not change that, but a contract edit made there leaves `**Approval**` and `design-preview.html` stale; re-run `/belmont:tech-plan` after such an edit.\"\nCarry the same qualification into the new `knowledge/cross-cutting/design-authority.md` (\u00a77) so it does not contradict `debug-spec-reconciliation.md`.\nDo NOT apply the finding's suggested fix: do not add `_src/debug-manual.md` or `_src/references/debug-manual-spec-reconcile.md` to \u00a75, do not add a fifth hard limit at `debug-manual.md:297-303`, and do not add a `Revisions` line to `debug-spec-reconciliation.md` \u2014 no debug file changes in this PR."
  },
  {
   "spec_location": "\u00a74.5 (line 215); \u00a75 In-scope table; \u00a77 docs list",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Spec quote confirmed (line 215: \"`tech-plan` also writes **`{base}/design-preview.html`**\"), and reset.md/cleanup.md are indeed absent from \u00a75. But the finding's central consequence is FALSE and its stated impact is overstated; only a small kernel survives.\n\nFALSE \u2014 the reset half. reset.md:65 is quoted correctly, but the finding stops one line short. reset.md:66 reads `3. Remove the feature directory`, and Option 2 at :70ff reads `1. Delete all subdirectories under `.belmont/features/``. The parenthetical at :65 is illustrative; the operative deletion removes the directory wholesale, taking `design-preview.html` with it. \"A per-feature reset ... orphans a preview page\" is simply wrong, and `_src/reset.md` needs no change.\n\nOVERSTATED \u2014 rung 0b. Archiving a sibling does not \"silently kill derivation rung 0b for every future feature\". Rung 0 (master `.belmont/TECH_PLAN.md`, line 205) and rung 2 (\"Project config \u2014 `tailwind.config.*`, CSS custom properties, `components.json`, theme files\", line 208) still supply the token family \u2014 and by the time a feature is complete enough to archive, its design system *is* in project config, which is rung 2 by construction. Archiving is also an explicit user-chosen destructive action that already discards PRD.md and TECH_PLAN.md; losing the contract with the tech plan is the semantics the user asked for. The size-accounting point is trivial.\n\nSURVIVES \u2014 one enumeration. cleanup.md:139 (`2. Delete all other files in `.belmont/features/<slug>/`: PRD.md, TECH_PLAN.md, PROGRESS.md, NOTES.md, MILESTONE.md, all MILESTONE-*.done.md`) is the one path that keeps the directory alive (ARCHIVE.md is written into it at :120-137, confirmed: `## Summary` :129, `## Key Decisions` :132, `## Key Files` :135). Read as an exhaustive list \u2014 which it plausibly is \u2014 it leaves `design-preview.html` sitting beside ARCHIVE.md, an orphaned page asserting an approved design for a feature whose plan is gone. Same for the scan enumeration at :49. This PR introduces the first non-Markdown file in the feature dir, so a name-based enumeration is a real, if small, gap. Minor, not major.",
   "fix_to_apply": "Add ONE row to \u00a75's In-scope table:\n\n| `skills/belmont/_src/cleanup.md` | Add `design-preview.html` to the feature-dir file enumerations at `:49` (scan/count) and `:139` (archive deletion) |\n\nAnd append one sentence to \u00a74.5, after the paragraph ending \"...say so rather than promising a diff that may not exist.\":\n\n\"`/belmont:cleanup` enumerates feature-dir files by name at `cleanup.md:49` and `:139`; `design-preview.html` must be added to both, so the Archive path does not leave an orphaned preview beside `ARCHIVE.md`. `/belmont:reset` needs no change \u2014 it removes the whole feature directory (`reset.md:66`, `:71`).\"\n\nDo NOT add `_src/reset.md` to scope. Do NOT add a DoD item or a \u00a710 risk row for archived sibling contracts, and do NOT extend the ARCHIVE.md template to carry the contract \u2014 rungs 0 and 2 still supply tokens after an archive."
  },
  {
   "spec_location": "\u00a77 (lines 328-333); \u00a712 minors (line 491, \"`docs/` list extended to five pages\")",
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes. \u00a77 (lines 328-333) lists exactly five surfaces \u2014 agent-pipeline.md, skills-reference.md, workflow.md, prd-format.md, README.md \u2014 plus AGENTS.md at :333; line 6 counts \"5 docs pages\" and line 491 says \"`docs/` list extended to five pages\", so the list is presented as closed, not illustrative. `grep -n -i \"directory-structure\"` over the whole 492-line proposal returns zero hits, so failure mode (b) is excluded: the page is addressed nowhere. Evidence check passes verbatim: docs/directory-structure.md:86-94 is the quoted tree, and :91 is `\u2502   \u2502       \u251c\u2500\u2500 models.yaml      # Per-feature model tiers (optional, written by /belmont:tech-plan)` \u2014 an exact precedent for an optional, tech-plan-authored per-feature artifact being enumerated on that page. The proposal adds `{base}/design-preview.html` (\u00a74.5 line 215, R7 line 102, smoke line 354) into the same directory, and AGENTS.md's Notes section requires docs/ to be kept current with \"new changes/paths\". So the omission is real. Two corrections to the finding's grading, neither fatal: (1) the tree already omits NOTES.md \u2014 a real per-feature file, per docs/skills-reference.md:140 \"each selected feature's `PRD.md`, `TECH_PLAN.md`, `PROGRESS.md`, `NOTES.md`\" \u2014 so \"the one page a new file in the feature directory unambiguously invalidates\" is overstated; (2) the README half is wrong: README.md's tree at 234-249 lists only PRD/TECH_PLAN/PROGRESS/MILESTONE/MILESTONE-M1.done and already omits `models.yaml`, `worktree.json` and `NOTES.md`, so it is deliberately illustrative and does not go stale for this reason. Minor stands on the directory-structure.md half only.",
   "fix_to_apply": "In \u00a77, insert after the `docs/prd-format.md` bullet (line 331):\n\n`- \\`docs/directory-structure.md\\` \u2014 add \\`design-preview.html\\` to the \\`.belmont/features/<slug>/\\` tree (after the \\`models.yaml\\` line at \\`:91\\`), annotated \\`# Contract preview (optional, written by /belmont:tech-plan)\\` to match that line's style; extend the \\`.belmont/\\` bullet at \\`:121\\` to name the design contract + preview`\n\nThen update the two counts: line 6, `+ 5 docs pages` \u2192 `+ 6 docs pages`; line 491, `\\`docs/\\` list extended to five pages` \u2192 `\\`docs/\\` list extended to six pages`. Do NOT add any requirement to update README.md's `.belmont/` tree at :234-249 \u2014 that tree already omits `models.yaml`, `worktree.json` and `NOTES.md` and is illustrative; the existing README row (reframe the \"Figma-first design workflow\" bullet) is sufficient."
  }
 ],
 "refuted": [
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:94 (R1) vs :379 (smoke Step 7) vs :402 (DoD)",
   "reasoning": "The finding is refuted by the sentence it quotes itself. :94 in full: \"Run only when the feature has a user interface **and** no active task carries a Figma URL. Record the outcome in the contract's `**Mode**` field, which is the single machine-read signal (\u00a74.4) \u2014 there is no separate `**Design contract**` field. Backend features record `N/A \u2014 no "
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:289-308 (\u00a75 In scope) vs :328-335 (\u00a77) and :437 (DoD)",
   "reasoning": "The factual observation checks out \u2014 \u00a75's 18-row table (:291-308) contains no `docs/*.md` row, no `knowledge/KNOWLEDGE.md` row and no `model-tier-economics.md` row (verified by grep: the only `docs/` hits in the file are :283, :328-331 and :491). But the inference that this is a defect breaks on three independent statements elsewhere in the same do"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:207 (\u00a74.4 derivation rung 1)",
   "reasoning": "The spec quote is accurate but both refuting prongs fail against the code and the partials.\n\nPRONG (a) \u2014 \"Belmont handles Storybook by dynamic free-port allocation precisely so it does not collide\" \u2014 is contradicted by the very line the finding cites. skills/belmont/_partials/worktree-awareness.md:35 reads in full: \"No, it's a secondary server (Sto"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:190/:199 (`**Applies**`), :252 (verification), :99 (R4), :82 (\u00a74.1 write rule)",
   "reasoning": "QUOTE CHECK: all quotes are accurate (:190, :199, :252, :99, :82, :168, :180, :191, :249, :410 all verified verbatim). The finding fails on its reasoning, not its quoting.\n\nSUB-CLAIM (a) IS AN OVERREACH. :168 reads \"Elevation \u2014 [levels]. Interactive elements rise one level on hover.\" That mandates a *state change*, not a *transition*. An instant ho"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:225-227 (\u00a74.6 branches), :231, :267 and :296 (\u00a75 verify.md row)",
   "reasoning": "Quotes are accurate (verification-agent.md:20, :60, :63, :69-71, :129 and verify.md:70, :114-119 all read as claimed), but every load-bearing inference fails.\n\n(1) \"\u00a74.6 cites :60 and then draws the opposite conclusion from what it says\" is false. :60 is \"- **TECH_PLAN or NOTES**: Any visual specifications or reference material\", sitting under `###"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:269 (\u00a74.7 post-verify-triage) and :422 (DoD)",
   "reasoning": "The finding's quotes of the spec (:269 \"Add a non-deferrable contract list\"; :422 \"post-verify-triage.md lists contract failures as non-deferrable\") and of the prompt file (:22, :32, :42, :63-65, :102) are all accurate. But its load-bearing causal claim is false, and I broke it against the Go code.\n\nThe claim is that the circuit breaker overrides t"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:98 (R3) and :386 (Step 9c)",
   "reasoning": "The quotes are accurate and the line cites are right \u2014 `skills/belmont/_partials/user-questions.md:8` is rule 4 (\"NEVER ask questions as plain inline text when a structured question tool exists\"), `:11` is rule 7 (Codex strict), `:14` is rule 8 (\"Non-Codex fallback: outside Codex, if no structured question tool exists at all, ask one clearly format"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:372-373 (Step 5)",
   "reasoning": "The mechanical evidence checks out, but the reading of the spec does not. (a) is refuted by the document itself, in two places the finding did not consult. Line 358 (Step 1b) states outright: \"*This is the only step exercising `verify.md`'s and `implement.md`'s interactive path.*\" \u2014 so Step 5's \"second verify\" is on the auto path by construction; t"
  },
  {
   "spec_location": "docs/proposals/0003-design-quality-without-figma.md:203-207 (derivation ladder rung 1) and :156-157 (**Source** field)",
   "reasoning": "The quotes are accurate but the stop-rule is misread. Line 203 is conditional: \"stop at the first rung **that supplies a token family**\". On the CSF-only Storybook-plus-Tailwind project the finding describes, rung 1 supplies no token family, so by the literal rule the walk does not stop there \u2014 it continues to rung 2 and `**Source**: tailwind.confi"
  },
  {
   "spec_location": "\u00a74.7 line 269; \u00a75 line 303; DoD line 422",
   "reasoning": "The quotes are accurate (proposal line 269 'Add a non-deferrable contract list'; DoD line 422; post-verify-triage.md rule 1 and the 'absolute' circuit breaker at line 102), but the mechanism the finding is built on does not exist, and the dilemma it poses is false.\n\n(a) THE CIRCUIT BREAKER IS INERT. `cmd/belmont/main.go:6999-7001` \u2014 '// Determine f"
  },
  {
   "spec_location": "\u00a74.4 rung 1 line 207; State Inventory lines 179-182; `**Source**` line 156",
   "reasoning": "Three sub-claims, each defeated by text the finding elided or by the ladder's own stop-rule.\n\n(a) 'STORYBOOK CANNOT SUPPLY WHAT STATE INVENTORY NEEDS' \u2014 over-broad. Line 182 reads 'Components the feature **only consumes** get no entry' (emphasis on 'only'). A component the feature *modifies or extends* is not only-consumed, gets an entry, and does "
  },
  {
   "spec_location": "\u00a74.2 R1 (line 94); \u00a75 product-plan.md row (line 294); \u00a79 \"Gating and scope\" (line 402)",
   "reasoning": "The quote at line 94 is accurate, and it is literally true that the document never states how a planner determines \"the feature has a user interface\". But the finding breaks on three counts.\n\n(1) The claimed asymmetry does not exist. R2 (line 97) is quoted as \"a defined detection method\" \u2014 it is not. R2 reads \"**R2 \u2014 Figma detection only, no loadin"
  },
  {
   "spec_location": "\u00a74.7 post-verify-triage bullet (line 269); \u00a75 post-verify-triage row (line 303); \u00a79 (line 422)",
   "reasoning": "Quotes are accurate (`post-verify-triage.md:22`, `:102`, `:50` verbatim; the Deferrable header is at `:42` not `:43`, immaterial). The finding still fails on both legs. Leg 1 \u2014 \"a non-deferrable list is unsatisfiable beside an absolute circuit breaker\" \u2014 that tension is pre-existing, universal, and not created by this PR: the same prompt already sa"
  },
  {
   "spec_location": "\u00a74.7 post-verify-triage bullet (line 269)",
   "reasoning": "Every code fact checks out \u2014 `cmd/belmont/main.go:7000-7002` `fixRound := 0` with the comment \"We'll pass 0 and let the template handle it\", `:7018-7019` `\"FixRound\": \u2026` / `\"VerifyOutput\": \"\", // verify output is available from lastOutput in the loop`, the single call site at `:6880`, and the inline fallback at `:7008-7012`. The inference built on "
  },
  {
   "spec_location": "\u00a74.6 (lines 235, 237-250, 260); \u00a75 In-scope table",
   "reasoning": "Quotes check out (code-review-agent.md:78 \"**Design Fidelity** - UI matches specifications?\", :17 TECH_PLAN read, :77 Tech Plan Match, :199/:201 FWLUP-CR1 + Critical/Warning-only note; grep for browser|Playwright|screenshot|MCP returns exit 1, zero hits; verify.md:129 dispatches Agent 2 and :146 tells it to read {base}/TECH_PLAN.md). The finding st"
  },
  {
   "spec_location": "\u00a74.6 \"Focused re-verification\" (line 256); \u00a76 invocation-path table (lines 316-320)",
   "reasoning": "The premises check out \u2014 line 256 does cite only `verify.md:50` and `main.go:7184`, \u00a76's table does list only Auto/Interactive/Plugin, `grep -i reverify` over the proposal returns only focused-re-verification hits, and the prompt at cmd/belmont/main.go:10900-10901 is quoted accurately. But the asserted consequence is false on two independent code c"
  }
 ],
 "lenses": [
  "facts:8",
  "consistency:14",
  "gating-logic:11",
  "executability:15",
  "design-soundness:10",
  "completeness:11"
 ]
}