{
 "survived_blockers": [
  {
   "idx": 0,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "blocker",
   "reasoning": "QUOTE CHECK \u2014 accurate, verbatim. Spec :293-295 reads exactly \"**Step 2 \u2014 the contract survives `[r]`-resume.** `belmont auto --feature <slug> --from M1 --to M1`, interrupt mid-run, resume with `[r]`. / Expect `{base}/TECH_PLAN.md` in the worktree still carries the identical `## Design Contract` after resume. / This is the step that proves \u00a74.1.\" and :297-298 reads \"...and `git diff` shows **no** modification to `{base}/TECH_PLAN.md` from any auto phase.\" No misquote, no ellipsis trickery.\n\nWHOLE-SPEC SEARCH \u2014 I grepped for `depends`/`parallel`/`worktree`/`serial` across all 413 lines. Nothing anywhere adds a `(depends: \u2026)` precondition to the smoke test; the only Preconditions block (:279) covers requireCleanWorkingTree and validate. Nothing rescues Step 3's `git diff` either \u2014 and :72 in the spec's OWN mechanics table already states \"Tracked `.belmont/` files are `--assume-unchanged` in a worktree | `main.go:10104-10122`\", so this is an internal contradiction, not an omission the spec elsewhere handles.\n\nCODE VERIFICATION (line numbers checked, not trusted) \u2014 main.go:5441 `hasExplicitDeps := false`, :5444 set only when `len(m.Deps) > 0` over `inRange`, :5449-5450 `if hasExplicitDeps { return runAutoParallel(cfg, inRange) }`, :5453 `return runLoop(cfg)`. Cited lines are exact. `milestonesInRange` (:6634) with from=to=M1 returns exactly [M1]. The shipped progress template (`_partials/progress-template.md:12-18`) gives M1 no `(depends: \u2026)` and starts deps at M2, so `--from M1 --to M1` reliably yields hasExplicitDeps=false \u2192 `runLoop`. `runLoop` (:6325) is a master-tree serial loop; it creates no worktree and no branch. `grep '\\[r\\]' cmd/belmont/*.go` returns exactly ONE hit \u2014 main.go:6100 inside `handleStaleWorktree` (:6080) \u2014 whose only callers are :5879/:5926 (multi-feature) and :8876/:8926 (runWaveParallel). So on the command as written there is no worktree to inspect and the `[r]` prompt is unreachable. The step is literally unrunnable, and it is the step the spec designates as the proof of \u00a74.1 (:295) and the one DoD :328 cites.\n\nSTEP 3 \u2014 TESTED, not reasoned. Built a scratch repo at scratchpad/au-test: init, commit `.belmont/features/feat/TECH_PLAN.md`, `git worktree add`, applied the same two mechanisms the installer applies (`git update-index --assume-unchanged` per `git ls-files .belmont/`, plus `.belmont/` in `.git/info/exclude`), then completely rewrote the file. Result: `git diff`, `git diff --name-only`, `git diff -- .belmont/` and `git status --porcelain` ALL returned empty while `cat` showed \"COMPLETELY REWRITTEN BY AGENT\". Step 3's assertion passes unconditionally. Worse, it is not merely blind: `syncFeatureStateAfterMerge` (:10160) does `os.RemoveAll(dstFeature)` then `copyDir(srcFeature, dstFeature)`, so a worktree-authored rewrite is copied back OVER master after merge \u2014 the exact loss the risk table :369 claims \"fails loudly at review time\". And in the serial regime the command actually produces, `git diff` is equally vacuous because the agent commits its state, so an unstaged-only diff shows nothing there either. The check cannot fail in either regime.\n\nNOT an acknowledged limitation: the spec acknowledges R4 being prose-enforced and two out-of-scope bugs; it nowhere acknowledges that its own durability proof is unrunnable. Severity confirmed at blocker \u2014 \u00a74.1 placement/durability is the whole thesis of rev 5, and both of its verification steps are inert.",
   "corrected_statement": "Smoke Steps 2 and 3 are both inert. Step 2 (`belmont auto --feature <slug> --from M1 --to M1`) cannot engage the worktree path: `inRange` is [M1], M1 carries no `(depends: \u2026)` in the shipped template, so `hasExplicitDeps` is false (main.go:5441-5447) and `runAuto` falls through to `runLoop` (:5453) \u2014 a master-tree serial loop that creates no worktree or branch. The `[r]` prompt is the single occurrence at main.go:6100 inside `handleStaleWorktree`, reachable only from `runWaveParallel` and multi-feature mode, so \"interrupt mid-run, resume with `[r]`\" and \"in the worktree\" are both unperformable. Step 3's `git diff` assertion passes unconditionally: measured in a scratch repo, with `.belmont/` files marked `--assume-unchanged` plus `.belmont/` in `.git/info/exclude` \u2014 the two mechanisms the spec itself documents at :72 \u2014 `git diff`, `git diff --name-only`, `git diff -- .belmont/` and `git status --porcelain` were all empty after the file was fully rewritten. In the serial regime the command actually produces, an unstaged `git diff` is equally blind because agent state edits are committed. The real loss point is `syncFeatureStateAfterMerge` (main.go:10160), which `RemoveAll`s the master feature dir and copies the worktree's version back over it.",
   "fix_to_apply": "\u00a78 Author smoke test \u2014 replace Steps 2 and 3.\n\nStep 2 (:293-295) becomes:\n\"**Step 2 \u2014 the contract survives `[r]`-resume.** *Precondition: the worktree path engages only when some milestone in range carries `(depends: \u2026)`; otherwise `main.go:5441-5453` routes to `runLoop`, which runs in the master tree with no worktree and no `[r]` prompt. Confirm `{base}/PROGRESS.md` has `### M2: \u2026 (depends: M1)` before running.* `belmont auto --feature <slug> --from M1 --to M2`; Ctrl-C during wave 1 (the tracker preserves the worktree); re-run the same command and answer `r` at the `Branch '<branch>' exists from a previous run` prompt (`main.go:6100`).\nExpect the worktree copy at `<worktree>/.belmont/features/<slug>/TECH_PLAN.md` still carries the identical `## Design Contract` after resume \u2014 assert with `shasum` against the master copy, not by eye.\nThis is the step that proves \u00a74.1. Fail here and the placement is wrong again.\n\n**Step 2b \u2014 serial regime (the default).** With no `(depends: \u2026)` anywhere, `belmont auto --feature <slug> --from M1 --to M1` runs serially in the master tree; there is no `[r]` prompt on this path. Interrupt and re-run; expect `{base}/TECH_PLAN.md` byte-identical to the Step 1 commit.\"\n\nStep 3 (:297-298) becomes:\n\"**Step 3 \u2014 design-agent consumes, never writes.** Same run. Capture `shasum {base}/TECH_PLAN.md` in the master tree right after Step 1's commit, and re-check it *after the wave merges* \u2014 `syncFeatureStateAfterMerge` (`main.go:10160`) `RemoveAll`s the master feature dir and copies the worktree's version back over it, so that is where a worktree-authored edit actually lands.\nExpect MILESTONE `## Design Specifications` carries per-task `### Design:` sections referencing the contract, and the shasum is unchanged.\nDo **not** use `git diff` for this: `.belmont/` is `--assume-unchanged` in a worktree (\u00a74.1, `main.go:10100-10122`), so `git diff`, `git diff -- .belmont/` and `git status --porcelain` all report nothing even after the file has been completely rewritten (measured).\"\n\nAdjunct in the same edit \u2014 risk table :369 currently claims `--assume-unchanged` means such a write \"fails loudly at review time rather than shipping\". Change to: \"`--assume-unchanged` makes such a write invisible to git, so it fails *silently* and is propagated to master by `syncFeatureStateAfterMerge`; the shasum check in smoke Step 3 is what catches it.\""
  }
 ],
 "survived_majors": [
  {
   "idx": 1,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 the finding quotes the spec accurately. Line 64: \"**Content authored in the master tree survives, because master is the copy source. Content authored inside a worktree does not.**\" Line 66: \"A worktree-local edit to that same path is destroyed exactly like MILESTONE.\" Line 74 (\u00a74.1 table, row 3) names syncFeatureStateAfterMerge only as \"has a second `os.RemoveAll(dstFeature)`\" / \"Any mechanical durability change must patch **both** sites\" \u2014 it never states the function's DIRECTION. Line 369 (\u00a710 row 1) reads: \"Contract written from a worktree and silently lost | Hard rule in \u00a74.1; smoke Steps 2 and 3; `--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping.\" No misquote, no strawman.\n\nWHOLE-SPEC SEARCH for the write-back direction: the only other places touching this are line 38 (\"survives worktree creation, `[r]`-resume and merge \u2014 see \u00a74.1\") and line 173, which repeats the SAME wrong model (\"anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff\"). Nowhere does the spec model worktree\u2192master write-back. The \"missing\" claim survives the search.\n\nCODE VERIFIED INDEPENDENTLY (not trusting the snippet): cmd/belmont/main.go:10160-10173 \u2014 `syncFeatureStateAfterMerge(mainRoot, wtPath, slug)` does `os.RemoveAll(dstFeature)` at :10169 then `copyDir(srcFeature, dstFeature)` at :10170. `copyDir` at :4748-4769 is unconditional recursion with no path filter. Callers confirmed at :6297 (mergeFeatureBranch, comment at :6295 \"`.belmont/` was excluded from the merge (assume-unchanged), so we sync it separately\") and :9157 (mergeWorktreeBranch), both immediately after a successful `attemptMerge`. Worktree-scoped loop confirmed: `mCfg.Root = wtPath` at :9105 \u2192 `runLoop(mCfg)` at :9123; `cmd.Dir = cfg.Root` at :6897; `case actionReplan: return fmt.Sprintf(\"/belmont:tech-plan --feature %s\", feature)` at :7188-7189; actionReplan is in the AI-decision allowlist at :7887 and reachable from the \u22652-verify-failure branch at :7642-7644.\n\nRAN TWO TESTS. (1) Temporary Go test in package main calling the real syncFeatureStateAfterMerge \u2014 master's `**Approval**: approved 2026-08-06` was replaced by `**Approval**: unreviewed (headless replan)`, and a master-only `design-preview.html` absent from the worktree was deleted by the RemoveAll. Test file removed; `git status --short` clean. (2) Real git 2.50.1 worktree repro in the scratch dir: after `git update-index --assume-unchanged` on `.belmont/features/demo/TECH_PLAN.md`, editing it in the worktree produced EMPTY `git status --short` and EMPTY `git diff --stat` there, and the file was absent from the worktree commit \u2014 but after merge + the sync copy, master's file held the worktree text and master's `git status --short` showed ` M .belmont/features/demo/TECH_PLAN.md`, i.e. an ordinary committable modification (assume-unchanged is per-worktree-index and does not reach master's index).\n\nSO: the spec's unqualified rule is wrong in the merge direction, and \u00a710 row 1's mitigation clause is false end-to-end \u2014 the write is not \"uncommittable\", it becomes committable in master after write-back, and the approved contract is destroyed on disk before any review. A further consequence the finding did not name: smoke Step 3 (line 298) asserts \"`git diff` shows **no** modification to `{base}/TECH_PLAN.md` from any auto phase\" without naming a tree \u2014 run in the worktree (the obvious reading during an auto wave) assume-unchanged guarantees a false pass, so one of the two smoke steps \u00a710 row 1 cites cannot detect the failure it is cited for.\n\nNOT AN ACKNOWLEDGED LIMITATION. The spec acknowledges (a) replan-in-worktree as \"the live threat\" (line 82) and (b) that R4 is prose-enforced because runScopeGuard exempts actionReplan (line 93, \u00a710 row 2). Neither acknowledges that the worktree write PROPAGATES TO MASTER rather than being lost \u2014 the spec asserts the opposite.\n\nMISGRADE. Downgrading blocker \u2192 major: the architecture is untouched by the correction. R4 (preserve-verbatim) is already the right and only mitigation, is already mandatory in \u00a74.2, has its own DoD item and smoke Step 6, and \u00a710 row 2 already states it has no mechanical backstop. What is wrong is a false general sentence, a false mitigation clause, an unlocated smoke assertion and one stale sentence in \u00a74.5 \u2014 all prose-local. Two overstatements in the finding are trimmed in corrected_statement: Step 6 does not \"only inspect the worktree\" (it names no location at all), and the finding's fix option (b) \u2014 a Go change excluding TECH_PLAN.md/design-preview.html from the write-back \u2014 must be rejected, as it contradicts the \"prose only. No Go changes\" header at line 5 and the \u00a75 out-of-scope line 253 (\"any Go change\").",
   "corrected_statement": "\u00a74.1 states the durability rule in only one direction. It is correct that master-authored content survives `[r]`-resume (copyBelmontStateToWorktree wipes and re-copies from master), but the unqualified companion claims \u2014 line 64 \"Content authored inside a worktree does not [survive]\" and line 66 \"A worktree-local edit to that same path is destroyed exactly like MILESTONE\" \u2014 are false for the merge path. `syncFeatureStateAfterMerge` (main.go:10160) runs after every successful merge (:6297, :9157), does `os.RemoveAll` on MASTER's feature dir (:10169) and `copyDir`s the WORKTREE's over it (:10170) with no path filter, so a worktree-authored `{base}/TECH_PLAN.md` is not lost \u2014 it overwrites master's approved `## Design Contract` and lands in master's working tree as an ordinary committable modification (assume-unchanged is per-worktree-index and never reaches master's index). Confirmed by running the real function and by a git 2.50.1 worktree+merge repro. Consequently \u00a710 row 1's clause \"`--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping\" is false, and smoke Step 3's \"`git diff` shows no modification\" is a false pass if run in the worktree, where assume-unchanged suppresses the diff entirely. \u00a74.5:173 (\"an auto-authored preview would never reach a diff\") repeats the same wrong model. R4 remains the correct and sole mitigation; no Go change should be adopted, since \u00a71's header and \u00a75's out-of-scope list both bar one.",
   "fix_to_apply": "Four prose edits, no design change.\n\n1. \u00a74.1, replace the sentence at line 66 (\"A worktree-local edit to that same path is destroyed exactly like MILESTONE.\") with: \"That holds for the resume direction only. The opposite direction also exists: after every successful merge, `syncFeatureStateAfterMerge` (`main.go:10160`, called at `:6297` and `:9157`) does `os.RemoveAll` on **master's** feature dir (`:10169`) and `copyDir`s the **worktree's** over it (`:10170`) \u2014 unconditionally, no path filter. A worktree-authored `{base}/TECH_PLAN.md` is therefore not lost: it overwrites master's approved contract at merge and appears in master's working tree as an ordinary uncommitted modification, because `--assume-unchanged` lives in the worktree's index and never reaches master's. Verified on git 2.50.1: the worktree write is absent from `git status`, `git diff` and the worktree commit, and present in master after the sync.\" Then restate the bolded rule as two-directional: \"> **Master-authored content survives resume. Worktree-authored content is invisible to git inside the worktree \u2014 and wins in master after merge.** Neither direction protects the contract; only R4 does.\"\n\n2. \u00a74.1 table, row 3 \u2014 replace the Consequence cell with: \"The write-back direction: master's feature dir is replaced wholesale by the worktree's after every successful merge, so a worktree-authored contract reaches master. Any mechanical durability change must patch **both** sites.\"\n\n3. \u00a710, replace row 1 entirely with: \"| Contract regenerated inside a worktree overwrites master's approved copy at merge (`syncFeatureStateAfterMerge`, `main.go:10169-10170`) | R4 preserve-verbatim; hard rule in \u00a74.1; smoke Step 6's post-merge master diff. **No mechanical backstop** \u2014 the write is invisible to git inside the worktree and is copied over master unconditionally at merge |\". Delete the clause \"`--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping\".\n\n4. \u00a78 smoke: in Step 3 (line 298) change \"`git diff` shows **no** modification\" to \"`git diff` **run in the master tree after the wave merges** shows no modification to `{base}/TECH_PLAN.md` \u2014 running it inside the worktree is a false pass, since `--assume-unchanged` suppresses the diff there\". In Step 6 (lines 307-309) append: \"Then let the wave merge and re-check `{base}/TECH_PLAN.md` **in the master tree**: the `**Approval**: approved <date>` line must still be there. This is the step that catches the merge write-back.\" Also correct \u00a74.5:173: \"an auto-authored preview would never reach a diff\" \u2192 \"a worktree-authored preview is invisible to git inside the worktree and reaches master only via the merge write-back \u2014 arriving as a file nobody approved\"."
  },
  {
   "idx": 2,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 both quotes are accurate. Spec line 173 (\u00a74.5): \"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\" Spec line 369 (\u00a710 row 1): \"\u2026`--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping\". No misquote, no ellipsis, no strawman.\n\nCODE CHECK \u2014 main.go:10104 `func untrackBelmontInWorktree(wtPath, slug string) {` and :10106 `lsCmd := exec.Command(\"git\", \"ls-files\", \".belmont/\")` \u2192 it iterates only files already in the index and applies `git update-index --assume-unchanged` (:10119). Untracked files are outside its reach, exactly as the finding says. main.go:10057 `writeWorktreeGitExcludes` resolves the worktree's `gitdir:` pointer and appends `.belmont/` to `<gitdir>/info/exclude` \u2014 i.e. `.git/worktrees/<name>/info/exclude`.\n\nRAN IT (real repo, git 2.50.1 Apple Git-155, scratchpad/wt-test). Replicated both guards verbatim, then created a new `.belmont/features/demo/design-preview.html` inside the worktree, edited the tracked `.belmont/features/demo/TECH_PLAN.md`, and edited `code.txt`:\n  `git status --porcelain` \u2192 ` M code.txt` / `?? .belmont/features/demo/design-preview.html`\n  `git check-ignore -v .belmont/features/demo/design-preview.html` \u2192 NOT ignored\n  `git add -A; git commit` \u2192 `A .belmont/features/demo/design-preview.html`, `M code.txt`\n  `git merge --no-ff feat` into main \u2192 `git ls-files .belmont/` on main now lists `design-preview.html`.\nControl: appending the same `.belmont/` line to the COMMON dir `.git/info/exclude` DID ignore a second probe file. So the pattern is fine and the location is the bug \u2014 git reads `$GIT_COMMON_DIR/info/exclude`, never the per-worktree one. Finding's mechanism claim is exactly right.\n\nREACHABILITY \u2014 this is not hypothetical, and this PR creates the path. R7 (line 96) adds `{base}/design-preview.html` to tech-plan's ALLOWED ACTIONS with no headless carve-out; \u00a74.1 line 82 itself says \"Replan is the live threat. `actionReplan` emits `/belmont:tech-plan --feature <slug>` headlessly mid-run\", and main.go:7188-7189 confirms it. R4 (line 93) constrains only the `## Design Contract`, saying nothing about the preview. Worse than the finding claims: the staging is done by Belmont itself, not the agent \u2014 `commitWorktreeChanges` runs `git add -A` at main.go:10192 and is called from `mergeFeatureBranch` (main.go:6276) at :6279, immediately before the merge. So a feature whose preview did not already exist on the branch (backend-only feature that gains UI, R1 `N/A` flipped, or any feature planned before this PR) gets an unreviewed machine-authored HTML file committed and merged to main \u2014 the precise outcome \u00a74.5 says \"would never\" happen.\n\nNOT AN ACKNOWLEDGED LIMITATION. \u00a75 Out of scope (line 253) does name \"`writeWorktreeGitExcludes` writing to the wrong gitdir\" as an out-of-scope `main` bug. But acknowledging the bug exists while \u00a74.5 asserts a safety property that only holds if the bug does NOT exist is an internal contradiction, not a re-report. \u00a74.5 is in the same document that bills \u00a74.1 as \"the exact durability claim\u2026 now an evidence question rather than an assertion\".\n\n\u00a710 HALF \u2014 separately true. For the tracked `{base}/TECH_PLAN.md` the \"uncommittable\" clause is correct (my run: the worktree edit produced zero `git status --porcelain` output and never reached the commit). But that is the definition of silent, and the risk row it mitigates is literally titled \"Contract written from a worktree and **silently lost**\". \"Fails loudly at review time\" is contradicted by the mechanism it cites; \u00a74.1's own table row (line 72) states it correctly and claims no loudness.\n\nGRADE \u2014 real defect, but blocker overstates it. The design decision (preview authored in the master tree by tech-plan) stays correct; what is wrong is the justification and the absent enforcement. Unfixed fallout is an unreviewed generated HTML file in the diff and on main \u2014 hygiene and a false mechanism claim, not data loss and not a redesign. Fix is wording plus one R4 clause, one risk row and one smoke/DoD assertion. Downgraded blocker \u2192 major.",
   "corrected_statement": "\u00a74.5's \"anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`)\" is false for the exact file it is about. `untrackBelmontInWorktree` (main.go:10104-10122) only walks `git ls-files .belmont/`, so it covers tracked files and nothing else; a NEWLY CREATED `.belmont/` file in a worktree is untracked, is not ignored (the companion `writeWorktreeGitExcludes` writes `.belmont/` to the per-worktree `<gitdir>/info/exclude`, which git never consults \u2014 reproduced on git 2.50.1; the common-dir `.git/info/exclude` does work), is staged by Belmont's own `commitWorktreeChanges` `git add -A` (main.go:10192, called from `mergeFeatureBranch` main.go:6279), is committed, and lands on `main` through the merge. Since R7 grants tech-plan write access to `{base}/design-preview.html` and `actionReplan` invokes tech-plan headlessly inside a worktree (main.go:7188-7189, acknowledged at \u00a74.1 line 82), an auto-authored preview DOES reach the diff and `main` whenever the file did not already exist on the branch. Separately, \u00a710 row 1's \"fails loudly at review time\" is wrong in the opposite direction: for the tracked `{base}/TECH_PLAN.md`, assume-unchanged suppresses the edit with zero `git status` output \u2014 the loss is maximally silent, which is what the row's own title (\"silently lost\") says. The spec names the gitdir bug as out-of-scope in \u00a75 but then leans on the guard being effective in \u00a74.5.",
   "fix_to_apply": "1. \u00a74.5, line 173 \u2014 replace the sentence \"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\" with the measured behaviour, e.g.: \"It is **not** written during an auto wave \u2014 and nothing mechanical enforces that. `untrackBelmontInWorktree` (`main.go:10104-10122`) marks only files already in the index (`git ls-files .belmont/`), so an *existing* `.belmont/` file is assume-unchanged and cannot be committed from a worktree; a **newly created** one is untracked, is staged by `commitWorktreeChanges`'s `git add -A` (`main.go:10192`, called from `mergeFeatureBranch` at `:6279`), and merges into `main`. `writeWorktreeGitExcludes` does not stop it \u2014 it writes `.belmont/` to the *per-worktree* gitdir's `info/exclude`, which git never reads (git reads `$GIT_COMMON_DIR/info/exclude`; reproduced on git 2.50.1). That is the out-of-scope `main` bug named in \u00a75. 'Never auto-authored' is therefore a **prose rule (R4), not a guarantee**.\"\n2. \u00a74.2 R4, line 93 \u2014 extend the rule list with: \"and never write or regenerate `{base}/design-preview.html` \u2014 a headless replan has no reviewer, and unlike the contract a new preview file *is* committable from a worktree and would merge into `main` unreviewed.\"\n3. \u00a710 row 1, line 369 \u2014 replace the mitigation \"`--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping\" with: \"`--assume-unchanged` makes a worktree write to the *tracked* `{base}/TECH_PLAN.md` uncommittable \u2014 silently, not loudly: it produces no `git status` output and is overwritten by the next state copy. Detection is smoke Step 3's positive assertion, not a loud failure.\"\n4. \u00a710 \u2014 add a new risk row: \"| Auto-authored `design-preview.html` merged into `main` | New `.belmont/` files created in a worktree ARE committed (`git add -A`, `main.go:10192`); the per-worktree `info/exclude` guard is inert. Prose-enforced by R4 plus the smoke/DoD `git log --name-status -- .belmont/` assertion |\"\n5. \u00a78 smoke Step 3, line 298 \u2014 add to the expectation: \"and `git log --name-status $BASE..HEAD -- .belmont/` shows no `A` (added) entries \u2014 no auto-created `.belmont/` file, preview or otherwise, reached the merge.\"\n6. \u00a79 DoD, \"Placement and durability\" block \u2014 add: \"- [ ] No auto-run creates a new `.belmont/` file: `git log --name-status $BASE..HEAD -- .belmont/` has no `A` entries (Step 3)\"."
  },
  {
   "idx": 3,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 the spec really says it. `docs/proposals/0003-design-quality-without-figma.md:173`: \"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\" No ellipsis, no strawman.\n\nCITATION CHECK \u2014 the finding's line numbers are all correct. `main.go:10104` is `func untrackBelmontInWorktree(wtPath, slug string) {`, body runs to `}` at `:10123`; `:10106` is `lsCmd := exec.Command(\"git\", \"ls-files\", \".belmont/\")` \u2014 i.e. it enumerates files ALREADY IN THE INDEX and marks each `--assume-unchanged`. It cannot cover a file that is not yet tracked. `writeWorktreeGitExcludes` is `main.go:10057`\u2013`:10098` (its own comment at `:10055` says \"This prevents new `.belmont/` files from being tracked\"), and \u00a74.5 never cites it. So the cited mechanism does not support the word \"anything\".\n\nBEHAVIOUR CHECK \u2014 tested, not reasoned. Scratch repo, git 2.50.1, replicating both functions exactly (per-worktree `$GIT_DIR/info/exclude` containing `.belmont/`, plus assume-unchanged on every tracked `.belmont/` file), then creating a NEW file in the worktree:\n  `git check-ignore -v .belmont/features/foo/design-preview.html` \u2192 (NOT ignored)\n  `git status --porcelain` \u2192 `?? .belmont/features/foo/design-preview.html`\n  after `git add -A && git commit` (i.e. `commitWorktreeChanges`, `main.go:10179-10207`, whose `git add -A` is at `:10192`): `de2ab38 belmont: finalize M1 / .belmont/features/foo/design-preview.html | 1 +`\nThe same run confirms the *tracked*-file half IS true: a worktree edit to `.belmont/features/foo/TECH_PLAN.md` did not enter the commit. So `main.go:10104-10122` supports \u00a74.1's table row at `:72` (\"Tracked `.belmont/` files are `--assume-unchanged`\") but does NOT support \u00a74.5's blanket \"anything written under `.belmont/`\".\n\nWHOLE-SPEC CHECK \u2014 \u00a75 line 253 does list \"`writeWorktreeGitExcludes` writing to the wrong gitdir\" as an out-of-scope `main` bug. That does not rescue \u00a74.5; it convicts it. \u00a75 says the mechanism is broken while \u00a74.5 asserts a safety property that only holds if it works, and the two are never connected. This is an internal contradiction, not a re-report of an acknowledged limitation.\n\nNot a taste issue and not handled elsewhere: no other section states the new-file case. Grading kept at major \u2014 this is a factually false git-behaviour claim inside the document whose \u00a74.1 exists specifically to state such behaviour \"precisely\", and \u00a712 records that four prior documents already shipped on a mis-cited durability line (`:9980` vs `:10010`).",
   "corrected_statement": "\u00a74.5 (line 173) makes a false and mis-cited behavioural claim. `main.go:10104-10122` is `untrackBelmontInWorktree`, which only marks files already present in the worktree index as `--assume-unchanged`; it cannot make a *new* `.belmont/` file invisible. The function intended to cover new files, `writeWorktreeGitExcludes` (`main.go:10057-10098`, never cited in \u00a74.5), writes to the per-worktree `$GIT_DIR/info/exclude`, which git does not read (git resolves `info/exclude` against `$GIT_COMMON_DIR`). Verified on git 2.50.1: a new `.belmont/features/<slug>/design-preview.html` created inside a worktree is not ignored, is reported by `git status`, and is committed by `commitWorktreeChanges`'s `git add -A` (`main.go:10192`). The sentence's tracked-file half is true and is already correctly cited in \u00a74.1's table (`:72`); the blanket \"anything written under `.belmont/`\" is not. \u00a75 (`:253`) separately lists this exact `writeWorktreeGitExcludes` bug as out of scope, so the document contradicts itself.",
   "fix_to_apply": "In \u00a74.5, replace the second sentence of line 173 (from \"It is **not** written during an auto wave\" to \"would never reach a diff.\") with:\n\n\"It is **not** written during an auto wave \u2014 that is the \u00a74.1 rule (tech-plan owns the path), not a property git enforces. `untrackBelmontInWorktree` (`main.go:10104-10123`) only marks `.belmont/` files **already in the worktree index** as `--assume-unchanged`, so a worktree edit to an *existing* preview cannot be committed; a *new* `.belmont/` file is not covered by it. The function that should cover new files, `writeWorktreeGitExcludes` (`main.go:10057-10098`), writes to the per-worktree `$GIT_DIR/info/exclude`, which git ignores \u2014 git resolves `info/exclude` against `$GIT_COMMON_DIR` \u2014 so a new `.belmont/` file created in a worktree IS staged by `commitWorktreeChanges`'s `git add -A` (`main.go:10192`) and lands on the feature branch (verified, git 2.50.1). That is the out-of-scope `main` bug named in \u00a75; this PR's placement rule does not depend on it being fixed.\"\n\nAlso add a back-reference in \u00a75's out-of-scope line (`:253`) after \"`writeWorktreeGitExcludes` writing to the wrong gitdir\": \"(see \u00a74.5 \u2014 the preview's durability argument is the \u00a74.1 authoring rule, not this broken exclude)\"."
  },
  {
   "idx": 4,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTE CHECK \u2014 accurate. `0003-design-quality-without-figma.md:298`: \"Expect MILESTONE `## Design Specifications` carries per-task `### Design:` sections referencing the contract, and `git diff` shows **no** modification to `{base}/TECH_PLAN.md` from any auto phase.\" Step 3 is headed \"Same run\" (`:297`), i.e. it is checked after the `belmont auto` invocation from Step 2.\n\nCODE CHECK \u2014 every cited call verified. `syncFeatureStateAfterMerge` is `main.go:10160`; it does `os.RemoveAll(dstFeature)` at `:10169` then `copyDir(srcFeature, dstFeature)` \u2014 an unconditional worktree\u2192master copy of the feature dir, called at `main.go:9157` (single-feature milestone merge) and `:6297` (multi-feature merge). `commitBelmontState` is `main.go:10211`, does `git add .belmont/` (`:10230`) + `git commit` (`:10235`), and is called at the tail of `runAutoParallel` (`main.go:8650`) and after all feature merges (`main.go:6020`).\n\nEND-TO-END TEST (git 2.50.1, scratch repo, full sequence replicated: worktree + exclude + assume-unchanged \u2192 agent rewrites `.belmont/features/foo/TECH_PLAN.md` in the worktree \u2192 `git add -A && git commit` \u2192 merge \u2192 sync \u2192 `git add .belmont/ && git commit`):\n  worktree commit stat: `app.js | 1 +` only \u2014 the rogue TECH_PLAN edit was indeed NOT committed from the worktree;\n  after sync, before `commitBelmontState`: `git diff --stat` \u2192 `.belmont/features/foo/TECH_PLAN.md | 2 +-`;\n  after `commitBelmontState`: `git diff --stat` \u2192 EMPTY, `git status --porcelain` \u2192 EMPTY;\n  master file content afterwards: `CONTRACT v1 PLUS ROGUE AUTO EDIT`;\n  `git diff $PLAN..HEAD -- .belmont/features/foo/TECH_PLAN.md` \u2192 surfaces the change.\nSo the rogue auto-phase write reaches and ships on main, and `git diff` at the point Step 3 runs is clean \u2014 pass and fail are indistinguishable. The step is a no-op detector for the PR's central invariant, exactly as claimed.\n\nWHOLE-SPEC CHECK \u2014 nothing elsewhere covers it. DoD `:326` states the invariant with no mechanism. \u00a710's first risk row (`:369`) offers \"`--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping\" \u2014 the test shows the write is uncommittable *from the worktree* but still ships via `syncFeatureStateAfterMerge` + `commitBelmontState`, so that row does not supply the missing detection either.\n\nONE CORRECTION TO THE FINDING'S FIX: its suggested `git diff $BASE..HEAD` is wrong. `$BASE` is captured at `:285` BEFORE Step 1, so that range includes tech-plan's own contract-authoring commit and would fail on a correct run. The baseline must be captured after Step 1. Corrected below. Severity stays major \u2014 \u00a712 (`:406`) records this same class of defect shipping in v4 (\"Smoke Step 3 expects contract checks after a fix round | Unsatisfiable\").",
   "corrected_statement": "Smoke Step 3's `git diff` assertion (line 298) cannot detect the failure it exists to detect, and the DoD item at line 326 names no mechanism at all. A rogue auto-phase edit to the worktree's `{base}/TECH_PLAN.md` is not committed from the worktree (`--assume-unchanged`), but `syncFeatureStateAfterMerge` (`main.go:10160`, `os.RemoveAll` + `copyDir` at `:10169`, called from `:9157` and `:6297`) copies the worktree's feature dir over master's, and `commitBelmontState` (`main.go:10211`, called at `:8650` and `:6020`) then commits it as \"belmont: update state files\". Verified end-to-end on git 2.50.1: after that sequence `git diff` and `git status --porcelain` are both empty while master's TECH_PLAN.md carries the rogue edit \u2014 so Step 3 passes on exactly the run it is meant to fail. The finding's own proposed `git diff $BASE..HEAD` is also wrong: `$BASE` (`:285`) predates Step 1, so it would include tech-plan's legitimate contract-authoring commit and fail a correct run.",
   "fix_to_apply": "1. \u00a78 preconditions block: after Step 1 add a baseline capture. Append to Step 1 (`:290`) a final line: \"Then record the post-approval baseline: `PLAN=$(git rev-parse HEAD)`.\"\n2. \u00a78 Step 3 (`:298`): replace \"and `git diff` shows **no** modification to `{base}/TECH_PLAN.md` from any auto phase\" with: \"and `git diff $PLAN..HEAD -- .belmont/features/<slug>/TECH_PLAN.md` is **empty** \u2014 a plain `git diff` cannot detect this, because a worktree edit round-trips to master via `syncFeatureStateAfterMerge` (`main.go:10160`) and is then committed by `commitBelmontState` (`main.go:10211`), leaving the working tree clean. Additionally assert the `## Design Contract` block is byte-identical to the version approved in Step 1 (`git show $PLAN:.belmont/features/<slug>/TECH_PLAN.md | diff - .belmont/features/<slug>/TECH_PLAN.md`).\"\n3. \u00a79 DoD (`:326`): change to \"Contract written to `{base}/TECH_PLAN.md` by `tech-plan` only; no auto-mode phase writes that path \u2014 asserted by `git diff $PLAN..HEAD -- {base}/TECH_PLAN.md` being empty in smoke Step 3, not by a working-tree `git diff`\".\n4. \u00a710 risk row 1 (`:369`): the clause \"`--assume-unchanged` makes such a write uncommittable, so it fails loudly at review time rather than shipping\" is over-stated for the same reason \u2014 qualify it to \"uncommittable *from the worktree*, but `syncFeatureStateAfterMerge` still copies it back to master where `commitBelmontState` commits it; detection is the Step 3 range diff, not assume-unchanged\"."
  },
  {
   "idx": 9,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quotes verified verbatim. :105 reads \"`ui-designer` \u2192 Token Contract \u00b7 `ux-designer` \u2192 UX Strategy and state inventory \u00b7 `ux-copywriter` \u2192 Microcopy Rules \u00b7 `ux-motion` \u2192 Motion Contract \u00b7 `interactive-prototype` \u2192 interaction and gesture states.\" The \u00a74.4 fence (:128-163) contains exactly five headings \u2014 :135 `### Token Contract`, :143 `### Accessibility Floor`, :148 `### UX Strategy`, :151 `### Microcopy Rules`, :156 `### Motion Contract`. :149 is \"User \u00b7 emotional state on arrival \u00b7 hero element \u00b7 primary action \u00b7 biggest UX risk. Five lines.\" \u2014 no enumeration. I grepped all 413 lines for \"state\": the only other hits are :47 (credit table, \"all-states enumeration\"), :153 (\"Empty states\" inside Microcopy), :157 (\"state change 150\u2013200ms\" \u2014 a duration band, not an inventory), :171, :200, :203, :211. So the \"missing\" claim survives a whole-file search. Three downstream consumers do depend on it: :171 \"a state row per enumerated component state\" in the preview, :200 \"| Enumerated states present | exercise each state, screenshot |\" under the \u00a74.6 contract-checks table, :211 \"missing state ... \u2192 Warning\". An independent corroboration the finding did not spot: \u00a75:238 describes the new baseline reference as covering \"all five contract sections (tokens, strategy, microcopy, motion, interaction)\" \u2014 naming an *interaction* section the schema lacks while omitting Accessibility Floor, so the spec contradicts itself in a second place. DoD :334 (\"all five tier-1 skills named and mapped to the contract section each feeds\") is therefore unsatisfiable for `interactive-prototype` as the schema stands. Two grading corrections to the finding: (a) `ux-designer` IS mapped to a real section (UX Strategy) plus a phantom one, so \"two of five mapped to sections that do not exist\" overstates \u2014 only `interactive-prototype` has no real section at all; (b) \"unimplementable\" is slightly strong for verification row :200 in isolation, since design-agent.md:201 already emits a per-task `**States**: Default, Hover, Active, Disabled, Focus` line in MILESTONE that a verifier could fall back on \u2014 but the \u00a74.5 preview genuinely has no source, because tech-plan writes it before any MILESTONE per-task spec exists. Net: a real internal-consistency defect, correctly graded major, with the specifics tightened.",
   "corrected_statement": "\u00a74.4's contract schema (:128-163) has no state/interaction section, but four other places in the spec assume one exists: \u00a74.3:105 maps `interactive-prototype` to \"interaction and gesture states\" (a section that does not exist) and `ux-designer` to \"UX Strategy **and state inventory**\" (half of which does not exist); \u00a75:238 calls the baseline's coverage \"all five contract sections (tokens, strategy, microcopy, motion, **interaction**)\", naming the missing section and dropping Accessibility Floor; \u00a74.5:171 requires the preview to render \"a state row per enumerated component state\"; \u00a74.6:200 lists \"Enumerated states present\" as a contract check and \u00a74.6:211 grades \"missing state\" as Warning. The preview requirement is the hard one \u2014 tech-plan writes design-preview.html before any MILESTONE per-task spec exists, so there is no other source it could draw states from. DoD :334 (\"all five tier-1 skills \u2026 mapped to the contract section each feeds\") cannot be satisfied for `interactive-prototype` until the schema gains the section.",
   "fix_to_apply": "Four edits. (1) \u00a74.4, inside the fenced schema, insert a new section between `### UX Strategy` (:148-149) and `### Microcopy Rules` (:151):\n\n### State Inventory\nPer interactive component: default \u00b7 hover \u00b7 focus \u00b7 active \u00b7 disabled \u00b7 loading \u00b7 empty \u00b7 error,\nplus any gesture or transition state the interaction introduces (drag, swipe, long-press, optimistic\nupdate). A component with no state beyond default says so explicitly \u2014 silence is not \"none\".\n\n(2) \u00a74.3:105 \u2014 rewrite the mapping line so every tier-1 skill names a real schema heading: \"`ui-designer` \u2192 Token Contract \u00b7 `ux-designer` \u2192 UX Strategy + State Inventory \u00b7 `ux-copywriter` \u2192 Microcopy Rules \u00b7 `ux-motion` \u2192 Motion Contract \u00b7 `interactive-prototype` \u2192 State Inventory (the gesture and transition rows)\". (3) \u00a75:238 \u2014 replace the parenthetical \"(tokens, strategy, microcopy, motion, interaction)\" with \"(tokens, accessibility floor, strategy, state inventory, microcopy, motion)\" and change \"all five contract sections\" to \"all six contract sections\"; also extend \u00a75:237's feature-template row to say the generalised `## Design Contract` carries all six sections. (4) \u00a79 DoD:336 \u2014 widen the existing item to \"`### State Inventory` and `### Motion Contract` present in the contract schema and the feature template, with their check rows in verification\". No change needed at :171, :200 or :211 once the section exists."
  },
  {
   "idx": 10,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quote verified verbatim at :173: \"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\" I checked all three sub-claims.\n\n(a) Nothing in the design produces that behaviour. R7 (:96) is unconditional: \"`tech-plan.md:96-105` enumerates the write surface exhaustively. Add `{base}/design-preview.html`.\" R4 (:93) governs headless replan and never mentions the preview. \u00a74.1's hard rule (:78) forbids auto-mode phases writing `{base}/TECH_PLAN.md` \u2014 it says nothing about the preview, and `actionReplan` *is* tech-plan (`buildLoopPrompt` :7188-7189 returns `/belmont:tech-plan --feature %s`, confirmed at main.go:7188-7189). I grepped every \"preview\" hit in the spec (:94, :96, :171, :173, :175, :236, :290, :317, :329, :377) \u2014 no other clause creates an auto-mode exception. R5 (:94) actually pushes the other way: it requires the preview to be enumerable as a `BELMONT_PLAN_PACKET` operation, and smoke Step 9a runs `belmont auto --tool codex`.\n\n(b) The rationale is false, and I tested it rather than reasoning about it. Built a scratch repo + linked worktree at git 2.50.1: `writeWorktreeGitExcludes` (main.go:10057-10099) resolves `gitdir:` from the worktree's `.git` file and appends `.belmont/` to `<gitdir>/info/exclude` \u2014 i.e. `main/.git/worktrees/wt/info/exclude`. Git reads info/exclude from the **common** dir, so a new `.belmont/features/foo/design-preview.html` came back `?? .belmont/features/foo/design-preview.html` from `git status --porcelain` and `git check-ignore -v` reported NOT IGNORED. Separately, the cited lines main.go:10104-10122 are `untrackBelmontInWorktree`, which iterates `git ls-files .belmont/` and marks only files **already in the index** `--assume-unchanged`; a brand-new file is not in the index. \u00a74.1's own table at :72 states this correctly (\"**Tracked** `.belmont/` files are `--assume-unchanged`\"), so :173's \"anything written\" contradicts the spec's own evidence table. Then `commitWorktreeChanges` runs `git add -A` at main.go:10192 immediately before merge (called at :6279 and :9141), and `syncFeatureStateAfterMerge` (main.go:10160-10173) does `os.RemoveAll(dstFeature)` at :10169 + `copyDir(srcFeature, dstFeature)` at :10170 with srcFeature = the worktree (:10161). `.gitignore` only carries `.belmont/worktrees/` and `.belmont/auto.json` (ensureGitignoreEntry callers at :4611-4612, :9915-9916), so the preview is not ignored in master either. Every path leads to the diff.\n\n(c) \u00a710:377 \"Both authored in the same step from the same source; the preview is generated, never hand-edited\" \u2014 R4 lets replan derive missing contract sections; if the preview is not regenerated the two diverge (the risk row's own failure mode), and if it is regenerated then :173 is false. The spec cannot have both.\n\nNot an acknowledged limitation: \u00a74.2 R4 and \u00a710:370 acknowledge that *contract regeneration* is prose-enforced because runScopeGuard exempts actionReplan; neither mentions the preview. \u00a75:253 lists the `writeWorktreeGitExcludes` wrong-gitdir bug as out of scope, but that acknowledgement makes :173's rationale *more* wrong, not less \u2014 :173 leans on an isolation mechanism the spec elsewhere says is broken. Major, not blocker: the PR is still implementable, but the implementer is handed a false code claim and an unenforced rule, and the :377 risk goes unmitigated.",
   "corrected_statement": "\u00a74.5:173's two assertions about `{base}/design-preview.html` are both unsupported. (i) \"It is **not** written during an auto wave\" is produced by no rule in the design: R7 (:96) adds the preview to tech-plan's ALLOWED ACTIONS unconditionally, R4 (:93) is silent on it, and `actionReplan` invokes `/belmont:tech-plan` inside the worktree (main.go:7188-7189) \u2014 R5 (:94) even requires the preview in the Codex plan packet, which smoke Step 9a exercises under `belmont auto`. (ii) The rationale is factually wrong. The cited main.go:10104-10122 (`untrackBelmontInWorktree`) marks only files already in `git ls-files .belmont/` \u2014 matching \u00a74.1:72's own wording, \"**Tracked** `.belmont/` files are `--assume-unchanged`\" \u2014 so a new file is uncovered; `writeWorktreeGitExcludes` (main.go:10057-10099) writes the exclude to the per-worktree gitdir, which git does not read (verified at git 2.50.1: the new file shows as `??` and `git check-ignore` says NOT IGNORED); `commitWorktreeChanges` stages it with `git add -A` (main.go:10192) before merge; and `syncFeatureStateAfterMerge` (main.go:10169-10170) copies the worktree feature dir wholesale over master's regardless. An auto-authored preview does reach the diff. Consequently \u00a710:377's \"Both authored in the same step from the same source\" mitigation holds only if R4 mandates regeneration, which it does not.",
   "fix_to_apply": "Take the regenerate branch (R5 already requires the preview in the Codex packet, and smoke 9a runs auto under codex). Three edits. (1) \u00a74.2 R4 \u2014 append to the rule's sentence list: \"; and, whenever any contract section is derived or amended, regenerate `{base}/design-preview.html` in the same step and stamp it `<!-- unreviewed: headless replan <ISO date> -->`. If no section changed, leave the preview untouched.\" (2) \u00a74.5:173 \u2014 replace the second sentence entirely. Delete \"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\" and substitute: \"A headless replan may rewrite it (R4), and such a rewrite is visible: `untrackBelmontInWorktree` (`main.go:10104-10122`) only marks files already in `git ls-files .belmont/`, so a new preview is untracked-but-stageable, `commitWorktreeChanges` stages it with `git add -A` (`main.go:10192`) before merge, and `syncFeatureStateAfterMerge` (`main.go:10169-10170`) copies the worktree feature dir over master's in any case. Auto-authored previews therefore reach the diff carrying the `unreviewed` stamp, which is the intended signal \u2014 not an isolation failure.\" (3) \u00a710:377 \u2014 change the mitigation cell to \"Contract and preview are written in the same step, including under headless replan (R4), which must regenerate the preview whenever it amends a section; the preview is generated, never hand-edited\". Do not reintroduce a blanket \"anything under `.belmont/` in a worktree is invisible to git\" claim anywhere \u2014 it contradicts \u00a74.1:72's own \"Tracked\" qualifier."
  },
  {
   "idx": 14,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "REPRODUCED. The quote is exact \u2014 docs/proposals/0003-design-quality-without-figma.md:173 reads \"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\" The cited range is `untrackBelmontInWorktree`, whose first act is cmd/belmont/main.go:10106 `lsCmd := exec.Command(\"git\", \"ls-files\", \".belmont/\")` \u2014 index-only, so it can never cover a file that does not yet exist on the branch. \u00a74.1's own table row is correctly qualified (\"**Tracked** `.belmont/` files are `--assume-unchanged`\"); \u00a74.5 drops the qualifier and generalises a true statement about tracked files into a false one about \"anything written\".\n\nThe compensating mechanism is genuinely broken, and the spec knows it: \u00a75 Out of scope (:253) lists \"`writeWorktreeGitExcludes` writing to the wrong gitdir\" as a `main` bug it declines to fix. `excludePath` is built at main.go:10074 from the `gitdir:` in `<wt>/.git`.\n\nReproduced at git 2.50.1 in a scratch repo at .../scratchpad/wtest: with `.belmont/` written to `main/.git/worktrees/wt/info/exclude`, `git status --porcelain` in the worktree reports `?? .belmont/features/foo/design-preview.html` and `git check-ignore -v` exits 1. Moving the same line to `main/.git/info/exclude` makes check-ignore exit 0. Then, with assume-unchanged applied to the indexed `.belmont/` files and TECH_PLAN.md modified, `git add .belmont/ && git commit` produced exactly one changed file: `.belmont/features/foo/design-preview.html | 1 +` \u2014 the new HTML committed, the tracked TECH_PLAN.md correctly suppressed. Both the staging commands are real: skills/belmont/_partials/commit-belmont-changes.md:19 `git add .belmont/ && git commit -m \"belmont: update planning files {{commit_context}}\"`, included by tech-plan.md:295, and main.go:10192 `addCmd := exec.Command(\"git\", \"add\", \"-A\")`.\n\nThe gap the finding identifies is real: R4 (:93) covers only the `## Design Contract` (\"preserve an existing `## Design Contract` verbatim \u2026 mark it `**Approval**: unreviewed`\") and says nothing about the preview, while R7 (:96) adds `{base}/design-preview.html` to ALLOWED ACTIONS unconditionally. \u00a74.1's hard rule is scoped to `{base}/TECH_PLAN.md` and to \"`implement`, `verify`, `next` and `debug`\" \u2014 tech-plan under `actionReplan` is neither. So \u00a74.5's \"not written during an auto wave\" is asserted as a mechanical fact that no rule anywhere encodes.\n\nONE OVER-REACH, corrected below: the \u00a710 row at :369 (\"Contract written from a worktree and silently lost \u2026 `--assume-unchanged` makes such a write uncommittable\") is about the **contract in TECH_PLAN.md**, a tracked file \u2014 and I verified that mechanism DOES hold. That row should not be rewritten as prose-only; the fix is a new row for the preview.",
   "corrected_statement": "\u00a74.5:173 over-generalises \u00a74.1's own correctly-qualified row. `--assume-unchanged` (main.go:10104-10122) covers only files already in the worktree index, so it cannot make a *new* `{base}/design-preview.html` uncommittable; the mechanism that would \u2014 `writeWorktreeGitExcludes` \u2014 writes to the linked worktree's own gitdir, which git does not read, and the spec already lists that as an out-of-scope `main` bug (:253). Verified at git 2.50.1: `git add .belmont/ && git commit` in a linked worktree commits a new `design-preview.html` while leaving an assume-unchanged `TECH_PLAN.md` unstaged. Since R4 says nothing about the preview and R7 adds it to ALLOWED ACTIONS unconditionally, a headless `actionReplan` tech-plan may generate and commit an unreviewed preview onto the feature branch. \u00a74.5's guarantee must become an explicit prose rule. The \u00a710 contract row at :369 is NOT affected \u2014 its `--assume-unchanged` claim is correct for the tracked TECH_PLAN.md.",
   "fix_to_apply": "Three edits.\n\n(1) \u00a74.5, replace the sentence at :173 (\"It is **not** written during an auto wave \u2014 anything written under `.belmont/` inside a worktree is invisible to git (`main.go:10104-10122`), so an auto-authored preview would never reach a diff.\") with:\n\n\"It is **not** written during an auto wave \u2014 and that is a **prose rule, not a mechanism**. `--assume-unchanged` (`main.go:10104-10122`) is applied only to paths returned by `git ls-files .belmont/`, so it covers edits to the *tracked* TECH_PLAN.md but not a *new* `design-preview.html`. The compensating exclude is written to the linked worktree's own gitdir (`main.go:10074`), which git resolves from the common dir instead \u2014 the out-of-scope `main` bug named in \u00a75. Verified at git 2.50.1: in a linked worktree, `git add .belmont/ && git commit` stages and commits a new `.belmont/**/*.html` while leaving an assume-unchanged `TECH_PLAN.md` unstaged. The guarantee therefore has to be written down, not assumed \u2014 see R4.\"\n\n(2) \u00a74.2, append to R4 (:93): \"The preview is covered by the same rule: under headless replan, do **not** write or regenerate `{base}/design-preview.html`. It is authored only in an interactive `tech-plan` session in the master tree. Unlike the contract \u2014 whose worktree write is blocked mechanically because TECH_PLAN.md is tracked and `--assume-unchanged` \u2014 a new preview file *is* committable from a worktree, so this rule is prose-enforced and must be stated as such.\"\n\n(3) \u00a710, ADD a new risk row (do **not** modify the existing \"Contract written from a worktree and silently lost\" row at :369 \u2014 its `--assume-unchanged` mitigation is correct for the tracked TECH_PLAN.md):\n\"| Preview generated headlessly during replan and committed unreviewed | R4 forbids it. **Prose-enforced only** \u2014 new `.belmont/` files are committable from a worktree; smoke Step 6 asserts the branch diff contains no `design-preview.html` |\"\n\nAnd extend smoke Step 6 (:308-310) with: \"Also assert `git show --stat HEAD` (and `git log --oneline --name-only <fork>..HEAD`) on the worktree branch contains no `design-preview.html`.\" Optionally add the matching DoD checkbox under \"Placement and durability\"."
  },
  {
   "idx": 17,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quote check passes: docs/proposals/0003-design-quality-without-figma.md:318 reads exactly \"9c: `belmont auto --tool opencode` \u2014 confirm Tier 2 runs and the contract is schema-identical to Step 1's.\" Code check confirms the mechanism claim. `grep -n \"tech-plan\" cmd/belmont/main.go` yields exactly one prompt producer: main.go:7188-7189 `case actionReplan: return fmt.Sprintf(\"/belmont:tech-plan --feature %s\", feature)`. No other loop action emits tech-plan, and there is no plan-first action \u2014 `grep -n TECH_PLAN cmd/belmont/main.go` hits only :1832 (status), :1907 (techPlanReady for master), :7160 (pi/opencode prompt rewrite) and :10022 (worktree context copy); the auto decision logic never checks for a missing TECH_PLAN and never schedules planning. The sole route to REPLAN is confirmed at main.go:7642-7644 `if verifyFailCount >= 2 { // Delegate to AI for REPLAN/DEBUG decision \\n return nil }`, which hands off to the AI decision layer (prompts/belmont/ai-decision.md:22 \"If verification failed 2+ times on the SAME issue, choose REPLAN or DEBUG.\"). The spec agrees with this model elsewhere \u2014 \u00a76 :259 says actionReplan *may* invoke tech-plan, and Step 6 (:307) says \"Drive the loop to `actionReplan` (two verify failures, `main.go:7642-7644`)\" \u2014 so :318 contradicts the spec's own mechanics. Searched the whole spec for a rescuing reading: the ladder (Tier 1/Tier 2) exists only in the tech-plan reference (\u00a74.3 :100, DoD :332 requires it in `skills/belmont/tech-plan/references/`), so no implement/verify/next phase can \"run Tier 2\". And if replan did fire, R4 (:93) requires preserving an existing contract verbatim, deriving only what is missing \u2014 with a complete Step-1 contract present, nothing is derived and \"schema-identical\" is satisfied by byte-identity. Both branches make the step unfalsifiable. This is the same class of defect the spec itself fixed from v4 (\u00a712 :406: smoke Step 3 \"Unsatisfiable \u2026 Rewritten as Step 5\"), and CLAUDE.md requires smoke steps to \"List the exact expected output at each step so success/failure is unambiguous\". One overstatement in the finding: 9b already runs `/plan $belmont:tech-plan` interactively on Codex, which is a non-Claude CLI with no access to `~/.claude/skills/`, so the tier-2-only authoring path does get exercised once \u2014 it just isn't asserted there (9b only asserts the interview and the packet). So the degradation design is under-asserted rather than wholly untested.",
   "corrected_statement": "Smoke Step 9c (:318) is unfalsifiable as written: `belmont auto` never invokes `/belmont:tech-plan` except via `actionReplan` (the only producer is main.go:7188-7189, reachable only after two verify failures plus an AI REPLAN decision at main.go:7642-7644), and the Tier 1/Tier 2 ladder lives only in the tech-plan reference (\u00a74.3, DoD :332). So a normal `belmont auto --tool opencode` run cannot \"confirm Tier 2 runs\", and \"schema-identical to Step 1's\" is satisfied trivially by the untouched Step-1 file \u2014 the step passes whether or not the per-section degradation design works. (The finding's rhetoric slightly overreaches: Step 9b does exercise tier-2-only authoring on Codex interactively, it just never asserts the `**Authorities**` line \u2014 so the ladder is under-asserted on non-Claude CLIs, not entirely untested.)",
   "fix_to_apply": "In \u00a78, replace line 318 with a two-part step. (1) Interactive authoring on opencode, on a SECOND UI-bearing feature with no Figma URLs: \"9c: interactive `opencode` \u2192 type `/belmont` and pick `/belmont/tech-plan --feature <slug2>` (opencode namespaces commands with `/`, see docs/supported-tools.md:176). Expect `{base2}/TECH_PLAN.md` to carry a `## Design Contract` with the same heading set as Step 1's (Token Contract / Accessibility Floor / UX Strategy / Microcopy Rules / Motion Contract) and `**Authorities**: baseline only (no design skills available in this session)` \u2014 opencode does not scan `~/.claude/skills/`, so tier 1 is empty and tier 2 must carry every section. Fail: a missing section, or an `**Authorities**` line naming a skill that opencode cannot see.\" (2) Keep the auto run as a separate consumption-only check: \"9d: `belmont auto --tool opencode --feature <slug2> --from M1 --to M1` \u2014 expect design-agent and verification-agent to read the contract and `git diff` to show no modification to `{base2}/TECH_PLAN.md`. Note explicitly: auto never authors a contract \u2014 `/belmont:tech-plan` is emitted only by `actionReplan` (main.go:7188-7189), so this step tests consumption, not the ladder.\" Also update DoD :361 to name the interactive opencode authoring step as the non-Claude ladder check."
  },
  {
   "idx": 20,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "SURVIVES; quote and code both check out. Spec lines 307-309 read verbatim: '**Step 6 \u2014 headless replan preserves the contract.** Drive the loop to `actionReplan` (two verify failures, `main.go:7642-7644`). / Expect the existing `## Design Contract` preserved verbatim, `**Approval**` rewritten to `unreviewed (headless replan <date>)`, and no regeneration. / Fail: a fresh contract \u2192 R4 not implemented, and the approved design is silently gone.' The finding's ellipsis drops only the `**Approval**` clause, which disambiguates nothing. (a) VERIFIED: main.go:7642-7644 is `if verifyFailCount >= 2 { // Delegate to AI for REPLAN/DEBUG decision \\n return nil }` \u2014 it returns nil, it does not construct actionReplan. `grep -n actionReplan cmd/belmont/main.go` \u2192 237, 250, 403, 6831 (label), 7188 (prompt: `case actionReplan:` / `return fmt.Sprintf(\"/belmont:tech-plan --feature %s\", feature)` at :7189), 7220 (checkpoint), 7887 (AI-decision validator), 12066 (scope-guard exemption). The only construction site is the AI decider. The non-AI fallback `decideLoopAction` (6563-6620) has no REPLAN rule either \u2014 its rule 4 sends post-verify FWLUP to TRIAGE. I also checked for an escape hatch: there is no --replan flag, and the checkpoint path (main.go:6458-6469) only prints '\u23f8 Checkpoint' and returns nil, offering no action selection. And prompts/belmont/ai-decision.md:22 '3. If verification failed 2+ times on the SAME issue, choose REPLAN or DEBUG.' plus :24 '5. If a milestone has recurring failures across multiple cycles, use DEBUG.' actively bias the model toward DEBUG, so the branch is genuinely unforceable. (b) VERIFIED: VerifyFailed increments only at main.go:7435, inside `case actionVerify:` guarded by `if h.Result.Success { \u2026 } else { s.VerifyFailed++ }`; Success is set solely from the subprocess exit status (main.go:6973-6989 \u2014 `err := cmd.Wait()` \u2192 `Success: false` on err, `Success: true` otherwise). `grep -n \"Success\\s*=\\|Success:\" cmd/belmont/main.go` returns only struct literals plus `item.Success = h.Result.Success` at :7772 \u2014 no post-hoc override, and nothing parses the verify report for FAIL. A verification agent that writes a FAIL report and exits 0 is a success and routes to TRIAGE (6583 / 7542), never toward this branch. So an author reading 'two verify failures' the natural way never reaches the code under test. This is NOT a re-report of the acknowledged \u00a710:370 limitation ('Prose-enforced only \u2014 `runScopeGuard` exempts `actionReplan`'): that row names smoke Step 6 as the compensating control, and the finding shows the compensating control is unrunnable. Severity major is correct \u2014 DoD line 328 ('Contract survives `[r]`-resume (smoke Step 2) and headless replan (Step 6)') cannot be ticked, and CLAUDE.md requires each smoke step to list exact expected output so success/failure is unambiguous. Not a blocker: the feature design is sound, only the test recipe is.",
   "corrected_statement": "\u00a78 Step 6 is unrunnable as written, which leaves R4 \u2014 the only defence of an approved contract under headless replan, and prose-enforced only per \u00a710:370 \u2014 with no executable check, and DoD line 328 unsatisfiable. Two independent reasons: (a) `actionReplan` is never produced by a deterministic rule; at main.go:7642-7644 two verify failures make `decideLoopActionSmart` return nil, handing the choice to the AI decider, which prompts/belmont/ai-decision.md:22 lets pick DEBUG and :24 pushes toward DEBUG for recurring failures \u2014 and no flag or checkpoint lets the author force REPLAN. (b) 'two verify failures' does not mean two failing verify reports: VerifyFailed (main.go:7435) increments only when Result.Success is false, and Success comes solely from the subprocess exit status (main.go:6973-6989), so a verify that reports FAIL and exits 0 counts as a success and routes to TRIAGE.",
   "fix_to_apply": "Replace \u00a78 Step 6 (lines 307-309) with a deterministic recipe that tests R4 in isolation, and state why the loop cannot be driven there:\n\n'**Step 6 \u2014 headless replan preserves the contract.** `actionReplan` cannot be forced: after two *non-zero-exit* verify runs `decideLoopActionSmart` returns nil (`main.go:7642-7644`) and the AI decider may pick DEBUG instead (`prompts/belmont/ai-decision.md:22`, `:24`). A verify that reports FAIL but exits 0 is recorded as a *success* (`main.go:7435`, `:6973-6989`) and routes to TRIAGE, so \"make verification fail\" never reaches this branch. Test R4 directly by issuing the exact prompt `buildLoopPrompt` emits for `actionReplan` (`main.go:7189`), against a feature whose `{base}/TECH_PLAN.md` already carries an approved contract from Step 1:\n```bash\ncp {base}/TECH_PLAN.md /tmp/tp-before.md\nclaude -p \"/belmont:tech-plan --feature <slug>\"\ndiff /tmp/tp-before.md {base}/TECH_PLAN.md\n```\nExpect the `## Design Contract` body byte-identical, the only diff being `**Approval**` rewritten to `unreviewed (headless replan <date>)`.\nFail: a fresh contract, or any change to the Token Contract / Authorities lines \u2192 R4 not implemented, and the approved design is silently gone.'\n\nThen amend DoD line 328 to read '\u2026 and the headless replan prompt (Step 6)', and amend the \u00a710 row at line 370 so its mitigation column reads 'R4 preserve-verbatim rule; smoke Step 6 (direct headless re-invocation \u2014 the loop cannot be driven to `actionReplan` deterministically). **Prose-enforced only** \u2014 `runScopeGuard` exempts `actionReplan`'."
  },
  {
   "idx": 24,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quotes check out verbatim. Line 197: \"| Focus visible on all interactive | `browser_press_key Tab` + `browser_snapshot` per stop |\". Line 211: \"**Severity** uses the existing Critical / Warning / Polish ladder: contrast failure, missing focus indicator, missing label \u2192 Critical\".\n\nThe mechanism claim is confirmed against the live schema, not the finding's paraphrase. I loaded `mcp__plugin_playwright_playwright__browser_snapshot`: description \"Capture accessibility snapshot of the current page, this is better than screenshot\"; its only parameters are `boxes` (\"Include each element's bounding box as [box=x,y,width,height] \u2026 Element.getBoundingClientRect\"), `depth`, `filename`, `target`. There is no style payload of any kind \u2014 `boxes` is geometry. An accessibility snapshot can report *which* node holds focus; it cannot report whether an indicator is painted. A button with `outline: none` and no replacement ring yields a byte-identical snapshot to one with a 2px ring, so the row PASSES on the precise defect it exists to catch \u2014 and line 211 grades that defect Critical, so the false PASS lands on the highest-severity check in the contract.\n\nI searched the whole spec for a rescuing mechanism elsewhere and found none. Line 144-145 (\"visible focus on every interactive element (SC 2.4.7)\") is the contract's Accessibility Floor \u2014 the thing being checked, not a check. Line 171's `design-preview.html` \"state row per enumerated component state\" is a human-reviewed artifact, not the gate. No other row in the 193-203 table reads focus styling. Rows 193-199 otherwise pair every style assertion with `browser_evaluate` \u2192 `getComputedStyle`, which makes this row the lone style assertion checked by a non-style tool \u2014 an internal inconsistency, not a taste call.\n\nThis is not the acknowledged-limitation case either: line 189's UNVERIFIABLE rule protects against a *missing* mechanism, and line 375 (\"Checks unimplementable \u2192 silent pass | `UNVERIFIABLE` never counts as pass\") likewise. Neither catches a mechanism that is present, runs cleanly, and returns a green result for the wrong property. That is the one failure mode the UNVERIFIABLE rule cannot intercept.\n\nGrading: major, as filed. The fix is one table cell, but the ship impact is a substantively empty Critical gate in the section \u00a73 says this PR exists to create, and the edit is purely additive so applying it carries no regression risk. One correction to the finding's suggested_fix: it proposes reading \"its `:focus-visible` pseudo-style\" via `getComputedStyle`, which does not work \u2014 the second argument of `getComputedStyle` takes pseudo-*elements*, not pseudo-classes. It is also unnecessary: after `browser_press_key Tab` the element is genuinely focused, so `getComputedStyle(document.activeElement)` already reflects any matching `:focus-visible` rules. The fix below drops that error.",
   "corrected_statement": "\u00a74.6 line 197 pairs a visual-style assertion (\"Focus visible on all interactive\") with `browser_snapshot`, whose schema exposes only the accessibility tree plus optional `getBoundingClientRect` boxes and carries no computed-style data. The mechanism can establish tab order and focus location but never whether a focus indicator renders, so the row returns PASS for a component with `outline: none` and no replacement ring \u2014 a false PASS on a check line 211 grades Critical, and one the UNVERIFIABLE rule at line 189 cannot catch because the mechanism is present and runs successfully. Every other style row in the table (193-199) correctly uses `browser_evaluate` \u2192 `getComputedStyle`.",
   "fix_to_apply": "In \u00a74.6, replace the table row at line 197 with:\n\n| Focus visible on all interactive | `browser_press_key Tab` per stop to walk the tab order, then `browser_evaluate` \u2192 `getComputedStyle(document.activeElement)` asserting non-`none` `outline-style` with non-zero `outline-width`, or a `box-shadow` differing from the same element's unfocused computed style |\n\nDo NOT write `getComputedStyle(el, ':focus-visible')` \u2014 the second argument takes pseudo-elements only; after Tab the element is genuinely focused so its computed style already reflects matching `:focus-visible` rules.\n\nOptionally add one sentence to the \"Named tooling per check\" paragraph (after line 189): \"`browser_snapshot` returns the accessibility tree and, with `boxes`, geometry \u2014 never computed style. It may enumerate tab stops but must not be the sole mechanism for any row asserting a rendered style.\"\n\nOptionally strengthen the DoD item at line 342 from \"Every check row names a mechanism\" to \"Every check row names a mechanism that can measure the property the row asserts; unavailable \u21d2 `UNVERIFIABLE`, never `PASS`\"."
  },
  {
   "idx": 31,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "STEP 1 (quote check) \u2014 accurate. Spec :205: \"**Fix the stale MCP tool names in the same PR.** `verification-agent.md:93-94` and `implementation-agent.md:192-193` name `mcp__playwright__browser_*`, which does not exist; the installed plugin exposes `mcp__plugin_playwright_playwright__browser_*`. The same files already use the correct `mcp__plugin_figma_figma__` prefix, so these are simply stale\". DoD :345: \"- [ ] Playwright MCP tool names corrected to `mcp__plugin_playwright_playwright__*` in both agents\". Both quoted verbatim.\n\nSTEP 2 (whole-spec search) \u2014 `grep -n mcp__ docs/proposals/0003-*.md` returns only :205 and :345. Nothing elsewhere qualifies the prefix or makes it install-conditional.\n\nSTEP 3 (independent verification, run on this machine) \u2014 the finding's evidence checks out and its mechanism claim is provable here:\n\u2022 agents/belmont/verification-agent.md:93 \"Navigate to the implemented UI using `mcp__playwright__browser_navigate`\"; :94 `mcp__playwright__browser_take_screenshot`; :69 and :129 `mcp__plugin_figma_figma__get_screenshot`. implementation-agent.md:192-193 `mcp__playwright__browser_*`, :195 `mcp__plugin_figma_figma__get_screenshot`. The two prefixes do disagree inside one file.\n\u2022 README.md:13 \"install [figma-mcp](https://github.com/nichochar/figma-mcp)\"; README.md:443 \"- [playwright-mcp](https://github.com/microsoft/playwright-mcp) (recommended)\". Both are direct-install paths.\n\u2022 Decisive: `~/.claude/plugins/cache/claude-plugins-official/playwright/unknown/.mcp.json` = `{\"playwright\": {\"command\": \"npx\", \"args\": [\"@playwright/mcp@latest\"]}}`. The server is literally named \"playwright\" and runs the same upstream binary README:443 recommends; the `mcp__plugin_playwright_playwright__` namespace exists only because it was registered through the plugin system (installed_plugins.json lists playwright@/figma@claude-plugins-official). `~/.claude.json` has NO `mcpServers` entries at user or project scope \u2014 so every `mcp__plugin_*` name in this repo traces to one machine's plugin install. Registering the identical server directly (`claude mcp add playwright npx @playwright/mcp@latest`) yields exactly `mcp__playwright__browser_*`, the name the spec calls nonexistent. Corroborating: my own session's tool list mixes both shapes (`mcp__plugin_vercel_vercel__*` vs `mcp__claude-in-chrome__*`), i.e. `mcp__<server>__` for direct, `mcp__plugin_<plugin>_<server>__` for plugin.\n\u2022 No Belmont-canonical prefix exists to standardise on: `plugin/.claude-plugin/plugin.json` declares no `mcpServers` and there is no `.mcp.json` anywhere in the repo (`find . -name .mcp.json` \u2192 empty). git history shows the figma plugin-prefix came from one commit by the repo owner (03fb711, \"Name the exact tool (mcp__plugin_figma_figma__get_screenshot)\") \u2014 i.e. his own install, not a standard.\n\nSTEP 4 (judgement) \u2014 real defect, not taste and not handled elsewhere. \u00a74.6 makes all 11 check rows depend on those names, an unresolvable mechanism is `UNVERIFIABLE` (:189), and the \u00a710 risk row (:375) escalates it: \"enforcement rule makes a missing check a FAIL\". So the mandated edit would ship a one-machine hardcode into agent prose distributed to arbitrary installs, and contradict Belmont's own README.\n\nSTEP 5 (grade) \u2014 the finding says major and major is right, not blocker: agents see their real tool list and generally adapt by suffix, so the practical failure is degraded/contradictory prose plus a possible spurious FAIL, not a broken mechanism. The finding's premise that the Figma names are \"already correct\" is equally environment-bound, and it is right about that too.",
   "corrected_statement": "\u00a74.6 :205 asserts `mcp__playwright__browser_*` \"does not exist\" and DoD :345 mandates rewriting both agents to `mcp__plugin_playwright_playwright__*`. That prefix is not a property of the Playwright MCP server \u2014 it is the namespace Claude Code synthesises for a server registered through the plugin system (`~/.claude/plugins/cache/claude-plugins-official/playwright/unknown/.mcp.json` declares server `\"playwright\"` running `npx @playwright/mcp@latest`; plugin name + server name \u2192 `mcp__plugin_playwright_playwright__*`). Registering the identical upstream server directly produces exactly `mcp__playwright__browser_*`. This machine has no `mcpServers` entries in `~/.claude.json` at all, so every `mcp__plugin_*` name in the repo traces to one user's plugin install \u2014 including the `mcp__plugin_figma_figma__` names the spec calls \"correct\" (introduced by commit 03fb711). Belmont documents the non-plugin path (README:13 nichochar/figma-mcp, README:443 microsoft/playwright-mcp) and its own plugin declares no MCP servers, so there is no canonical prefix. The edit as specified swaps one environment-specific hardcode for another, breaks the documented install path, and \u2014 because \u00a74.6 ties all 11 check rows to these names, `UNVERIFIABLE` on an unavailable mechanism, and the \u00a710 row at :375 escalating a missing check to FAIL \u2014 can turn a correct implementation into a milestone FAIL for users on the README-documented install.",
   "fix_to_apply": "\u00a74.6: replace the paragraph at :205 with \u2014 \"**Make the MCP tool references install-independent.** `verification-agent.md:93-94` and `implementation-agent.md:192-193` hardcode `mcp__playwright__browser_*` while `verification-agent.md:69`/`:129` and `implementation-agent.md:195` hardcode `mcp__plugin_figma_figma__get_screenshot`. Neither prefix is canonical: Claude Code names MCP tools `mcp__<server>__<tool>` when the server is registered directly and `mcp__plugin_<plugin>_<server>__<tool>` when it is registered through a plugin, so both spellings are real and each is wrong on the other install. Belmont documents the direct path (README:13 figma-mcp, README:443 playwright-mcp) and ships no MCP servers of its own (`plugin/.claude-plugin/plugin.json` has no `mcpServers`). Both agents must therefore name the tool by **suffix** and resolve the prefix from their live tool list \u2014 e.g. 'use your Playwright MCP `browser_navigate` tool (`mcp__playwright__browser_navigate` or `mcp__plugin_playwright_playwright__browser_navigate`, depending on how the server is registered \u2014 match by suffix against your available tools)'. Apply the same treatment to the Figma calls. A tool that cannot be resolved by suffix makes its row `UNVERIFIABLE`, per the rule above.\" \u00a79 DoD: replace :345 with \"- [ ] Playwright and Figma MCP tools referenced by suffix in both agents, with no install-specific `mcp__\u2026__` prefix asserted as the only correct name\". \u00a75 table rows for `verification-agent.md` (:241) and `implementation-agent.md` (:242): change \"fix Playwright MCP names\" to \"de-hardcode Playwright/Figma MCP tool prefixes\"."
  },
  {
   "idx": 34,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "QUOTES CHECKED FIRST \u2014 all three are byte-exact against prompts/belmont/post-verify-triage.md:\n- :37 \"- Significant visual mismatches from Figma design (layout broken, wrong colors, missing components)\" (under \"### Blocking (must fix now)\")\n- :16 \"**Deferrable** \u2014 Polish items, minor improvements, aria-labels, code style, documentation gaps, small accessibility notes that don't affect usability, minor spacing tweaks.\"\n- :101 \"- **UI/visual issues are usually blocking** \u2014 design fidelity matters. Only defer truly minor visual polish (1-2px tweaks, animation smoothness).\"\nNo ellipsis surgery, no strawman.\n\nABSENCE CLAIM CHECKED ACROSS THE WHOLE SPEC \u2014 `grep -niE 'prompts/|post-verify|triage'` over all 413 lines returns ZERO hits. `prompts/` appears in \u00a75's in-scope table, \u00a75's out-of-scope list, \u00a76's surfaces table and \u00a77 exactly nowhere.\n\nTHE CONSUMER IS REAL AND LOAD-BEARING, verified in code:\n- cmd/belmont/main.go:7005 `tmpl, tmplErr := loadPromptTemplate(\"post-verify-triage\")` inside `executeTriageAction`.\n- main.go:6478-6488 parses the JSON decision, and on a parse failure defaults to `action.TriageDecision = \"defer_and_proceed\"` \u2014 deferral is literally the fallback.\n- The prompt itself performs the state edit (:63-74): remove the task from PRD.md and PROGRESS.md, append to NOTES.md, commit. So triage, not verify, decides whether a finding survives.\n\nI FOUND A STRONGER FACT THAN THE FINDING HAD, which settles the mechanism the finding only asserted. \u00a74.6:211's severity ladder has NO transport into this decision, on two independent counts:\n- main.go:7020 passes `\"VerifyOutput\": \"\"` with the comment `// verify output is available from lastOutput in the loop`. The triage agent's \"Verification output summary: {{.VerifyOutput}}\" (:8) renders EMPTY.\n- skills/belmont/_src/verify.md:180: follow-ups are written as \"plain tasks, not specially tagged: `- [ ] P1-M17-FIX-1: [Issue Description]`\". No Critical/Warning marker reaches PROGRESS.md.\nSo the triage agent classifies from an untagged one-line description against its own blocking list (Figma-scoped at :37) and its own deferral list (:43 aria-labels, :45 \"color contrast close to threshold\", :50 1-2px spacing, :101 \"animation smoothness\"). \u00a74.6 can grade a contrast failure Critical all it likes; nothing carries that grade to the agent that decides.\n\nTHE SHIPPING CLAIM ALSO HOLDS. `loadPromptTemplate` (main.go:8341-8350) tries the embedded FS FIRST, and cmd/belmont/embed.go has `//go:embed all:prompts`. `belmont install` never extracts prompts into a project (the only `prompts` copy at main.go:10031 is `.belmont/prompts`, unrelated). So with a release binary \u2014 which \u00a78's smoke test uses (`belmont install --source ~/belmont`) \u2014 an edit to prompts/belmont/post-verify-triage.md is invisible until `scripts/build.sh` rebuilds. \u00a78 never rebuilds.\n\nWHERE THE FINDING OVERREACHES (corrected, not fatal): \"would be triaged as deferrable\" is stated as a certainty. It is not determined \u2014 :100 \"Err on the side of fixing\" and :101 \"UI/visual issues are usually blocking\" push the other way, and :45's deferral is qualified to contrast \"close to threshold\". The defensible claim is that the prompt gives no instruction that a contract violation blocks, while its deferral list plausibly captures several contract-check categories, and the outcome is left to an untagged one-line description. Also \"a third distribution surface\" is off by one \u2014 \u00a76 already assigns \"third\" to `plugin/`.\n\nGrade stands at major: a real, verified, unlisted consumer that can nullify the gate's outcome, needing a \u00a75 row and a bounded prose edit. Not a blocker \u2014 the gate still produces the findings, and triage may well block them.",
   "corrected_statement": "`prompts/belmont/post-verify-triage.md` is an unlisted consumer of verification findings that decides whether they survive (`executeTriageAction`, cmd/belmont/main.go:7005; decision parsed at :6478-6488, with `defer_and_proceed` as the parse-failure default; the prompt itself deletes deferred tasks from PROGRESS.md/PRD.md and appends them to NOTES.md at :63-74). Its blocking list is Figma-scoped (:37 \"Significant visual mismatches from Figma design\") and its deferral list names categories the new contract checks produce (:43 aria-labels, :45 \"color contrast close to threshold\", :50 1-2px spacing, :101 \"animation smoothness\"). Critically, \u00a74.6:211's Critical/Warning/Polish ladder has no transport into that decision: main.go:7020 passes `VerifyOutput: \"\"`, and verify.md:180 writes follow-ups as \"plain tasks, not specially tagged\". So the triage agent grades an untagged one-line description against a Figma-era rubric. It is a further distribution surface beyond \u00a76's three \u2014 embedded via `//go:embed all:prompts` (cmd/belmont/embed.go) and preferred over disk by `loadPromptTemplate` (main.go:8345-8350) \u2014 never extracted by `belmont install`, so an edit ships only through `scripts/build.sh`, which \u00a78's smoke test never runs. The outcome is not certain deferral (:100 and :101 push toward blocking); the defect is that the prompt gives no instruction either way and the spec never touches it.",
   "fix_to_apply": "Four edits.\n\n(1) \u00a75 In-scope table \u2014 add a row after `skills/belmont/_src/references/models-yaml-format.md`:\n| `prompts/belmont/post-verify-triage.md` | Generalise the Figma-scoped blocking bullet; add a non-deferrable contract list. **Ships only via `scripts/build.sh` (embedded, never installed into a project)** |\n\n(2) \u00a75 header line :5-6 \u2014 update the counts: \"prose only\" now also covers 1 CLI prompt template; \"9 tracked source files\" becomes 10.\n\n(3) \u00a74.6, immediately after the Severity paragraph at :211, add:\n\n\"**The severity ladder must reach triage.** `prompts/belmont/post-verify-triage.md` decides whether a verify finding survives (`executeTriageAction`, `main.go:7005`; `defer_and_proceed` is the parse-failure default at `:6485`), and today it cannot see the ladder: `main.go:7020` passes an empty `VerifyOutput`, and `verify.md:180` writes follow-ups as untagged plain tasks. This PR therefore (a) generalises the blocking bullet at `post-verify-triage.md:37` from \\\"Significant visual mismatches from Figma design\\\" to \\\"Significant visual mismatches from the design reference **or the approved Design Contract**\\\"; (b) adds an explicit non-deferrable list that overrides the `:16`/`:45` \\\"small accessibility notes\\\" and `:101` \\\"animation smoothness\\\" clauses \u2014 contrast below 4.5:1 body / 3:1 large, missing visible focus indicator, missing visible input label, target below 44\u00d744 \u2014 leaving off-scale spacing and radius/elevation drift deferrable per the ladder above; and (c) has verify prefix Critical contract follow-ups with `[Critical]` in the task description so the classification is legible without the report.\"\n\n(4) \u00a76 \u2014 add a fourth row to the surfaces table:\n| **CLI prompt** | `prompts/belmont/post-verify-triage.md` is embedded (`//go:embed all:prompts`) and preferred over disk (`main.go:8345-8350`); it is never extracted into a project. A change to it reaches users only after `scripts/build.sh` \u2014 rebuild before smoke Step 5 or the edit is not under test |\nand add one DoD checkbox under **Gate**: \"- [ ] Triage prompt generalised off Figma and given a non-deferrable contract list; binary rebuilt via `scripts/build.sh` before the smoke run\"."
  },
  {
   "idx": 35,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Every quote checks out verbatim. Spec line 211: \"**Severity** uses the existing Critical / Warning / Polish ladder: contrast failure, missing focus indicator, missing label \u2192 Critical; missing state, off-scale spacing \u2192 Warning; radius or elevation inconsistency \u2192 Polish.\" Spec line 189: \"Every row names its mechanism; a check whose mechanism is unavailable is recorded **`UNVERIFIABLE`**, never `PASS`.\" Spec line 300 (smoke Step 4): \"Verify report `## Visual Verification` shows per-row pass/fail/UNVERIFIABLE...\". \u00a75 row (line 241) reads exactly: \"Three-way Phase 2; narrow `:131`; fourth enforcement rule; attestation field; focused-reverify exemption; fix Playwright MCP names\" \u2014 no severity-guide edit, no report-table edit.\n\nCode evidence verified line-by-line with `grep -n`: verification-agent.md:311 (under `### Warning (Blocks Milestone \u2014 Should Fix)`) = \"- Accessibility issues that affect usability (missing focus management, no keyboard nav for interactive elements)\"; :212 = \"**IMPORTANT**: The \\\"Expected\\\" column MUST reference values from the design reference (Figma screenshot, reference image, or design specs). ... If no design reference exists, base Expected on acceptance criteria and note this.\" \u2014 only two sources, no contract branch; :214-221 = a fixed six-row table `| Aspect | Expected (from design) | Actual (from Playwright) | Status |` with Layout structure / Spacing / Typography / Colors / Component shapes / Alignment and MATCH/MISMATCH values only. Confirmed by whole-spec grep that \"Visual Verification\" appears in the spec exactly once (line 300, the smoke step) and \"Severity\" exactly once (line 211) \u2014 so neither edit is picked up in another section. `grep -rn UNVERIFIABLE agents/ skills/ plugin/` returns nothing: the token has no home anywhere in the shipped report surfaces. `verify-report-format.md:23` = \"- Visual Verification: [PASS/FAIL/N/A]\" and that file is absent from \u00a75.\n\nTwo corrections to the finding's grading. (a) The \u00a75 row is not wholly blind to `## Output Format`: the \"attestation field\" edit necessarily touches the `### Visual Comparison Attestation` block that also lives there (:231-235). The gap is specifically the Visual Verification *table* and the UNVERIFIABLE status token. (b) The finding's second severity citation is wrong: :317 \"Minor accessibility notes (color contrast close to threshold but **not failing**)\" is about contrast that does NOT fail, so it does not contradict \"contrast failure \u2192 Critical\". The real contradiction is the single general clause at :311, which classifies any accessibility issue affecting usability as Warning \u2014 that covers a missing visible focus indicator and contradicts \u00a74.6's Critical mapping. The roll-up sub-point is also softer than claimed: UNVERIFIABLE \u21d2 never PASS, so it would roll up as FAIL, not indistinguishably from a pass. The output-format half alone carries the major grade \u2014 smoke Step 4 and the DoD line \"Every check row names a mechanism; unavailable \u21d2 `UNVERIFIABLE`, never `PASS`\" are unimplementable against any defined output surface.",
   "corrected_statement": "\u00a75's `agents/belmont/verification-agent.md` row omits two edits that \u00a74.6 and smoke Step 4 require. (1) Report shape (the substantive half): the `## Output Format` block's `## Visual Verification` table (:210-221) is Figma-shaped \u2014 six fixed Aspect rows, columns `Expected (from design)` / `Actual (from Playwright)` / `Status`, MATCH/MISMATCH values, no Mechanism column \u2014 and its :212 preamble admits only two sources for `Expected` (design reference, else acceptance criteria). There is nowhere to report the 11 contract rows, their mechanisms, or an `UNVERIFIABLE` status; `grep -rn UNVERIFIABLE` over `agents/ skills/ plugin/` returns zero hits, so the token is new and homeless. Smoke Step 4 (\"`## Visual Verification` shows per-row pass/fail/UNVERIFIABLE\") is therefore unsatisfiable by an implementer who follows \u00a75 literally. `skills/belmont/_src/references/verify-report-format.md` \u2014 whose roll-up line :23 is `- Visual Verification: [PASS/FAIL/N/A]` \u2014 is likewise absent from \u00a75. (2) Severity (narrower than the finding states): \u00a74.6 maps missing focus indicator \u2192 Critical, but the shipped guide's general Warning clause at :311, \"Accessibility issues that affect usability (missing focus management, no keyboard nav for interactive elements)\", classifies it as Warning; no \u00a75 row or DoD item authorises amending the Severity Classification Guide, so the agent would ship with contradictory severity guidance for that class. The finding's citation of :317 is not a contradiction \u2014 that Polish entry is explicitly about contrast \"close to threshold but not failing\".",
   "fix_to_apply": "\u00a75 In-scope table \u2014 replace the `agents/belmont/verification-agent.md` row's Change cell with: \"Three-way Phase 2; narrow `:131`; fourth enforcement rule; attestation field; focused-reverify exemption; fix Playwright MCP names; **rework the `## Output Format` Visual Verification block (`:210-221`)** \u2014 add a contract branch to the `:212` \\\"Expected\\\" preamble, and replace the six fixed Aspect rows with a contract-mode table `| Check | Mechanism | Expected (from contract) | Actual (from running UI) | Status |` whose Status vocabulary is PASS / FAIL / UNVERIFIABLE (Mode-A runs keep the existing MATCH/MISMATCH table); **amend the Severity Classification Guide (`:295-320`)** so contract-check failures \u2014 contrast below threshold, missing visible focus indicator, missing input label \u2014 are listed under Critical and carved out of the general Warning clause at `:311`\". Add one new \u00a75 row: | `skills/belmont/_src/references/verify-report-format.md` | Roll-up line `:23` \u2014 state that any UNVERIFIABLE contract row forces the `Visual Verification:` roll-up to something other than PASS |. Add to \u00a74.6, after the mechanism table: \"These rows are reported in the agent's `## Output Format` Visual Verification block, which gains a contract-mode table and the PASS/FAIL/UNVERIFIABLE vocabulary; the existing Figma comparison table is retained for branch 1.\" Add two DoD items under **Gate**: \"- [ ] `## Output Format`'s Visual Verification block carries a contract-mode table with a Mechanism column and PASS/FAIL/UNVERIFIABLE, and the `:212` preamble names the contract as a third source for `Expected`\" and \"- [ ] Severity Classification Guide amended so contrast / focus-indicator / label failures are Critical, not caught by the `:311` Warning clause\". Bump the \u00a7Size line accordingly (10 tracked sources)."
  },
  {
   "idx": 37,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quotes check out exactly. skills/belmont/_src/implement.md:96 reads \"**Purpose**: Analyze Figma designs (if provided) for ALL tasks, write design specifications to the MILESTONE file.\" and :126 reads \"...take screenshots to compare against Figma designs. Do NOT silently skip this step.\" implement-milestone-template.md:57 reads \"[Written by design-agent \u2014 tokens, component specs, layout code, accessibility]\" and :32 does carry \"**Master TECH_PLAN**: .belmont/TECH_PLAN.md \u2014 cross-cutting architecture (if present)\", exactly as the finding concedes. I grepped the whole 413-line spec for `implement.md` and `milestone-template` \u2014 exit 1, zero hits. Neither file appears in \u00a75, \u00a74.7, \u00a77 or anywhere else; the only mentions of the skill are `/belmont:implement` at :3, :13, :260. So the omission is real, not an artifact of looking in one section. It is also load-bearing rather than cosmetic: \u00a71:17 says Phase 2 is currently \"a paid no-op\" without Figma, \u00a77:272 explicitly rejects \"skipping the design dispatch (rejected \u2014 it now consumes a contract)\", yet the orchestrator prose the skill actually executes still frames Phase 2 as Figma analysis \"(if provided)\" \u2014 an interactive orchestrator on a no-Figma feature can read that as \"nothing to analyze\" and never dispatch design-agent, which is the exact failure \u00a77 forbids. :126 is the skill-side twin of the implementation-agent Visual Validation branch that \u00a74.7/\u00a75 DO scope; editing one surface and not the other is the same dual-surface staleness this PR elsewhere fixes (the `vercel-react-best-practices` name at :160/:165). WHERE THE FINDING OVERREACHES: the milestone-template half. \u00a74.4:167 states \"Per-task specs stay in MILESTONE, unchanged in shape\", so the placeholder describing what design-agent writes is not made wrong by rev 5. Moreover the driver of any token duplication is design-agent.md's own output format (`### Shared Design Tokens (if applicable)` at design-agent.md:146-149), and design-agent.md IS already in \u00a75. The template placeholder is a bracketed hint that gets overwritten, not a directive; making it the fix target aims at the weaker lever. Verified plugin duplication is already covered by \u00a75's `plugin/ | Regenerate` row.",
   "corrected_statement": "\u00a75's In-scope table omits `skills/belmont/_src/implement.md`, which is the orchestrator for the very phase rev 5 redefines. `:96` states Phase 2's purpose in Figma-only terms (\"Analyze Figma designs (if provided) for ALL tasks\") \u2014 on a no-Figma feature that reads as a phase with nothing to do, which would skip the Mode-B dispatch that \u00a77:272 explicitly forbids skipping. `:126` tells the orchestrator the implementation agent's Visual Validation compares \"against Figma designs\", contradicting the contract branch \u00a74.7/\u00a75 add to implementation-agent.md. The second half of the finding is refuted: `_src/references/implement-milestone-template.md` needs no edit \u2014 \u00a74.4:167 keeps MILESTONE's shape unchanged, its `### File Paths` already carries the master TECH_PLAN line at :32, and any token-restatement risk lives in design-agent.md's own `### Shared Design Tokens` format (:146-149), a file already in scope.",
   "fix_to_apply": "1) \u00a75 In scope \u2014 add one row after the `product-plan.md` row: `| skills/belmont/_src/implement.md | Phase 2 Purpose restated as contract consumption (Mode A/B); Visual Validation generalised beyond Figma |`. 2) \u00a74.7 Downstream consumers \u2014 add a bullet: \"- **`skills/belmont/_src/implement.md`** \u2014 the orchestrator prose for Phase 2. `:96` must be restated as: '**Purpose**: Produce per-task design specifications for ALL tasks into the MILESTONE file \u2014 Mode A extracts them from Figma where the PRD carries URLs, Mode B derives them against the approved `## Design Contract` in `{base}/TECH_PLAN.md`. This phase runs on every feature with UI, with or without Figma.' `:126` must end '\u2026take screenshots to compare against the per-task spec in `## Design Specifications` and, in Mode B, the `## Design Contract` in `{base}/TECH_PLAN.md`' instead of 'compare against Figma designs'. `:106` (Figma-load failures block only those tasks) is mode-agnostic and stands.\" 3) Do NOT add `implement-milestone-template.md` to \u00a75 \u2014 \u00a74.4 keeps the MILESTONE shape unchanged. If the drift risk is to be addressed at all, put it in the existing `design-agent.md` row as: 'Mode B references the contract's tokens rather than restating them in `### Shared Design Tokens`.' 4) Update the \u00a7Type/Size line at :5-6 to count the extra tracked source file (10, not 9)."
  },
  {
   "idx": 38,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Every quote verified verbatim. skills/belmont/_src/verify.md:114 \"> **Design References for Visual Verification**:\", :119 \"> If no MILESTONE files or references were found, write: \\\"No design references found in archived MILESTONE files or PRD.\\\"]\", :121 the six-dimension comparison (layout, spacing, typography, colors, component shapes, alignment) plus \"Include the Visual Comparison Attestation in your report\", and :50 \"4. **Do NOT** re-check visual specs against design references unless a follow-up task specifically addressed UI changes\u2026\". Plugin duplicate confirmed at plugin/skills/verify/SKILL.md:428-430. The channel claim is real and I checked it end to end: Step 1b:72 says \"You will pass these to the verification agent in Step 2\", and verification-agent.md:61 tells the agent \"**Orchestrator prompt**: The verify orchestrator may list design references directly in your prompt \u2014 check for a `**Design References**` section\". So Step 1b's output has exactly one delivery channel \u2014 the :114-119 block \u2014 and editing only Step 1b leaves the contract with nowhere to go while the block's fallback still emits \"No design references found\". \u00a75:243's row enumerates one edit; \u00a74.7:217 calls the edit \"small\". Second half also verified: \u00a74.6:207 names `verify.md:50` (alongside the un-editable `main.go:7185`) as instructing the skip and requires contract checks be \"exempt from that skip\", yet \u00a75 assigns \"focused-reverify exemption\" only to the verification-agent.md row. TWO CORRECTIONS TO THE FINDING'S FRAMING. First, \"a direct contradiction at dispatch time\" is overstated: \"no design references found\" is literally true in the Mode-B case and does not preclude branch 2, and :121's side-by-side mandate is conditional (\"If design references are listed above\"). The defect is a missing channel plus a countervailing signal, not a logical contradiction. Second, \"mandates the OLD attestation\" is over-reach \u2014 the prompt names the attestation generically; its field list is defined in verification-agent.md, which \u00a75 already scopes to gain `Contract checks performed:`. I also found something the finding missed that strengthens it: \u00a711:385 bases its rebase-safety claim on \"0004 rewriting `:110` and **a three-line insertion before `:114`**\" \u2014 i.e. \u00a711 already assumes this PR inserts into the dispatch block that \u00a74.7 and \u00a75 never authorize. That is an internal inconsistency in rev 5, and the conflict analysis is measuring an edit the scope table does not contain.",
   "corrected_statement": "\u00a74.7:217 (\"The edit is therefore small\") and \u00a75:243 (\"Step 1b recognises the contract shape\") under-scope `_src/verify.md` to a single edit, and \u00a711:385 contradicts them by pricing its rebase-safety test on \"a three-line insertion before `:114`\" \u2014 inside the dispatch block. Three edits are needed: (a) Step 1b (:62-72), since :72's collect list is Figma/image/URL-only and its sole delivery channel is the Step 2 prompt; (b) the dispatch block (:114-121), whose fallback at :119 writes \"No design references found in archived MILESTONE files or PRD\" in precisely the Mode-B condition, while verification-agent.md:61 instructs the agent to consult that block for references; and (c) Focused Re-verification :50, which \u00a74.6:207 explicitly names as one of the two places instructing the skip that contract checks must be exempt from \u2014 and it is the only one of the two that is editable, since \u00a75 puts all Go changes out of scope. Not a logical contradiction with branch 2, and no attestation-wording edit is needed in the prompt (the attestation's fields are defined in verification-agent.md, already in scope). The plugin duplicate needs no separate row \u2014 \u00a75's `plugin/ | Regenerate` covers it.",
   "fix_to_apply": "1) \u00a75 \u2014 replace the verify.md row's Change cell with: `Step 1b collects the contract; dispatch prompt carries it and the no-reference fallback names it; focused-reverify contract exemption at :50`. 2) \u00a74.7 bullet 3 \u2014 replace \"The edit is therefore small: tell it what shape to look for (`## Design Contract`), not where to look.\" with \"The edit is therefore three narrow ones, not a rewrite: Step 1b learns the shape (`## Design Contract`) rather than the location; the Step 2 dispatch block gains a line carrying the contract path and a fallback that distinguishes 'no images/Figma' from 'no contract'; and Focused Re-verification (:50) carries the exemption \u00a74.6 requires.\" 3) Give the concrete wording in \u00a74.6 or \u00a74.7: Step 1b \u2014 add \"4. Check `{base}/TECH_PLAN.md` for a `## Design Contract` section; if present, record its path \u2014 it is a check target, not a reference image\" and extend :72's collect sentence with \"\u2026plus the Design Contract path if one exists\". Dispatch block \u2014 insert after :118 \"> - Design Contract: `{base}/TECH_PLAN.md` `## Design Contract` (present \u21d2 contract checks are mandatory)\", and replace :119 with \"> If no MILESTONE files or image/Figma references were found but a `## Design Contract` exists, write: 'No image or Figma references; verify against the Design Contract at {base}/TECH_PLAN.md.' If neither exists, write: 'No design references and no design contract found.'\" Focused Re-verification :50 \u2014 append \"Design Contract checks are **exempt** from this skip when any follow-up touched UI; otherwise the attestation records `Contract checks performed: NO \u2014 focused re-verification, no UI follow-ups`.\" 4) \u00a711:385 \u2014 restate the rebase test as covering the Step 1b edit **and** the :114-119 dispatch insertion, so the conflict claim matches the scope table."
  },
  {
   "idx": 39,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "major",
   "reasoning": "Quote check first: line 165 reads verbatim \"**Derivation order \u2014 reuse before invention:** probe `tailwind.config.*`, CSS custom properties, `components.json`, theme files; if found, **record** it and never invent a competing scale; only where nothing exists, establish the defaults and set `**Source**: none`.\" The finding quotes it exactly, no ellipsis, no strawman. Whole-spec search for the \"missing\" claim: grep for sibling/reconcil/cross-feature/master TECH_PLAN over all 413 lines returns only :215 and :240, both about design-agent READING master `.belmont/TECH_PLAN.md`. Nothing anywhere tells Phase 3.5 (\u00a74.2 R1\u2013R7) or the derivation order (\u00a74.4) to consult master or a sibling feature's contract; the `**Source**` enum at :131 is a closed list `[tailwind.config.ts | globals.css | components.json | Figma <file> | none \u2014 established here]` with no master/sibling option; \u00a75's in-scope table lists `tech-plan-feature-format.md` (:237) and not `tech-plan-master-format.md`; \u00a75 Out of scope (:253) lists only Mode A, NO FALLBACK, Go changes and the two main bugs \u2014 cross-feature consistency is not an acknowledged limitation. Evidence independently verified against the code, not the snippet: skills/belmont/_src/references/tech-plan-master-format.md:16 is `| Styling         | e.g. Tailwind CSS 4      | [why]     |` and is indeed the only design-adjacent line \u2014 the nearest candidate, `## Shared Patterns` (:62-63), is scoped to \"data fetching wrapper, error boundaries, auth guards, form handling\", i.e. code patterns, not tokens. skills/belmont/_src/tech-plan.md:231 reads exactly \"2. **Cross-cutting drift check**: if any new cross-cutting decisions emerged during the interview (new conventions, tooling changes, shared patterns), update `.belmont/TECH_PLAN.md`\" under \"**Scenario B \u2014 Feature plan with existing master:**\" (:229) inside `### Phase 4 - Write Plan` (:219). tech-plan-feature-format.md:124 is \"- When in doubt about design, check Figma node [id]\", a second Figma-only directive that \u00a74.4:126 does not name (it names only :44 and the :86 checklist line, which I confirmed is \"- [ ] Matches Figma design pixel-perfect\"). Two counters considered and rejected: (a) \"theme files\" in the probe list could cover a sibling contract \u2014 no, a sibling contract is `.belmont/` state, not project source, and the Source enum has no slot for it; (b) feature 1's implementation writes tailwind.config so feature 2 probes and finds it \u2014 only true when feature 2 is planned after feature 1 ships. Belmont's first-class multi-feature path (product-plan \u2192 tech-plan per feature \u2192 `auto --features/--all`, master PROGRESS deps table) has every feature's TECH_PLAN authored before any implementation, so on greenfield each Phase 3.5 probes an empty project and mints its own scale. The result is worse than no contract: \u00a74.6's gate then certifies both divergent scales as correct. There is also an internal inconsistency the finding half-states: \u00a74.7:215 asserts \"cross-cutting styling decisions are routed there by tech-plan's Scenario A/B rules\" and makes design-agent read master, yet nothing in this PR ever writes design content to master and the master template has no section to hold it. Fix feasibility checked: tech-plan.md:100 already allows \"Writing to `.belmont/TECH_PLAN.md` (master tech plan \u2014 create or update)\", so no ALLOWED ACTIONS widening is needed, and main.go:10021-10027 copies master `.belmont/TECH_PLAN.md` into every worktree as a read-only context file, so a master-level contract is durably readable by design-agent in auto waves. Severity stays major, not blocker: it does not break the single-feature flagship path the smoke test exercises, and the fix is prose in two sections plus one template.",
   "corrected_statement": "\u00a74.4's derivation order probes only project config (`tailwind.config.*`, CSS custom properties, `components.json`, theme files) and, failing that, mints new defaults with `**Source**: none`. It never consults the master `.belmont/TECH_PLAN.md` or a sibling feature's already-approved contract, and the `**Source**` enum at :131 has no value for either. On a greenfield multi-feature project \u2014 the case this PR exists for \u2014 every feature's tech-plan runs before any implementation, so each one probes an empty project and establishes its own spacing/type/radius scale. Result: N approved contracts, N token scales, one app, and \u00a74.6's gate certifies each divergence as correct. This also contradicts \u00a74.7:215, which makes design-agent read master `.belmont/TECH_PLAN.md` because \"cross-cutting styling decisions are routed there\" \u2014 nothing in this PR ever writes design content there, and `tech-plan-master-format.md` has no section to receive it (its only design-adjacent line is the `| Styling |` stack row at :16). \u00a75 lists the feature format reference but not the master one. Secondary: \u00a74.4:126 generalises `tech-plan-feature-format.md:44` and the :86 checklist but leaves :124 (\"When in doubt about design, check Figma node [id]\") Figma-only.",
   "fix_to_apply": "1. \u00a74.4, replace line 165 wholesale with a rung ladder: \"**Derivation order \u2014 reuse before invention.** Walk in order and stop at the first rung that supplies a token family: (0) a `## Design Contract` in the master `.belmont/TECH_PLAN.md`; (0b) an approved `## Design Contract` in a sibling `.belmont/features/*/TECH_PLAN.md` (most recently approved wins; name which); (1) project config \u2014 `tailwind.config.*`, CSS custom properties, `components.json`, theme files; (2) only where no rung supplies it, establish the defaults here. Never invent a competing scale when an earlier rung has one; any deliberate deviation from an inherited value must be recorded inline with its justification. On a greenfield project the first feature's approved contract is promoted verbatim into master `.belmont/TECH_PLAN.md` under `## Design Contract (cross-cutting)` at Phase 4's Scenario-B cross-cutting drift check (`tech-plan.md:231`); master `.belmont/TECH_PLAN.md` is already in tech-plan's ALLOWED ACTIONS (`tech-plan.md:100`), so no permission widening is needed, and `main.go:10021-10027` copies it into every worktree, so design-agent can read it in auto waves.\"\n2. \u00a74.4, contract schema line 131: extend the enum to `**Source**: [master TECH_PLAN | sibling feature <slug> | tailwind.config.ts | globals.css | components.json | Figma <file> | none \u2014 established here]`.\n3. \u00a74.2, add **R8 \u2014 Inherit before you invent.** \"Phase 3.5 reads master `.belmont/TECH_PLAN.md` and every sibling `.belmont/features/*/TECH_PLAN.md` for an existing `## Design Contract` before deriving anything. If one exists, reuse it and set `**Source**` accordingly; only deltas are re-approved. Promotion of a first-feature contract to master happens in Phase 4 Scenario B, never from a worktree.\" Extend R4's preserve-verbatim rule to cover the master `## Design Contract (cross-cutting)` as well as the feature one.\n4. \u00a74.4:126, fold the third Figma-only line into the generalisation: after \"...must gain a contract branch\", add \"`:124`'s `When in doubt about design, check Figma node [id]` is likewise generalised to point at this plan's `## Design Contract`, naming a Figma node only in Mode A.\"\n5. \u00a75 In scope, add a row: \"| `skills/belmont/_src/references/tech-plan-master-format.md` | Add `## Design Contract (cross-cutting)` \u2014 the receptacle \u00a74.7 already assumes design-agent will find when it reads master |\", and bump \u00a72's count from \"9 tracked source files\" / \"2 skill references\" to 10 / 3.\n6. \u00a79 DoD, under \"Placement and durability\", add: \"- [ ] Second feature on a greenfield project inherits feature 1's contract (`**Source**: master TECH_PLAN`) rather than minting a second scale\"; and in \u00a78 add a sub-step to Step 1: run `/belmont:tech-plan --feature <slug2>` on a second UI feature and expect its `## Design Contract` to carry `**Source**: master TECH_PLAN` with an identical Token Contract, plus a `## Design Contract (cross-cutting)` in `.belmont/TECH_PLAN.md`.\n7. \u00a77, add to `design-authority.md`'s `Don't re-do`: \"per-feature contracts with no cross-feature reuse rung (rejected \u2014 greenfield multi-feature planning mints one scale per feature and the gate then certifies the divergence)\"."
  }
 ],
 "survived_minors": [
  {
   "idx": 6,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "STEP 1 (quote check) \u2014 quotes verify. Line 189: \"**Named tooling per check.** Every row names its mechanism; a check whose mechanism is unavailable is recorded **`UNVERIFIABLE`**, never `PASS`.\" DoD line 342: \"- [ ] Every check row names a mechanism; unavailable => `UNVERIFIABLE`, never `PASS`\". The table is 11 rows (lines 193-203). Line 200: \"| Enumerated states present | exercise each state, screenshot |\". Line 203: \"| Reduced motion removes movement, not function | emulate `prefers-reduced-motion: reduce`, re-run the state pass, assert the element still reaches its end state |\". Neither names a tool \u2014 confirmed.\n\nSTEP 2 (whole-spec search) \u2014 `grep -n \"reduced\\|screenshot\\|emulate\\|UNVERIFIABLE\\|run_code\"` over all 413 lines: reduced motion appears at 49, 146, 162, 203, 412 and no tool is named for it anywhere; `browser_run_code_unsafe` appears nowhere. Line 205 pins the surface: \"the installed plugin exposes `mcp__plugin_playwright_playwright__browser_*` ... and every row above depends on them.\" So the gap is not covered elsewhere.\n\nSTEP 3 (independent verification) \u2014 I loaded the real schemas. `mcp__plugin_playwright_playwright__browser_evaluate` is \"Evaluate JavaScript expression on page or element\" \u2014 in-page JS, which cannot change the emulated media state (that is a CDP `Emulation.setEmulatedMedia` operation behind `page.emulateMedia()`). The enumerated plugin surface (click, close, console_messages, drag, drop, evaluate, file_upload, fill_form, find, handle_dialog, hover, navigate, navigate_back, network_request(s), press_key, resize, run_code_unsafe, select_option, snapshot, tabs, take_screenshot, type, wait_for) has no emulation tool \u2014 the finding's enumeration matches exactly. `mcp__plugin_playwright_playwright__browser_run_code_unsafe` description verifies verbatim: \"Run a Playwright code snippet. Unsafe: executes arbitrary JavaScript in the Playwright server process and is RCE-equivalent.\" `grep -rn \"prefers-reduced-motion\\|emulateMedia\" agents/ skills/` finds only `product-plan.md:68` (\"reduced motion\" as a PRD question) \u2014 no existing Belmont precedent for the check. All evidence in the finding holds.\n\nSTEP 5 (misgrading) \u2014 two corrections. (a) The \"false-internal-claim\" framing overreaches: the sentence at :189 says rows name a *mechanism*, and both rows do state one in operational terms; it is the bold lead-in \"Named tooling per check\" that is unmet, not the sentence. (b) The consequence is overstated. `main.go:7927` allowlists `\"--allowedTools\", \"Bash,Read,Write,Edit,Glob,Grep,Agent,Skill,WebFetch,WebSearch,mcp__*\"` \u2014 `mcp__*` permits `browser_run_code_unsafe`, so the mechanism is not unavailable, merely unnamed and risk-bearing; and the spec's own rules (:189, and risk row :375 \"`UNVERIFIABLE` never counts as pass\") already force an honest `UNVERIFIABLE` rather than the \"silently skip\" outcome the finding predicts. The realistic failure is one of eleven rows degrading to UNVERIFIABLE-by-default, which weakens but does not break the gate \u2014 and it does hollow out DoD line 336's \"three check rows\" for motion. Real, worth a one-line fix, but minor rather than major.",
   "corrected_statement": "\u00a74.6's bold lead-in at line 189 promises \"Named tooling per check\", but rows 200 and 203 name no tool, and for row 203 no read-only tool in the surface the spec pins at line 205 (`mcp__plugin_playwright_playwright__browser_*`) can set the emulated media state: `browser_evaluate` runs in-page JS and cannot reach `page.emulateMedia()`, and the plugin exposes no emulation tool. The only route is `browser_run_code_unsafe` (\"executes arbitrary JavaScript in the Playwright server process and is RCE-equivalent\"), which Belmont's headless allowlist (`main.go:7927`, `mcp__*`) does permit but which the spec never names or risk-accepts. The consequence is bounded, not severe: :189 and the :375 risk row already force `UNVERIFIABLE` over a silent pass, so the row degrades honestly \u2014 but it degrades for every user by default, which hollows out DoD line 336's requirement of three working motion check rows.",
   "fix_to_apply": "In \u00a74.6: (1) Replace the mechanism cell of the row at line 200 with \"`browser_click` / `browser_hover` / `browser_press_key` to enter each state + `browser_take_screenshot`\". (2) Replace the mechanism cell of the row at line 203 with \"`browser_evaluate` asserting a `@media (prefers-reduced-motion: reduce)` block exists in `document.styleSheets` and covers the animated selectors; runtime emulation needs `browser_run_code_unsafe` (`page.emulateMedia({ reducedMotion: 'reduce' })`) \u2014 if that tool is not permitted, record `UNVERIFIABLE`\". (3) Append one sentence to the paragraph at line 189: \"The reduced-motion row is static-artifact-checked by default: no read-only `browser_*` tool sets the emulated media state, and the only tool that can (`browser_run_code_unsafe`) is RCE-equivalent, so runtime emulation is optional and its absence is recorded `UNVERIFIABLE`, never `PASS`.\" (4) Align DoD line 342's wording with the lead-in \u2014 change \"Every check row names a mechanism\" to \"Every check row names a tool (or is explicitly marked mechanism-only with its UNVERIFIABLE condition stated)\"."
  },
  {
   "idx": 7,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes. docs/proposals/0003-design-quality-without-figma.md:6 reads verbatim: \"**Size:** ~520 lines net across 9 tracked source files + regenerated `plugin/` + 1 new knowledge entry + 2 corrected knowledge/AGENTS statements. (v4 said \\\"~450 / 6 files\\\" and undercounted \u2014 its own \u00a75 table names seven, and \u00a77 adds three more.)\" and :410 reads \"| \\\"~450 lines / 6 files\\\" | ~520 / 9 tracked sources + 2 corrections |\". Count check confirms the finding: the \u00a75 In-scope table is header :234, rule :235, then 14 rows :236-:249. Ten of those are tracked source files \u2014 `_src/tech-plan.md` (:236), `_src/references/tech-plan-feature-format.md` (:237), `_src/references/design-authority-baseline.md` (:238), `_src/product-plan.md` (:239), `agents/belmont/design-agent.md` (:240), `verification-agent.md` (:241), `implementation-agent.md` (:242), `_src/verify.md` (:243), `_src/next.md` (:244), `_src/references/models-yaml-format.md` (:245) \u2014 with the remaining four rows (`plugin/` :246, `AGENTS.md` :247, `dual-invocation-paths.md` :248, `design-authority.md` :249) accounted for separately in the header as \"regenerated `plugin/` + 1 new knowledge entry + 2 corrected knowledge/AGENTS statements\" (the two corrections being AGENTS.md:26 and dual-invocation-paths.md:9, per DoD :354). So 10 vs the stated 9 \u2014 off by one, under every reading of \"tracked source files\". The \u00a77 half also checks out: :269 `docs/agent-pipeline.md`, :270 `docs/skills-reference.md`, and :273 \"Routing row in `knowledge/KNOWLEDGE.md`; `Revisions` lines on `model-tier-economics.md` and `dual-invocation-paths.md`\" name four artifacts absent from both the header tally and the \u00a75 table (only dual-invocation-paths.md overlaps). I searched the whole file: those items are NOT missing from the spec \u2014 \u00a77 and DoD :362 carry them \u2014 so the only real defect is the arithmetic in the header/\u00a712 plus an inventory split across two sections. That is a documentation-accounting error, not something that changes what gets implemented (\u00a75 + \u00a77 together are complete and \u00a77 is the conventional home for docs/knowledge work), so \"major\" is overgraded. It is still worth fixing because the same header sentence criticises v4 for precisely this error.",
   "corrected_statement": "The header's file count is off by one and its accounting is incomplete. :6 (\"9 tracked source files\") and :410 (\"~520 / 9 tracked sources + 2 corrections\") both say nine, but the \u00a75 In-scope table names ten tracked source files (:236-:245) before `plugin/`, `AGENTS.md` and the two knowledge rows. Additionally, \u00a77 names `docs/agent-pipeline.md` (:269), `docs/skills-reference.md` (:270), a `knowledge/KNOWLEDGE.md` routing row and a `Revisions` line on `model-tier-economics.md` (:273, echoed in DoD :362) \u2014 none of which fall under the header's \"+ 1 new knowledge entry + 2 corrected knowledge/AGENTS statements\". Nothing is missing from the spec as a whole; the header tally alone is wrong, which is ironic given the same sentence faults v4 for undercounting.",
   "fix_to_apply": "Header, line 6 \u2014 replace with: \"**Size:** ~520 lines net across 10 tracked source files (\u00a75) + regenerated `plugin/` + 2 docs pages + 1 new knowledge entry + 1 `KNOWLEDGE.md` routing row + 2 corrected knowledge/AGENTS statements + 2 knowledge `Revisions` lines. (v4 said \\\"~450 / 6 files\\\" and undercounted \u2014 its own \u00a75 table names seven, and \u00a77 adds three more.)\" \u00a712, line 410 \u2014 change the right-hand cell to \"~520 / 10 tracked sources + 2 docs pages + 2 corrections\". Optionally add one line under the \u00a75 table: \"Docs and knowledge files are inventoried in \u00a77.\" Do NOT fold `docs/agent-pipeline.md`, `docs/skills-reference.md`, `KNOWLEDGE.md` or `model-tier-economics.md` into the \u00a75 table \u2014 \u00a77 already owns them and duplicating creates a second inventory to drift."
  },
  {
   "idx": 11,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes exactly. :90 reads \"A backend-only feature skips it and the template records `**Design contract**: N/A \u2014 no UI in this feature`\" and :130 reads \"**Mode**: [A \u2014 Figma-derived | B \u2014 derived, no Figma | N/A \u2014 no UI in this feature]\". `grep -n \"Design contract\"` returns exactly one hit (:90) \u2014 the field name `**Design contract**:` appears nowhere else in the 413-line document, and the schema block (:129-163) has no such field. So the same signal, with a byte-identical value string, is given two field names, and R1's line has no defined home in the schema. I searched the whole spec for a second definition (:182, :209, :237, :290) and found none \u2014 the claim is not an \"it's handled elsewhere\" miss. Two sub-claims are weaker than the headline: (a) the routing consequence is bounded, because \u00a74.6 branch 2 keys on the `## Design Contract` HEADING, not on the field, so a backend-only feature that writes only R1's line falls to branch 3 (acceptance-criteria) \u2014 which is the behaviourally correct outcome anyway; (b) \"the attestation has an `N/A` value that only makes sense if the heading exists\" is speculative \u2014 `N/A` makes sense for any no-UI feature regardless of heading. The real, non-speculative defect is narrower: R1 names a field the schema does not define and does not say where it goes, so an implementer writing tech-plan.md prose has two defensible readings. That is a one-line wording fix with limited blast radius, hence minor rather than major. Important caveat for rev 6: the suggested fix as written would introduce a new defect if half-applied \u2014 writing `## Design Contract` for backend-only features makes \u00a74.6 branch 2 fire on an empty contract. The \u00a74.6 edit must land with it.",
   "corrected_statement": "R1 (:90) instructs the template to record `**Design contract**: N/A \u2014 no UI in this feature`, but that field name is defined nowhere \u2014 the contract schema (:129-163) carries the identical value under `**Mode**:` (:130), and `**Design contract**:` appears exactly once in the document. R1 also does not say whether its line sits under the `## Design Contract` heading, leaving the backend-only case textually undefined. The routing consequence is smaller than the finding claims (\u00a74.6 branch 2 keys on the heading, and a missing heading correctly routes a backend feature to branch 3), so this is an unambiguous but low-blast-radius schema-naming inconsistency, not a major mis-read risk.",
   "fix_to_apply": "\u00a74.2, rule R1 (:90) \u2014 replace the sentence \"A backend-only feature skips it and the template records `**Design contract**: N/A \u2014 no UI in this feature`, so downstream agents can distinguish \\\"no UI\\\" from \\\"not done\\\". Silence is not allowed.\" with: \"A backend-only feature skips the derivation but still writes the `## Design Contract` heading carrying `**Mode**: N/A \u2014 no UI in this feature` and nothing else \u2014 every subsection is omitted. Downstream agents can then distinguish \\\"no UI\\\" from \\\"not done\\\". Silence is not allowed.\" MUST land together with the matching \u00a74.6 edit (:182), otherwise this introduces a new defect: amend branch 2 to read \"No references, but `{base}/TECH_PLAN.md` carries a `## Design Contract` \u2192 read `**Mode**`; `A` or `B` \u2192 contract checks (below); `N/A` \u2192 no contract checks, attestation records `Contract checks performed: N/A`.\" No other edit needed \u2014 `**Mode**` already carries the `N/A` option at :130 and the attestation already has an `N/A` value at :209."
  },
  {
   "idx": 12,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Both quotes are verbatim-accurate and both cross-references are independently confirmed broken. (1) \u00a74.4 spans :124-167 (\u00a74.5 opens at :169). Grepping that range for forbid/never/guess/infer/fallback returns only prose inside the schema block plus the derivation-order note at :165 (\"never invent a competing scale\"), which is about token scales when no design system exists \u2014 not about a failed Figma load. The failed-load prohibition is at :215 (\u00a74.7, \"mode selection keyed on Figma-URL presence in the PRD, never on load outcome\") and :27 (\u00a72, NO FALLBACK \"correct and untouched\"); the Step 8 half of the citation is correct (:313). (2) `grep -n \"spot-check\"` returns exactly one hit in the entire document \u2014 :376 itself. Step 4 (:300-301) reads only \"Verify report `## Visual Verification` shows per-row pass/fail/UNVERIFIABLE and the attestation reads `Contract checks performed: YES against {base}/TECH_PLAN.md`. Fail: `NO`, or an acceptance-criteria pass \u2192 `:131` was not narrowed.\" No DOM assertion. The config-sourcing prohibition is at :189 as the finding says. Notably DoD :343 independently states \"`Actual` sourced from the running UI; config-sourcing forbidden **in prose**\" \u2014 so the spec's own DoD says the enforcement is prose-only, directly contradicting the risk row's claim of a smoke assertion. This is not an acknowledged limitation (the spec acknowledges R4/actionReplan and the two main bugs; neither covers this). Downgrading major\u2192minor: these are citation errors in the risk table, and every mechanism the rows are supposed to point at does exist somewhere in the design (\u00a74.7:215, \u00a72:27, \u00a74.6:189, Step 8:313, DoD:343). Nothing is missing from the design; the pointers are wrong. The :376 half is the more substantive of the two because it overstates verification coverage and contradicts DoD :343.",
   "corrected_statement": "Two \u00a710 risk-table mitigations cite the wrong location. :372 credits \u00a74.4 with forbidding inference on a failed Figma load, but \u00a74.4 (:124-167) is the contract-format section and contains no such prohibition \u2014 it lives at \u00a74.7:215 and \u00a72:27. :376 claims \"smoke Step 4 spot-checks one row against the live DOM\", but `spot-check` appears nowhere else in the document and Step 4 (:300-301) asserts only report shape and attestation wording; DoD :343 separately confirms the rule is \"forbidden in prose\" with no smoke assertion. Both are broken cross-references, not missing mechanisms \u2014 the underlying prohibitions exist at \u00a74.7:215 and \u00a74.6:189 \u2014 so this is minor citation hygiene rather than a design gap, though the :376 row does contradict DoD :343 by overstating coverage.",
   "fix_to_apply": "\u00a710 risk table, two rows. (a) :372 \u2014 replace the mitigation cell \"Mode keyed on URL presence only; \u00a74.4 forbids the inference; smoke Step 8\" with \"Mode keyed on URL presence only (\u00a74.7); \u00a72's NO FALLBACK rule untouched; smoke Step 8\". (b) :376 \u2014 replace the mitigation cell \"Explicitly forbidden; smoke Step 4 spot-checks one row against the live DOM\" with \"Explicitly forbidden at \u00a74.6 \u2014 `Actual` must come from the running UI. **Prose-enforced only**; DoD asserts it, no smoke step verifies it\" (this matches DoD :343's existing \"forbidden in prose\" wording). If the author instead wants real coverage, do BOTH of these together, never one alone: append to Step 4 (:300) the sentence \"Then pick one spacing or contrast row and confirm its `Actual` matches `getComputedStyle` on the running page, not `tailwind.config`.\", and change DoD :343 from \"config-sourcing forbidden in prose\" to \"config-sourcing forbidden in prose and spot-checked by smoke Step 4\"."
  },
  {
   "idx": 15,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK \u2014 accurate. Spec :118 reads verbatim \"`ux-designer` and `ui-designer` are user-scope personal skills at `~/.claude/skills/`. They exist for one person.\" and :120 reads \"What is absent is the **content** \u2014 those two skills live under `~/.claude/skills/`, which no non-Claude CLI scans.\" No misquote.\n\nEVIDENCE CHECK \u2014 confirmed by running it. `ls ~/.claude/skills/` \u2192 animate, contract-review, docx, find-skills, interactive-prototype, remotion, ui-designer, ux-copywriter, ux-designer, ux-motion. `find ~/.claude -maxdepth 6 -type d -name <skill>` resolves ALL FIVE tier-1 names (ui-designer, ux-designer, ux-copywriter, ux-motion, interactive-prototype) only under /Users/benlavender/.claude/skills/. Neither `dataviz` nor a project-scope copy exists; `frontend-design` is a Claude plugin (~/.claude/plugins/...), also Claude-only. docs/supported-tools.md:3 confirms the shared cross-CLI surface is `.agents/skills/belmont/<skill>/SKILL.md`, not `~/.claude/skills/`. So the factual core \u2014 five are user-scope, the spec names two \u2014 is true.\n\nPROVENANCE \u2014 this is a revision-introduced leftover, the exact class this review hunts. `git show d1236f1` (\"credit all five design skills, per-section ladder\") expanded tier 1 from two skills to five at :50, :105, :113, :132 and :412-413, but did NOT touch :118 or :120, which still carry the pre-expansion two-skill wording. :120's \"those two skills\" is now a dangling back-reference in a document whose ladder names five.\n\nBUT THE ASSERTED CONSEQUENCE IS REFUTED SIX WAYS. The finding claims the undercount produces \"Microcopy Rules, Motion Contract and interaction-state sections built on the assumption of enrichment that never arrives on 7 of 8 CLIs.\" The spec forecloses that everywhere else: :106 \"A section whose skill is absent falls back to the baseline rules for that section alone\"; :108 \"Tier 2 \u2014 normative floor, all tools, always \u2026 the floor that runs *underneath* it\"; :110 \"The contract schema is identical whichever tiers ran, and no downstream consumer may branch on it\"; :113 gives the literal degradation string \"baseline only (no design skills available in this session)\"; :151-162 write out Microcopy Rules and Motion Contract as concrete baseline prose in the contract template itself (not skill-dependent); :238 requires the new reference file to carry \"baseline rules for all five contract sections (tokens, strategy, microcopy, motion, interaction)\"; DoD :334 \"a missing skill degrades only its own section\"; risk row :373. Also :102 instructs \"Check by **name**, in your available skills. Do not probe the filesystem\" \u2014 the ladder is scope-agnostic by construction, so where the skills live cannot drive an implementer's section design.\n\nJUDGEMENT \u2014 real but cosmetic: two stale rationale sentences undercounting by three, in a doc where staleness has repeatedly bitten. Not major: no design decision, no downstream artifact and no DoD item depends on the count in those two sentences.\n\nCAUTION ON THE OFFERED FIX \u2014 do not apply it as written. Its replacement sentence asserts that on the seven non-Claude CLIs \"**every** contract section falls to the tier-2 baseline and the Authorities line reads `baseline only`.\" That over-claims: the ladder deliberately checks availability by name, so a user who installs equivalents into `.agents/skills/` gets enrichment on any CLI. And its \"add a DoD checkbox\" step duplicates the existing :334 box.",
   "corrected_statement": "\u00a74.3 :118 and :120 are stale leftovers from the pre-rev-5 two-skill era: commit d1236f1 expanded tier 1 from two skills to five at :50, :105, :113 and :132 but left both portability paragraphs saying \"`ux-designer` and `ui-designer`\" and \"those two skills\". Verified: all five tier-1 skills (ui-designer, ux-designer, ux-copywriter, ux-motion, interactive-prototype) resolve only under `~/.claude/skills/`, and tier-1b `frontend-design` is a Claude plugin \u2014 so the absent content is five (six with 1b), not two. This is an editorial undercount, not a design defect: the finding's claimed consequence (Microcopy/Motion/interaction sections silently assuming enrichment) is foreclosed by :106, :108, :110, :113, the literal baseline prose at :151-162, :238 and DoD :334.",
   "fix_to_apply": "Two sentence edits in \u00a74.3, nothing else.\n\n1. Line 118, replace \"`ux-designer` and `ui-designer` are user-scope personal skills at `~/.claude/skills/`. They exist for one person.\" with: \"All five tier-1 skills \u2014 `ui-designer`, `ux-designer`, `ux-copywriter`, `ux-motion`, `interactive-prototype` \u2014 are user-scope personal skills at `~/.claude/skills/`, and tier-1b's `frontend-design` is a Claude plugin. They exist for one person.\"\n\n2. Line 120, replace \"those two skills live under `~/.claude/skills/`, which no non-Claude CLI scans\" with: \"all five tier-1 skills live under `~/.claude/skills/`, which no non-Claude CLI scans, so as shipped every contract section degrades to the tier-2 baseline on the other seven CLIs unless the user installs equivalents into `.agents/skills/` \u2014 the ladder checks availability by name (:102), never by path.\"\n\nDo NOT add a new DoD checkbox \u2014 :334 already carries \"all five tier-1 skills named and mapped to the contract section each feeds; a missing skill degrades only its own section.\" Do NOT state flatly that the Authorities line \"reads `baseline only`\" on non-Claude CLIs; that is true of the default install, not a property of the design."
  },
  {
   "idx": 16,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "QUOTE CHECK \u2014 accurate. :260 reads verbatim \"| **Interactive** | `/belmont:tech-plan` in a live REPL, contract approved via structured questions \u2192 `/belmont:implement` \u2192 `/belmont:verify` |\" and :319 reads \"Steps 1\u20136 are the Claude-in-both-modes check.\"\n\nWHOLE-SPEC SEARCH \u2014 the gap is real. Grepping the full 413 lines for interactive|REPL|live returns exactly two REPL invocations in \u00a78: :289 Step 1 (`claude` \u2192 `/belmont:tech-plan`) and :317 Step 9b (`codex` \u2192 `/plan $belmont:tech-plan`). Both are tech-plan. Steps 2\u20136 are all auto: :293 `belmont auto --feature <slug> --from M1 --to M1`, :297 \"Same run\", :300, :303 (\"Force one FWLUP round so `/belmont:next` runs\" \u2014 inside that auto run), :307 \"Drive the loop to `actionReplan`\". Nowhere in the document is `/belmont:implement` or `/belmont:verify` typed into a REPL. So \u00a76:260's Interactive row and DoD :361 (\"Auto, interactive and plugin surfaces all exercised\") promise coverage \u00a78 does not deliver, and :319's \"Claude-in-both-modes\" is carried entirely by one tech-plan step.\n\nEVIDENCE CHECK \u2014 both external citations verified exactly. AGENTS.md:56 = \"1. Exercise **both invocation paths** for each affected tool (auto mode + interactive mode \u2014 see \\\"Both invocation paths or it's not done\\\").\" knowledge/cross-cutting/dual-invocation-paths.md:27 = \"**Symptom A \u2014 interactive-side regression.**\" Line numbers correct. \u00a75:243/:244 do list `_src/verify.md` and `_src/next.md` as changed.\n\nWHY IT IS NOT MAJOR \u2014 the finding's stated failure mechanism is wrong, and the spec itself says so. It claims the risk is \"the auto prompt hands the sub-agent context that the interactive skill body must locate for itself.\" \u00a76:263 is an in-scope correction of precisely that sentence: \"`buildLoopPrompt` emits a bare slash reference (`main.go:7168`, `:7189`); Belmont injects **only** steering text.\" I confirmed this at cmd/belmont/main.go:7179-7186 \u2014 actionVerify emits `/belmont:verify --feature %s` plus milestone-scope prose, no skill body, no agent files. The finding cites dual-invocation-paths.md:27 as authority for a mechanism this PR is deleting (\u00a75:248, DoD :354). Both paths read the same generated SKILL.md, so a prose edit to verify.md Step 1b cannot regress interactive-only.\n\nThe genuinely path-sensitive parts of THIS PR are already covered in both paths: R3 structured-question approval is interactive-only and is Step 1; R4 headless replan is auto-only and is Step 6. The one real auto/interactive delta I found for verify is main.go:7184's \"FOCUSED RE-VERIFICATION\" clause, which verify.md:40-42 gates on (\"If the invoking prompt contains 'FOCUSED RE-VERIFICATION'\"). That clause never appears interactively, so interactive verify always runs full contract checks \u2014 the safe direction, not a regression.\n\nJUDGEMENT \u2014 a real internal inconsistency (\u00a76:260 and DoD :361 vs \u00a78) and a literal miss against AGENTS.md:56's non-negotiable, but the substantive regression risk is near zero. Minor, and cheap to close either way.",
   "corrected_statement": "\u00a78 contains no interactive step beyond Step 1 (tech-plan) and 9b (Codex tech-plan), so \u00a76:260's Interactive row (\"\u2026 \u2192 `/belmont:implement` \u2192 `/belmont:verify`\"), :319's \"Steps 1\u20136 are the Claude-in-both-modes check\" and DoD :361 (\"Auto, interactive and plugin surfaces all exercised\") each overstate what the plan performs, and the plan does not literally satisfy AGENTS.md:56. The regression risk is low, not major: the path-sensitive rules of this PR are already covered in both modes (R3 by Step 1, R4 by Step 6), and per the PR's own \u00a76:263 correction \u2014 verified at cmd/belmont/main.go:7179-7186 \u2014 auto mode emits a bare slash reference and both paths read the same generated SKILL.md, so the finding's cited \"Symptom A\" mechanism does not apply to Belmont's actual auto mode.",
   "fix_to_apply": "Close the \u00a76\u2194\u00a78 inconsistency. Preferred (cheaper, and satisfies AGENTS.md:56 literally): in \u00a78, insert after Step 1 \u2014\n\n\"**Step 1b \u2014 interactive implement + verify.** From a clean tree after Step 1: `claude` \u2192 `/belmont:implement --feature <slug>`, then `/clear` \u2192 `/belmont:verify --feature <slug>`.\nExpect: `{base}/MILESTONE-M1.done.md` carries populated per-task `### Design:` sections, and the verify report's attestation reads `Contract checks performed: YES against {base}/TECH_PLAN.md`. Note that no `FOCUSED RE-VERIFICATION` clause is injected interactively (`main.go:7184` is auto-only), so interactive verify always runs full contract checks \u2014 the \u00a74.6 focused-reverify exemption is an auto-path behaviour and is covered by Step 5.\"\n\nThen renumber :319 to \"Steps 1\u20136 are the Claude-in-both-modes check\" \u2192 \"Steps 1, 1b and 2\u20136 are the Claude-in-both-modes check.\"\n\nAlternative if the extra REPL round is not wanted: narrow \u00a76:260 to \"`/belmont:tech-plan` in a live REPL, contract approved via structured questions\" and append to the row \"\u2014 `implement` and `verify` are covered auto-only; both paths read the same generated SKILL.md (\u00a76, `main.go:7168`/`:7189`), and the only auto/interactive delta is the `FOCUSED RE-VERIFICATION` clause at `main.go:7184`, whose absence interactively means contract checks always run.\" Also soften DoD :361 accordingly. Do NOT do both."
  },
  {
   "idx": 18,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes: :354 reads exactly \"- [ ] `AGENTS.md:26` and `dual-invocation-paths.md:9` corrected \u2014 auto mode does **not** bypass skill discovery and Belmont injects only steering text\". Both sub-claims verify. (a) main.go:7142-7145 `func adaptPromptForTool(prompt, tool string) string { if tool != \"pi\" && tool != \"opencode\" { return prompt }` and :7156 `\"Run the belmont:%s skill. Read .agents/skills/belmont/%s/SKILL.md fully and follow the instructions in its body to completion.\"`; the comment at :7132-7137 states the rationale verbatim as quoted (\"neither matches an opencode command \u2026 nor reliably maps to the skill tool. The same explicit rewrite removes the ambiguity\"). For those two tools the prompt deliberately does not rely on the tool's skill resolution, so an unqualified \"auto mode does not bypass skill discovery\" is wrong for 2 of 8 CLIs. (b) I read AGENTS.md:26 in full \u2014 \"the Go CLI shells out to the AI tool's headless print mode (`claude -p`, `codex exec`, `pi -p`, \u2026) and parses its stdout. Belmont assembles the prompt; the tool's own skill discovery is bypassed.\" It makes no injection claim; only knowledge/cross-cutting/dual-invocation-paths.md:9 does (\"Belmont injects the skill body, agent files, project state, and steering text directly into the subprocess prompt\"). So the checkbox's two clauses do not map one-to-one onto the two files. Two corrections to the finding's grading and scope. First, it is misgraded as major: \u00a76's body at :263 already carries the pi/opencode carve-out (\"For Pi and opencode, `adaptPromptForTool` rewrites the reference into a read-the-file instruction\"), and that body prose \u2014 not the checklist line \u2014 is what specifies the edit; the defect is a lossy DoD summary, a wording fix, not a design error. Second, the finding under-locates the misattribution: \u00a76 :263 (\"`AGENTS.md:26` and `\u2026dual-invocation-paths.md:9` state that auto mode bypasses the tool's skill discovery **and that Belmont injects the skill body, agent files and project state**\") and the \u00a712 row at :408 (\"Auto mode 'bypasses skill discovery; Belmont injects the skill body' | False (`AGENTS.md:26`, `dual-invocation-paths.md:9`)\") make the same joint attribution, so a fix confined to :354 leaves the error in two other places. This is not an acknowledged limitation \u2014 the spec nowhere flags the DoD line as a deliberate compression.",
   "corrected_statement": "The DoD checkbox at :354 states the correction tool-blind and mis-attributes it across the two files, and the same two errors recur at \u00a76:263 and \u00a712:408. (a) \"auto mode does not bypass skill discovery\" holds for six CLIs but not for Pi and opencode, where `adaptPromptForTool` (main.go:7142-7145, :7156) deliberately rewrites `/belmont:<skill>` into an explicit \"read `.agents/skills/belmont/<skill>/SKILL.md`\" instruction. (b) AGENTS.md:26 never claims Belmont injects the skill body \u2014 it says only \"Belmont assembles the prompt; the tool's own skill discovery is bypassed\" \u2014 so the \"injects only steering text\" clause applies solely to dual-invocation-paths.md:9. \u00a76:263 already carries the pi/opencode carve-out, so this is a lossy-summary defect in the checklist and the \u00a712 row, not a design error: minor, not major.",
   "fix_to_apply": "Three edits. (1) \u00a79 DoD \u2014 replace the single checkbox at :354 with two tool-scoped items: \"- [ ] `dual-invocation-paths.md:9` corrected \u2014 Belmont injects **only** steering text and milestone-scope prose; the agent reads the skill body, agent files and project state from disk\" and \"- [ ] `AGENTS.md:26` corrected \u2014 auto mode emits a bare `/belmont:<skill>` reference that the tool's own skill discovery resolves, **except for Pi and opencode**, where `adaptPromptForTool` (`main.go:7142-7145`) rewrites it into an explicit read-`.agents/skills/belmont/<skill>/SKILL.md` instruction\". (2) \u00a76 :263 \u2014 change the opening attribution to \"`AGENTS.md:26` states that auto mode bypasses the tool's skill discovery; `knowledge/cross-cutting/dual-invocation-paths.md:9` adds that Belmont injects the skill body, agent files and project state. Both are wrong for the six CLIs that resolve the bare slash reference themselves; `AGENTS.md:26` is accidentally right for Pi and opencode only, and for the wrong reason.\" (3) \u00a712 :408 \u2014 retitle the row's v5 cell to \"False for six of eight CLIs (bare slash reference; only steering injected). Pi and opencode are the carve-out \u2014 `adaptPromptForTool` rewrites to read-the-file. Note that `AGENTS.md:26` never made the injection claim; only `dual-invocation-paths.md:9` did. Correcting both, tool-scoped, is in scope.\""
  },
  {
   "idx": 19,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "REFUTED IN SUBSTANCE, SURVIVES ONLY AS A WRONG-LINE CITE. (1) The portability claim is a misreading. Spec line 92 reads: '**R3 \u2014 Structured questions or stop.** The approval step inherits `user-questions.md:4` (\"NEVER ask questions as plain inline text when a structured question tool exists\") and the strict Codex fallback at `:11`. A Markdown pick-list is not an approval gate.' R3 says the approval step *inherits* the partial \u2014 it does not enumerate an exclusive subset, and the sentence it quotes carries the disambiguating conditional 'when a structured question tool exists' in full. The partial is `@include`d into the skill at skills/belmont/_src/tech-plan.md:53, so rule 8 ('8. **Non-Codex fallback**: outside Codex, if no structured question tool exists at all, ask one clearly formatted plain-text question at a time\u2026', user-questions.md:14) ships inside the same generated SKILL.md and governs the approval question like every other question. There is nothing for R3 to override. Rule 8's remedy is a plain-text question, not a Markdown pick-list, so R3's closing sentence does not collide with it either; the heading is a compressed label, not a normative two-branch rule. (2) The enum argument is also wrong. \u00a74.4:133 `**Approval**: [approved <ISO date> | unreviewed (headless replan <ISO date>)]` distinguishes human-reviewed from headless-unreviewed. A user confirming via rule 8's plain-text fallback IS a human approval, so `approved <date>` is correct and unambiguous; no consumer branches on *how* the human approved (\u00a74.3 explicitly forbids downstream branching on tier for the same reason). Adding a third value would thread a distinction with no consumer through \u00a74.4, \u00a78 Step 1's expectations and the DoD \u2014 exactly the kind of wrong edit that ships. (3) The one verified grain is the finding's own 'Secondary' point. `cat -n skills/belmont/_partials/user-questions.md` shows line 4 is blank (line 3 = 'When you need to ask the user a question:', line 5 = '1. **Use your structured question tool**\u2026'), and the quoted sentence is line 8. The sibling cite `:11` in the same sentence is a line number (line 11 = '7. **Codex fallback is strict**: \u2026'), so the convention is file:line, and the author's other cites are accurate (skills/belmont/_src/tech-plan.md:158 = 'sub-agents cannot get MCP tool permissions approved'; :197 = 'Design system details (colors, spacing, typography \u2014 especially if no Figma)'). So `:4` is a real slip, of the wrong-line-cite class the review is watching for \u2014 but it is minor, not major, and it does not carry the portability defect the finding builds on it.",
   "corrected_statement": "Minor citation error only. \u00a74.2 R3 (line 92) cites `user-questions.md:4` for the sentence 'NEVER ask questions as plain inline text when a structured question tool exists'; that sentence is at `user-questions.md:8` (line 4 is blank). The sibling cite `:11` in the same sentence is a line number, confirming the file:line convention. The alleged contradiction with the partial's rule 8 (non-Codex plain-text fallback) is NOT real \u2014 R3 inherits the whole partial, quotes rule 4 with its conditional clause intact, and the partial is @include'd into tech-plan at _src/tech-plan.md:53 so rule 8 governs unchanged. \u00a74.4's two-value Approval enum is likewise adequate: a plain-text human confirmation is `approved <ISO date>`.",
   "fix_to_apply": "In \u00a74.2, line 92, change the single token `user-questions.md:4` to `user-questions.md:8`. Make no other edit to R3. Do NOT restructure R3 into three branches and do NOT add a third value to the `**Approval**` enum at \u00a74.4:133 \u2014 the partial's rule 8 already governs the no-structured-tool case, and a plain-text human approval is correctly recorded as `approved <ISO date>`."
  },
  {
   "idx": 22,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote verified verbatim at line 359: \"- [ ] `./scripts/generate-skills.sh --check` and `./scripts/generate-plugin.sh <ver> && git diff --exit-code plugin/` pass\". Every factual claim reproduces on the clean tree at d1236f1: `./scripts/generate-plugin.sh 0.10.14 && git diff --exit-code plugin/` \u2192 exit 0; `./scripts/generate-plugin.sh 0.10.15 && git diff --exit-code plugin/` \u2192 exit 1 with `plugin/.claude-plugin/plugin.json | 2 +-` (`-  \"version\": \"0.10.14\"` / `+  \"version\": \"0.10.15\"`), restored with `git checkout -- plugin/`; bare `./scripts/generate-plugin.sh --check` \u2192 \"STALE: .claude-plugin/plugin.json\", exit 1. Script confirmed at scripts/generate-plugin.sh:19 `VERSION=\"dev\"` and :44 `\"version\": \"$VERSION\",`; committed value is 0.10.14. 0006's diagnosis and fix are quoted correctly (0006:52 \"a bare `./scripts/generate-plugin.sh --check` **exits 1 on unmodified main**\"; :104-106 \"Preferred: ignore `version` in the `--check` comparison\"), and 0003:7 and :246 do declare 0006 a hard dependency. Grep confirms line 359 is the sole mention of the generator command in 0003 \u2014 nothing elsewhere tells the author which version to substitute, so the 'missing instruction' half of the claim is not answered in another section. BUT the severity is overstated: the item is satisfiable \u2014 it passes with `<ver>` = the committed version, which is the only substitution that makes the check mean what it is meant to mean \u2014 so 'unsatisfiable-dod / major' is wrong. The mutation complaint is also partly inherent: generate-plugin.sh always rewrites the tracked plugin/ tree (that is how the PR's regenerated plugin/ gets committed), so mutation is expected, not a surprise; only the needless version-line churn is. The 'author bumping a version is the normal reason to run the generator with an argument' rationale is weak \u2014 build.sh:33 and release.sh:41 pass VERSION programmatically at release time, not at DoD time. Real but small: an ambiguous placeholder producing a false red, fixable in one line, and 0003 does reach past its own declared dependency for a weaker gate.",
   "corrected_statement": "DoD line 359's plugin half is ambiguous rather than unsatisfiable: `./scripts/generate-plugin.sh <ver> && git diff --exit-code plugin/` is green only when `<ver>` is the version already committed in plugin/.claude-plugin/plugin.json (0.10.14 today), and the spec never says so \u2014 any other substitution diffs the version line alone and leaves the tracked file mutated until `git checkout -- plugin/`. Since 0003 declares 0006 a hard dependency (:7, :246) and 0006 \u00a73.2 makes `--check` version-agnostic and non-mutating (it regenerates into a mktemp dir), the DoD should use that gate instead.",
   "fix_to_apply": "In \u00a79 Definition of Done \u2192 Mechanics, replace line 359 with: \"- [ ] `./scripts/generate-skills.sh --check` and `./scripts/generate-plugin.sh --check` pass \u2014 the version-agnostic `--check` delivered by 0006 \u00a73.2; if 0006 lands its documented alternative instead (require the version argument), use `./scripts/generate-plugin.sh --check 0.10.14` with the version currently committed in `plugin/.claude-plugin/plugin.json`. `--check` regenerates into a temp dir, so unlike `generate-plugin.sh <ver>` it never mutates the tracked `plugin/` tree.\""
  },
  {
   "idx": 25,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Every factual leg checks out. (a) Sequencing quote is real \u2014 0003:7 says \"Lands **after 0005 and 0004** per the build order set 2026-08-06\", and docs/proposals/README.md:31 says \"**0005 \u2192 0004 \u2192 0003.**\" with :33 \"Its `main.go` split also touches every later diff's line numbers, so going first spares the others a rebase.\" (b) 0005's split is real and large: 0005:23 \"`cmd/belmont/main.go` is **12,613 lines**\" (confirmed: `wc -l` = 12613), 0005:200 DoD \"`main.go` under ~1,500 lines\", and the \u00a74.3 table at 0005:80-96 explicitly relocates `copyBelmontStateToWorktree` \u2192 `worktree.go`, `runScopeGuard` \u2192 `guards.go`, `buildLoopPrompt` \u2192 `auto_loop.go`, `decideLoopActionSmart` \u2192 `auto_decide.go`, `modelTiers` \u2192 `toolexec.go`. (c) 0005:143 already states the lesson: \"**Prefer symbol-only references** so they cannot go stale again.\" (d) 0003 carries main.go citations on 14 lines (60, 72, 73, 74, 82, 93, 173, 207, 222, 263, 279, 307, 392, 393); I spot-verified six against HEAD and all are correct TODAY \u2014 :9980 = `func copyBelmontStateToWorktree`, :10010 = `os.RemoveAll(dstFeature)`, :12065-12068 = `if action.Type == actionReplan { return }` inside runScopeGuard, :102 = `const planningTier = \"high\"`, :249-252 = `func tierForAction`, :9818 = `func requireCleanWorkingTree`. Post-0005 all of them past ~1500 point past EOF. So the staleness is real and guaranteed by the spec's own stated build order.\n\nBUT the grade is wrong. 0003 is declared \"**Type:** prose only \u2026 No Go changes\" (:5). Every main.go citation is parenthetical *evidence* for a claim, never an instruction: smoke Step 6 reads \"Drive the loop to `actionReplan` (two verify failures, `main.go:7642-7644`)\" \u2014 the instruction is \"two verify failures\"; the citation is a pointer. No smoke step, DoD checkbox or named verification mechanism becomes unrunnable, which is precisely the lens this finding was raised under. Roughly half the citations already name their symbol (`copyBelmontStateToWorktree`, `syncFeatureStateAfterMerge`, `runScopeGuard`, `planningTier`, `tierForAction`, `requireCleanWorkingTree`, `buildLoopPrompt`), so they stay grep-findable after the move. The genuine loss is the ~8 bare-line citations with no symbol at all, plus a reviewer's ability to spot-check \u00a74.1's durability proof cheaply. That is documentation hygiene with real friction, not a major defect.",
   "corrected_statement": "0003 anchors ~25 `main.go:NNNN` citations to a line-numbering scheme its own \u00a7Sequencing schedules for destruction: 0005 lands first and takes main.go from 12,613 lines to under ~1,500, relocating copyBelmontStateToWorktree, runScopeGuard, buildLoopPrompt, decideLoopActionSmart and modelTiers into new files. All citations above ~1,500 will point past EOF; the ~8 that name no symbol (`:10104-10122`, `:10169`, `:9042`, `:6178-6195`, `:7184`, `:6887`, `:7927`, `:5391-5397`, `:7642-7644`) become unresolvable rather than merely wrong. Because 0003 is prose-only and every citation is parenthetical evidence rather than an instruction, nothing in \u00a78 or \u00a79 becomes unrunnable \u2014 this is a re-verification-cost and reviewability defect, not an executability blocker. 0005:143 already adopted the fix for its own case (\"Prefer symbol-only references so they cannot go stale again\"); 0003 should do the same.",
   "fix_to_apply": "Two edits to 0003. (1) \u00a7Sequencing (:7) \u2014 append: \"All `main.go:NNNN` line numbers in this document were resolved against HEAD d1236f1 (main.go = 12,613 lines) and are **pre-0005**. 0005's split takes main.go under ~1,500 lines, so re-resolve every citation **by symbol** at branch time; treat the line number as a hint, not an address.\" (2) Add the enclosing symbol to each bare-line citation, keeping the number in parentheses as `(pre-0005)`: :72 and :173 `main.go:10104-10122` \u2192 `untrackBelmontInWorktree`; :73 `main.go:9042` \u2192 `runMilestoneInWorktree` (the unconditional `copyBelmontStateToWorktree` call) and `:6178-6195` \u2192 `runFeatureInWorktree`'s `if !resumed` guard; :207 `main.go:7184` \u2192 `buildLoopPrompt`'s `action.ReverifyScope == \"focused\"` branch; :263 `:6887` \u2192 `executeLoopAction`'s `prompt = steeringBlock + prompt`, `main.go:7927` \u2192 `toolHeadlessArgs`' claude `--allowedTools` list; :279 `main.go:5391-5397` \u2192 `runAutoCmd`'s `detectViolations` TTY prompt; :307 `main.go:7642-7644` \u2192 `decideLoopActionSmart`'s `verifyFailCount >= 2` branch; :74 `main.go:10169` \u2192 keep `syncFeatureStateAfterMerge` (already named)."
  },
  {
   "idx": 26,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Every quote checks out \u2014 I verified each against the real files. 0003:93 does say the step must \"**preserve an existing `## Design Contract` verbatim** if one is present, deriving only what is missing\"; 0003:86 does insert Phase 3.5 \"between `:217` and `:219`\" (tech-plan.md:217 = \"Once you are confident, ask the user if they have more input or if you should finalize writing the plan.\", :219 = \"### Phase 4 - Write Plan\" \u2014 correct); tech-plan.md:310 is verbatim \"**Read `references/tech-plan-feature-format.md` for the feature template**, then write `{base}/TECH_PLAN.md` using that structure.\"; tech-plan-feature-format.md:3 is \"Write to `{base}/TECH_PLAN.md` with this structure.\"; main.go:7927 is `\"--allowedTools\", \"Bash,Read,Write,Edit,Glob,Grep,Agent,Skill,WebFetch,WebSearch,mcp__*\"` with no `AskUserQuestion`; and `grep -cniE \"headless|non-interactive|no user is present\"` on tech-plan.md, user-questions.md and dynamic-questioning.md returns 0/0/0. I also confirmed \u00a75's scope rows: the tech-plan.md row lists \"Phase 3.5; ALLOWED ACTIONS + `design-preview.html`; CRITICAL RULE 6; fix skill names at `:160`; Phase 4.6 prose\" (Phase 4.6, not Phase 4) and the tech-plan-feature-format.md row is limited to \"Generalise `## Design Tokens (from Figma)` \u2192 `## Design Contract`; add contract branch to the Figma-only verification checklist\". I searched the whole 413-line spec for \"verbatim\"/\"preserve\" \u2014 the rule appears only at :93 (R4), :308 (Step 6 expectation), :328 (DoD) and :370 (Risks), never at the write site. So the gap the finding names is real and \u00a75 genuinely does not scope it.\n\nBut the finding overstates its consequence twice, and those overstatements are what earn it \"major\". Phase 3.5 is inserted *between* Phase 3 and Phase 4, and R4 is explicitly the headless branch (\"Under `belmont auto` (`actionReplan`) there is no user and `AskUserQuestion` is unavailable\"). An agent executing top-to-bottom therefore reads the preserve-verbatim rule *before* reaching the write, and carries the preserved section into Phase 4's file assembly \u2014 the claim \"even a fully compliant agent following the skill top-to-bottom regenerates the file\" does not follow, and \"this makes smoke Step 6 the expected outcome of the PR as specified\" is false. The residual risk is narrower: after \u00a75's template edit, `tech-plan-feature-format.md` will carry a fresh `## Design Contract` skeleton, and an agent told to \"write using that structure\" may emit the skeleton instead of the preserved section. That is a real, cheap-to-close belt-and-braces gap consistent with the document's own \u00a74.1 method (address the instruction that destroys state, at the site that destroys it) \u2014 a minor hardening, not a design failure.",
   "corrected_statement": "R4's preserve-verbatim rule is stated only at the derivation step (Phase 3.5) and at outcome level (\u00a78 Step 6, \u00a79 DoD, \u00a710 Risks); the instruction that actually rewrites the file \u2014 tech-plan.md:310 / tech-plan-feature-format.md:3, an unconditional whole-file write from a template that will now contain a fresh `## Design Contract` skeleton \u2014 never mentions it, and \u00a75 scopes no edit to either. A compliant agent reading R4 before Phase 4 will normally carry the preserved section through, so this is a robustness gap rather than a guaranteed Step-6 failure; but the belt-and-braces read-before-write clause at the write site is cheap and matches the document's own \u00a74.1 reasoning.",
   "fix_to_apply": "Two edits to 0003. (1) \u00a75 In-scope table, `skills/belmont/_src/references/tech-plan-feature-format.md` row \u2014 extend to: \"Generalise `## Design Tokens (from Figma)` \u2192 `## Design Contract`; add contract branch to the Figma-only verification checklist; **add a read-before-write clause to the `:3` write instruction**.\" Give the wording in \u00a74.2 under R4: \"Before writing `{base}/TECH_PLAN.md`, read the existing file if present. If it already contains a `## Design Contract`, reproduce that section byte-for-byte in the new file and append only sections that are absent; never fill the template's Design Contract skeleton over an existing one.\" (2) \u00a79 DoD, \"Placement and durability\" block \u2014 add: \"- [ ] The feature-template write instruction carries the read-before-write preserve clause, so the rule sits at the site that rewrites the file, not only at the step that derives it\"."
  },
  {
   "idx": 29,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Quotes check out verbatim. :377 = \"| Preview rots against the contract | Both authored in the same step from the same source; the preview is generated, never hand-edited |\". :93 (R4) = \"preserve an existing `## Design Contract` verbatim if one is present, deriving only what is missing; mark it `**Approval**: unreviewed (headless replan <ISO date>)`\". :173 (\u00a74.5) = \"It is **not** written during an auto wave\". I grepped every occurrence of \"preview\" in the spec (:94, :96, :171, :173, :236, :290, :317, :329, :377, :394): confirmed the \u00a74.4 schema (:128-163) has only Mode/Source/Authorities/Approval \u2014 no binding field \u2014 and no DoD item or check compares preview to contract (:329 only requires it exists and opens). So the mechanism gap is real: R4 is the one step that may amend the contract, and it is the one step \u00a74.5 says does not author the preview. R4 mutates the file even in the narrowest reading, since it rewrites the `**Approval**` line.\n\nBut the finding is overgraded and partly overstated on three counts I verified in code. (1) Reachability: `runMilestoneInWorktree` sets `mCfg.Root = wtPath` and calls `runLoop` (main.go:9105, :9123), so in the parallel regime the R4-amended contract lands in a worktree where `untrackBelmontInWorktree` (main.go:10104-10122) marks `.belmont/` assume-unchanged \u2014 the amendment cannot be committed and is wiped by `os.RemoveAll(dstFeature)` at main.go:10010 on resume. Drift only reaches a human in the serial regime, where `runLoop` (main.go:6325) executes in the master tree \u2014 and there \u00a74.5's prohibition is phrased \"during an auto wave\", which serial arguably is not, so the spec is ambiguous rather than flatly self-contradictory. (2) \"A user \u2026 sees a contract that no longer exists\" is false: R4 preserves the existing contract verbatim, so the preview is stale/incomplete, never contradictory. (3) The hand-edited-contract tail is partly answered by \u00a74.1 (:78), which names `debug` among the read-only consumers.\n\nResidue: a risk-table mitigation that is inaccurate for one path, fixable in two short clauses. That is minor, not major.",
   "corrected_statement": "Risk row 9 (:377) overstates its mitigation. \"Both authored in the same step from the same source\" is true for interactive tech-plan but not for R4's headless replan (:93), which may rewrite `**Approval**` and derive missing sections in a step where \u00a74.5 (:173) does not author the preview \u2014 and nothing in the \u00a74.4 schema (:128-163) or the DoD (:329) binds the two artifacts or detects the skew. In the parallel regime the amended contract is itself uncommittable (`untrackBelmontInWorktree`, main.go:10104-10122) so no skew reaches a diff; the exposure is the serial regime, where `runLoop` runs in the master tree (main.go:6325). Consequence is a stale review aid, not a broken pipeline: minor.",
   "fix_to_apply": "Two edits, no behaviour change.\n\n1. \u00a710 Risks, row 9 \u2014 replace the mitigation cell with: \"Both authored in the same step by interactive `tech-plan`. The one exception is R4's headless replan, which may amend the contract without authoring a preview; R4 therefore stamps the contract `**Preview**: STALE \u2014 amended by headless replan <ISO date>, regenerate with /belmont:tech-plan`, so the skew is visible in the diff rather than silent.\"\n\n2. \u00a74.2 R4 (:93) \u2014 append a clause to the existing list: \"\u2026and, whenever it rewrites `**Approval**` or derives any missing section, stamp `**Preview**: STALE \u2014 amended by headless replan <ISO date>, regenerate with /belmont:tech-plan` on the contract.\" Add a matching `**Preview**: [design-preview.html <ISO date> | STALE \u2014 <reason>]` line to the \u00a74.4 header block (after `**Approval**`, :133), and one DoD item under Placement and durability: \"[ ] Headless replan stamps `**Preview**: STALE` when it amends the contract (smoke Step 6).\" Extend smoke Step 6 (:308) expectation to include the STALE stamp.\n\nDo NOT adopt the finding's option (a) (forbid R4 from deriving anything): it breaks the legitimate case of an auto run on a feature planned before this PR, where no contract exists at all."
  },
  {
   "idx": 30,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Split verdict \u2014 one of the two problems is refuted outright, the other survives shrunken.\n\nProblem (1), the severity ladder, is largely refuted. :211 says \"**Severity** uses the existing Critical / Warning / Polish ladder\" \u2014 it is an addendum to `verification-agent.md`'s Severity Classification Guide, not a replacement, and that guide already lists \"Animation/transition polish\" under **Polish** (verification-agent.md:320), with the key principle at :327: \"If removing the issue would not affect a user's ability to use the feature or cause a visually broken experience, it's Polish, not Warning.\" Its Critical bucket (:299-305) is acceptance criteria, broken functionality, security, scope, missing components \u2014 motion is nowhere near it. So \"graded by guess, and most likely guessed Critical\" does not hold against the real file. The third-party-component worry is also partly pre-answered by verification-agent.md:31 (\"Only flag code that was NEWLY WRITTEN by the current task\"). The zero-duration edge (`getComputedStyle` returns `0s` for an unanimated element, and the :157 band \"state change 150\u2013200ms\" has a lower bound) is a genuine but small clarity gap.\n\nProblem (2) has a verified factual core and a misread consequence. Verified: :203 names no tool, and I loaded the actual Playwright MCP schemas \u2014 the surface is browser_click/close/console_messages/drag/drop/evaluate/file_upload/fill_form/find/handle_dialog/hover/navigate/navigate_back/network_request(s)/press_key/resize/run_code_unsafe/select_option/snapshot/tabs/take_screenshot/type/wait_for. There is no media-emulation tool, and `browser_run_code_unsafe`'s description is exactly \"Run a Playwright code snippet. Unsafe: executes arbitrary JavaScript in the Playwright server process and is RCE-equivalent.\" Misread: the finding's own evidence sentence \"the third names no tool while every other row in the table does\" is false \u2014 :200 (\"| Enumerated states present | exercise each state, screenshot |\") names none either. And \"permanently UNVERIFIABLE \u2026 fails DoD :342\" inverts the line it cites: :342 reads \"Every check row names a mechanism; unavailable \u21d2 `UNVERIFIABLE`, never `PASS`\" \u2014 UNVERIFIABLE is the sanctioned outcome, not a DoD failure. The fourth enforcement rule (:187) fires when \"contract checks were not performed\", which an honestly recorded UNVERIFIABLE is not, so the FAIL-escalation is speculative. The finding also missed a compliant route: a `browser_evaluate` walk of `document.styleSheets` for a `CSSMediaRule` whose `conditionText` contains `prefers-reduced-motion` reads the running UI's CSSOM, so it does not violate :189's ban on sourcing from `tailwind.config`/`globals.css`.",
   "corrected_statement": "Narrow residue only: the reduced-motion row (:203) is the sole check whose mechanism cannot be executed with any tool the Playwright MCP plugin exposes \u2014 media emulation requires `browser_run_code_unsafe` (`page.emulateMedia({ reducedMotion: 'reduce' })`), which is RCE-flagged and routinely not granted, and the row names no tool at all. Rev 6 should name the route and its fallback so the row is not silently unrunnable. Secondary: the duration row (:201) should say a component with no transition is compliant, since `transitionDuration: 0s` is otherwise outside the 150\u2013200ms band at :157. The severity-ladder half of the finding is refuted \u2014 `verification-agent.md:320` already grades \"Animation/transition polish\" as Polish and :211 explicitly defers to that existing ladder \u2014 as are the DoD-failure and milestone-FAIL escalation claims.",
   "fix_to_apply": "\u00a74.6 check table only; leave :211 alone.\n\n1. Replace row :203 with: \"| Reduced motion removes movement, not function | `browser_run_code_unsafe` \u2192 `page.emulateMedia({ reducedMotion: 'reduce' })`, then re-run the state pass and assert the element still reaches its end state. If that tool is not granted, fall back to `browser_evaluate` walking `document.styleSheets` for a `CSSMediaRule` whose `conditionText` matches `prefers-reduced-motion` and covering the animated selectors; if neither is available the row is `UNVERIFIABLE` |\".\n\n2. Append to row :201's mechanism cell: \"\u2014 an element with no transition (`transitionDuration: 0s`) is compliant; the bands constrain declared durations, not the decision to animate.\"\n\n3. Add one sentence after :211: \"Motion rows inherit the existing guide's `Animation/transition polish` classification (verification-agent.md:320) \u2014 Polish unless the deviation breaks functionality, in which case it is Critical under `prefers-reduced-motion` (SC 2.3.3).\"\n\nDo not add motion entries to the :211 ladder as a new severity mapping and do not scope the checks to milestone-touched components \u2014 verification-agent.md:31 already carries that rule and duplicating it invites drift."
  },
  {
   "idx": 33,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "QUOTES CHECKED FIRST \u2014 both are exact. \u00a74.2 R1 (spec :90) reads verbatim \"A backend-only feature skips it and the template records `**Design contract**: N/A \u2014 no UI in this feature`, so downstream agents can distinguish \\\"no UI\\\" from \\\"not done\\\". Silence is not allowed.\" \u00a74.6 branch 3 (:183) reads verbatim \"No references and no contract \u2192 existing acceptance-criteria fallback, unchanged.\"\n\nABSENCE CLAIM CHECKED ACROSS THE WHOLE 413-LINE SPEC \u2014 `grep -niE 'migrat|backfill|existing project|already have|pre-rev|upgrade|legacy|in-flight|existing feature|already planned|retrofit'` returns ZERO hits. Genuinely absent, not addressed in another section.\n\nSUPPORTING EVIDENCE INDEPENDENTLY VERIFIED (all four hold):\n- `belmont validate` is PROGRESS.md-only: `validateFeature` at cmd/belmont/main.go:11838-11850 builds `progressPath := filepath.Join(featuresDir, \"PROGRESS.md\")` and reads nothing else. TECH_PLAN is never opened.\n- review-plans Layer 2 has no contract check: skills/belmont/_src/review-plans.md:107-109 reads exactly \"3. **Feature tech plan vs master tech plan** / - Check if feature tech plan follows master tech plan's architecture decisions \u2192 **Drift** if it diverges / - If feature has no tech plan \u2192 **Gap**\". (The finding cited :46,50 for the TECH_PLAN reads; they are actually :48 and :51 \u2014 a trivial two-line drift, not a misquote of substance.)\n- Backfill-by-rerun is a full rewrite: skills/belmont/_src/tech-plan.md:229-230 reads \"**Scenario B \u2014 Feature plan with existing master:** / 1. Write `{base}/TECH_PLAN.md` using the **Feature TECH_PLAN.md Format** below.\" and :245 \"### Phase 4.5 - PRD Reconciliation (MANDATORY)\".\n\nBUT THE FINDING OVERSTATES ITS CONSEQUENCE ON TWO POINTS, BOTH CONTRADICTED BY THE SPEC ITSELF:\n1. \"nothing re-runs tech-plan on an in-flight feature\" is FALSE, and the spec says so twenty lines above the passage being attacked. \u00a74.1:82: \"**Replan is the live threat.** `actionReplan` emits `/belmont:tech-plan --feature <slug>` headlessly mid-run (`main.go:7188-7189`)\". Combined with R4 (:93) \u2014 \"**preserve an existing `## Design Contract` verbatim** if one is present, deriving only what is missing\" \u2014 a pre-contract feature that reaches replan gets the whole contract derived and stamped `unreviewed (headless replan <date>)`. So an unlabelled partial backfill path already exists in the design, and \"silently and forever\" is wrong.\n2. \"absent-heading, N/A-mode, and pre-rev-5 all collapse into the same permissive path\" is FALSE for N/A-mode. R1 exists precisely to prevent that collapse and says so in the sentence the finding itself quotes (\"Silence is not allowed\"), and \u00a74.6:209 already provides an `N/A` attestation value. What actually collapses is only the third case.\n\nWHAT SURVIVES is narrower and smaller than \"major\": a legacy feature is indistinguishable from a feature where the contract step was simply not done, and the spec never states that the gate is non-retroactive. Note also that nothing REGRESSES \u2014 a pre-contract feature keeps exactly today's behaviour. The residue is one missing paragraph plus one missing attestation value, both one-line edits. That is minor, not major. The finding's fourth sub-suggestion (adding `review-plans.md` to \u00a75) is optional polish, not required by the gap.",
   "corrected_statement": "The spec never states what happens to a feature planned before this PR. Such a feature's `{base}/TECH_PLAN.md` has no `## Design Contract`, so \u00a74.6 branch 3 applies and it keeps today's acceptance-criteria verification \u2014 the gate is not retroactive, and the spec nowhere says so. Two consequences: (a) the \u00a74.6:209 attestation vocabulary (`YES against <path> | NO \u2014 <reason> | N/A`) has no value that distinguishes \"this feature predates the contract\" from \"checks were skipped\" or \"no UI\", so the gap is invisible in reports; (b) the only backfill is re-running `/belmont:tech-plan --feature <slug>`, which at tech-plan.md:230 rewrites the whole feature plan and drags in mandatory Phase 4.5 PRD Reconciliation \u2014 a deliberate act the spec should name rather than leave the reader to discover. NOT true, contra the finding: tech-plan IS re-run on in-flight features (\u00a74.1:82, `actionReplan`), and R4 makes that path derive the missing contract, so the gap is not permanent; and N/A-mode does NOT collapse with absent-heading, because R1 mandates the `N/A \u2014 no UI in this feature` marker for exactly that reason.",
   "fix_to_apply": "Two edits, both small.\n\n(1) Add a \u00a74.9 immediately after \u00a74.8, titled \"Pre-contract features\":\n\n\"### 4.9 Pre-contract features\n\nThis gate is **not retroactive**. A feature planned before this PR has a `{base}/TECH_PLAN.md` with no `## Design Contract`; \u00a74.6 branch 3 applies and it keeps today's acceptance-criteria verification unchanged. Nothing regresses, and nothing silently upgrades.\n\nTwo ways a pre-contract feature acquires one, neither of them automatic:\n- **Deliberate backfill** \u2014 re-run `/belmont:tech-plan --feature <slug>` interactively. Note this is not an additive edit: Phase 4 Scenario B (`tech-plan.md:230`) writes the whole feature plan from the template and Phase 4.5 PRD Reconciliation is mandatory, so backfill a feature between milestones, not mid-milestone.\n- **Headless replan** \u2014 under R4, `actionReplan` derives what is missing and stamps `**Approval**: unreviewed (headless replan <date>)`. This is a side effect of failure recovery, not a migration mechanism, and the result is unreviewed by construction.\"\n\n(2) In \u00a74.6, replace the attestation line at :209 with:\n\n\"**Attestation gains a field:** `Contract checks performed: [YES against <path> | NO \u2014 <reason> | N/A \u2014 no UI in this feature | N/A \u2014 pre-contract feature (TECH_PLAN carries no ## Design Contract)]`.\"\n\nand add one DoD checkbox under **Gate** (after the `:131` narrowing item): \"- [ ] Branch 3 records `N/A \u2014 pre-contract feature`, distinguishable from a skipped or no-UI check; \u00a74.9 states the gate is non-retroactive\"."
  },
  {
   "idx": 40,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes. Spec line 92 reads verbatim: \"- **R3 \u2014 Structured questions or stop.** The approval step inherits `user-questions.md:4` (\\\"NEVER ask questions as plain inline text when a structured question tool exists\\\") and the strict Codex fallback at `:11`.\" `cat -n skills/belmont/_partials/user-questions.md` shows line 4 is blank (\"When you need to ask the user a question:\" is line 3, line 4 empty), the quoted rule is list-item **4** sitting at **line 8** (\"4. **NEVER ask questions as plain inline text** when a structured question tool exists. No \\\"Question 1: ...\\\" followed by more text. Use the tool.\"), and line 11 is list-item 7 (\"7. **Codex fallback is strict**: if you are running in Codex and the structured question tool is unavailable...\"), so `:11` is genuinely correct. The spec's `:N` convention is unambiguously line numbers, not ordinals \u2014 verified against two neighbouring citations in the same section: `tech-plan.md:158` is exactly the line \"...sub-agents cannot get MCP tool permissions approved\", and `main.go:12065-12068` is exactly the `if action.Type == actionReplan { return }` early return in `runScopeGuard`. Only one `user-questions.md` exists in the tree (generated skills are gitignored), so there is no other file where line 4 would carry that text. Real but cosmetic: an off-by-four citation, no behavioural consequence.",
   "corrected_statement": "\u00a74.2 rule R3 (spec line 92) cites `user-questions.md:4` for the rule \"NEVER ask questions as plain inline text when a structured question tool exists\". Line 4 of `skills/belmont/_partials/user-questions.md` is blank; that rule is list-item 4 at line 8. The companion citation `:11` (Codex fallback is strict) is correct.",
   "fix_to_apply": "In \u00a74.2, rule R3 (line 92): change `user-questions.md:4` to `user-questions.md:8`. Leave the trailing `and the strict Codex fallback at `:11`` unchanged \u2014 it is correct."
  },
  {
   "idx": 41,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes: spec line 107 reads \"> - **Tier 1b \u2014 enrichment, conditional.** If `frontend-design` is available, load it for aesthetic direction. Load `dataviz` **only** if this feature renders charts. Load `vercel:shadcn` **only** if...\". Verified on disk that `frontend-design` is a *plugin* skill, not a user-scope skill: `~/.claude/plugins/installed_plugins.json:44` has `\"frontend-design@claude-plugins-official\"`, `~/.claude/settings.json:27` enables it, and the payload lives at `~/.claude/plugins/cache/claude-plugins-official/frontend-design`. It is therefore registered as `frontend-design:frontend-design` (the Skill tool contract: \"Plugin skills use `plugin:skill`\"), and the session skill listing confirms that exact string. By contrast `ls ~/.claude/skills/` returns `ui-designer, ux-designer, ux-copywriter, ux-motion, interactive-prototype` (plus animate/docx/etc.) as genuine unprefixed user-scope skills, and `dataviz` / `vercel:shadcn` are listed exactly as the spec writes them \u2014 so every other name in the ladder is right and only this one is wrong. The spec holds itself to this standard 15 lines later at line 122: \"**Use exact registered names.** `tech-plan.md:160` and `product-plan.md:165` currently name `vercel-react-best-practices` and `security`; neither resolves... do not copy that line's style.\" Notably `skills/belmont/_src/tech-plan.md:160` reads \"Load relevant skills (figma:*, frontend-design, vercel-react-best-practices, security, etc.)\" \u2014 the spec flags two of the three bad names on that line and leaves `frontend-design` unflagged, then reproduces it in its own new reference file. (The finding's aside that line 122 is \"four lines later\" is wrong \u2014 it is 15 lines later \u2014 but that does not touch the substance.) Real, minor: a Claude agent would likely still fuzzy-match the listing, but the spec is shipping the exact class of name error it wrote a section to eliminate.",
   "corrected_statement": "\u00a74.3 line 107 writes `frontend-design`, but that skill is installed as a plugin (`frontend-design@claude-plugins-official`) and its registered invocable name is `frontend-design:frontend-design`. The same wrong name already sits in `skills/belmont/_src/tech-plan.md:160`, which \u00a74.3 line 122 tells the implementer to fix for `vercel-react-best-practices` and `security` while leaving `frontend-design` uncorrected \u2014 so the new reference file would ship the exact error line 122 exists to prevent.",
   "fix_to_apply": "Three coordinated edits: (1) line 107 \u2014 replace \"If `frontend-design` is available\" with \"If `frontend-design:frontend-design` is available\"; (2) line 122 \u2014 extend the sentence to \"...currently name `vercel-react-best-practices`, `security` and `frontend-design`; none resolves \u2014 the real names are `vercel:react-best-practices` and `frontend-design:frontend-design`, and `security` is a slash command, not a skill\"; (3) DoD line 338 \u2014 extend to \"`vercel-react-best-practices` \u2192 `vercel:react-best-practices`, `frontend-design` \u2192 `frontend-design:frontend-design`, and the bogus `security` entry fixed in both `tech-plan.md:160` and `product-plan.md:165`\". Leave line 132's `frontend-design (aesthetic)` alone or update it to match \u2014 it is an Authorities label, not an invocation."
  },
  {
   "idx": 42,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes exactly: line 5 is \"**Type:** prose only (2 agents, 2 skill sources, 2 skill references, 1 new skill reference). No Go changes.\" and line 6 is \"**Size:** ~520 lines net across 9 tracked source files + regenerated `plugin/` + 1 new knowledge entry + 2 corrected knowledge/AGENTS statements. (v4 said \\\"~450 / 6 files\\\" and undercounted \u2014 its own \u00a75 table names seven, and \u00a77 adds three more.)\". The header is self-inconsistent before you even reach \u00a75: the parenthetical sums to 7, the Size line says 9. \u00a75's in-scope table (lines 234-249) enumerates exactly what the finding says \u2014 3 agents (`design-agent.md`, `verification-agent.md`, `implementation-agent.md` at lines 240-242), 4 skill sources (`_src/tech-plan.md`, `_src/product-plan.md`, `_src/verify.md`, `_src/next.md` at 236/239/243/244), 2 existing references (`tech-plan-feature-format.md` 237, `models-yaml-format.md` 245) and 1 new reference (`design-authority-baseline.md` 238) = 10 tracked source files, plus `plugin/`, `AGENTS.md`, `dual-invocation-paths.md`, `design-authority.md`. The \"9\" looks like a stale count taken before rev 5's \u00a74.8 point 2 added `models-yaml-format.md` to scope. \u00a77 (lines 269-273) further names `docs/agent-pipeline.md`, `docs/skills-reference.md`, the `knowledge/KNOWLEDGE.md` routing row and a `Revisions` line on `knowledge/cross-cutting/model-tier-economics.md`, none of which the \"+ 1 new knowledge entry + 2 corrected knowledge/AGENTS statements\" tail covers. The finding's own suggested tail (\"4 amended\") is itself undercounted \u2014 the true figure is 6. Real, minor: header metadata only, but it is the same defect the header mocks v4 for.",
   "corrected_statement": "The header's file counts are wrong and self-inconsistent: line 5's breakdown sums to 7 while line 6 claims 9, and \u00a75's in-scope table actually names 10 tracked source files (3 agents, 4 skill sources, 2 existing skill references, 1 new reference). The Size line's tail also fails to account for `docs/agent-pipeline.md`, `docs/skills-reference.md`, the `KNOWLEDGE.md` routing row and the `model-tier-economics.md` Revisions line named in \u00a77 \u2014 6 amended non-source files, not 2.",
   "fix_to_apply": "Header, lines 5-6. Line 5 \u2192 \"**Type:** prose only (3 agents, 4 skill sources, 2 skill references, 1 new skill reference). No Go changes.\" Line 6 \u2192 \"**Size:** ~520 lines net across 10 tracked source files + regenerated `plugin/` + 1 new knowledge entry + 6 amended knowledge/AGENTS/docs files (\u00a75 and \u00a77 are the authoritative lists).\" Delete the trailing v4 jab \"(v4 said \\\"~450 / 6 files\\\" and undercounted \u2014 its own \u00a75 table names seven, and \u00a77 adds three more.)\" rather than restating it, since rev 5 committed the same error."
  },
  {
   "idx": 43,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Quote check passes: \u00a74.8 point 2 (line 227) reads \"`models-yaml-format.md:30` describes the `design` tier as \\\"Figma extraction, token mapping, visual spec\\\" and offers `design=low` heuristics for backend/infra features. Both go stale under rev 5 and must be rewritten \u2014 design-agent is now a contract consumer *and* per-task spec author.\" File check confirms the split: `skills/belmont/_src/references/models-yaml-format.md:30` is exactly \"  - `design` \u2014 Figma extraction, token mapping, visual spec\" (inside the `tiers` agent list), while the profile heuristics are 31 lines away under \"## Starting-point examples (non-definitive)\" \u2014 `:61` frontend-heavy `design=high`, `:62` backend-heavy `design=low (no UI)`, `:63` infra `design=low`. `grep -n design=low` returns only 62 and 63. So the sentence's grammatical subject (`:30`) is credited with content that lives at `:61-63` \u2014 a genuine citation imprecision in a spec whose stated discipline is exact file:line citations. Materiality is limited, which is why I hold this at minor rather than anything higher: \u00a75's scope row (line 245) already says \"Rewrite the `design` tier description **and heuristics**\", so an implementer working from the scope table would not in fact leave the `design=low` guidance stale. The finding's suggestion to \"mirror the two locations in the \u00a75 scope row\" overstates \u2014 that row already names both edits, it just carries no line numbers. Fix is purely additive and carries no regression risk.",
   "corrected_statement": "\u00a74.8 point 2 (line 227) attaches a single citation, `models-yaml-format.md:30`, to a two-part claim. `:30` carries only the tier description (\"`design` \u2014 Figma extraction, token mapping, visual spec\"); the profile heuristics that set `design=low` for backend-heavy and infra features are at `:62-63` (with `design=high` for frontend-heavy at `:61`), under the separate \"## Starting-point examples\" heading. The consequence is smaller than the finding implies, since \u00a75's scope row already says \"Rewrite the `design` tier description and heuristics\".",
   "fix_to_apply": "\u00a74.8, point 2 (line 227): replace the first sentence with \"`models-yaml-format.md:30` describes the `design` tier as \\\"Figma extraction, token mapping, visual spec\\\", and the profile heuristics at `:61-63` set `design=low` for backend-heavy and infra features.\" Leave the rest of the point (\"Both go stale under rev 5 and must be rewritten \u2014 design-agent is now a contract consumer *and* per-task spec author.\") unchanged. Optionally add the two line refs to the \u00a75 scope row at line 245, which already names both edits without them."
  },
  {
   "idx": 46,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quotes verified verbatim at :118 (\"**Why the fallback is normative and not the fallback.** `ux-designer` and `ui-designer` are user-scope personal skills at `~/.claude/skills/`. They exist for one person.\") and :120 (\"What is absent is the **content** \u2014 those two skills live under `~/.claude/skills/`, which no non-Claude CLI scans.\"). This is stale text the rev-5 per-section edit missed: `git show d1236f1` shows both paragraphs appearing as unchanged CONTEXT while the surrounding ladder was rewritten from two tier-1 skills to five (:104-106 now maps `ui-designer` \u00b7 `ux-designer` \u00b7 `ux-copywriter` \u00b7 `ux-motion` \u00b7 `interactive-prototype` to five contract sections). The document contradicts itself: :52 \"All five are distributed as downloads\", :116 \"The five skills are complementary\", :238 \"crediting the five Yummy Labs skills\", DoD :334 \"all five tier-1 skills named\". Independently verified on this machine that all five are user-scope and none is plugin-provided: `~/.claude/skills/{ui-designer,ux-designer,ux-copywriter,ux-motion,interactive-prototype}/SKILL.md` all exist as real directories (contrast the plugin-symlinked `docx`, `find-skills`, `contract-review`), and all five appear unprefixed in the session skill list while plugin skills are namespaced (`frontend-design:frontend-design`). So the portability gap is five skills, not two, and :120's \"the content is absent for non-Claude CLIs\" enumeration is now incomplete by three. Minor, not major: the argument's conclusion (baseline must be normative, not a fallback) is unaffected \u2014 only the enumeration understates the gap, and the fix is a two-phrase substitution with no design consequence.",
   "corrected_statement": "\u00a74.3's portability paragraphs at :118 and :120 still name only `ux-designer` and `ui-designer` as the user-scope skills, stale text left over from when tier 1 had two members. Since d1236f1 all five tier-1 skills are load-bearing (:104-106, :334) and all five are user-scope at `~/.claude/skills/` (verified on disk), so both sentences understate the portability gap by three skills and contradict :52, :116, :238 and :334.",
   "fix_to_apply": "\u00a74.3, two edits. (1) At :118 replace \"`ux-designer` and `ui-designer` are user-scope personal skills at `~/.claude/skills/`. They exist for one person.\" with \"All five tier-1 skills \u2014 `ui-designer`, `ux-designer`, `ux-copywriter`, `ux-motion`, `interactive-prototype` \u2014 are user-scope personal skills at `~/.claude/skills/`. They exist for one person.\" (2) At :120 replace \"those two skills live under `~/.claude/skills/`, which no non-Claude CLI scans\" with \"all five tier-1 skills live under `~/.claude/skills/`, which no non-Claude CLI scans\"."
  },
  {
   "idx": 47,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote verified verbatim at :359. Every mechanical claim reproduces on this clean tree: scripts/generate-plugin.sh:19 `VERSION=\"dev\"`, :25 `VERSION=\"$arg\"`, :41-44 writes `\"version\": \"$VERSION\"` into `$PLUGIN_DIR/.claude-plugin/plugin.json`, :29-32 puts `--check` in a `mktemp -d`; committed `plugin/.claude-plugin/plugin.json` says `\"version\": \"0.10.14\"`. So `generate-plugin.sh <ver> && git diff --exit-code plugin/` is green only when `<ver>` is exactly 0.10.14, and any other substitution produces a one-line version diff plus a mutated tracked tree. It also silently misses NEW files (untracked additions never appear in `git diff`), which matters for a PR that adds `references/design-authority-baseline.md`. HOWEVER the finding is misgraded twice and its fix is wrong as written: (a) category \"unsatisfiable-check\" overstates \u2014 the item IS satisfiable with `<ver>` = the committed version; (b) I RAN the proposed fix: bare `./scripts/generate-plugin.sh --check` exits 1 today with \"STALE: .claude-plugin/plugin.json\" (dev vs 0.10.14), while `./scripts/generate-plugin.sh --check 0.10.14` exits 0. That is 0006's third bug (0006:52, :104-106), which 0003 declares a hard dependency (:7, :246) and whose PREFERRED fix makes `--check` version-agnostic \u2014 but 0006 \u00a73.2 also permits the \"require the version argument\" branch, so rev 6 must not assume the bare form works. Note for the parent: this duplicates round-2 idx 22, already judged refuted=false/minor with essentially this fix \u2014 apply the edit once.",
   "corrected_statement": "DoD \u00a79 Mechanics :359 is ambiguous rather than unsatisfiable: `./scripts/generate-plugin.sh <ver> && git diff --exit-code plugin/` passes only when `<ver>` is the version already committed in `plugin/.claude-plugin/plugin.json` (0.10.14 today), which the spec never says; any other substitution reports a spurious failure and leaves the tracked tree mutated, and `git diff` additionally cannot see newly added plugin files. The purpose-built non-mutating gate is `--check`, but a bare `--check` exits 1 on this tree until 0006 \u00a73.2 lands.",
   "fix_to_apply": "\u00a79 Definition of Done \u2192 Mechanics, replace line 359 with: \"- [ ] `./scripts/generate-skills.sh --check` passes; `plugin/` regenerated and committed, verified with `./scripts/generate-plugin.sh --check` (pass the committed `plugin.json` version, e.g. `--check 0.10.14`, until 0006 \u00a73.2 makes the bare form version-agnostic \u2014 a bare `--check` exits 1 today with `STALE: .claude-plugin/plugin.json`). Do not gate on `generate-plugin.sh <ver> && git diff --exit-code plugin/`: it mutates the tracked tree and `git diff` cannot see newly added files such as the new reference.\""
  },
  {
   "idx": 48,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Quote checked and accurate. :211 reads verbatim: \"**Severity** uses the existing Critical / Warning / Polish ladder: contrast failure, missing focus indicator, missing label \u2192 Critical; missing state, off-scale spacing \u2192 Warning; radius or elevation inconsistency \u2192 Polish.\" The finding's ellipsis elides nothing disambiguating. I grepped the whole 413-line file for \"elevation\": it appears at :46 (ui-designer credited for \"radius, elevation\" in the token contract), :141 (schema \u2014 \"Elevation \u2014 [levels]. Interactive elements rise one level on hover.\"), :171 (preview renders \"radius and elevation samples\"), and :211 (the ladder). It appears NOWHERE in the \u00a74.6 check table at :191-203, whose eleven rows are Spacing, Type sizes, Contrast, Touch targets, Focus visible, Labels, Radius, Enumerated states, Transition durations, transform/opacity only, Reduced motion. So elevation is a declared, previewed, graded contract element with no verification mechanism \u2014 while \u00a74.6 is explicitly the normative check set (:187 fourth enforcement rule makes an unperformed check a FAIL; DoD :342 requires every row to name a mechanism). That asymmetry is real and minor.\n\nThe finding's SECONDARY claims are weak and I am dropping them. It says touch targets and the three motion rows are \"ungraded\". But :211 says the severity \"uses the **existing** Critical / Warning / Polish ladder\" \u2014 and that existing ladder is verification-agent.md:298-320, which I read: it already categorises \"Accessibility issues that affect usability\" \u2192 Warning and \"Animation/transition polish\" \u2192 Polish. Unlisted rows fall back there by construction; :211 supplies overrides, not an exhaustive mapping. Attacking that part would produce a wrong edit.",
   "corrected_statement": "\u00a74.6's check table (:191-203) has no elevation row, yet the Token Contract declares elevation with a behavioural rule (:141 \"Elevation \u2014 [levels]. Interactive elements rise one level on hover.\"), design-preview.html renders \"radius and elevation samples\" (:171), and the severity ladder grades \"radius or elevation inconsistency \u2192 Polish\" (:211). Elevation is therefore contracted, rendered for review, and graded, but never checked. The gap is in the table, not the ladder. (The finding's further claim that touch targets and the motion rows are ungraded is refuted: :211 defers to \"the existing \u2026 ladder\", i.e. verification-agent.md:298-320, which already classifies usability-affecting a11y issues as Warning and animation/transition issues as Polish.)",
   "fix_to_apply": "In \u00a74.6, insert one row into the check table immediately after the `Radius consistent, nested smaller` row (:199):\n\n| Elevation levels as declared; interactive elements rise one level on hover | `browser_evaluate` \u2192 `getComputedStyle` `boxShadow`, re-read after `browser_hover` |\n\nLeave :211 unchanged (elevation now has a check to grade). Do NOT add touch-target or motion entries to the :211 ladder \u2014 :211 already inherits verification-agent.md:298-320 for rows it does not override."
  },
  {
   "idx": 49,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Quotes verified. DoD :361 reads verbatim \"- [ ] Auto, interactive and plugin surfaces all exercised, including Codex packet and one non-Claude CLI\". \u00a76:261 reads \"| **Plugin** | `plugin/agents/*`, `plugin/skills/{tech-plan,product-plan,verify}/**` are a third tracked distribution surface; `release.sh:41` regenerates and `:96` stages it |\". I grepped \u00a78 (lines 275-321) for \"plugin\": zero hits \u2014 confirmed the whole-file grep shows plugin mentions only at :6, :7, :122, :205, :246, :261, :265, :345, :359, :361, :385. \u00a78's steps are: 1-6 Claude interactive+auto, 7-8 Figma regressions, 9a/9b/9c codex+opencode. Step 1's `/belmont:tech-plan` resolves through `belmont install`'s `.claude/commands/belmont/` symlink, not through `plugin/`; 9b uses the `.agents/skills/` Codex surface. So nothing loads the plugin build, and the DoD item has no procedure behind it.\n\nSupporting evidence I verified independently: DoD :332 requires the new reference to be \"present in `skills/belmont/tech-plan/references/` after generation\" but says nothing about `plugin/skills/tech-plan/references/`, and :359's `git diff --exit-code plugin/` passes as soon as the developer commits whatever was generated \u2014 it proves plugin/ is current, not that the baseline reference reached it or loads.\n\nWhy medium and not high: there is a defensible reading in which \u00a76's own \"Exercise\" cell for the Plugin row IS regeneration+staging, which DoD :359 covers, making :361 merely loose wording rather than an untestable claim. Either way rev 6 owes one edit \u2014 I've written the fix so it works under both readings.",
   "corrected_statement": "DoD :361 asserts the plugin surface is exercised, but \u00a78 contains no step that installs or runs the plugin build (grep for \"plugin\" over :275-321 returns nothing), so the item has no procedure behind it. The only plugin mechanic anywhere in the DoD is :359's regeneration diff, which proves plugin/ is up to date, not that the shipped skills load from plugin/ \u2014 and DoD :332 checks the new reference only in `skills/belmont/tech-plan/references/`, never in `plugin/skills/tech-plan/references/`.",
   "fix_to_apply": "Add to \u00a78 after Step 9, before **Diagnostics**:\n\n**Step 10 \u2014 plugin surface.** `./scripts/generate-plugin.sh <ver>`; confirm `plugin/skills/tech-plan/references/design-authority-baseline.md` exists and that `plugin/skills/{tech-plan,product-plan}/SKILL.md` carry the corrected skill names. Then install the plugin build in a clean checkout (`/plugin marketplace add <repo>` \u2192 `/plugin install belmont`) and run `/belmont:tech-plan` on the smoke feature from the plugin \u2014 expect the same `## Design Contract` schema and `**Authorities**` line as Step 1.\nFail: reference missing under `plugin/` \u2192 generate-plugin.sh's `cp \"$skill_dir/references\"/*.md` did not see it; regenerate skills first.\n\nAnd amend DoD :361 to read \"\u2026all exercised (smoke Steps 1-6, 9, 10), including Codex packet and one non-Claude CLI\"."
  },
  {
   "idx": 50,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Both quotes verified verbatim at :246 and :261. Every element of the evidence tested independently:\n(1) `git ls-files plugin/` returns 34 tracked files including `plugin/skills/next/SKILL.md` \u2014 confirmed tracked.\n(2) scripts/generate-plugin.sh:61-75 is `for skill_dir in \"$SKILLS_SRC\"/*/ \u2026 cp \"$skill_dir/SKILL.md\" \"$dest/SKILL.md\"` with only `_*` dirs skipped \u2014 every skill folder is copied unconditionally, so any `_src/next.md` edit propagates.\n(3) Propagation confirmed by content: `_src/next.md:118` \"[Not populated \u2014 lightweight mode skips the design agent. Note any Figma URLs here if present.]\" appears verbatim at `plugin/skills/next/SKILL.md:176`. That is exactly the line \u00a74.7:218 says this PR must change.\n(4) `_src/next.md` is unambiguously in scope \u2014 \u00a75:244 \"| `skills/belmont/_src/next.md` | Archive merges rather than clobbers\u2026\", reinforced at \u00a74.7:218 \"**stays in scope**\" and DoD :346.\n(5) The finding's parenthetical also checks out: `plugin/skills/tech-plan/references/models-yaml-format.md` is the only tracked copy, and grep shows only tech-plan's SKILL.md body cites `references/models-yaml-format.md`, so generate-skills.sh:140's grep places it under tech-plan alone \u2014 `tech-plan/**` does cover it.\nSo both enumerations omit a git-tracked plugin path this PR will change. Minor, but it will make `git diff --exit-code plugin/` (DoD :359) surface a file neither list accounts for.",
   "corrected_statement": "Both plugin enumerations are incomplete by exactly one skill. \u00a75:246 and \u00a76:261 list `tech-plan`, `product-plan` and `verify`, but \u00a75:244 also puts `skills/belmont/_src/next.md` in scope, and generate-plugin.sh:61-75 copies every skill folder unconditionally, so `plugin/skills/next/SKILL.md` (git-tracked; carries the `[Not populated \u2014 lightweight mode skips the design agent\u2026]` line at :176 that \u00a74.7:218 requires changing) will also change and is accounted for in neither list.",
   "fix_to_apply": "\u00a75:246 \u2014 replace with: \"| `plugin/` | Regenerate \u2014 now includes `plugin/skills/{tech-plan,product-plan,verify,next}/**` as well as `plugin/agents/*`. **Requires 0006** |\"\n\u00a76:261 \u2014 replace \"`plugin/agents/*`, `plugin/skills/{tech-plan,product-plan,verify}/**`\" with \"`plugin/agents/*`, `plugin/skills/{tech-plan,product-plan,verify,next}/**`\"."
  },
  {
   "idx": 51,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quotes verified verbatim: :107 \"**Tier 1b \u2014 enrichment, conditional.** If `frontend-design` is available, load it for aesthetic direction.\" and :132 \"**Authorities**: baseline[; \u2026 interactive-prototype (interaction); frontend-design (aesthetic)]\". The governing rule at :122 is also verbatim: \"**Use exact registered names.** \u2026 neither resolves \u2014 the real name is `vercel:react-best-practices`, and `security` is a slash command, not a skill.\"\n\nEvidence tested on disk, not taken on trust:\n- `ls -d ~/.claude/skills/*/` \u2192 animate, contract-review, docx, find-skills, interactive-prototype, remotion, ui-designer, ux-copywriter, ux-designer, ux-motion. `frontend-design` is NOT among them, so every Tier-1 name at :105 is correctly bare but `frontend-design` is not.\n- `find ~/.claude -path \"*frontend-design*\" -name SKILL.md` \u2192 `/Users/benlavender/.claude/plugins/cache/claude-plugins-official/frontend-design/unknown/skills/frontend-design/SKILL.md`. It is a plugin skill, and this session's registered-skill listing carries it as `frontend-design:frontend-design`, exactly parallel to `vercel:shadcn` (:107) and `vercel:react-best-practices` (:122), both of which the proposal namespaces correctly.\n- Inheritance confirmed: `skills/belmont/_src/tech-plan.md:160` = \"- Load relevant skills (figma:*, frontend-design, vercel-react-best-practices, security, etc.)\" and `_src/product-plan.md:165` = \"1. Load relevant skills for the domain (figma:*, frontend-design, vercel-react-best-practices, security, etc.)\" \u2014 the bare form comes from the very lines :122 is correcting, and DoD :338 covers only the `vercel-react-best-practices` and `security` fixes, so `frontend-design` survives the edit and is copied forward into the new `design-authority-baseline.md` (\u00a74.3's blockquote is that file's content) and into the contract schema.\nReal but minor: a naming-precision defect in the section whose whole purpose is naming precision.",
   "corrected_statement": "\u00a74.3 writes the bare name `frontend-design` at :107 and :132, but it is a plugin skill (`~/.claude/plugins/cache/claude-plugins-official/frontend-design/\u2026/skills/frontend-design/SKILL.md`) whose registered name is `frontend-design:frontend-design` \u2014 the same namespaced form the proposal already uses correctly for `vercel:shadcn` and `vercel:react-best-practices`. The bare form violates the \"Use exact registered names\" rule stated 15 lines later at :122, and since DoD :338 enumerates only the `vercel-react-best-practices` and `security` corrections, the bare name is inherited unfixed from `tech-plan.md:160` / `product-plan.md:165` into the new baseline reference file and the contract schema.",
   "fix_to_apply": "\u00a74.3 :107 \u2014 replace \"If `frontend-design` is available\" with \"If `frontend-design:frontend-design` is available (Anthropic plugin skill; the bare `frontend-design` is not a registered name)\".\n\u00a74.4 :132 \u2014 replace \"frontend-design (aesthetic)\" with \"frontend-design:frontend-design (aesthetic)\".\nDoD :338 \u2014 replace with: \"- [ ] `vercel-react-best-practices` \u2192 `vercel:react-best-practices`, `frontend-design` \u2192 `frontend-design:frontend-design`, and the bogus `security` entry fixed in both `tech-plan.md:160` and `product-plan.md:165` \u2014 and the namespaced forms used in `design-authority-baseline.md` and the contract schema\"."
  },
  {
   "idx": 53,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Quotes verified. \u00a75:239 is `| skills/belmont/_src/product-plan.md | Fix the same wrong skill names at `:165` |` and \u00a75:246 puts `plugin/skills/product-plan/**` in scope. `grep -n product-plan` over the spec returns only :96, :122, :239, :246, :261, :338 \u2014 nothing in \u00a78 (lines 275-322), whose only skill invocations are `/belmont:tech-plan` (Step 1), `/belmont:next` (Step 5), `/plan $belmont:tech-plan` and `$belmont:codex-plan-apply` (Step 9b). The Go evidence is correct: `buildLoopPrompt` (main.go:7165-7204) enumerates actionImplementMilestone, actionImplementNext, actionVerify, actionReplan, actionDebug, actionFixAll, actionTriage \u2014 no product-plan case \u2014 so product-plan's only invocation path is interactive. `skills/belmont/_src/product-plan.md:165` is verbatim `1. Load relevant skills for the domain (figma:*, frontend-design, vercel-react-best-practices, security, etc.)`, and `git ls-files plugin/` confirms `plugin/skills/product-plan/SKILL.md`. Two overreaches I corrected. (1) The cited rule is misapplied: AGENTS.md:56 is per **tool** (\"for each affected tool\"), and \u00a78 does exercise Claude, Codex and opencode; the applicable rule is AGENTS.md:29 (\"skill behaviour ... must explicitly address both paths in the plan and in tests\"). (2) \"the one changed file in \u00a75 with zero coverage\" is inaccurate \u2014 `skills/belmont/_src/references/models-yaml-format.md` (\u00a75:245) is equally unexercised by any smoke step. The residual gap is real but thin: a two-token string fix whose only runtime path gets no assertion anywhere in \u00a78. The right remedy is a grep line, not a full interactive product-plan interview.",
   "corrected_statement": "\u00a78 contains no assertion of any kind for `skills/belmont/_src/product-plan.md`, whose only invocation path is interactive (`buildLoopPrompt`, main.go:7165-7204, has no product-plan case). Per AGENTS.md:29 a skill-behaviour change must address both paths in the plan and in tests; here that collapses to one path, and it is untested. The edit is a two-token name fix, so a grep assertion in \u00a78's Preconditions is sufficient \u2014 a full `/belmont:product-plan` interview is not warranted. Note `models-yaml-format.md` (\u00a75:245) is unexercised on the same terms.",
   "fix_to_apply": "In \u00a78, inside the Preconditions bash block (after the `grep -c \"Figma\" .belmont/features/<slug>/PRD.md   # expect 0` line, ~:286), add:\n\n```bash\n# both name fixes landed on the generated surface (DoD :338)\ngrep -c \"vercel:react-best-practices\" .agents/skills/belmont/tech-plan/SKILL.md .agents/skills/belmont/product-plan/SKILL.md   # expect 1 and 1\ngrep -c \"vercel-react-best-practices\" .agents/skills/belmont/tech-plan/SKILL.md .agents/skills/belmont/product-plan/SKILL.md   # expect 0 and 0\n```\n\nThen append one sentence to \u00a78's Diagnostics paragraph (:321): \"`product-plan` has no auto path \u2014 `buildLoopPrompt` (`main.go:7165-7204`) carries no case for it \u2014 so its only invocation path is interactive, and the name fix there is covered by the Preconditions grep plus the tracked `plugin/skills/product-plan/**` diff rather than by a step of its own. `models-yaml-format.md` is covered on the same terms.\""
  },
  {
   "idx": 54,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Both quoted lines are accurate (:332 and :359). Two of the finding's mechanics are wrong and one is understated, but the core defect survives. WRONG: \"it regenerates into a temp dir first\". `scripts/generate-skills.sh:24` sets `DEST_DIR=\"$ROOT/skills/belmont\"` in both modes; I proved by execution that `gen=\"$DEST_DIR/$n/SKILL.md\"` and `committed=\"$ROOT/skills/belmont/$n/SKILL.md\"` are the *same path*, so the `diff -q` compares a file with itself. `--check` therefore regenerates in place and exits 0 unconditionally (ran it: \"All generated files are up to date.\", EXIT=0) \u2014 it can only fail if generation itself errors (e.g. a missing partial under `set -e`). CONSTRUCTED: the spec never pairs :359 with :332; they are separate checkboxes in separate groups (\"Mechanics\" vs \"Authority and portability\"), so \"naming it in the DoD implies coverage\" is the finding's inference, not the spec's claim. MISSED MITIGATION, on the very line attacked: :359's second clause, `./scripts/generate-plugin.sh <ver> && git diff --exit-code plugin/`, does surface the new reference \u2014 `generate-plugin.sh:70-72` copies `$skill_dir/references/*.md` wholesale (no prefix matching), so a successful copy lands as tracked `plugin/skills/tech-plan/references/design-authority-baseline.md` and is diff-visible. What genuinely survives is narrower and confirmed: `generate-skills.sh:141-147` guards the copy with `if [ -f \"$ref_src\" ]`, so a mis-named or absent `_src/references/design-authority-baseline.md` silently no-ops; generated output is gitignored (`.gitignore:12` `skills/belmont/*/`); and no named mechanism in \u00a79 detects the absence \u2014 `--check` cannot, and `git diff --exit-code plugin/` passes when the file is missing from both the regenerated and committed plugin trees. DoD :332 then rests on a human noticing an absence. Real, minor, and fixable with one `test -f`.",
   "corrected_statement": "`./scripts/generate-skills.sh --check` cannot verify DoD :332's reference-file assertion: it compares `$DEST_DIR/$n/SKILL.md` against `$ROOT/skills/belmont/$n/SKILL.md`, which are the same path (`generate-skills.sh:24`), so it only proves generation ran without error. Combined with the silent-skip guard at `generate-skills.sh:141-147` (`if [ -f \"$ref_src\" ]`) and gitignored output (`.gitignore:12`), a mis-named `_src/references/design-authority-baseline.md` is a no-op that no named mechanism in \u00a79 catches \u2014 `git diff --exit-code plugin/` also passes when the file is missing from both trees. :332 needs an explicit `test -f`. The spec does not, however, claim `--check` backs :332, and :359's plugin clause does make a successfully-copied reference diff-visible.",
   "fix_to_apply": "In \u00a79 \"Mechanics\", replace line 359 with two checkboxes:\n\n- [ ] `./scripts/generate-skills.sh && test -f skills/belmont/tech-plan/references/design-authority-baseline.md` \u2014 the copy at `generate-skills.sh:141-147` is guarded by `if [ -f \"$ref_src\" ]`, so a mis-named `_src/references/` source silently no-ops and nothing else catches it\n- [ ] `./scripts/generate-skills.sh --check` and `./scripts/generate-plugin.sh <ver> && git diff --exit-code plugin/` pass. Note what each covers: `--check` compares `$DEST_DIR/<skill>/SKILL.md` with `skills/belmont/<skill>/SKILL.md`, which are the same path, so it only proves generation ran without error; the plugin diff is the check that puts the new reference in the PR as tracked `plugin/skills/tech-plan/references/design-authority-baseline.md` (`generate-plugin.sh:70-72` copies the whole `references/` dir)."
  },
  {
   "idx": 55,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Every claim verified. `grep -n \"#10\\|#11\\|agents/belmont/references\"` over the spec returns exactly four hits: :265 (\"v4 warned about PRs #10 and #11 ... both were **closed on 2026-08-06**\" \u2014 a correction note, not a live conflict block), :355 (the DoD item itself), :356 (the DoD item itself), :397 and :404 (the \u00a712 changes table, which already records both: \"Both leftovers deleted...\" and \"Both closed 2026-08-06. Block and its DoD item deleted\"). So both deletions were performed by rev 5 on this proposal document and were already true before any implementation begins; no action an implementer takes can flip either checkbox. Scope confirmed: \u00a75's in-scope table (:234-249) contains zero `docs/` paths (`grep -c \"docs/\"` over that range returns 0), and \u00a77 lists only `docs/agent-pipeline.md`, `docs/skills-reference.md`, `AGENTS.md` and two knowledge files \u2014 `docs/proposals/` is nowhere in scope, so these are not PR deliverables. The finding is also right to separate the third item in that group: :354 names `AGENTS.md:26` and `dual-invocation-paths.md:9`, both of which ARE in \u00a75's table at :247-248, so it is a genuine deliverable and must stay. Two pre-satisfied checkboxes under a heading that reads \"Corrections carried by this PR\" is a real category error and dilutes a gate list a reviewer walks \u2014 but it is cosmetic, and deleting them is zero-risk. Minor is the right grade.",
   "corrected_statement": "DoD :355 and :356 describe edits rev 5 already made to this proposal document, not deliverables of the PR \u2014 `docs/proposals/` appears in neither \u00a75's in-scope table (:234-249) nor \u00a77. Both are already recorded in \u00a712 at :404 and :397. They cannot fail and should be deleted from the \"Corrections carried by this PR\" group; :354 stays, since `AGENTS.md` and `dual-invocation-paths.md` are in scope at \u00a75:247-248.",
   "fix_to_apply": "In \u00a79, under \"**Corrections carried by this PR**\", delete these two lines verbatim:\n\n- `- [ ] v4's dead `#10`/`#11` conflict block and its DoD item deleted`\n- `- [ ] v4's `agents/belmont/references/` paragraph (\u00a75) and its DoD item deleted \u2014 they contradicted the inlining decision and each other`\n\nKeep the remaining item (`- [ ] `AGENTS.md:26` and `dual-invocation-paths.md:9` corrected \u2014 auto mode does **not** bypass skill discovery and Belmont injects only steering text`) unchanged. No other edit needed: \u00a712 rows at :397 and :404 already carry both records."
  },
  {
   "idx": 56,
   "refuted": false,
   "confidence": "medium",
   "severity_corrected": "minor",
   "reasoning": "Every factual claim checks out. skills/belmont/_src/codex-plan-apply.md:20 = \"3. Do NOT edit source code.\"; :60 = \"3. Validate that no source-code paths are listed.\"; :75-81 = \"## Refusal Cases\\n\\nStop without editing if: ... - The packet asks for source-code changes.\" Three prohibitions, one a hard refusal. Spec :94 (R5) reads \"Every target path is under `.belmont/`, which `codex-plan-apply` requires\" \u2014 it reasons only about the path constraint (skill rule 4 / workflow step 2), never the content constraint. grep of the whole 413-line spec for `codex-plan-apply` returns only :94 and :317; \u00a75's In-scope table (:234-249) confirmed to omit `skills/belmont/_src/codex-plan-apply.md`, and the `plugin/` row (:246) enumerates only tech-plan, product-plan, verify and the agents \u2014 `plugin/skills/codex-plan-apply/` exists in the repo. So the applying agent, in a Codex session, reads only the skill file, which gives it no basis to classify `.belmont/features/<slug>/design-preview.html` as anything other than a possible source-code path. Two mitigating facts I weighed: (a) the spec DOES articulate the right principle at :96 (R7) \u2014 \"The preview is a planning artifact under `.belmont/`, never project source\" \u2014 but only inside the tech-plan/CRITICAL-RULE-2 discussion, so it never reaches the shipped codex-plan-apply surface; (b) a competent agent would probably infer that `.belmont/**` is planning state, so this is a conservative-refusal risk rather than a certain failure. That keeps it minor, not major. Two rhetorical overstatements in the finding, corrected below: the packet already carries a non-Markdown payload (`models.yaml`, named in codex-plan-apply.md:19 and knowledge/cross-cutting/codex-plan-handoff.md:17), so `design-preview.html` is the first *markup-bearing* payload, not the first non-Markdown one; and only one new file payload is added (the contract is a section inside TECH_PLAN.md, already a payload). Substance stands: smoke Step 9b (:317) and DoD :361 depend on an apply step the PR never scopes.",
   "corrected_statement": "R5 (:94) reasons about `codex-plan-apply`'s path constraint but not its content constraint. The skill carries three source-code prohibitions \u2014 rule 3 (`:20`), workflow validation step 3 (`:60`) and a hard refusal case (`:81` \"The packet asks for source-code changes\") \u2014 and `design-preview.html` is the first markup-bearing payload a packet has ever carried (prior payloads: PRD/PROGRESS/TECH_PLAN Markdown plus `models.yaml`). An applying agent that classifies `.html` as source code stops without editing. The spec states the disambiguating principle at :96 (\"The preview is a planning artifact under `.belmont/`, never project source\") but only for tech-plan; `skills/belmont/_src/codex-plan-apply.md` is absent from \u00a75's In-scope table, so nothing in the PR propagates it to the file the Codex applying agent actually reads. Smoke Step 9b (:317) and DoD :361 can then fail for a reason the spec did not anticipate.",
   "fix_to_apply": "In \u00a75 In-scope, add a row after the `skills/belmont/_src/product-plan.md` row: \"| `skills/belmont/_src/codex-plan-apply.md` | Carve planning artifacts out of the source-code prohibition: after rule 3, add \u2014 `.html` and other non-Markdown files under `.belmont/` are planning artifacts, not source code; apply them normally. Mirror the same clause in workflow step 3 and the `## Refusal Cases` list |\". Also extend the `plugin/` row (:246) to name `plugin/skills/codex-plan-apply/**` in the regenerated set. Optionally add a clause to R5 (:94) after \"which `codex-plan-apply` requires\": \"\u2014 and `codex-plan-apply`'s source-code refusal case must be amended to exempt `.belmont/**` planning artifacts, or it will refuse the preview.\""
  },
  {
   "idx": 60,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote verified. \u00a77 (docs/proposals/0003-design-quality-without-figma.md:267-273) lists exactly: `docs/agent-pipeline.md`, `docs/skills-reference.md`, `AGENTS.md`, the new `knowledge/cross-cutting/design-authority.md`, a KNOWLEDGE.md routing row, and Revisions lines. I grepped the full 413-line spec case-insensitively for \"README\", \"workflow.md\" and \"directory-structure\" \u2014 zero hits, so the omission is not covered elsewhere (\u00a75's in-scope table lists no docs/ pages at all). All three evidence citations are accurate as read: README.md:13 = \"**Figma-first design workflow** -- Belmont is built heavily around understanding Figma designs. The design-agent extracts exact tokens ... The verification-agent compares your implementation against the Figma source using Playwright headless screenshots. For the best experience, install [figma-mcp](...) so Belmont can load and analyze your designs automatically.\"; README.md:126 = \"| 2. Design Analysis | `design-agent`         | Session | MILESTONE + Figma    | `## Design Specifications` |\"; docs/workflow.md:77 = \"3. `design-agent` reads MILESTONE, loads Figma, writes design specs to MILESTONE *(parallel with 2)*\"; docs/directory-structure.md:87-92 lists the feature dir as `<feature-slug>/` \u2192 PRD.md, TECH_PLAN.md, PROGRESS.md, models.yaml (glossed \"Per-feature model tiers (optional, written by /belmont:tech-plan)\"), MILESTONE.md \u2014 no design-preview.html, even though the models.yaml row is the exact analogue for a tech-plan-authored optional feature-dir artifact. CLAUDE.md/AGENTS.md makes the README update mandatory (\"always ensure the README and docs/ are up to date ... If a change affects both the README summary and a docs page, update both\"), and \u00a79's DoD carries no docs checkbox at all. One sub-claim is weaker than stated: README:126's \"Reads: MILESTONE + Figma\" is already imprecise today \u2014 design-agent.md:30 already lists \"**The TECH_PLAN** ... Read for any design-adjacent technical constraints\" \u2014 so that row is pre-existing drift this PR worsens rather than creates. That does not refute the finding. The finding also undercounts slightly: docs/prd-format.md has 4 Figma mentions and docs/troubleshooting.md 1, though neither is clearly falsified by rev 5. Grade minor is correct \u2014 docs completeness, no behavioural consequence.",
   "corrected_statement": "\u00a77 \"Docs and knowledge\" enumerates only `docs/agent-pipeline.md` and `docs/skills-reference.md`, but rev 5 also falsifies or leaves incomplete three further user-facing pages: `README.md:13` (the \"Figma-first design workflow\" bullet, which presents Figma-MCP as the path to design quality), `docs/workflow.md:77` and its tech-plan \"What happens\" list at :55-62 (which never mentions a design contract or preview), and `docs/directory-structure.md:87-92` (feature-dir tree missing `design-preview.html`, despite listing the analogous tech-plan-authored `models.yaml`). AGENTS.md mandates README + docs updates, and \u00a79 currently has no docs DoD item. Note README:126's \"MILESTONE + Figma\" is pre-existing drift (design-agent.md:30 already reads TECH_PLAN), not a new break.",
   "fix_to_apply": "In \u00a77 \"Docs and knowledge\", add three bullets after the `docs/skills-reference.md` line: (1) \"`README.md` \u2014 reframe the `:13` \"Figma-first design workflow\" bullet: Figma-first where a Figma exists, and a derived Design Contract authored in `/belmont:tech-plan` where it does not; update the `:126` pipeline row so design-agent's Reads column names the contract, not just Figma\"; (2) \"`docs/workflow.md` \u2014 the tech-plan \"What happens\" list (`:55-62`) gains Phase 3.5 and `design-preview.html`; `:77`'s design-agent line gains \"reads the Design Contract in TECH_PLAN\"\"; (3) \"`docs/directory-structure.md` \u2014 add `design-preview.html` to the feature-dir tree at `:87-92`, glossed like `models.yaml`: \"Rendered design contract (optional, written by /belmont:tech-plan)\"\". Add one DoD item under **Mechanics** in \u00a79: \"- [ ] `README.md`, `docs/workflow.md` and `docs/directory-structure.md` updated \u2014 AGENTS.md requires README and docs/ to track the change\". Update the \u00a7Size line (:6) from \"9 tracked source files + regenerated `plugin/` + 1 new knowledge entry + 2 corrected knowledge/AGENTS statements\" to add \"+ 3 docs pages\"."
  },
  {
   "idx": 62,
   "refuted": false,
   "confidence": "high",
   "severity_corrected": "minor",
   "reasoning": "Quote verified: spec :173 reads \"This is durable and reviewable on the same terms as the contract: authored in the master tree, staged by `commit-belmont-changes.md`'s `git add .belmont/`, and therefore present in the PR diff.\" \u2014 stated unconditionally. Evidence verified independently: skills/belmont/_partials/commit-belmont-changes.md:5-9 reads \"1. **Check if `.belmont/` is git-ignored** \u2014 run: `git check-ignore -q .belmont/ 2>/dev/null`. If exit code is 0, `.belmont/` is ignored \u2014 skip this section entirely.\" I confirmed tech-plan is a consumer: skills/belmont/_src/tech-plan.md:295 = `<!-- @include commit-belmont-changes.md commit_context=\"after technical planning\" -->`, so the short-circuit is on the exact path \u00a74.5 relies on. docs/directory-structure.md:124-126 confirms the configuration is supported: \"## Should I gitignore `.belmont/`? Generally, no ... The only case to gitignore it is if you're a solo developer who wants to keep planning state purely local and ephemeral.\" The finding is also honest about what still holds (durability is filesystem-based via copyBelmontStateToWorktree, unaffected). Two things temper the grade rather than refute it: (a) \u00a74.5:175 already states the primary review mechanism independent of git \u2014 \"The user opens it from the path printed at approval time ... the file is the deliverable\" \u2014 so the finding's suggested second fix is already in the spec; (b) the consequence is documentation precision, not a design flaw. But the unconditional claim propagates: DoD :329 reads \"- [ ] `{base}/design-preview.html` written, **committed**, and opens standalone with no network references\", which is unsatisfiable on a gitignored install, and smoke Step 1 (:290) asserts \"both are committed by the skill's commit step\". Minor is the right grade; the fix is a one-clause qualification with no downside risk.",
   "corrected_statement": "\u00a74.5:173 states unconditionally that the contract and preview are \"staged by `commit-belmont-changes.md`'s `git add .belmont/`, and therefore present in the PR diff\", but that partial short-circuits entirely when `.belmont/` is git-ignored (commit-belmont-changes.md:5-9, included by tech-plan.md:295) \u2014 a documented, supported configuration (docs/directory-structure.md:124-126). On such an install durability still holds (the copy paths are filesystem-based) and \u00a74.5:175's printed-path review still works, but the PR-diff claim does not, and DoD :329's unconditional \"committed\" becomes unsatisfiable.",
   "fix_to_apply": "In \u00a74.5, replace the first sentence of the paragraph at :173 with: \"This is durable and reviewable on the same terms as the contract: authored in the master tree and staged by `commit-belmont-changes.md`'s `git add .belmont/`, so on the default install \u2014 `.belmont/` tracked \u2014 both appear in the PR diff. Where `.belmont/` is git-ignored (a supported configuration, `docs/directory-structure.md:124-126`, on which `commit-belmont-changes.md:5-9` short-circuits), the contract and preview remain durable on disk and are reviewed locally at the path printed at approval time.\" Then amend DoD :329 to \"- [ ] `{base}/design-preview.html` written, committed where `.belmont/` is tracked, and opens standalone with no network references\", and append to smoke Step 1 (:290) \"\u2014 skip the commit assertion if the smoke project git-ignores `.belmont/`\"."
  }
 ],
 "refuted": [
  {
   "idx": 5,
   "reasoning": "STEP 1 (quote check) \u2014 the two anchor quotes are accurate but the load-bearing framing is not. Line 40 reads: \"...are Belmont-original prose stating published standards: WCAG 2.1 SC 1.4.3 (4.5:1 body, 3:1 large text), SC 2.5.5 (44x44 targets), SC 2.4.7 (visible focus), SC 2.3.3 (motion from interaction), the 8pt spacing grid, ratio-derived type scales, and the 60-30-10 colour convention.\" The find"
  },
  {
   "idx": 8,
   "reasoning": "The finding misquotes the spec \u2014 it quotes the PREVIOUS revision. At HEAD (d1236f1, the revision under review) line 132 reads: \"**Authorities**: baseline[; ui-designer (tokens); ux-designer (strategy); ux-copywriter (microcopy); ux-motion (motion); interactive-prototype (interaction); frontend-design (aesthetic)]\". That IS the per-section form, and it names all five tier-1 skills (ui-designer, ux-"
  },
  {
   "idx": 13,
   "reasoning": "MISREADS R7 by truncating the disambiguating clause. The finding's evidence says the proposal \"acknowledges the same ambiguity and declines to resolve it\" and quotes 0003:96 as only `CRITICAL RULE 2 (\"no source code files\") stands unchanged for .tsx/.ts/.css`. The full sentence at docs/proposals/0003-design-quality-without-figma.md:96 reads: \"The preview is a planning artifact under `.belmont/`, n"
  },
  {
   "idx": 21,
   "reasoning": "The finding's own evidence misquotes R4 by ellipsis, removing the clause that settles it. Spec line 93 in full: \"The step must: **preserve an existing `## Design Contract` verbatim** if one is present, deriving only what is missing; mark it `**Approval**: unreviewed (headless replan <ISO date>)`; and never silently regenerate an approved contract.\" The finding renders this as \"preserve an existing"
  },
  {
   "idx": 23,
   "reasoning": "Quote is accurate (line 203 reads exactly as claimed; line 189 reads \"**Named tooling per check.** Every row names its mechanism; a check whose mechanism is unavailable is recorded **`UNVERIFIABLE`**, never `PASS`.\"), but the four load-bearing assertions built on it are each false.\n\n(1) \"This is the only row in the table that names no tool\" \u2014 false. Line 200 reads \"| Enumerated states present | ex"
  },
  {
   "idx": 27,
   "reasoning": "The quote is accurate but the reading is not. Spec :78 in full: \"> **`tech-plan` writes the contract. `implement`, `verify`, `next` and `debug` only read it.** No auto-mode phase may write or amend `{base}/TECH_PLAN.md`. A phase that 'enriches the contract it consumes' reintroduces exactly the bug this PR removes.\" The second sentence is the disambiguator and it scopes the prohibition to auto-mode"
  },
  {
   "idx": 28,
   "reasoning": "Both halves fail on the actual text. Claim (a): R1 at spec :90 reads \"A backend-only feature skips it and the template records `**Design contract**: N/A \u2014 no UI in this feature`\" \u2014 that is a bold *field line*, not the `## Design Contract` H2 heading that branch 2 (:182) keys on (\"`{base}/TECH_PLAN.md` carries a `## Design Contract`\"). R1 therefore does not \"guarantee the section is always present\""
  },
  {
   "idx": 32,
   "reasoning": "STEP 1 (quote check) \u2014 the quotes are accurate (:91 R2, :215 \u00a74.7, :253 out of scope, :311 Step 7, and tech-plan.md:158 / product-plan.md:92 verbatim). The failure is in the inference, not the citation.\n\nSTEP 3 (independent verification) \u2014 the alleged contradiction dissolves on the code:\n\u2022 tech-plan.md:158 says sub-agents \"cannot get MCP tool permissions **approved**\" \u2014 a statement about surfacing"
  },
  {
   "idx": 36,
   "reasoning": "The spec quote is accurate (line 78) and the debug-file evidence is accurate (debug-phase1-design.md:1,3; debug-auto.md:30,60-61; the Phase 3 verification dispatch at :126-155 with its \"DEBUG MODE OVERRIDE\" and \"Primary check: does the specific bug still reproduce?\"). But the load-bearing harm is refuted by code the finding did not read.\n\n(1) \"Turning every no-UI bugfix verification into a full 11"
  },
  {
   "idx": 44,
   "reasoning": "Quotes verified verbatim (0003:18 \"`verification-agent.md` Phase 2 is *comparison-only*: its check target is a loaded Figma screenshot or image (`:53-60`)\"; 0003:185 \"verification-agent **already reads** TECH_PLAN (`:20`, `:60`), but only as a place to *search for references*\"). Both citations point at real, on-topic content and neither claim is false, so this is citation-style preference, not a d"
  },
  {
   "idx": 45,
   "reasoning": "Line 403 reads (raw, no bold \u2014 the finding's quote adds `**` emphasis that is not in the file): \"| \\\"nothing runs the project's lint/typecheck\\\" | False \u2014 implementation-agent runs both per task (`:120`, `:146`, `:149`). Restated as \\\"verification never re-runs them\\\"; \u00a74.5 dropped |\". Every fact in the row checks out: implementation-agent.md:120 \"#### Step 3: Build & Test Checks\", :146 \"<pkg> run"
  },
  {
   "idx": 52,
   "reasoning": "The finding misquotes rev 5 by pasting v4's text. It claims \"\u00a74.4's canonical schema block at :132 renders it as `**Authorities**: baseline[; ux-designer; ui-designer; frontend-design]`\". Line 132 of the spec under review actually reads: `**Authorities**: baseline[; ui-designer (tokens); ux-designer (strategy); ux-copywriter (microcopy); ux-motion (motion); interactive-prototype (interaction); fro"
  },
  {
   "idx": 57,
   "reasoning": "The quotes are accurate \u2014 ~/.claude/skills/interactive-prototype/SKILL.md:3 does end \"Do NOT use for static mockups, dashboards, landing pages, or marketing sites\", spec :102 does say \"Load **every** tier-1 skill that is present\", :105 does map `interactive-prototype` \u2192 \"interaction and gesture states\", and _src/tech-plan.md:15 is \"2. Do NOT create or edit any source code files (no .tsx, .ts, .css"
  },
  {
   "idx": 58,
   "reasoning": "The three quotes are real \u2014 product-plan.md:191 is \"- Design intent (if no Figma: what should it look and feel like?)\" under \"Always ASK about\", product-plan-prd-format.md has no design section, and tech-plan.md:208-213 is the DO NOT RE-ASK list containing \"What the user wants to build or why\". The conclusion does not follow, for one decisive reason the finding never engages: **tech-plan asks the "
  },
  {
   "idx": 59,
   "reasoning": "Locations verified: \u00a77 (:269-273) lists only design-authority.md, the KNOWLEDGE.md routing row, and Revisions on model-tier-economics.md and dual-invocation-paths.md; codex-plan-handoff.md is absent, and KNOWLEDGE.md:38's trigger text is quoted correctly. The defect claim nonetheless fails on its own evidence. (a) The entry does NOT go stale. I read all 49 lines. Invariant :18 is \"The packet is se"
  },
  {
   "idx": 61,
   "reasoning": "The finding's load-bearing premise \u2014 \"both lists are closed\" \u2014 is a misreading of both quoted lines. skills/belmont/_src/reset.md:65 actually reads: \"2. Delete **all files** in `.belmont/features/<slug>/` (PRD.md, PROGRESS.md, TECH_PLAN.md, NOTES.md, MILESTONE.md, MILESTONE-*.done.md)\" \u2014 \"all files\" is the directive; the names are a parenthetical gloss. skills/belmont/_src/cleanup.md:139 reads: \"2"
  }
 ],
 "no_verdict": []
}